# `_대기/` — 창업자 확인 전이라 **아직 앱에 안 올린** 변경분

> ⚠️ 여기 있는 `.patch`는 **적용하면 안 되는 게 아니라, 아직 적용 안 한 것**이다.
> 우리 구조상 배포 브랜치에 `hankki/**`를 푸시하면 **곧바로 배포**된다(규칙 9).
> 그래서 창업자가 "배포해 / ㄱㄱ / 픽스" 하기 전엔 앱 코드를 커밋·푸시하지 않는데,
> 그러면 **컨테이너가 날아갈 때 작업이 통째로 사라진다.**
> → 코드 변경분을 patch로 떠서 문서 폴더에 보관한다. 문서 저장은 배포와 무관.

## 적용법

```bash
cd hankki && git apply docs/_대기/<파일>.patch
```

패치가 안 맞으면(그 사이 파일이 바뀌었으면) `git apply --3way` 로.
**적용해서 배포까지 끝났으면 그 patch는 지운다.**

---

## 목록

### `카드-캐릭터풀확장-2026-07-29.patch`
레꾸자랑 뽑기카드 `src/components/ShareDrawCard.jsx` 전면 개편.

- 카드 6종을 **색만 다른 게 아니라 구조가 다르게** 재작성
  (warm=좌상단 볼드타이포+blob / panel=컬러패널 / pola=폴라로이드 / mag=매거진 / summer / night=수집카드)
- **캐릭터 풀을 요리·음식 컷으로 교체** — 창업자 "음식 빼 카드 이상해져 / 캐릭터에 음식있는걸 쓰던가"
- `SUMMER` 계절 분리 — 여름 컷은 여름 스킨에서만
- `scenepool` 신설 — 배경 있는 씬컷을 **폴라로이드 사진 자리**에만
- 레이아웃 수정 4건(폴라 캡션 중복 · 여름 물결 잠김 · 밤 홀로원판 · 웜 캐릭터 크기)

**같이 필요한 자산**(패치엔 안 들어감, 바이너리라):
`src/assets/sharepool/` 25개 추가 + `src/assets/scenepool/` 10개 신설.
원본은 전부 `docs/stickers/` 안에 있으니 거기서 복사하면 된다 —
`음식라이프-이모티콘-2507/콤비장면/낱개`(6) · `곰펭-에피소드-2507/낱개`(9) ·
`src/assets/stickers/photo/sm_*`(10) → `sharepool` /
`음식라이프-이모티콘-2507/콤비장면/낱개-씬`(7) + 에피소드·여름 3 → `scenepool`

상세 = `docs/작업복기-2026-07-29-카드-캐릭터풀-전수조사.md`
