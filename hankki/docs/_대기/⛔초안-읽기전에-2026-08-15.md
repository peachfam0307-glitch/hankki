# ⛔⛔ `레시피-정리-초안-2026-08-10.json` 을 근거로 쓰기 «전»에 읽을 것

> 📮 2026-08-15 에 **내가 이걸로 틀렸다.** 같은 자리를 또 파지 않게 남긴다.

## 이 JSON 은 «화석»이다

- **8/10 초안**이고, 편마다 내가 그때 적어둔 `fix`(내가 고친 것)·`ask`(물어볼 것)·`status` 가 들어 있다.
- ⭐⭐ **그 뒤 창업자 검수와 정정이 «코드»에 반영됐고, 이 파일은 그때 상태로 얼어 있다.**

| 커밋 | 무엇 |
|---|---|
| `8825138` | 창업자 검수 반영 — ✏️24편 고치고 ⛔2편 뺐다 (38 → 36편) |
| `81995ed` | 창업자 2차 정정 20곳 (식용유 전수 · 아우노 환산 · 육수 이름 · 비빔갈비찜) |
| `fb604d8` | 36편 검수판 2판 |

## ⛔ 그래서 이렇게 읽으면 «틀린다»

- ⛔ `ask` 가 있다 → 「아직 답을 못 받았다」  **← 틀렸다.** 그건 **그때 물었다는 기록**이다.
- ⛔ `status` 가 비었다 → 「확정 못 받았다」  **← 틀렸다.**

## ✅ 맞게 읽는 법

1. ⭐ **레시피의 현행은 `src/data/basics.js` 다.** 여기부터 본다.
2. ⭐ 그 줄이 언제 어떻게 바뀌었는지는 **`git log -S"<낱말>" -- hankki/src/data/basics.js`**.
3. ⭐ 이 JSON 은 **「창업자 원문(orig)이 무엇이었나」를 확인할 때**만 쓴다 — 그게 이 파일의 진짜 값이다.

## 📌 2026-08-15 에 무슨 일이 있었나

내가 이 파일의 `ask` 여섯 개를 보고 **「창업자 확인을 못 받은 여섯 자리」** 라는 문서를 만들어 보고했다.
창업자가 *"이것도 다 고쳤었는데 · 다 적어준 기억이 나. 찾아봐"* 라고 해서 코드를 봤더니 **여섯 편 전부 반영돼 있었다.**
📄 전말 = `docs/레시피-확인받을것-2026-08-15.md`
