# 작업복기 2026-09-12 ① — 계측 전수검사와 v13.20 배포

## 무엇을 했나
GA4·Play·인스타 광고 숫자를 읽고, **계측을 전수검사해 아홉 군데를 고치고**,
새 행동 일곱을 심어 **v13.20 을 배포**했다.

## 📮 창업자 말
- *"짐작하고 단편정보만 보고 침소봉대하지말고.."*
- *"이상하게 심어놓고 침소봉대하고;; 당황스럽네"* → *"다 확인해 하나하나"* → *"다 재고 고쳐"*
- *"엉뚱하게 잘못재면 타격이 커"*
- *"폭발적이진 않지만 늘고있어 인스타 팔로워도80명넘었고"*

## ✅ 심은 것 (새 행동 일곱)
| 이름 | 언제 |
|---|---|
| `first_open` | 처음 연 사람 |
| `return_d1` | 어제 왔다 오늘 또 옴 ⭐ |
| `return_d2_7` | 2~7일 만 |
| `return_d8plus` | 8일 넘어 |
| `import_read_ok` / `import_read_fail` | 사진에서 글자를 읽어냈나 |
| `cook_done` | 요리 끝냄 |
| `diary_new` | 일기 생김 |

⛔ 재방문은 **하루 한 번**만 · **통계 끄면 한 건도 안 나간다** · 날짜가 미래면 **침묵**(지어내지 않는다).
⛔ Firebase 「로그인 날짜」는 못 쓴다 — 창업자 = *"앱을 켤때마다 로그인 새로하는 사람은 없잖아"*.

## ✅ 고친 아홉 (전부 코드를 직접 열어 확인)
| | 무엇 | 방향 |
|---|---|---|
| ⓐ | `recipe_saved` 세 길 중 하나만 세짐 | 빠짐 |
| ⓑ | `shop_added` 네 길 중 하나만 | 빠짐 |
| ⓒ | `cook_done` 이 `if (!오늘것)` 안에 갇힘 | 빠짐 |
| ⓓ | `갈래고름` 여덟 길이 안 세짐 | 빠짐 |
| ⓔ | `갈래고름` 되돌아오면 또 셈 | 부풂 |
| ⓕ | `decor_saved` 가 열었다 나가도 셈 | 부풂 |
| ⓖ | 꾸미기 둘이 `보내기()` 를 써서 화면칸을 덮음 | 화면 1건 손실 |
| ⓗ | 「사진으로 시작하기」가 빈 종이를 엶 | 동작 오류 |
| ⓘ | `import_read_ok` 가 두 번 날 수 있음 | 안전벨트 |

## ⛔ 못 잰 것 — 정직하게 남긴다
**사진 여러 장 흐름은 재현판이 못 몰았다.** 자르기 시트를 다섯 번 눌러도 편집 화면 글자 0자.
앱 탓인지 재현판 탓인지 **못 갈랐다.**
→ 코드 안전벨트(`읽기셈보냄`)로 막고, 재현판은 **소스 모양 검사**로만 잰다.
→ 🚨 **실물 확인은 창업자 몫** = 캡처 2~3장 넣어 가져오기 하고 GA4 에서 `import_read_ok` 가 **1건**인지 본다. 2건이면 다시 본다.

## ⏳ 아직 안 심은 계측 둘
- `brag_shared` (BragScreen.jsx:186)
- `pantry_added` (PantryView.jsx:102·581)

## 🧪 새 재현판 셋 (스모크에 넣었다)
- `_repro-다시옴-0912.mjs` — 11칸
- `_repro-계측전수-0912.mjs` — 10칸
- `_repro-읽기걸음-0912.mjs` — 7칸

## 🚀 배포
- 커밋 = `d1670aeb`(재현판 마무리) · `a2658a92`(v13.20)
- 로컬 스모크 = **178/178 · 457초**
- Actions run **2397** (id 34681000128) · `conclusion: success` · sha `a2658a92`
- CI 스모크 14분 24초 → deploy 잡까지 초록 → **2026-09-12 16:48 KST 배포 완료**

## 🩹 이번에 내가 틀려서 취소한 것 (남겨둔다)
- 「자연 유입 반토막」 — 노출수(1,270)를 유입으로 읽었다. 실제 설치 16건. **취소**
- 「프로필 방문 145 → 설치 15 = 10명 중 1명」 — 기간 다른 두 숫자를 나눴다. **취소**
- 「지금 광고 멈춰라」 — 멈추면 계측 데이터도 멈춘다. **취소**
- 「재방문 0」 — `session_start 47 · first_visit 34` = 재방문 세션 13. **취소**

## 🔧 이번에 배운 도구 함정
- `subst.py --count 1` 이 3군데 매칭 → **앞뒤 줄을 붙여 유일하게** 만든다 (도구가 제대로 막아줬다)
- `new URL(...).pathname` 은 한글을 %인코딩한다 → `join(ROOT, ...)` 를 쓴다
- Playwright 컨텍스트에 `serviceWorkers: 'block'` 없으면 `ctx.route` 가 한 건도 못 잡는다
- `dataLayer` 에 배열 아닌 것이 섞인다 → 길이 있는 것만 걸러낸 뒤 펼친다
- 빈 배열의 every() 는 true — **건수를 따로 세야** 빈 칸이 공짜 통과 안 한다
- 스모크 전에 **반드시 `npm run build`** (dist 가 src 보다 낡으면 게이트가 죽인다)
