# 작업복기 2026-08-10 — 9/1 검수판 · 카드 컷 게이트 · 한복 펭펭 다시 뽑기

> 배포 = **v10.22 → v10.23** ＋ 그 뒤 자산 교체 두 판
> run **1206 · 1211 · 1213 · 1215 · 1216** 전부 success

---

## 1️⃣ v10.23 — 「이번 주 픽」이 매주 저절로 바뀐다

📮 창업자 *"주부장바구니픽도 매주 꼭 바꿔줘."*

- 주차를 **에폭 기준**으로 센다(`Math.floor(Date.parse(ymd) / 604800000)`)
  ⛔ 연중 주차(1주·2주…)는 **12월 말~1월 초에 튄다** — 그래서 안 쓴다.
- 그 주 번호로 제품 목록을 **n칸씩 민다**. 레시피 재료에 걸린 제품(`matched`)은 앞에 그대로 둔다.
- 🔒 게이트 `check-weeklypick.mjs` 신설 — 26주를 훑어 ①빈 주 0 ②앞뒤 주 겹침 0 ③한 바퀴 ≥8주 ④한 주 안 중복 0

⛔ **처음엔 한 칸씩만 밀어서 4개 중 3개가 다음 주에도 그대로였다.**
   내 검사는 「통째로 똑같은가」만 봐서 **통과시켰다** → 「몇 개 겹치나」로 바꿨다.
⛔ **「26주에 20가지」 기준은 수학적으로 불가능했다** — 제품 40개 ÷ 4 = 10주면 한 바퀴다.
   기준을 「한 바퀴 ≥ 8주」로 고쳤다(재고가 늘면 저절로 길어진다).
⛔ `import.meta.glob` 은 Vite 전용이라 Node 게이트가 그 파일을 못 연다
   → 순수 로직만 `src/data/weeklypick.js` 로 갈랐다.

---

## 2️⃣ 자동 공개 달력이 «주석»을 그룹으로 읽던 것

`release-calendar.mjs` 가 「? 탭 · (이름 없음) · 0컷」 유령 줄을 하나 뱉었다.
범인 = `Stickers.jsx` 의 **주석**(`… --on 2026-09-01`)이 날짜를 갖고 있어 그룹으로 잡힌 것.

📌 이 도구는 **8/31 검수의 유일한 눈**이다 — 없는 줄을 만들면 그날 그걸 찾느라 헤맨다.
→ 주석 줄 건너뛰기 추가. 고친 뒤 **9/1 = 78컷 · 12묶음** 으로 깨끗하게 나온다.

---

## 3️⃣ 9/1 자동 공개 검수판 — 타임아웃 두 번, 둘 다 «내 판»이 범인

⛔⛔ **첫 판이 4분 타임아웃으로 죽었다. 앱이 아니라 검수판이 문제였다.**
   ⑴ `spawn` 한 로컬 서버가 노드 이벤트 루프를 붙잡아 **브라우저를 닫아도 안 끝났다**
      → 맨 끝 `srv.kill()` ＋ `process.exit(0)`
   ⑵ **9/1 첫 화면엔 「한끼 소식」 팝업이 뜬다**(정상 동작인데 안 닫고 탭을 눌렀다)
   ⑶ 꾸미기를 열면 **「출시 기념 선물」 시트**가 또 뜬다

📌 **규칙 18 그대로** — 「막혔다」의 이유를 내가 정하지 않고 **화면 글자를 읽어서** 알아냈다.
✅ 그래서 시트를 «읽고 찍고 치우는» 헬퍼를 넣었다 — 진단이자 고침이고, **그날 유저가 실제로 보는 화면**이라 캡처도 남긴다.

### 검수 결과 — 이상 없음
- 팝업이 말한 **78컷** = 달력이 약속한 **78컷** (숫자가 맞는다)
- 서랍 **62컷이 전부 화면에 떴다** (약속 ↔ 실물 키 대조)
- 깨진 그림 0 · 진짜 빈 칸 0 · `pageerror` 0
- 열 그룹 **전부 눈으로 확인** ＋ 카드 16컷 컨택트시트 ＋ `cs_b27` 의 한자 **福** 6배 판독(획 온전)

⛔ **첫 검사가 「빈 그룹 7개」라고 거짓 경보를 냈다** — `.decor-cell` 만 세었는데
   선물 줄·글자 넣기·형광펜·배경지 칸은 **다른 종류 버튼**을 그린다.
   → **진짜 빈 칸 = 버튼이 하나도 없는 칸.** 시끄러운 게이트는 죽은 게이트다.
⛔ **서랍 맨 위 한 장만 찍으면 아래 그룹이 아예 안 찍힌다** → 그룹마다 따로 찍게 고쳤다.

⚠️ 흐려 보이던 `wt_dy21` 은 고장이 아니라 **크림색 수채 올리브 마테**였다(알파 66.9%).

---

## 4️⃣ ⭐ 카드 컷 게이트가 «주석과 코드의 어긋남»을 잡았다

`cardSeasons.js` 머리말 = *"긴변 400px 미만 9컷은 카드에서 뺐다 … **cs_b02** …"*
그런데 **목록엔 `cs_b02` 가 그대로 있었다.**

**이력으로 확인한 경위**(짐작 아님)
1. **2026-07-30** — 400px 미만이라 카드 풀에서 **뺐다**
2. **2026-08-03** — 창업자가 추석 컷을 콕 집었다(*"csb25,27/ nb02 cs02 이렇게 4컷"*) → **되돌아왔다**
3. 그때 **아무도 크기를 안 쟀고**, 주석만 「뺐다」인 채로 **8/10 까지 거짓말을 했다**

📌 「명단에 적었다 ≠ 앱에 들어왔다」의 **거울상** — **「뺐다고 적었다 ≠ 진짜 뺐다」.**

🔒 `check-cardcuts.mjs` 에 **「히어로에서 몇 배로 커지나」** 를 넣었다.
⛔ **실패로는 안 만든다** — 창업자가 «보고 고른» 컷을 기계가 물면 안 된다(규칙 11).

⛔⛔ **그 게이트를 「대표값 560px」으로 재서 `cs_b02` 가 1.70 «바로 밑»으로 빠져나갔다.**
   히어로 최댓값은 **648px** 이다(`hero({…height})` 여덟 곳 실측 = 496·524·540·560·560·596·600·**648**).
   📌 **뽑기는 어느 레이아웃에 걸릴지 모른다 — 대표값이 아니라 «제일 큰 값»으로 잰다.**
   고치니 그제서야 물었다: `cs_b02` 330px → **1.96배**.

---

## 5️⃣ 한복 펭펭 다시 뽑기 — 창업자 확정 「다시 뽑자」

**다시 자르는 걸론 안 됐다** — 원본 시트가 1254×1254 인데 그 안에서 이 컷이 236×330 을 차지한다.
같은 시트를 다시 잘라도 픽셀은 그대로다. **없는 걸 만들 수는 없다.**

창업자가 새 시트를 뽑아 줬다(1089×1444) → 자르니 **829×1183**(긴변 **3.6배**).

| | 옛것 | 새것 |
|---|---:|---:|
| 원본 | 236×330 | **829×1183** |
| 카드(648px) | **1.96배 늘림** | **0.55배 줄임** |

자르기 표준 3단계 전부 통과 — 숫자(격자 0.1% · 회갈색 0.3% · 의심 0) · 진한 판 흰 잔재 0 ·
빨간 판 갇힌 흰 판 0 · 흰 테두리 `--diecut 6`(긴변 0.5%).
`PHOTO_RATIO` 0.7152 → **0.7008** 같이 고쳤다(PNG 를 바꾸면 비율도 같이).
옛 컷은 안 지우고 `_옛컷-백업-2026-08-10/` 에 뒀다.

⭐ **규칙 13 을 끝까지 지킨 판** — `hold/한복펭펭-0810` 에 담아두고 창업자 「ㄱㄱ」 뒤에 `cherry-pick` 으로 옮겼다.

---

## 6️⃣ 청소 — 최신 지도가 «거짓말»을 하고 있었다

⛔⛔ 같은 날 여러 판이 있을 때 **파일 수정 시각**으로 가르게 해놨는데,
   이 컨테이너에선 그게 **체크아웃 시각**이라 늘 동점이다.
   그러면 파일명 역순이 결정하는데 **한글이 숫자보다 뒤라**
   `…-08-09-온보딩열장`(그날 **첫** 판)이 `…-08-09-3-폴드패드`(그날 **마지막** 판)를 이겼다.

📌 **세션 브리핑이 계속 틀린 「최신」을 들이밀고 있었다** — 규칙 12 를 지키라고 만든 장치가 거짓말을 하면 규칙보다 나쁘다.

✅ **우리 이름 관례를 코드가 읽게 했다** — 번호 없음 = 그날 첫 판 · `-2`·`-3` = 그 뒤 판.
   ⚠️ 날짜 «바로 뒤» 숫자만 판으로 본다(제목 속 `AAB13`·`낱개24` 에 걸리면 안 된다).
   고치기 전후를 둘 다 돌려 실제로 뒤집히는 것 확인.

＋ **미사용 import 전수 검사 = 0건**(앱 코드 깨끗)
  ⛔ 첫 스캔은 **15건**이라 했는데 **내 검사가 틀린 것**이었다 —
     `replace('as',' ')` 가 `basicRecipes` 를 `b`＋`icRecipes` 로 쪼갰다. 낱말 경계로 다시 재니 0.
＋ 작업복기 넷 중 오래된 둘을 `_archive/` 로(최근 것만 위에)
＋ 루트 `tools/` 는 **잔재가 아니다** — `hankki/scripts` 가 git 뿌리 기준으로 부른다. 그대로 둔다.

---

## 7️⃣ 📥 창업자 레시피 원본 도착 (한글 파일)

📮 창업자 *"나 레시피 정리해놓은거 있는데 친절하지는 않아. 내가 보면서 요리하던거라.
   근데 이거 일일이 적기가 너무 힘든데 네가 나만 저장할 수 있게 앱에 올려줄 수 있어?"*

**126줄 · 7,165자 · 여섯 갈래 · 100편 넘음**
(소스·양념장 33 · 밥면덮밥 19 · 고기 24 · 해물 10 · 국탕찌개 10 · 반찬기타 13)

⭐ CLAUDE.md 핀의 *"창업자의 «안 푼» 레시피"* 가 **실물로 도착한 것**이다.

### 넣는 법 = 백업 파일 (코드 변경 0)
⚠️⚠️ **불러오기는 «덮어쓴다»** — `ProfileScreen.jsx:183` *"불러오면 지금 데이터가 이 백업으로 바뀌어요"*
   → 그냥 새 파일을 불러오면 **기존 레시피·일기·장보기가 다 날아간다.**

✅ **안전한 순서** = ①창업자가 지금 백업을 파일로 저장해 준다 → ②내가 거기에 **더해서** 새 파일을 만든다 → ③불러오기
   백업엔 `recipes`·`folders`·`diary`·`shops`·`shoppingList`·`pantry`·`profile` 이 다 들어 있어 그대로 보존된다.

### 정한 것
- **양 표기는 원문 그대로** — 「간장 3」처럼 단위가 없는 게 많은데 큰술인지 작은술인지는 **창업자 손에만 있다.** 채우면 틀린다(규칙 15).
- **소스 33개도 각각 한 편씩** — 그래야 「골뱅이」 로 검색해서 바로 찾는다.
- ⏳ **저장소 보관 여부는 창업자 판단 대기** — 이 저장소는 **공개**다. 「아직 안 푼」 재고를 지금 올리면 누구나 본다.

⏳ **막힌 것 = 창업자 폰 백업 파일 하나.** 그게 오면 바로 만든다.

---

## 8️⃣ 오늘 배운 것 한 줄씩

1. **「막혔다」의 이유를 내가 정하지 말 것** — 화면 글자를 읽으면 답이 거기 있다(팝업 두 개).
2. **대표값이 아니라 «제일 큰 값»으로 잰다** — 뽑기는 어느 레이아웃에 걸릴지 모른다.
3. **주석이 「뺐다」고 해도 코드를 본다** — 주석과 목록은 따로 낡는다.
4. **장치가 거짓말하면 규칙보다 나쁘다** — 최신 지도가 석 달째 틀린 최신을 가리키고 있었다.
5. **내 검사가 0이 아닌 값을 뱉으면 검사부터 의심한다** — 미사용 import 15건은 전부 내 정규식 탓이었다.
6. **없는 픽셀은 못 만든다** — 다시 자르기로 안 되는 건 다시 뽑아야 한다.
