# 🔐 로그인 화면 그림 — 창업자 2026-08-23

📮 창업자 = *"이거 내일 표준도구로 잘라서 로그인화면에 넣어줘. 내일해도 돼."* (2026-08-23 00:20 KST)

## 🖼 원본

| 파일 | 크기 | 비고 |
|---|---|---|
| `원본시트/로그인-곰펭-2026-08-23.webp` | 1369×1149 · 151KB | **창업자가 보낸 그대로** |
| `원본시트/로그인-곰펭-2026-08-23.png` | 1369×1149 · 886KB | 자르기 도구용으로 바꾼 것 (내용 동일) |

**무엇이 그려져 있나** — 꼬르곰(셰프모자 · 계란프라이 주머니 앞치마 · 하트 나무주걱 · 손 흔듦)과
펭펭(베레모 · 트렌치코트 · 리본 · 손 흔듦 · 무표정).

⛔ **채팅 첨부는 세션이 끝나면 날아간다.** 그래서 받은 날 바로 저장소에 담았다(절대원칙 20).
   ⭐ 2026-08-19 에 같은 이유로 시트를 잃을 뻔했다 — 그날은 대화 기록에서 겨우 꺼냈다.

## 🔢 미리 재둔 것 (내일 헤매지 말라고)

| | 값 |
|---|---|
| 배경 | **순백에 가깝다** (네 모서리 254,254,254) — 자르기 도구 전제와 맞는다 |
| 거의 흰 픽셀 | 57.8% |
| 그림 덩어리 | **둘로 갈린다** — 가로 74~792(곰) · 815~1265(펭) · 사이에 **22px 빈 띠** |

## ⚠️ 내일 «먼저 물어야» 하는 것 — 한 덩어리냐 낱개 둘이냐

⛔ 창업자가 말한 「표준 도구」 = `hankki/tools/cut.py` 인데, **그건 낱개로 «쪼갠다».**
   위 실측대로 사이에 빈 띠가 있어서 **그대로 돌리면 곰 하나 · 펭 하나로 갈라진다.**

📌 **이미 겪은 자리다** — 2026-08-20 홍보물 5인 라인업.
   > `tools/배경빼기-한장.py` — ⛔ `tools/cut.py` 와 다른 일이다. cut.py 는 낱개로 쪼갠다.
   > **5인 라인업처럼 한 덩어리로 써야 하는 그림은 쪼개면 다섯으로 흩어진다.**

   → 로그인 화면에 **둘이 나란히 인사하는 한 장**으로 넣을 거면 `배경빼기-한장.py` 가 맞고,
     **따로 배치**할 거면 `cut.py` 가 맞다. ⛔내가 정하지 않는다(규칙 11).

⛔ **자르기 도구는 손대지 않는다** (창업자 확정 2026-08-18 *"자르기도구는 건드리지마."*)
   못 자르는 시트를 만나도 도구를 고쳐서 풀지 않는다. 입력을 바꾸거나 창업자에게 묻는다.

## 🚨 그리고 더 큰 것 — **로그인 화면이 아직 «없다»**

🔢 실측 = `src/` 어디에도 로그인 화면이 없다. 오히려 앱이 스스로 이렇게 말하고 있다:

> `ProfileScreen.jsx:632` — *"한끼는 **로그인이 없어요.** 모든 게 이 기기 안에만 있어서…"*
> `CLAUDE.md:148` — *"⛔ **아직 «앱 안»에는 없다** — 지금은 로그인이 0이라 Play 가 요구하지 않는다."*

⭐ 그러니 이 그림이 들어갈 자리는 **클라우드 저장(구글 로그인)을 만들 때 같이 생긴다.**
   ✅ 순서 = ① 그림 자르기(지금 가능) → ② 로그인 화면 만들기 → ③ 거기에 얹기
   ⛔ ②는 🎨앱 세션·클라우드 갈래 일이다. 나(🏛바깥 갈래)는 ①까지.
   📌 그래서 **자른 컷을 자산으로 먼저 담아두는 것**이 맞다 — 화면이 생기는 날 바로 얹는다.

## 🛠 내일 시작할 때 — 이것부터

⛔ **이 컨테이너엔 PIL 이 없다**(되감기며 날아갔다). 자르기 도구가 그 자리에서 죽는다.

```
pip install pillow numpy scipy
```

📌 2026-08-18 에 `npm ci` 로 같은 일을 겪었다. 되감기면 또 없어진다 — 죽으면 이 줄을 보고 다시 깐다.

## 🪤 ＋ 오늘 걸린 훅 거짓 표식 하나 (내일 고칠 것)

⛔ **자른 게 하나도 없는데** `cut-guard` 가 커밋을 막았다.
   🔢 실측 = 이 세션에서 자르는 도구를 부른 적 0회 · 오늘 만들어진 `_검수/` 폴더 0개.
   그런데 `/tmp/hankki-cut-unseen` 표식이 찍혀 있었다.

📌 **뿌리는 훅이 자기 머리말에 «이미 적어둔» 그것이다** —
   > *"명령과 「명령을 적은 글」은 다르다"* (2026-08-13 거짓 표식 · heredoc 본문 때문에)

   그때는 heredoc 을 잘라내서 막았는데, **오늘은 다른 자리로 새어 들어왔다.**
   ⚠️ 어느 명령이 찍었는지까지는 **못 밝혔다** — 짐작을 사실로 적지 않는다.
   ⭐ 다만 확실한 것 = **자른 것이 없으니 표식은 틀렸다.** 그래서 지우고 커밋했다.

✅ 내일 할 일 = 표식을 찍는 조건을 「글자에 이름이 보이나」가 아니라
   **「그 도구가 «진짜로» 돌았나」**(자른 폴더·`_검수/` 가 새로 생겼나)로 바꾼다.
   ⛔ 느슨하게 하는 게 아니다 — 오히려 «진짜 자른 것»은 지금보다 확실하게 잡힌다.
   ⛔ 그리고 **자르기 도구 자체는 안 건드린다**(창업자 확정 2026-08-18). 고칠 것은 훅뿐이다.
