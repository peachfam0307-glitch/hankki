# 🦀🍚 음식 아이콘 — 창업자 2026-08-18 (2컷)

> 📮 창업자가 **9월 검수 도중에** 바로 뽑아 보냈다. *"아 다시뽑아야 겠네 이건 그냥 찜이라서"* (05:57) → 몇 시간 만에 도착.

| 키 | 요리 | 크기 | 비율 | 창업자 라벨 원문 |
|---|---|---|---|---|
| `fe_306` | **꽃게간장조림** | 795×728 | 1.0920 | 「양파가 위에 올라간 꽃게간장조림」 |
| `fe_307` | **나물비빔밥** | 769×733 | 1.0491 | 「일반나물비빔밥」 |

## ⭐ 이 두 컷이 «푸는» 것

- **`fe_306`** — 꽃게찜을 어머니 레시피 「꽃게간장조림」으로 갈면서 **찜 그림이 안 맞게 됐다.**
  창업자가 스스로 알아채고(*"이건 그냥 찜이라서"*) 조림 컷을 뽑았다.
- **`fe_307`** — ⛔ 「나물비빔밥」이 `ICON_RULES` 의 **「비빔밥」에 걸려 `fh_k01`(돌솥비빔밥)** 을 쓰고 있었다.
  **둘 다 9월에 열리는데 카드 그림이 똑같았다.** 이제 갈라진다.

## ✂️ 자른 방법 — ⚠️ 라벨 지우개가 «막았다»

⛔ `tools/시트-라벨지우기.py` 를 먼저 돌렸더니 **그림을 7181px 먹어서 도구가 스스로 멈췄다**
   (「⛔⛔ 그림을 7181px 먹었다 — 저장하지 않는다」). **안전장치가 제대로 값을 했다.**

⭐ 원인 = **왼쪽 라벨이 «두 줄»**(819~873 · 884~934)이라 첫 줄이 그림 띠(도구 판정 0~882) 안으로 들어갔다.

✅ **자르기 도구는 «손대지 않고»**(창업자 확정 2026-08-18) **입력을 바꿔서 풀었다** —
   라벨 아래를 통째로 버리고 그림만 남겼다.

🔢 실측으로 자를 자리를 정했다(짐작 안 함):

| | 그림이 있는 줄 | 라벨 시작 | 사이 빈 띠 |
|---|---|---|---|
| 칸1 (꽃게) | 113 ~ **791** | 819 | 792~818 (27줄) |
| 칸2 (비빔밥) | 133 ~ **809** | 841 | 810~840 (31줄) |

→ **높이 818 로 crop** = 라벨 통째 제외 · 칸2 그림 바닥 아래 8px 여백 남김(가장자리에 안 닿게)

```bash
# ① 라벨 아래를 버린다 (818 = 칸1 라벨 시작 819 바로 앞)
# ② 표준 도구로 자른다 — ⛔다른 컷 도구 금지
python3 tools/cut.py <라벨없는시트> <낼폴더> fe_3 --diecut 8
```

⚠️ **`--diecut 4` 는 도구가 막았다** — 0.25% 로 규칙(0.35~1.30%) 밖. 시트 긴변 1536 기준 0.5% ≈ 8px 이 맞다.

## ✅ 검수 (절대원칙 5가지)

| | 결과 |
|---|---|
| ①투명판 | 구석 알파 0 · 진한 판·빨간 판 확인 |
| ②잘림 | **가장자리 1px 에 alpha>40 = 0개** (두 컷 다) |
| ③해상도 | 795·769px — 음식 아이콘 표시 크기보다 훨씬 크다 |
| ④비율 | `PHOTO_RATIO` 에 실측값 그대로 (1.0920 · 1.0491) |
| ⑤앱 렌더 | `npm run smoke` exit 0 · 게이트 `check-foodtab`·`check-cutouts`·`check-diecut`·`check-sticker-res`·`check-iconlabels` 전부 통과 |

⭐ 격자 찌꺼기 0.0% · 회갈색 잔재 0.0% · 떨어진 조각 0개

## 🔒 게이트에 못 박은 것 (`check-foodtab` 못박기)

「구체어 먼저」 함정이 **셋**이었다 — 규칙에 넣기 «전»에 재서 확인했다(규칙 12):

| 이름 | 먹히던 그림 | 왜 |
|---|---|---|
| 꽃게간장조림 | `fe_34` 장조림 | 「장조림」이 더 짧다 |
| 꽃게조림 | `fe_66` 목살조림 | 「조림」이 더 짧다 — **이건 예상 못 했다** |
| 나물비빔밥 | `fh_k01` 돌솥비빔밥 | 「비빔밥」이 더 짧다 |

＋ 반대 방향도 못 박았다 — 「장조림」·「돌솥비빔밥」이 **안 깨지는지**(새 규칙이 옛것을 밀어내지 않게).

## 📁 파일

- 원본 시트 = `원본시트/시트1-꽃게간장조림-나물비빔밥.png` (1536×1024 · 라벨 포함)
- 낱개 = `낱개/fe_306.png` · `낱개/fe_307.png`
- 검수판 = `_검수/` (진한판·빨간판·실제크기)
- 앱 = `src/assets/stickers/photo/fe_306.png` · `fe_307.png`
