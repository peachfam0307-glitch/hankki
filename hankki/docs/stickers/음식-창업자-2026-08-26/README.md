# 🍽 음식 컷 — 창업자 2026-08-26 (108컷 · 18시트)

⭐ **새 세대 접두어 = `gr_`(그릇)** — 창업자 = *"새로자른건 이름을 다르게 붙여야 안헷갈리지..ㅠ"*
   ⛔ 그 전엔 새 컷을 `fe_389~494` 로 **옛 컷과 같은 접두어에 이어 붙여서** 파일명만 봐선 세대를 알 수 없었다.
   📌 그래서 창업자가 화면에서 *"새로 자른것이랑 옛날게 섞여있는데??"* 를 겪었다.

## 자른 방법
```
python3 tools/시트-라벨지우기.py <시트> <낼파일> 2 3          # 라벨 6개 · 그림 손실 0px
python3 tools/cut.py <라벨지운시트> <낼폴더> <접두어> --diecut auto --drop 0.001
```
⛔ **`--drop 0.001` 이 반드시 필요하다** — 기본값 0.12 는 접시 테를 「떨어진 조각」으로 보고 뜯는다.
   `0` 도 안 된다(먼지가 남아 「떨어진 조각 0개」 절대원칙에 걸린다).

## 결과
- 18시트 × 6 = **108컷** · 자르기 게이트 **18/18 통과**
- 고유 요리 **95개** · 겹치는 이름 11개(같은 요리를 두세 판 뽑았다)
- 눈으로 전수 대조 = **108/108 이름표 ↔ 그림 일치** (2026-08-26)

## 겹치는 이름 11개 — 어느 판을 픽커에 올릴지
같은 요리가 두세 시트에 있다. ⛔파일은 전부 남긴다(픽커에서만 하나를 고른다).
`3f0d5f41` 시트는 접시 테가 얇고 색이 달라 **다른 세대**다 — 겹치는 6컷 전부 다른 판을 골랐다.
