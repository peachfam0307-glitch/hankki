# ⛔ 못 쓰는 1차 컷 (2026-08-12) — 쓰지 말 것

통째로 `cut.py` 에 넣어 자른 판이다. **개수가 안 맞고 캡션이 안 딸려왔다.**

- 낱개2 = 34컷 (16컷이어야 하는데 덩어리로 갈라짐)
- 낱개3 = 31컷 · rgA14 에 「별점 높음」＋「별점 최고」 **두 컷이 한 덩어리**
- 캡션(글자) 없음

⭐ 쓸 것 = `../낱개-최종/` (칸을 하나씩 잘라 **16컷 정확** ＋ 글자있음·글자없음 두 판)

⛔ 지우지 않고 남기는 이유 = 같은 실수를 또 하면 여기를 열어 «무엇이 달랐는지» 본다.
