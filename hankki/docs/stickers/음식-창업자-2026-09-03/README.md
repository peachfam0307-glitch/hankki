# 🍱 음식 4컷 — 창업자 2026-09-03

> ⏳ **잘라만 뒀다. 앱엔 «아직» 안 들어갔다.**
> 📮 서 있는 지시 = *"음식은 잘라만 두고 **한번에 적용한다**"* (창업자 2026-08-24)

## 🔢 넉 장

| 열쇠 | 무엇 | 크기 |
|---|---|---|
| `n2701` | 꽈리고추 멸치볶음 | 567×510 |
| `n2702` | 계란후라이간장조림 | 566×507 |
| `n2703` | 광어깻잎무침 | 556×498 |
| `n2704` | 보쌈무김치 | 560×490 |

## ✅ 창업자 검수 = 통과 (2026-09-03 · *"됐엉"*)

원본 픽셀 **100%** 판(진한 판·밝은 판)으로 보고 받았다.

⛔⛔ **그 전에 «줄인 판»을 보냈다가 창업자가 *"왜 화질이 뿌옇지?"* 라고 짚었다.**
   🔢 실측 = 컷 실물 `n2701` 은 **567px** 인데 내가 보낸 판에선 **200px** — 3분의 1로 줄어 있었다.
   ⭐ 컷은 멀쩡했다 — 선명도 **2412** 로 원본 시트(1483)보다 오히려 쨍하다.
   📌 규칙 13 이 **「줄인 판을 보고 판정하지 말 것 — 줄이면 잔재도 같이 줄어 안 보인다」** 고
      못 박아뒀는데 내가 그 줄인 판을 보냈다. **검수판은 무조건 원본 픽셀 100%.**

## ✂️ 어떻게 잘랐나 — 차림표 그대로

```
python3 tools/시트-라벨지우기.py <시트> <라벨없음> 2 2
python3 tools/시트-둘레-지우기.py <라벨없음> <깨끗> 2 2
python3 tools/cut.py <깨끗> <낱개> n27 --diecut auto --drop 0
```

⭐ 이 시트는 **「흰 그릇 ＋ 흰 배경」 = 자르기 최악 조합**이다(30컷 중 16컷이 깨졌던 그 경우).
   그런데 넷 다 깨끗하게 나왔다 — **`--drop 0`** 이 값을 했다.
   ⛔ `--drop 0.001` 도 안 된다(2026-08-27 실측 = 손잡이 272px 을 먹었다).
   ⛔ `tools/칸-접시테두리-그리기.py`(길잡이 선)는 **안 쓴다** — 흰 테가 레이스처럼 물결친다.
      (핀 1470줄에 「길잡이 선을 그린다」가 남아 있는데 **낡은 세대**다 · 1505줄이 현행)

🔢 라벨 지우기 = 4개 지움 · **그림 손실 0px**
🔢 둘레 지우기 = 657,189px(빛무리) · 몸통 먹힘 0.9~1.3%

## 🔍 실물 확인

진한 판 · 밝은 판 · 실제 크기 — **셋 다 열어서 봤다**(절대원칙 21).
접시 테두리 안 파먹힘 · 흰 테 고르게 붙음 · 떨어진 조각 0.

## ⏭ 앱에 넣을 때

⚠️ **잘라두고 잊는 것이 진짜 위험이다** — `src/data/curation.js:353` 에 그 자국이 있다:
   *"«한 컷도» 앱에 안 들어가 있었다 — 2주 넘게. 그날 잘라만 두고 끝난 것이다."*

넣을 때 지킬 것 —
- ⭐ 열쇠(`n27**`)는 그대로 두고 PNG 만 갈아끼운다(이미 저장된 레시피가 저절로 새 그림을 받는다)
- 📐 `PHOTO_RATIO` 를 다시 잰다(PNG 를 갈면 비율도 바뀐다)
- 🔢 **`BASICS_VERSION` 올리기** — 안 올리면 «이미 깔린 폰»에 안 간다
- 🏷 `FoodIcon.jsx` 이름 규칙에 네 이름을 더한다(꽈리고추 멸치볶음·계란후라이간장조림·광어깻잎무침·보쌈무김치)
