# 🔒 결제 서버 — 「진짜 리스크 없이」 가기 위한 조사 (2026-08-19)

> ⭐ **이 주제는 이 문서가 현행이다.** (앞 판 = `docs/결제-준비상태-2026-08-07.md` · `docs/클라우드＋결제-한몸설계-2026-08-16.md`)
>
> 📮 창업자 = *"우리서버하는거 **너믿고하는거니까 진짜 리스크 없이 가야해.** 다른 우리같은 앱들이 어떻게 하는지도 조사해보고 **확인에 확인하고 가자.**"*
> 📮 → *"요즘은 대부분 코드로 앱을 만드니까 우리랑 비슷한케이스일거아냐. 그래도 다 유료판매하고 하는거니까. **구체적으로 조사해봐.**"*
>
> ⛔ **코드를 쓰기 «전»에** 만든 문서다(최우선 작업원칙 = 리서치 먼저).

---

## 0️⃣ ⭐⭐⭐ 제일 큰 소득 — **구글 «공식 샘플»이 우리와 «같은 구조»다**

`github.com/chromeos/pwa-play-billing` — 구글 크롬OS 팀이 만든 공식 샘플.
설명 = *"PWA 를 **TWA 로 Play 스토어에 게시**하고 **Digital Goods API ＋ Payment Request API** 로 Play Billing 결제를 받는 법"*

📌 **우리 앱 구조와 한 글자도 안 다르다.** 짐작으로 설계할 필요가 없다 — 참고할 «정답지»가 있다.

⚠️ 다만 **백엔드는 Firebase 를 쓴다**(Cloud Functions ＋ Firestore ＋ Pub/Sub).
   원문 = *"The sample uses Firebase … **However, you can use any other service**"*
   → 우리는 **Cloudflare Worker ＋ D1**. 절차는 같고 부품 이름만 다르다.

---

## 1️⃣ 🚨 **일정에 영향 있는 것 — 권한 전파에 «최대 24시간»**

공식 샘플이 적은 서비스 계정 5단계:

| | 무엇 | 어디서 |
|---|---|---|
| 1 | 서비스 계정 생성 ＋ **Service Account User** 역할 | Google Cloud |
| 2 | **Play Console 「사용자 및 권한」에서 그 서비스 계정 이메일을 초대** | ⛔**Play Console** |
| 3 | **금융 데이터 접근 권한** 부여 | Play Console |
| 4 | 🚨 **권한 전파 대기 — 최대 24시간** | 기다림 |
| 5 | 개인 키 생성 → 서버 비밀값에 | Google Cloud → Worker Secret |

⭐⭐ **4번을 몰랐다.** 승인 나고 나서 시작하면 **거기서 하루가 더 든다.**
   📌 그래서 「승인 당일에 다 된다」가 아니다 — **2·3번은 «먼저» 해둘 값어치가 있다.**
   ⚠️ 단 2번은 **Play Console 을 건드리는 일**이다. 재심사·소유권 이전 자격에 영향이 있는지는 **확인 못 했다.**
      ⛔ 그래서 «내가 정하지 않는다» → 아래 3️⃣ 갈래에서 창업자 판정.

---

## 2️⃣ 🩹 **진짜 무서운 자리 — 「돈은 냈는데 못 받는다」**

### 무슨 일이 나나

```
① 유저가 결제        → 구글이 돈을 받는다 · 앱이 purchaseToken 을 받는다
② 앱 → 우리 Worker   → 검증 · D1 기록 · acknowledge
   ⚠️ ②가 실패하면?  (Worker 다운 · 유저 네트워크 끊김 · 앱 강제종료)
   → 유저는 돈을 냈는데 팩을 못 받는다
   → ⛔ 그리고 3일 안에 acknowledge 를 못 하면 **구글이 자동 환불하고 회수한다**
```

### ✅ 막는 법 — **코드가 아니라 «설계»가 막는다**

⭐⭐⭐ **구매는 안 사라진다.** 실패해도 그 구매는 `listPurchases()` 에 그대로 남아 있다.
   → **앱이 켜질 때마다 `listPurchases()` 를 훑어서 «아직 우리 원장에 없는 것»을 다시 보낸다.**
   → 유저가 다음에 앱을 한 번만 열면 저절로 복구된다.

📌 이건 내가 지어낸 게 아니라 **표준 패턴**이다 —
> *"retry policy in the app and **resending all not yet acknowledged purchases** associated with the current Google Play Account"*

✅ **`acknowledge` 는 idempotent** — 여러 번 불러도 안전하다. 그래서 «의심되면 그냥 다시 보낸다»가 성립한다.
✅ 네트워크 오류엔 **백오프 재시도**. ⛔단 유저가 보고 있는 구매 흐름 «중»에는 재시도로 붙잡지 않는다(멈춘 것처럼 보인다).

### 🔢 3일이 얼마나 빡빡한가 — **안 빡빡하다**
유저가 3일 안에 앱을 **한 번만** 열면 된다. 우리 앱은 요리하며 여는 앱이라 그 조건이 쉽다.
⚠️ 그래도 **0%는 아니다** — 사고 뒤 3일 내내 앱을 안 여는 사람이 있으면 그 사람은 환불된다.
   ⭐ 그때도 **돈은 유저에게 돌아간다**(우리가 떼먹는 게 아니다). 잃는 건 매출 한 건과 신뢰다.

---

## 3️⃣ 🗄 **잔량이 날아가면? — 구조가 이미 반쯤 지켜준다**

| 파는 것 | 어디에 기억되나 | 우리 D1 이 날아가면 |
|---|---|---|
| 🎀 **꾸미기 팩 5개**(영구) | ⭐**구글이 기억한다** — `listPurchases()` 에 영영 남는다 | ✅ **저절로 복원된다** (앱이 구글에 다시 물어본다) |
| 🔍 **AI 스캔 20장**(소모성) | `consume` 하는 순간 구글에서 사라진다 → **남은 장수는 우리 D1 에만** | ⛔ **여기만 위험하다** |

### ✅ 그 하나도 안전망이 있다 — **D1 「Time Travel」**
공식 = **항상 켜져 있고 · 무료 플랜은 7일 시점 복구 · 추가 비용 0 · 따로 켤 것도 없다.**
📌 즉 «백업을 잊어서» 잃는 일은 구조적으로 안 난다. 7일 안에 알아채면 되돌린다.
⭐ ＋ 우리 원칙(내보내기 경로를 늘 유지)대로 **주기적 export** 도 붙일 수 있다(D1 은 내보내기 요금 0).

---

## 4️⃣ 💸 **환불을 우리가 모르면? — 지금은 폴링으로 충분하다**

구글 공식 샘플은 **RTDN(실시간 개발자 알림)** 을 Pub/Sub 로 받는다.
⛔ 우리는 Cloudflare 라 Pub/Sub 를 그대로 못 쓴다(HTTPS 푸시로 받을 순 있지만 부품이 하나 더 는다).

✅ **대안 = `Voided Purchases API` 를 가끔 폴링**(지피티 제안).
   우리 규모(수십~수백 명)면 **하루 한 번으로도 넘친다.**
⚠️ **정직하게 — 이게 충분한지 실측 못 했다.** 다만 못 잡아도 손해는 «환불된 사람이 팩을 조금 더 쓰는 것»이라 작다.
📌 사람이 늘면 그때 RTDN 을 붙인다. **지금 붙이면 안 쓰는 부품이 하나 는다.**

---

## 5️⃣ ⚖️ **그래서 우리 리스크 표 — 하나씩 이름 붙였다**

| # | 리스크 | 얼마나 | 막는 법 | 상태 |
|---|---|---|---|---|
| 1 | 돈 나감(청구) | ⭐**0** | **카드를 안 건다**(Workers·D1 무료·초과하면 청구 대신 막힘) | ✅이미 |
| 2 | 산 사람이 3일 뒤 팩을 잃음 | 🚨**큼** | 서버 `acknowledge` ＋ **앱이 켜질 때 미확인 구매 재전송** | 🔨만든다 |
| 3 | 돈 냈는데 못 받음(그 순간 실패) | ⚠️중간 | 위와 같은 재전송 · idempotent 재시도 | 🔨만든다 |
| 4 | 잔량이 날아감 | ⚠️작음 | 영구 상품은 구글이 기억 · 소모성은 **D1 Time Travel 7일** ＋ export | ✅구조적 |
| 5 | 같은 영수증으로 두 번 충전 | ⚠️중간 | `purchaseToken` **UNIQUE** ＋ 트랜잭션 | 🔨만든다 |
| 6 | 유저가 잔량을 조작 | ⚠️중간 | **서버가 원장**(클라이언트 값 안 믿는다) | 🔨만든다 |
| 7 | 환불을 우리가 모름 | 🟢작음 | Voided Purchases 폴링(나중에 RTDN) | ⏳나중 |
| 8 | 서비스 계정 키 유출 | ⚠️중간 | **Worker Secret** · 로그에 키·토큰 안 남김 · 새면 GCP 에서 교체 | 🔨만든다 |
| 9 | 권한 전파 24시간 | 🟡일정 | ✅**승인 뒤 하루를 계산에 넣는다**(창업자 확정 ⓑ) | ✅확정 |

---

## 6️⃣ ⏳ **창업자 판정이 필요한 것 — «하나»뿐**

> ✅✅ **[창업자 확정 2026-08-19] ⓑ — 「서비스 계정 초대」는 «승인 뒤»에 한다. ⛔재론 금지**
> 📮 창업자 = *"b"*  (＋ *"진짜 리스크 없이 가야해"* 가 이 판정의 뿌리다)
> ⭐ 그래서 **지금 Play Console 은 한 손가락도 안 댄다.** 권한 전파 24시간은 «승인 뒤 하루»로 계산에 넣는다.
>
> 🚨 (아래는 판정 근거 — 기록으로 남긴다)

| | ⓐ 지금 초대 | ⓑ 승인 뒤 초대 |
|---|---|---|
| 좋은 점 | 권한 전파 24시간을 **미리 태운다** → 승인 나면 바로 검증 가능 | 재심사·소유권 이전에 **아무 영향이 없는 게 확실하다** |
| 걸리는 점 | ⚠️ 재심사·소유권 이전 자격에 영향이 있는지 **확인 못 했다** | 승인 뒤 **하루가 더 든다** |

⭐ **내 추천 = ⓑ (승인 뒤).**
   근거 = ⑴우리가 지금 지키는 제일 큰 원칙이 *"되돌릴 수 없는 일을 모르는 채로 하지 않는다"* 이고
   ⑵하루는 **잃어도 되는 값**이다(어차피 클라우드·팩 판매는 출시 뒤 일정이다)
   ⑶⛔ **모르는 게 남으면 창업자를 움직이지 않는다**(규칙 15).
⏳ **판정은 창업자.** ＋ 정 궁금하면 구글 지원팀에 물을 수 있다(도장 게이트 통과 필요).

---

## 7️⃣ 🔨 **그래서 지금 만들 것 — 코드만, 배포 없이**

| | 무엇 | 왜 지금 되나 |
|---|---|---|
| ① | **Worker 에 결제 모듈** — 서비스계정 JWT(RS256) → 토큰 → `purchases.products.get`(검증) → `acknowledge` / `consume` | Cloudflare 쪽이라 **앱·AAB·신고를 안 건드린다** |
| ② | **D1 스키마** — `purchase_tokens`(UNIQUE) · `balances` · `entitlements` | 〃 |
| ③ | **앱 쪽 「미확인 구매 재전송」 설계** | ⛔코드는 쓰되 **배포는 창업자 신호 뒤**(규칙 9) |

⛔ **못 하는 것 = 「진짜 도는지」 확인.** 구매 토큰이 있어야 하는데 그건 **BILLING AAB ＋ 상품 등록** 뒤에만 나온다.
   📌 그래서 이 단계의 결과물은 **「코드」이지 「검증된 것」이 아니다.**
   🔒 **판매 스위치는 «검증»까지 끝난 뒤에만 켠다** — 이 구분이 이 문서에서 제일 중요한 줄이다.

---

## 8️⃣ ⚠️ **아직 모르는 것 (짐작으로 안 채운다)**

| | 모르는 것 | 행동이 바뀌나 |
|---|---|---|
| ⒜ | 권한 전파가 실제로 몇 시간인지(최대 24시간만 안다) | 🟡 6️⃣ 판정에 걸린다 |
| ⒝ | 서비스 계정 초대가 재심사·이전 자격에 영향을 주나 | 🟡 6️⃣ 판정에 걸린다 |
| ⒞ | Voided Purchases 폴링만으로 환불을 충분히 잡나 | ⛔안 바뀐다 — 못 잡아도 손해가 작다 |
| ⒟ | 우리 Worker 무료 한도(하루 10만 요청)에 결제가 얼마나 먹나 | ⛔안 바뀐다 — OCR 이 이미 훨씬 많이 쓴다 |

### 📚 근거
- 구글 공식 샘플 `github.com/chromeos/pwa-play-billing` (README 원문을 직접 열어 읽었다)
- Cloudflare D1 Time Travel 공식 문서 · Workers/D1 요금
- RevenueCat 엔지니어링 블로그(Play Billing 오류 처리·엣지 케이스)
- 우리 저장소 = `docs/클라우드저장-AI답변-원문-2026-08-19.md`(지피티·페이블 절차) · `docs/구글에-물어볼것-결제-2026-08-16.md` 7️⃣
- ⚠️ `developers.google.com` 은 이 환경이 **막는다**(EGRESS_BLOCKED · 규칙 15) — GitHub raw 로 우회해 원문을 읽었다

---

# 9️⃣ ✅✅ **[2026-08-19 · 만들었다] 코드 완성 — ⛔아직 «켜지지» 않았다**

> ⚠️ **[옛 세대] 아래 9️⃣ 는 「만들었다」까지다** — 그 뒤 🔟 에서 **구멍 여섯**을 더 찾았다. **🔟 이 현행이다.**
> 📮 창업자 = *"우리서버하는거 너믿고하는거니까 **진짜 리스크 없이 가야해**"* → 그래서 **가짜 구글·가짜 D1** 을 세워
>    우리 코드를 그대로 돌려 봤다. 「코드 다 짰다」로 넘기지 않았다.

## 9️⃣-1 무엇이 생겼나

| 파일 | 무엇 |
|---|---|
| `ocr-proxy/worker.js` | 💳 결제 길 두 개(`/billing/sync`·`/billing/state`) ＋ 서비스계정 JWT(RS256) ＋ 검증·acknowledge·consume ＋ 잔량 원장 |
| `ocr-proxy/schema.sql` | D1 표 셋 — `purchases` · `credits` · `entitlements` (⭐만드는 법이 파일 맨 위에 적혀 있다) |
| `src/proxy.js` | 🔌 **새 잎사귀 모듈** — 주소·앱토큰·기기번호·남은 장수. OCR 과 결제가 «같은 것»을 쓰게 한다 |
| `src/billing.js` | 🔁 `syncPurchases()` = **미확인 구매 재전송**(＋`purchaseHistory()` 되짚기) ＋ 산 직후 자동 확인 |
| `src/main.jsx` | 앱을 켤 때마다 재전송 한 번(3초 뒤 · 첫 화면을 안 늦춘다) |
| `scripts/_repro-결제서버-0819.mjs` | 🧪 재현판 **41칸** — smoke 에 물렸다(⑪은 앱 쪽까지 본다) |

## 9️⃣-2 ⭐ 5️⃣ 의 위험 아홉에 «각각» 무엇을 했나

| | 위험 | 지금 |
|---|---|---|
| ① | acknowledge 를 안 해 **3일 뒤 환불·회수** | ✅ 산 즉시 ＋ 앱 켤 때마다 재전송. 구글이 「이미 했다」면 안 부른다 |
| ② | 살 때 인터넷이 끊겨 **돈만 내고 못 받음** | ✅ `listPurchases()` 를 통째로 다시 보낸다 → 따라붙는다 |
| ③ | 같은 구매를 두 번 보내 **장수를 두 배로** | ✅ 토큰이 기본키(멱등) — 재현판 ④가 지킨다 |
| ④ | 앱이 «샀다»고 거짓말 | ✅ **구글에 직접 묻는다.** 가짜 토큰·취소된 구매는 장수를 못 받는다(재현판 ⑨) |
| ⑤ | **돈 냈는데 좋은 인식을 못 씀** | ✅ OCR 이 막기 «전»에 산 장수를 본다(재현판 ⑤) |
| ⑥ | 폰을 바꾸면 **산 장수가 사라짐** | ✅ 잔량을 «구매 토큰»에 매달고 `listPurchaseHistory()` 로 되짚어 새 기기로 옮긴다(재현판 ⑥·⑥-b) — 이메일·로그인 없이 |
| ⑦ | Vision 이 죽었는데 **장수만 깎임** | ✅ 되돌려 준다(재현판 ⑤-c) |
| ⑧ | 결제를 얹다가 **OCR 을 깨뜨림** | ✅ 라우터가 `/billing/` 만 가로챈다 · D1 이 없으면 **지금과 한 글자도 안 다르게** 돈다(재현판 ②) |
| ⑨ | 서비스계정 권한 전파(최대 24시간) | ✅ 창업자 확정 ⓑ — 승인 뒤에 만든다. 그때까지 결제는 **`billing_off`** 로 잠겨 있다 |

## 9️⃣-3 🧪 재현판이 «진짜로 잡는지»도 봤다 (규칙 12)

일부러 다섯을 망가뜨려 **전부 죽는 것**을 확인했다 — 되돌리면 41/41 통과.

| 망가뜨린 것 | 잡혔나 |
|---|---|
| acknowledge 를 뺀다 (3일 뒤 환불 사고) | ⛔ 5칸 실패 |
| OCR 이 「산 장수」를 안 본다 (돈 내고 못 쓰는 사고) | ⛔ 2칸 실패 |
| 폰 바꿨을 때 잔량 옮기기를 뺀다 (복원 깨짐) | ⛔ 6칸 실패 |
| 멱등을 깬다 | ⛔ 6칸 실패 |
| 「옛 주인 타고 나머지 팩까지 옮기기」를 뺀다 | ⛔ 2칸 실패 |
| 잔량을 셀 때 `consumed` 를 섞는다 (**진짜 버그였다**) | ⛔ 3칸 실패 ＋ 판이 죽었다 |

## 9️⃣-4 ⛔⛔ **이 판이 «증명하지 못하는» 것 — 여기가 제일 중요하다**

재현판은 **우리 코드**를 증명한다. **구글 쪽은 하나도 증명하지 못한다.**

| ⛔ 아직 모르는 것 | 언제 닫히나 |
|---|---|
| 구글이 «진짜로» 그 모양의 JSON 을 주는가 | 라이선스 테스트에서 실제 구매를 한 번 |
| 서비스 계정에 권한이 붙는가 (전파 최대 24시간) | 승인 뒤 초대 → 하루 |
| 상품 ID 다섯 개가 콘솔 등록값과 «한 글자도» 같은가 | 상품 등록 뒤 `getDetails()` 로 |
| TWA 에서 Digital Goods API 가 우리 앱에서 도는가 | **BILLING AAB** 를 올린 뒤 실물 폰에서 |

🔒 **그래서 규칙은 그대로다 — 판매 스위치(`sellable`)는 위 넷이 다 닫힌 «뒤»에만 켠다.**

## 9️⃣-5 ⛔⛔ **[같은 날 정정] 「다 쓸 때까지 미룬다」는 «죽은 길»이었다 — 문서에 이미 있었다**

📮 창업자 물음 = *"a,b 자세히 설명해줘"* → 답하려고 `decided.mjs` 를 돌렸더니 **이미 닫혀 있었다.**

⛔ **내 잘못** — 코드 기본값을 `'exhausted'`(미루기)로 뒀는데,
   `docs/구글에-물어볼것-결제-2026-08-16.md` **7️⃣(현행) 「바」** 가 이미 이렇게 적어 두고 있었다:
   *"`consume()` 을 미루면 **같은 상품 ID 재구매가 막힌다** → 「다 쓸 때까지 미룬다」 안은 **죽었다**. 잔량은 서버(Worker＋D1)로"*
   📌 나는 `CLAUDE.md:61` 의 **옛 세대**(*"내 안 = 다 쓸 때까지 미룬다 … ⚠️재구매가 막히나는 확인 못 했다"*)를 보고 썼다.
   **그 「확인 못 했다」가 8/16 에 확인됐는데 CLAUDE.md 가 안 고쳐져 있었다**(규칙 12 ⓑ — 정해지면 그 자리에서 고친다).

✅ **＋ 무게가 올라갔다: 🟡구글AI → ✅확정.** 공식 안드로이드 문서를 새로 찾아 붙였다 —
   *"소모하지 않으면 「보유 중」이라 다시 살 수 없고 `ITEM_ALREADY_OWNED` 가 돌아온다"* ·
   *"보유 상태의 항목은 Google Play 에서 구매할 수 없다. 소비 요청을 보내야 다시 구매 가능해진다"*
   (developer.android.com/google/play/billing/integrate)

### 🔢 그래서 두 갈래가 이렇게 갈린다

| | ⒜ 다 쓸 때까지 미룬다 (옛 안) | ⒝ 사자마자 비운다 ← **지금 값** |
|---|---|---|
| **또 살 수 있나** | ⛔ **다 쓰기 전엔 «한 팩도» 더 못 판다** (`ITEM_ALREADY_OWNED`) | ✅ 언제든 |
| 폰을 바꾸면 | ✅ 따라온다 | ✅ **따라온다** — 아래 방식으로 막았다 |
| 유저가 보는 것 | 「이미 소유한 항목입니다」 오류 | 그냥 사진다 |

⭐⭐ **⒜ 를 접어도 잃는 게 없다** — ⒜ 의 유일한 값어치였던 「복원」을 ⒝ 에서도 되게 만들었기 때문이다:

1. 잔량을 **기기(uid)가 아니라 «구매 토큰»에** 매단다
2. 앱이 `listPurchases()` ＋ **`listPurchaseHistory()`** 를 같이 보낸다
   — 뒤엣것은 **이미 비운 구매까지** 토큰째 돌려준다(공식·2026-08-19 확인)
3. 서버가 그 토큰의 **「옛 주인」을 찾아** 그 기기에 있던 **남은 팩을 전부** 새 기기로 옮긴다
   ⚠️ 3번이 필요한 이유 = `listPurchaseHistory()` 는 **상품마다 «가장 최근» 한 건**만 준다 →
      팩을 두 번 산 사람은 옛 토큰이 안 돌아온다. 그 구멍을 옛 주인 타고 메운다.

🧪 재현판 **⑥-b** 가 정확히 그 최악을 본다 — 「두 번 산 사람이 앱을 지웠다 깐다 · 토큰은 하나만 돌아온다」 → **두 팩이 다 따라온다.**
   ⛔ 그 줄을 빼면 재현판이 **죽는다**(규칙 12로 확인).

### 🐛 ＋ 이 갈아끼우기가 «진짜 버그»를 하나 잡았다
`'now'` 로 바꾸자마자 재현판이 **⑤에서 죽었다** — 잔량을 셀 때 `consumed=0` 조건을 걸어 뒀는데,
`'now'` 면 **사자마자 `consumed=1`** 이라 **산 순간 잔량이 0** 이 됐다.
📌 **「구글 쪽에서 비웠다」와 「우리가 줄 장수가 없다」는 다른 말이다.** 한 칸으로 쓰면 안 된다.
✅ 고쳤고, 옛 조건으로 되돌리면 재현판이 다시 죽는 것까지 봤다.

### ⏳ 그래서 창업자가 정할 게 «남아 있나»
⛔ **없다** — ⒜ 는 공식 문서로 닫혔고, ⒜ 를 고를 이유였던 복원은 ⒝ 에서도 된다.
⚠️ 다만 **한 낱말(`BILLING.CONSUME_WHEN`)은 남겨 뒀다** — 라이선스 테스트에서 `listPurchaseHistory()` 가
   우리 앱에서 «안 돌면» 그때 다시 견줄 수 있게. **그 전엔 재론하지 않는다.**

## 9️⃣-6 ⛔ **아직 «안» 한 것**

| | 무엇 | 왜 |
|---|---|---|
| ⒜ | 화면에 「산 장수」 뱃지 그리기 | 데이터는 `getOcrLeft().paid` 로 이미 온다 — **그리는 건 앱 갈래 일**(세션 분담) |
| ⒝ | 꾸미기 팩 잠금 풀기(`ownedPacks()` 연결) | 〃 · `syncPurchases()` 가 `entitlements` 를 돌려주니 한 줄이면 붙는다 |
| ⒞ | 환불 감시(Voided Purchases 폴링) | 5️⃣ 대로 **나중에** — 못 잡아도 손해가 작다 |
| ⒟ | 배포 | ⛔ 코드는 `hold/결제서버-0819` 에만 있다. **Worker 는 창업자가 대시보드에 붙여넣어야 돈다** |

---

# 🔟 🚨🚨 **[2026-08-19 저녁 · 재검토] 「어떻게 하면 돈이 새나」로 다시 봤다 — 구멍 «여섯»**

> ⭐ **이 절이 현행이다.** 위 9️⃣ 는 「만들었다」이고, 이 절은 **「적대적으로 다시 봤다」**이다.
> 📮 창업자 = *"다시한번 검토, **다양한 케이스로 재현**해봐."*
> ⛔ 9️⃣ 를 쓸 때 나는 **잘 될 때만** 재고 있었다. 틀어질 때를 재니 **여섯이 나왔다.**

## 🔟-1 찾은 구멍 여섯 — 전부 «돈이 새는» 쪽이다

| | 구멍 | 그대로 뒀으면 | 고침 |
|---|---|---|---|
| ① | **원장이 반만 들어가면 장수를 «영영» 못 받는다** — 「처음 보는 토큰인가」를 «다른 표»(purchases)로 판정해서, D1 이 한 번 삐끗하면 그 사람은 다시 보내도 0장 | 🚨**돈 내고 0장.** 게다가 조용하다 | 「처음인가」로 갈리지 않는다. **넣는 문장 자체를 멱등**으로 두고 매번 그냥 다시 넣는다 |
| ② | **20장 받고 환불해도 장수가 그대로** | 🚨 공짜로 20장 · 그게 소문나면 반복된다 | 취소로 확인되면 **그 자리에서 거둬들인다** |
| ③ | **꾸미기 팩을 환불해도 팩이 열린 채** | 🚨 같음 | 〃 (`entitlements` 도 지운다) |
| ④ | **마지막 한 장을 «동시에» 두 번** 쓰면 한 장 공짜 | 작지만 새는 건 새는 것 | 못 깎았으면 **쓰지 않는다** |
| ⑤ | **원장을 «못 읽은 것»을 「산 게 없다」로 앱에 알림** | 🚨 **산 사람의 팩이 도로 잠긴다** — 환불·분쟁 1순위 | `stale` 표를 세워 알리고, 앱은 그때 **화면을 안 건드린다** |
| ⑥ | 404(구글이 모르는 토큰)에 **회수해 버리면** | 🚨 오래돼 기록이 지워진 **멀쩡한 사람의 장수를 뺏는다** | **404 에는 절대 회수하지 않는다.** 「없다」와 「취소됐다」는 다른 말이다 |

⭐⭐ **⑥ 은 「고치다가 만든 구멍」이었다** — ②③ 을 고치면서 하마터면 404 까지 회수에 넣을 뻔했다.
   📌 **회수는 «구글이 취소라고 말했을 때»만** 한다. 못 물어본 것은 취소가 아니다.

## 🔟-2 🧪 사고 재현판 신설 = `scripts/_repro-결제사고-0819.mjs` (**59칸** · smoke 에 물렸다)

⭐ **두 판이 «보는 것»이 다르다** — 섞으면 정상 흐름이 사고에 파묻힌다.

| 판 | 보는 것 | 칸 |
|---|---|---|
| `_repro-결제서버-0819.mjs` | **잘 될 때** 잘 되나 (정상·복원·멱등·앱 쪽) | 41 |
| `_repro-결제사고-0819.mjs` | **틀어질 때** 손해가 나나 | **59** |

### 사고판이 재는 열여섯 가지
① 원장 반만 들어감 ② 20장 받고 환불 ③ 영구 팩 환불 ④ 구글이 404 ⑤ 마지막 한 장 동시 요청
⑥ 같은 구매 동시 sync ⑦ 원장 못 읽음 ⑧ **acknowledge 실패 → 재시도** ⑨ **consume 실패 → 재시도**
⑩ 서비스계정 열쇠 망가짐 넷(JSON·PEM·키 없음·로그인 실패) ⑪ **권한 미전파(403)** ⑫ 이상한 입력 열하나
⑬ 여러 개 한꺼번에 구매(＋말도 안 되는 수량) ⑭ **보류 결제(입금 대기)** ⑮ **OCR 이 안 다치나** 넷 ⑯ 운영자 모드

### 규칙 12 — 고친 여섯을 도로 망가뜨려 **전부 죽는 것**까지 확인했다
| 되살린 옛 버그 | 잡혔나 |
|---|---|
| 「처음 보는 토큰」을 다른 표로 판정 | ⛔ 2칸 |
| 환불 회수를 뺀다 | ⛔ 2칸 |
| 영구 팩 회수를 뺀다 | ⛔ 1칸 |
| 404 에도 회수한다 | ⛔ 2칸 |
| 동시 요청 막이를 뺀다 | ⛔ 1칸 |
| 「못 읽음」을 조용히 0으로 | ⛔ 1칸 |

⛔⛔ **＋ 판을 만들며 내가 한 번 속았다** — 「망가진 열쇠」 칸이 **전부 통과**했다.
   원인 = **구글 로그인 토큰이 한 판(isolate) 안에서 캐시**돼 앞 시험의 토큰을 그대로 쓰고 있었다.
   ✅ 그 칸만 **판을 새로 띄워** 재게 고쳤다. 📌 **「통과했다」가 아니라 「무엇을 보고 통과했나」**(규칙 18 ⓘ).
   ⭐ 캐시 자체는 정상 동작이다 — *「세 번 불러도 구글 로그인은 한 번뿐」* 이라는 칸을 따로 넣어 지켰다.

## 🔟-3 ⚠️ **재검토로 «드러났지만 안 고친» 것 — 알고 두는 것과 모르는 건 다르다**

| | 무엇 | 왜 그냥 두나 |
|---|---|---|
| ⒜ | **전역 월 900 은 돈 낸 사람도 못 넘는다** | 비용 $0 보장이 먼저다. ⭐다만 **판매를 켜는 날 `MONTHLY_GLOBAL` 을 올려야 한다** — 안 올리면 유료 유저가 월말에 막힌다. 사고판 ⑮ⓓ 가 이걸 못 박아 둔다 |
| ⒝ | 앱을 **다시는 안 여는** 사람의 환불은 못 잡는다 | 회수는 앱이 sync 를 보낼 때 걸린다. 완전히 닫으려면 **Voided Purchases 폴링**이 필요 — 5️⃣ 대로 나중에 |
| ⒞ | 사진에 글자가 없어도 **한 장이 나간다** | 무료 쪽과 «같은 규칙»이다. 다르게 하면 그게 더 헷갈린다 |
| ⒟ | `PLAY_SA_JSON` 을 바꿔도 **최대 1시간**은 옛 토큰이 산다 | 열쇠를 급히 갈 일이 생기면 Worker 를 다시 Deploy 하면 즉시 끊긴다 |

## 🔟-4 ⛔ **여전히 «증명 못 한» 것은 그대로다**
구글이 진짜 그 모양으로 답하는지 · 서비스 계정 권한 · 상품 ID · TWA 에서 Digital Goods 가 도는지.
🔒 **판매 스위치(`sellable`)는 라이선스 테스트로 그 넷이 닫힌 뒤에만 켠다.** 이 줄은 안 바뀐다.

---

# 1️⃣1️⃣ 🔌 **[2026-08-19 20:52 · 창업자 폰 실물] 인앱결제 창구에 «붙었다» — 그런데 거래는 안 된다**

> ⭐ **이 절이 현행이다.** 창업자가 `logintest.html` ⑥칸을 눌러 실제로 재봤다.
> ⛔ 돈은 1원도 안 나갔고 아무것도 안 샀다 — **서비스에 «연결»만** 해 본 것이다.

## 1️⃣1️⃣-1 실물 값

| | 값 |
|---|---|
| `결제창구` | **보임**(`getDigitalGoodsService` 함수가 window 에 있다) |
| `결제붙기` | ✅ **붙음** — 서비스 핸들이 «잡혔다» |
| `상품조회` | ⛔ **오류: `OperationError`** |

## 1️⃣1️⃣-2 ⛔⛔ **내가 적어둔 전제가 틀렸다**

판에 *"없음(아직 BILLING AAB 를 안 올렸으니 정상)"* 이라고 써 뒀는데 **있었다.**
⭐ `android/twa-manifest.json` = `"features": {}` (권한 없음)인데도 **창구 손잡이는 잡힌다.**
📌 **「함수가 있다」·「핸들이 잡힌다」·「거래가 된다」는 «세 가지 다른 것»이다.**(규칙 18 ⓘ)

## 1️⃣1️⃣-3 ⛔ **왜 실패했는지는 «모른다» — 짐작으로 안 채운다**

공식 명세(WICG Digital Goods) = *"결과가 오류면 **`OperationError`** 로 거절한다"*
→ **모든 실패가 같은 이름으로 온다.** 이유를 구분할 수 없다.

| 후보 | 근거 |
|---|---|
| ⓐ **BILLING 권한이 없어서** | 크롬 공식 = *"twa-manifest 에 `alphaDependencies` ＋ `playBilling` 을 켜야 한다"* |
| ⓑ **인앱 상품이 0개라서** | Play Console 에 상품을 아직 하나도 안 만들었다 |

## 1️⃣1️⃣-4 ⭐ **그래도 행동은 안 바뀐다** (규칙 31 ③)

어느 쪽이든 **BILLING AAB ＋ 상품 등록을 «둘 다»** 해야 한다. 공식 순서가 그렇고 우리 계획도 그대로다.
⛔ **그래서 더 파지 않는다.** 답이 어떻든 다음 행동이 같으면 파는 건 낭비다.

## 1️⃣1️⃣-5 ⭐⭐ **대신 값어치가 하나 생겼다 — 판이 「검증 도구」가 됐다**

승인 뒤 **BILLING AAB 를 올리고 상품을 등록한 다음** 같은 ⑥칸을 누르면 —
**`상품조회` 가 성공으로 바뀌는지**가 곧 「진짜 결제가 되는지」의 답이다. **폰에서 단추 하나로.**
📌 9️⃣-4 의 「증명 못 한 것 넷」 중 **「TWA 에서 Digital Goods 가 도는가」를 재는 자가 생겼다.**

⚠️ ＋ 그날 결과판의 `어디: 홈화면 앱` 은 **구글 로그인 왕복 뒤라 `referrer` 가 `accounts.google.com` 으로 바뀐 것**이다.
   앱 밖으로 나간 게 아니다(`표시모드: standalone` 그대로).
