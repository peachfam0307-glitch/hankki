# ☁️🔥 Firebase 설정 기록 — 2026-08-21 (출시 당일)

> 📮 창업자 = *"지금 이거 하면 돼?? 내 한끼계정으로?"* → *"하나씩 할거야"*
> ⭐ 창업자가 콘솔을 누르고 클로드가 화면마다 확인해 준 방식. **화면 캡처로 한 단계씩 실물 확인.**
> 🎯 목적 = **☁️클라우드 저장**(창업자 확정 「1번」) · 짝 = **🅰 파이어베이스**(2026-08-20 확정)

## ✅ 1단계 — 프로젝트

| | 값 | 확인 |
|---|---|---|
| 계정 | **안녕한끼** | 화면에 「한끼님, 안녕하세요」 |
| 프로젝트 이름 | `hankki` | |
| **프로젝트 ID** | **`hankki-6a768`** | ⭐이름이 겹쳐 구글이 뒤에 붙였다 — 정상 |
| 요금제 | **Spark 무료 ($0/월)** | ⭐카드 0 |
| Google 애널리틱스 | **끔** | 안 쓰고, 켜면 **유저 데이터 수집이 늘어 Play 신고를 또 바꿔야 한다** |
| Google 개발자 프로그램 | **끔** | 무료지만 필수 아님 · 나중에 가입 가능 |
| Firebase 의 Gemini | **끔** | 무료·콘솔 도우미라 앱과 무관 · 켜도 Spark 유지 · 나중에 켤 수 있다 |

⛔ **실물이 안내를 이겼다** — 첫 화면 버튼이 「프로젝트 만들기」가 아니라 **「Firebase 프로젝트를 설정하여 시작하기」**였다(프로젝트가 0개일 때 문구가 다르다).

## ✅ 2단계 — Firestore ⛔**되돌릴 수 없는 칸을 통과했다**

| | 값 |
|---|---|
| 버전 | **Standard**(기본) — ⛔Enterprise 아님(MongoDB·고급 쿼리용 · 우리 무료 1GB 조사는 Standard 기준) |
| 데이터베이스 ID | **`(default)`** |
| **위치** | ⭐⭐ **`asia-northeast3` (Seoul)** |
| 보안 규칙 | **프로덕션 모드에서 시작** — `allow read, write: if false` |

⛔⛔ **기본값이 `nam5 (United States)` = 미국이었다.** 그대로 뒀으면 미국에 굳었다.
   구글도 화면에 *"이 위치를 설정한 후에는 나중에 변경할 수 없습니다"* 라고 띄웠다.
   ⭐ 함정 = 목록에 **`asia-northeast1`(도쿄) · `2`(오사카) · `3`(서울)** 이 나란히 있다. **숫자 하나 차이.**
   📌 그래서 고른 «뒤에» 캡처로 한 번 더 대조하고 눌렀다.

⚠️ **왜 서울이 중요한가** — 개인정보 국외이전 신고 내용이 통째로 달라진다.
   ⛔ 단 **Auth(로그인)는 서울을 골라도 미국에서 처리된다** — 그건 못 바꾼다. 처리방침에 그대로 적는다.

⛔ **「＋ 컬렉션 시작」을 누르지 않았다** — 데이터 상자는 앱이 만든다. 손으로 만들면 모양이 어긋난다.

## ✅ 3단계 — 웹 앱 등록

| | 값 |
|---|---|
| 앱 닉네임 | `한끼 웹` |
| Firebase 호스팅 | ⛔**체크 안 함** — 우리는 **GitHub Pages** 를 쓴다 |
| 방식 | **npm 사용**(우리 앱이 Vite+npm) |

### 🔑 `firebaseConfig` — ⭐**공개돼도 되는 값이다**

```js
const firebaseConfig = {
  apiKey: "AIzaSyBngI2jsjsiEpvnyjV1mRm16XV3XnGjdAc",
  authDomain: "hankki-6a768.firebaseapp.com",
  projectId: "hankki-6a768",
  storageBucket: "hankki-6a768.firebasestorage.app",
  messagingSenderId: "741474124689",
  appId: "1:741474124689:web:c01a03f5a669e249f91030",
}
```

⭐⭐ **왜 저장소(public)에 적어도 되나 — 공식 원문으로 확인**
> *"API keys restricted to Firebase services **do not need to be treated as secrets**, and it's **safe to include them in your code or configuration files**."*
> *"API keys for Firebase services are **not used to control access** to backend resources; that can only be done with **Firebase Security Rules** and **App Check**."*
> — https://firebase.google.com/docs/projects/api-keys (확인일 2026-08-21)

📌 즉 이 열쇠는 **「어느 프로젝트냐」를 알려주는 이름표**이지 문을 여는 열쇠가 아니다.
   문지기는 **보안 규칙**이다. ＋ 어차피 앱 번들에 박혀 나가므로 **숨길 수도 없다.**
⛔ **단 Firebase 관련 API 에만 쓴다** — 공식이 *"Gemini Developer API 같은 다른 구글 서비스에 쓰지 말라"* 고 못 박는다.
⏳ 나중에 강화 = **HTTP 리퍼러 제한**을 걸 수 있다(필수 아님).

## ⏳ 4단계 — Authentication (진행 중)
「로그인 방법」 → **Google** 하나만. ⛔이메일/비밀번호·전화·익명·Facebook·Apple·GitHub 전부 안 쓴다.
⏳ 볼 것 = ①사용 설정 토글 ②**프로젝트 공개용 이름**(유저가 로그인 화면에서 본다) ③**프로젝트 지원 이메일**(공개될 수 있다)

## ⏭ 그다음 (클로드 몫)
① 보안 규칙 쓰기(로그인한 사람이 «자기 것만») → 창업자가 콘솔에 붙여넣기
② 로그인 붙이기 — ⭐열쇠는 구글 `sub`(⛔Firebase 전용 UID 금지 · 갈아타기 보험 ①)
③ 글자 데이터 올리기/내려받기 — ⭐모양은 **백업 JSON 과 동일**(보험 ②) · 쪼개서 올린다(문서 1MB 상한)
④ 잠긴 일기 = **비번으로 잠가서** 올린다(창업자 확정) — 소금이 고정이라 **새 폰에서 같은 비번이면 열린다**
⛔ **신고 넷(데이터보안 재신고·`privacy.html`·앱 내 계정삭제 줄·콘텐츠 등급)은 명의 이전이 끝난 뒤에** 한꺼번에
