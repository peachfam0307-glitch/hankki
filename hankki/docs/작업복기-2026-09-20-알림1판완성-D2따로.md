# 작업복기 2026-09-20 새벽 (00:2x ~ 01:1x KST) — 알림 1판 «완성» · 유통기한 D-2 «따로» · 레꾸 쓰나

> 갈래 = `hold/알림-0919` (⛔배포 안 됨 · 배포는 내일) · 배포 갈래에는 HANDOVER 만 밀었다

## 한 것
1. **🅱 월요일 카드 문구** (창업자 "b가 나아.") — 「버섯 레시피가 열렸어요」 · 냉장고 줄은 둘째 줄(`\n`). `push-schedule.mjs` 한 곳 · `sw.js` 한 곳 · 재현판 ⑥ 잣대 · 시안 다시 그림.
2. **📅 유통기한 D-2 «따로» 알림** (창업자 "장보기 유통기한 레시피 알림이랑 다르게 무조건 -2에는 알려줘야지" → 설계 관문 통과 00:27 → "ㄱ ㄱ" 00:34)
   - 앱 → 워커 = «날짜»만(`{ endpoint, dates }` · 유통기한−2일 · 14일치). 재료 이름·수량·사진 안 나간다. 문장은 그날 폰이 거울을 읽어 만든다.
   - `pantryExpiry.알림날짜들`·`날짜더하기` / `pushSubscribe.유통기한날짜맞추기(30초 모아)`·`유통기한날짜보내기(같으면 0번 · partial 이면 안 적음)` / `store` 저장 자리 ＋ 켤 때 12초 뒤
   - 워커 `/expiry`(`push:exp:<날짜>` · `push:expof:<해시>` · 바뀐 칸만 · 20칸 상한) · `임박보내기`(일정 없는 날 09:00 · 40명씩 · `push:sent:<날>:exp`) · 일정 있는 날은 둘째 줄 얹기 = 하루 한 번
   - sw = `/today` 404 → 냉장고 줄만(`hankki-exp`) · 거울에 없으면 안 띄움 · 404 ≠ 못 가져옴
   - `privacy.html` 6-1 폰 알림 절(기기 주소·알릴 날짜만) · 시행일은 「나가는 날」로 적어 둠 · README · 설계 문서 절
   - 재현판 = 워커 ⑨ 13칸 · 얹기 ⑦ 8칸 · 허락 ⑩ 7칸(소스) — 전부 초록 · 빌드 exit 0
   - 시안 4장(`docs/알림-시안-2026-09-20.png`) — 넷째 = D-2 따로 카드
3. **담기 뒤 알림 시트가 재현판 5개를 덮고 있었다** — 배포 갈래에서 온 식비·장바구니 재현판이 hold 에선 `.sheet-mask` 에 막혀 죽었다 → `hankki:push:consent=no` 씨앗. ⭐ 합치기 «전»에 잡았다.
4. 배포 갈래(v13.81)를 hold 에 합침 — version 충돌 → v13.82 로 옮겼는데 **갤럭시 세션이 00:5x 에 v13.82 를 또 썼다 → 내일 v13.83 으로.**
5. **레꾸·레꾸자랑 쓰나** (창업자 01:05) — 보고서 값: 꾸미기 열림 9/16~19 **0명** · 자랑 화면 하루 1~5명 · brag_shared **0**. 상세 화면은 하루 2~12명이 보는데 단추를 안 누른다 → 「안에서 실망」이 아니라 「단추를 안 누른다」에 가깝다(표본 작음). 「왜」를 알 길 셋 = ②사람 3명 옆에서 보기 · ①「단추가 보였다」 눈 · ③인스타 투표 → **창업자 = ①번 눈 내일 같이 넣는다.**

## 빠뜨린 것·틀린 것
- 스모크를 돌리는 «도중에» 브랜치를 바꿔 결과가 무효가 됐다(30 빨강 = 배포 갈래 src 를 hold dist 로 잰 것). ⛔ 스모크가 도는 동안 checkout 하지 않는다. 내일 끝까지 다시.
- `pkill -f` 패턴이 내 셸까지 잡아 명령이 통째로 죽었다(exit 144) → 메시지 파일이 안 써져 옛 메시지로 커밋 → amend. ⛔ pkill 은 pid 로.
- cwd-guard 가 «커밋 메시지 안의 파일 경로»에도 걸린다 → 앱 폴더에서 절대경로로.

## 내일
- ⓐ 창업자 워커 붙여넣기 → ⓑ hold 에 배포 갈래 합치고 v13.83 · ①「꾸미기 단추 보였다」 눈 · 스모크 끝까지 · 합쳐 푸시 · 초록불 → ⓒ 폰 확인
- 9/21 저절로 열리는 6편 = 검수 ✅ · 링크 없는 재료 6개(목살·떡·청경채·모짜렐라 치즈·위드잇 안심햄·볶음김치) = 창업자 몫
