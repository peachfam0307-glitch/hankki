# 🐛 가져오기 — 캡처 2장 넣으면 1장만, 그마저 분류가 뒤집힌다 (2026-08-02)

> 창업자 폰 제보: *"**레시피 가져오기에서 2장 가져오면 1장만 입력된다고.**"*
> ⛔ 클로드가 처음엔 「공유 2장」으로 잘못 알아들었다 → 창업자가 바로잡음.

## 📸 증거 (이 폴더)

| 파일 | 무엇 |
|---|---|
| `1-넣은것-캡처1-재료목록.jpg` | 인스타 캡처 ① — 제목 + **재료 목록 전체** + 양념 재료 + 순서 1번 첫 줄 |
| `2-넣은것-캡처2-순서1~5.jpg` | 인스타 캡처 ② — 양념 재료 + **순서 1~5번 전체** + 팁 |
| `3-나온것-재료비고-순서에재료.jpg` | **앱에 저장된 결과** |

원본 = `emily.at_home` 「시아버지가 전수해준 홍콩식 가지 볶음」 (인스타 캡션 캡처)

## ⛔ 실제로 나온 것

```
재료          →  「.」 하나뿐            ⛔ 통째로 비었다
만드는 법 1   →  emily.at home 시아버지가 전수해준 홍콩식 가지 볶음   ⛔ 제목이 순서로
만드는 법 2   →  티리난지 피렌체 웍 최저가 공구중! (영상 속 웍은 24cm) ⛔ 광고가 순서로
만드는 법 3   →  가지 적당히 큰걸로 5개                              ⛔ 재료가 순서로
만드는 법 4   →  돼지고기다짐육 250g 물 100ml        ⛔ 재료 «두 줄»이 한 줄로 붙음
만드는 법 5   →  작은 양파 1/2개 쪽파 5대            ⛔ 또 두 줄이 붙음
만드는 법 6   →  홍고추, 청고추 1개씩 다진마늘 2큰술  ⛔ 또
…
⛔⛔ 캡처 ②의 «진짜 순서 1~5번»은 결과에 아예 없다
```

## 🔎 증상이 «셋»이다 (하나가 아니다)

| | 증상 | 의심 지점 |
|---|---|---|
| **A** | **2장째가 안 들어왔다** | `EditorScreen.jsx` 221줄 `if (!img \|\| ocr.busy) return` — 225줄에서 `setOcr({busy:false})` 하지만 **React state 갱신은 비동기**라, 237줄이 곧바로 다음 장을 크롭에 넘기면 **두 번째 `runOcr` 가 busy=true 로 읽고 조용히 리턴**할 수 있다 |
| **B** | **재료가 통째로 「순서」로 갔다** | `parseRecipe.js` 분류 — 재료 줄이 붙어버려(C) 「분량 있는 짧은 줄」 판정을 잃었을 가능성 |
| **C** | **재료 두 줄이 한 줄로 붙었다** | `mergeWrappedLines`(v8.46) — **인스타 캡션이 여러 줄로 잘린 걸 합치려고** 만든 건데 **재료 목록까지 합쳤다.** 재료는 원래 한 줄에 하나다 |

⭐ **C가 B를 부르고, B 때문에 결과가 더 나빠 보인다.** A는 별개 문제.

## ✅ 내일 재현 순서 (규칙 7)

1. **캡처 두 장을 그대로** 넣어 재현 — 두 번째 크롭 화면이 뜨는지부터 본다(=A 확정)
2. 캡처 ①만 넣어 본다 — 재료가 순서로 가는지(=B·C 확정)
3. OCR 텍스트를 그대로 떠서 `parseRecipeText` 에 넣고 **어디서 갈리는지** 본다
4. 고치고 **다시 이 두 장으로 재현해서** 확인 → 그다음 보고

## ⚠️ 고칠 때 조심할 것

- `mergeWrappedLines` 는 **없애면 안 된다** — v8.46에 *"인스타 캡션이 여러 줄로 잘려 재료↔순서가 섞이던 것"*
  을 고치려고 넣은 것이다. **재료 목록엔 안 걸리게** 조건을 좁히는 방향으로.
- `parseRecipe.js` 회귀 테스트가 있다(`scripts/test-parser.mjs` · 스모크 체인). **기존 8종이 안 깨지게.**
- ⭐ **이 케이스를 테스트에 추가한다** — 안 그러면 다음에 또 터진다.

---

# 🐛 같은 화면에서 두 개 더 (창업자 2026-08-02 새벽)

> *"가져오기에서 **캡처 사진 자르기 할 때 내가 자른 것보다 더 작게 잘려.**"*
> *"가져오기에서 다 썼으면 **사진 닫기가 가끔 레시피를 가리는데 치울 방법이 없어.**"*

⭐⭐ **이로써 「가져오기」 한 화면에 버그가 다섯이다** (A·B·C ＋ D·E).
   우연이 아니라 **제일 복잡하고 손을 제일 많이 탄 화면**이라는 신호다.
   📌 하나씩 땜질하지 말고 **다섯을 같이 재현해서 한 번에 정리**한다.

## D. 크롭이 「내가 자른 것보다 더 작게」 잘린다

**의심 = 크롭 비율의 «기준»이 보이는 이미지가 아니라 컨테이너다**

`components/CropSheet.jsx`
```js
53:  const b = boxRef.current.getBoundingClientRect()     // ← 기준이 «박스»
89:  const sx = Math.round(rect.x * img.naturalWidth)     // ← 비율을 원본에 곱한다
90:  const sy = Math.round(rect.y * img.naturalHeight)
```

⭐ **왜 「더 작게」가 되나** — 캡처는 **세로로 아주 길다**(1080×2340).
   `object-fit: contain` 이면 **좌우에 빈 여백(레터박스)** 이 크게 생긴다.
   그런데 비율을 **여백까지 포함한 박스** 기준으로 재면, 같은 비율이 **실제 이미지에선 더 좁은 영역**이 된다.
   → **손가락으로 잡은 것보다 작게 잘린다.** 증상과 정확히 맞는다.

✅ **고칠 방향** = 비율의 기준을 **「실제로 그려진 이미지 영역」**으로 잡는다
   (컨테이너 크기와 `naturalWidth/Height` 비를 견줘 레터박스를 빼고 계산).
🔎 **재현** = 세로로 아주 긴 캡처(위 증거 사진 그대로)로 해야 티가 난다. **정사각에 가까운 사진은 오차가 작아 안 보인다.**

## E. 「사진 닫기」를 못 누른다 — 버튼이 화면 밖으로 나간다

`screens/EditorScreen.jsx` **443~450줄**
```js
onClick={() => setPin(null)}
aria-label="캡처 사진 닫기"
style={{ position: 'absolute', top: 8, right: 8, … }}   // ← «패널» 기준 absolute
```

⭐ **`absolute` 는 「패널의 위쪽」에 붙는다.** 패널이 길거나 스크롤되면
   **버튼이 화면 밖으로 밀려나 누를 수가 없다.** 그러면 사진이 레시피를 덮은 채로 **치울 방법이 사라진다.**

✅ **고칠 방향 (셋 중에 고른다 · 재현 뒤 결정)**
1. **버튼을 `sticky` 로** — 패널이 스크롤돼도 늘 화면 안에 남는다 (제일 작게 고치는 길)
2. **패널 자체를 접을 수 있게** — 「사진 보기/숨기기」 토글을 **고정 자리**에 둔다
3. **뒤로가기로도 닫히게** — 이미 전체보기(zoom)는 그렇게 돼 있다(159줄). 같은 방식을 여기에도

⚠️ **닫아도 사진이 사라지면 안 된다** — `refs` 에 남아 있어야 「크게 보기」로 다시 볼 수 있다.
   `setPin(null)` 은 «가리기»지 «삭제»가 아니다. 고칠 때 그 성질을 지킨다.

## ✅ 내일 한 번에 볼 순서

1. **증거 캡처 2장으로 재현** — 크롭 화면에서 **D부터** 보인다(자르는 단계가 먼저다)
2. 크롭 통과 → **A**(2장째 오나) 확인
3. 저장된 결과로 **B·C**(분류·줄 붙음) 확인
4. 저장 뒤 화면에서 **E**(사진 닫기) 확인
5. 고치고 **같은 2장으로 다시 재현** → 다섯 개 다 확인하고 보고 (규칙 7)

---

# ✅ 고침 (2026-08-02 낮 · `hold/검수대기` 브랜치 · ⛔아직 배포 안 함)

> 다섯 개를 **한 화면에서 같이 재현**했다(규칙 7). 재현 하네스 = 캡처 2장을 실제로 넣고
> OCR 프록시만 가짜로 세운 뒤, **잘린 결과의 픽셀을 직접 읽는** 방식.

| | 증상 | 진짜 원인 | 결과 |
|---|---|---|---|
| **B** | 재료가 통째로 순서로 | `SEC_STEP` 에 「레시피」가 있어 **「레시피 (3-4인분 기준)」을 헤더로 읽었다** | 재료 **0 → 19줄** |
| **C** | 재료 두 줄이 한 줄로 | ⛔**`mergeWrappedLines` 가 아니었다** — 찍어보니 5쌍 다 「안 붙는다」 | B와 한 뿌리 |
| **A** | 2장째가 안 들어옴 | ⚠️**재현 안 됐다**(아래 참고) | 굳어버리는 길 둘을 막음 |
| **D** | 내가 자른 것보다 작게 | **비율을 재는 「박스」와 실제로 그려진 「그림」이 다른 크기** | 손가락대로 잘림 |
| **E** | 사진 닫기를 못 누름 | sticky `top:0` = **폰 상태표시줄 자리**인데 버튼이 `top:8` | `+var(--safe-top)` |

## 🔎 D — 숫자로 잡았다

옛 코드로 **손잡이를 사진의 25%~75% 자리**까지 끌고 잘라보니:

```
기대  540 × 1170   (1080×2340 의 딱 절반)
실제  778 × 1621   ⛔ 44% 넓고 39% 높다 · 오른쪽 아래 표식이 그대로 남음
```

원인이 **두 겹**이었다.

1. **세로** — 그림엔 `maxHeight: calc(100vh - 200px)`, 박스엔 `maxHeight: 100%`.
   그 **200px 은 위아래 여백을 어림잡은 값**이라 실제 자리 높이와 안 맞는다.
   실측(390×844)에서 **71px** 어긋났고, 폰은 안전영역 때문에 **100px 넘게** 벌어진다.
2. **가로** — `objectFit: contain` 이라 **그림 좌우에 빈 띠(레터박스)** 가 생기는데,
   비율은 **띠까지 포함한 박스** 기준으로 쟀다.
   → 사진의 왼쪽·오른쪽 끝에 손잡이를 맞춰도 **양옆 10%씩이 빠진다.**
   ⭐ **이게 창업자가 말한 「더 작게 잘려」의 정체다.**

✅ **고친 방식** = 빈 자리를 직접 재서 `contain` 계산을 **내가** 하고 **박스를 그 크기로 못 박는다.**
   그림이 박스를 딱 채우니 **띠가 0** 이고 「보이는 것 = 자르는 것」이 된다.
   ⚠️ 한 번 헛디뎠다 — 원본 크기를 **상태에 담아뒀더니** 「사진 바뀜」 초기화가 `onLoad` 보다
   늦게 돌아 방금 잰 값을 지웠다. → **img 에서 그때그때 읽는다**(순서 싸움 자체가 없어짐).

## 🔎 E — 「가끔」의 정체 = 스크롤했을 때만

패널이 `position: sticky; top: 0` 이라 **스크롤하면 화면 맨 위에 붙는다.**
그 자리가 폰에선 **상태표시줄(시계·배터리)** 이다. 버튼은 `top: 8` 이라 **통째로 그 밑**으로 들어간다.

```
안 내렸을 때  버튼 y = 152px   → 잘 눌린다   (그래서 「가끔」)
내렸을 때     버튼 y =   8px   → 상태표시줄(44px) 밑 = 안 눌린다
고친 뒤       버튼 y =  52px   → ✅
```

`top: calc(8px + var(--safe-top, 0px))` — 캡처 닫기·영상 닫기 **둘 다**.
📌 **헤드리스는 `env(safe-area-inset-top)` 이 0이라 이 버그가 아예 안 보인다.** 검사에서 44px 을 심어야 드러난다.

## ⚠️ A — 정직하게: 재현이 안 됐다

캡처 2장을 넣고 **OCR 을 폰처럼 2.5초로 느리게** 해도 →
2장째 자르기 화면 뜸 ✅ · OCR 2번 호출 ✅ · 2장째 글이 칸에 들어옴 ✅.

📌 **그래서 「1장만 들어왔다」로 보인 건 B·C 때문일 가능성이 크다** —
옛 파서는 두 장을 합친 글을 **전부 「만드는 법」으로** 밀어넣어서,
화면에 처음 보이는 여섯 줄이 죄다 캡처① 내용이었다(캡처②는 한참 아래에 있었을 것).

그래도 **굳어버릴 수 있는 길 두 개**는 막았다 —
① 중복 방지를 `state` 가 아니라 `ref` 로 본다(옛 값을 붙들고 조용히 리턴하던 길)
② `try/finally` — **한 장이 실패해도 대기열이 계속 간다**(예전엔 여기서 터지면 `busy` 가 굳어
   남은 장이 영영 안 들어오고 버튼도 계속 흐렸다).

⏳ **폰에서 다시 한 번 봐야 하는 것** = 앨범에서 2장을 고를 때 **갤러리 앱이 2장을 다 주는지.**
   그건 앱 밖(안드로이드 사진 선택기) 일이라 우리 코드로는 확인이 안 된다.

## 🔒 다시 안 나게 — `scripts/test-crop.mjs` (`npm run smoke` 체인)

**눈으로는 절대 못 잡는다** — 몇 %씩 밀리는 거라 화면만 보면 멀쩡해 보인다.
그래서 원본 네 귀퉁이에 **색 표식**을 찍고 **잘린 결과의 픽셀을 읽는다.**

1. 박스 = 그려진 이미지인가
2. 손잡이를 25%~75% 로 끌면 원본의 **딱 절반**이 잘리는가 · **귀퉁이 표식이 0개**인가
3. 「사진 닫기」가 **상태표시줄(44px) 아래**에 있고 실제로 **눌리는가**(`elementFromPoint`)

⭐ **옛 코드로 되돌려 「진짜 걸리는지」 먼저 확인했다** — 네 줄 다 잡았다.
   (규칙: 검사를 만들면 옛 값으로 돌려본다. 안 걸리면 내 가정이 틀린 거다.)
