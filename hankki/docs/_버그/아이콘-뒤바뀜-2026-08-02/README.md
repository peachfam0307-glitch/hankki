# 🐛 아이콘 두 개가 서로 뒤바뀌었다 — `fe_124` ↔ `fe_128` (2026-08-02)

> 창업자 폰 제보: *"**소스에 저 그림 들어감.**"* (「고마다래소스」에 나물무침 그림)
> 증거 = `증거-고마다래소스에-나물그림.jpg`

## ✅✅ 닫힘 (2026-08-08) — 세 갈래 다 끝냈다

| | 무엇 | 결과 |
|---|---|---|
| ⑴ | **파일 스왑** | ✅ `0adab6c` 「음식 분류 수술 2차」에서 이미 고쳐져 있었다 — 3배 판독으로 확인(`fe_124`=나물 · `fe_128`=소스 세 종지) |
| ⑵ | **나머지 전수 확인** | ✅ **59컷 전부 판독 · 뒤바뀜 0** (`fe_97`~`fe_133` 37 ＋ 8/05 새 시트 22) |
| ⑶ | **장치로 만들기** | ✅ `tools/icon-labels.py` ＋ `scripts/check-iconlabels.mjs`(배포 게이트) ＋ `scripts/icon-checked.json` |

### 🔎 ⑵에서 두 컷이 걸렸다가 «둘 다 맞는 것»으로 풀렸다 — 푸는 방법이 서로 달랐다

- **`fe_97`(콩나물밥)** — 300px 판에선 **국수로 보였다.** ⭐**3배로 키우니 노란 콩나물 대가리가 보였다.**
  📌 2026-08-05 에 콩나물무침을 면으로 잘못 본 것과 **똑같은 자리**다.
- **`fe_113`(나박물김치)** — 흰 네모가 **두부로 읽혔다.** 확대해도 안 갈렸다.
  ⭐ **같은 세트의 이웃과 나란히 놓으니 갈렸다** — `fe_111`(열무물김치)에도 **같은 흰 네모**가 있다 = 그건 「무」다.
  (`fe_133` 된장찌개는 뚝배기라 아예 다른 그림이었다)

📌 **확대로 푸는 것과 이웃 대조로 푸는 것이 다르다** — 확대는 «안 보이던 것»을 보여주고, 이웃 대조는 «그 세트에서 그 모양이 무엇인지»를 알려준다.

### 🛡 ⑶ 장치 — 규칙이 아니라 기계가 막는다

```bash
python3 tools/icon-labels.py --new       # 판독 기록 없는 컷만 판으로 (그림＋앱에 박힌 이름표)
python3 tools/icon-labels.py --zoom A B  # 3배 · 나란히 (판정용)
npm run iconlabels                       # 게이트 (smoke 체인에 물려 있다)
```

게이트가 막는 것 = ①**판독 기록 없이 픽커에 새로 들어온 컷** ②**판독 뒤 PNG 가 바뀐 컷**(해시 대조).
⭐⭐ **규칙 12 로 «이 사고를 그대로 재현»해 봤다** — `fe_124`↔`fe_128` 을 다시 맞바꾸니 **해시가 서로 뒤집힌 채 정확히 걸렸다.**
**그때 이 게이트가 있었으면 배포가 막혔다.**

⛔ 「옛것」 178컷(2026-08-08 전부터 있던 것)은 **개수만 알리고 안 막는다** — 시끄러운 게이트는 죽은 게이트다.

## ⛔ 실제 상태

| 키 | 이름표(코드) | **실제 그림** | 비율 |
|---|---|---|---|
| `fe_124` | 궁채나물 | ⛔ **소스 세 종지**(케첩·마요·간장) | 1.2632 (336×266) |
| `fe_128` | 소스 | ⛔ **나물무침** | 1.0979 (314×286) |

**둘이 정확히 맞바뀌어 있다.** 규칙(`[['소스','디핑소스'], 'fe_128']`)은 제대로 걸리는데
**그림 파일이 반대**라 「소스」를 찾으면 나물이 나온다.

## ✅ 고치는 법 = **파일 두 개를 맞바꾼다** (규칙·이름표는 그대로)

⭐ **왜 파일 스왑인가** — 아이콘은 **레시피에 저장돼 굳는다**(`recipe.icon`).
   규칙만 고치면 **이미 저장된 「고마다래소스」는 그대로 나물**이다(제목을 다시 바꾸기 전엔).
   **파일을 바꾸면 저장된 레시피까지 저절로 고쳐진다.**

```
fe_124.png ↔ fe_128.png  스왑
Stickers.jsx  PHOTO_RATIO 도 같이 스왑   ← ⚠️ 1.2632 ↔ 1.0979 (비율이 다르다)
```

⚠️ **PNG 교체하면 `PHOTO_RATIO` 도 같이** — v8.90 교훈. 안 고치면 그림이 상자보다 작게 나오고
   **빈 클릭 영역**이 생긴다.

## ⚠️⚠️ 나머지 35컷도 의심해야 한다

`fe_124`·`fe_128` 은 **v9.38 에 한꺼번에 들어간 새 37컷(`fe_97`~`fe_133`)** 중 둘이다.
**둘이 뒤바뀌었다면 다른 것도 뒤바뀌었을 수 있다.**

📌 **내일 = 37컷 컨택트시트를 만들어 「그림 ↔ 이름표」를 나란히 놓고 눈으로 전수 확인.**

## 📌 같은 실수를 두 번째 하고 있다

**v9.31** 에 이미 겪었다 — 「달걀·두부」에 넣은 `ni_25` 가 계란이 아니라 **식빵**이었다.
그때 내가 적어둔 문장:

> *"컨택트시트에서 **번호만 보고 골랐고** 넣은 뒤 **이름표를 붙여 다시 안 봤다.**
>  창업자 화면에서 잡혔다 → **「고른 것」이 아니라 「들어간 것」을 본다.**"*

⛔ **똑같이 또 했다.** 규칙으로 적어뒀는데 안 지켜졌다 →
🛠 **장치로 만들어야 한다**: 새 음식 컷을 등록하면 **그림＋이름표 컨택트시트를 자동 생성**해서
   **눈으로 한 번 훑는 단계를 자르기처럼 명령에 묶는다**(`cut.py` 가 검수를 자동 호출하는 것과 같은 방식).
