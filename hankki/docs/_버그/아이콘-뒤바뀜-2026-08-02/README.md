# 🐛 아이콘 두 개가 서로 뒤바뀌었다 — `fe_124` ↔ `fe_128` (2026-08-02)

> 창업자 폰 제보: *"**소스에 저 그림 들어감.**"* (「고마다래소스」에 나물무침 그림)
> 증거 = `증거-고마다래소스에-나물그림.jpg`

## ⛔ 실제 상태

| 키 | 이름표(코드) | **실제 그림** | 비율 |
|---|---|---|---|
| `fe_124` | 궁채나물 | ⛔ **소스 세 종지**(케첩·마요·간장) | 1.2632 (336×266) |
| `fe_128` | 소스 | ⛔ **나물무침** | 1.0979 (314×286) |

**둘이 정확히 맞바뀌어 있다.** 규칙(`[['소스','디핑소스'], 'fe_128']`)은 제대로 걸리는데
**그림 파일이 반대**라 「소스」를 찾으면 나물이 나온다.

## ✅ 고치는 법 = **파일 두 개를 맞바꾼다** (규칙·이름표는 그대로)

⭐ **왜 파일 스왑인가** — 아이콘은 **레시피에 저장돼 굳는다**(`recipe.icon`).
   규칙만 고치면 **이미 저장된 「고마다래소스」는 그대로 나물**이다(제목을 다시 바꾸기 전엔).
   **파일을 바꾸면 저장된 레시피까지 저절로 고쳐진다.**

```
fe_124.png ↔ fe_128.png  스왑
Stickers.jsx  PHOTO_RATIO 도 같이 스왑   ← ⚠️ 1.2632 ↔ 1.0979 (비율이 다르다)
```

⚠️ **PNG 교체하면 `PHOTO_RATIO` 도 같이** — v8.90 교훈. 안 고치면 그림이 상자보다 작게 나오고
   **빈 클릭 영역**이 생긴다.

## ⚠️⚠️ 나머지 35컷도 의심해야 한다

`fe_124`·`fe_128` 은 **v9.38 에 한꺼번에 들어간 새 37컷(`fe_97`~`fe_133`)** 중 둘이다.
**둘이 뒤바뀌었다면 다른 것도 뒤바뀌었을 수 있다.**

📌 **내일 = 37컷 컨택트시트를 만들어 「그림 ↔ 이름표」를 나란히 놓고 눈으로 전수 확인.**

## 📌 같은 실수를 두 번째 하고 있다

**v9.31** 에 이미 겪었다 — 「달걀·두부」에 넣은 `ni_25` 가 계란이 아니라 **식빵**이었다.
그때 내가 적어둔 문장:

> *"컨택트시트에서 **번호만 보고 골랐고** 넣은 뒤 **이름표를 붙여 다시 안 봤다.**
>  창업자 화면에서 잡혔다 → **「고른 것」이 아니라 「들어간 것」을 본다.**"*

⛔ **똑같이 또 했다.** 규칙으로 적어뒀는데 안 지켜졌다 →
🛠 **장치로 만들어야 한다**: 새 음식 컷을 등록하면 **그림＋이름표 컨택트시트를 자동 생성**해서
   **눈으로 한 번 훑는 단계를 자르기처럼 명령에 묶는다**(`cut.py` 가 검수를 자동 호출하는 것과 같은 방식).
