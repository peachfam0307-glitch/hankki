# 🍎 아이폰은 「매일 · 잘자 전에」 빌드를 굽는다 — 절대원칙 전문 (2026-09-17 · CLAUDE.md 에서 2026-09-19 옮김)

> CLAUDE.md 가 150KB 경고선을 넘어 전문을 여기로 옮겼다. 한 글자도 지우지 않았다. CLAUDE.md 엔 요약 절이 남아 있다.

## 🍎🍎🍎 [절대원칙 · 창업자 2026-09-17 00:0x] **아이폰은 「매일 · 잘자 전에」 빌드를 굽는다 — 웹 배포는 아이폰에 «0»이다**

📮 창업자 = *"너도 이거 절대원칙박고 매일 매일 갤럭시끝나면 네가 아이폰빌드 올려"* · *"잘자전에 빌드굽고 잔다. 매일 절대원칙임"*
　　　　　*"헐.. 그럼 아이폰유저들은 갤럭시에서 반영된 최신버전을 하나도 못보고있다는거잖아"*

🌲 **뿌리 = 구조가 다르다.**
```
갤럭시 앱 ──켤 때마다──▶ 우리 사이트(웹)                       → 웹 배포 = 즉시 반영
아이폰 앱 ──빌드 때 웹을 안에 «구워 넣음»──▶ 애플 심사 → 스토어   → 웹 배포 = 아이폰은 그대로
```
- Capacitor `webDir: ../hankki/dist` (2026-09-05 결정 · 애플 2.5.2 · server.url 은 운영용 아님). ⛔ 이 결정을 뒤집지 않는다.
- 🔢 실제 사고(2026-09-16 밤) = 스토어 1.0.1 은 **13.48**에 멈춰 있는데 웹은 하루 반 만에 13.64 — 레시피 고침 7판(BASICS 153→160)·재료 묶음·iPad 고침이 아이폰 유저에겐 하나도 안 갔다.
  ⛔ 그리고 아이폰 세션(나)이 「웹만 배포하면 딸 폰도 바뀐다」고 **틀리게 말했다.** 09-15 출시 문서에 「아이폰은 푸시해도 안 바뀐다」고 적혀 있었는데 잊었다.

✅ **그래서 매일 이렇게 한다 — `/잘자` 순서 3(점검) 안에 들어 있다**
   ① 갤럭시(웹) 배포가 오늘 끝났나 본다 = 배포 갈래 마지막 Actions `success`.
   ② 그 판을 아이폰 갈래에 합친다(`git merge origin/claude/chatgpt-conversation-link-kvn5ph`) → 충돌 풀고 → 빌드 통과.
   ③ `ios-app/fastlane/Fastfile` 맨 위 판 기록에 「🍎 N판(날짜) = vX.Y 그대로 ＋ 무엇」 한 줄을 얹고(⭐이 워크플로는 `ios-app/**` 이 바뀐 푸시에만 돈다)
      커밋 제목에 `[testflight]` 를 넣어 아이폰 갈래에 푸시 → run N.
   ④ Actions `success` 확인 → **창업자에게 ASC 순서를 한 장으로** 준다(1.0.x 만들기 · 새 기능 문구 · 빌드 붙이기 · 제출). 심사는 밤에 넣으면 아침에 온다(1.0.1 = 9.5h).
   ⑤ HANDOVER 「아이폰」 줄에 «오늘 N판 · vX.Y · 제출 여부» 를 적는다.

⛔ **예외 = 오늘 웹이 «유저 눈에 보이는 것»이 하나도 안 바뀐 날**(문서·검사만) — 그날은 굽지 않고 HANDOVER 에 「오늘 아이폰 빌드 없음 · 이유」를 적는다. ⭐ 레시피(BASICS) 가 올라간 날은 «반드시» 굽는다 — 아이폰 유저의 레시피는 빌드로만 간다.
⛔ **`[testflight]` 를 연달아 두 번 푸시하지 않는다**(같은 판이 두 번 올라간다). 하루 한 판.
⛔ **관문 없이 굽지 않는다** — 규칙 13(smoke:seq 전부 초록) 은 아이폰 빌드에도 그대로다. 굽는 것이 늦어지면 늦어지는 것이지 관문을 빼지 않는다.
📌 **「새로운 기능」 문구 = 그 사이 웹 판들의 유저 보이는 변화만**(운영 장치·계측·문서는 안 적는다). 레시피 고침은 이름을 적는다.
📄 첫 실행 = 25판(2026-09-17 00:0x · v13.64 · run 25) → ASC 1.0.2.

