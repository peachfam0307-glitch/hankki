# 📦 AAB 16판(versionCode 16) — 무엇이 들어가나

> 📮 창업자 2026-08-18 = *"우리aab는 계속 구울거지? **반영한 것들 빠지지말고 적어둬.** 16번째 판 구워야하니까."*
> 🎯 이 문서 = **굽는 날 이것만 보면 된다.** 목록·출시노트·굽는 법이 한 장에.

---

## 1️⃣ 지금 어디까지 나갔나 — 실물로 확인한 값

✅✅ **16판 = 2026-08-18 08:41 UTC 에 «구웠다» (성공).**
🔗 **받는 곳 = https://github.com/peachfam0307-glitch/hankki/actions/runs/32117415820**
   · 아티팩트 = **`hankki-aab--16`** (⚠️이름에 버전이 빠졌다 — 아래 참조) · 3.9MB · **zip 이라 풀어야 한다**
   · 안엔 `app-release-bundle.aab`(Play 에 올리는 것) ＋ `app-release-signed.apk`(폰에 바로 까는 것)
   · **아티팩트는 2026-11-16 에 사라진다** — 그 전에 받아 둘 것

| | 값 | 어떻게 확인했나 |
|---|---|---|
| **versionCode** | **16** | 굽기 로그 `app/build.gradle` 60줄 |
| **versionName** | **11.08** | 같은 로그 61줄 (`version.js` 에서 자동으로 읽음) |
| **targetSdk** | **36** | 같은 로그 (`compileSdkVersion 36` · `targetSdkVersion 36`) |
| **바탕 커밋** | `fc4e70cd` | run 의 `head_sha` |
| **BILLING** | **안 들어갔다** | `twa-manifest.json` `features {}` 그대로 |
| **다음 판** | **versionCode 17** | |

⛔⛔ **이번에 아티팩트 이름이 `hankki-aab--16` 으로 나왔다 — 버전이 빠졌다.**
   · **AAB 속 값은 멀쩡하다**(로그가 `versionName "11.08"` 을 찍었다). 낡은 건 «이름표» 하나다.
   · 원인 = 굽기 입력칸에서 `versionName` 을 **비워 뒀고**(그러면 `version.js` 를 자동으로 읽는다),
     그런데 **아티팩트 이름만 입력칸을 다시 읽어** 빈칸이 됐다.
   · 📌 **우리 절대원칙이 「다음 판 번호는 굽기 아티팩트 «이름»이 답이다」**라서, 이름에서 버전이 빠지면
     **다음에 몇 판인지 알려주던 근거가 사라진다.** 그래서 그냥 두지 않았다.
   · ✅ **그날 워크플로를 고쳤다** — 버전 이름을 정하는 그 단계가 아티팩트 이름까지 «만들어서» 넘긴다.
     **17판부터는 비우고 구워도 `hankki-aab-<버전>-17` 로 나온다.**

⚠️ **＋ 화면 방향 검사가 「(못 읽음)」을 찍고 그냥 지나갔다** — 「됐다」도 「안 됐다」도 아닌 **아무것도 안 재는 검사**였다.
   ✅ 같이 고쳤다 — 이제 **우리가 넣은 값**(`twa-manifest.json` = `default`)을 먼저 보고, 세로면 **거기서 죽는다.**
   ⛔ **이번 16판은 「가로가 열려 있다」를 굽기 로그로 확인하지 못했다** — 다만 `twa-manifest.json` 은
      `"orientation": "default"` 그대로이고(직접 확인), 가로는 versionCode 13(v10.08)부터 이미 나가 있다.

✅ **`.github/workflows/build-aab.yml` 기본값도 `14` → `16` 으로 고쳐뒀다** → **다음엔 17 로 바꿔 넣을 것.**

📌 콘솔은 이미 쓴 번호를 `이미 사용되었습니다` 로 막는다. 16을 다시 넣으면 거기서 막힌다.

---

## 2️⃣ 16판에 들어가는 것 — **v10.86 ~ v11.08 (23판)**

⛔⛔ **커밋 «개수»는 이 문서에 적지 않는다 — 두 번 틀렸다.**
   · 처음엔 *"앱 커밋 62개"* 라고 적었는데, 그날 밤 **컨테이너가 되감겼다가 복구되면서
     배포 브랜치가 «뿌리부터 다시» 세워졌다**(`75e30dd0` 의 부모가 «없다» = 루트 커밋).
     그래서 지금 같은 명령을 돌리면 **22개**가 나온다. **개수가 저절로 바뀐다.**
   · ⭐⭐ **코드는 하나도 안 잃었다 — 트리로 직접 확인했다**(2026-08-18):
     한살림 `isHansalim` ✅ · 일기 잠금 `diaryLock` ✅ · 감정 36컷 ✅ · 「사는 양」 ✅ ·
     재료 아이콘 `ig_` 8갈래 ✅ · 개인정보 연락처 ✅ → **v10.86~v11.00 이 전부 살아 있다.**
   · 📌 **그러니 세는 잣대는 «커밋»이 아니라 «앱 버전»이다.** 버전은 `version.js` 한 곳이라 안 흔들린다.

⛔ **버전 목록을 커밋 제목 grep 으로 뽑지 말 것** — 제목 모양이 제각각이라(`fix:`·`feat:`·이모지 머리)
   `^v1[01]\.` 로는 **v10.86~v11.01 여섯 판과 v11.07·v11.08 두 판을 놓친다.** 아래 표가 정본이다.

### 🐛 고친 것 (창업자·테스터 제보)
| 판 | 무엇 |
|---|---|
| v10.86 | 내가 쓴 레시피와 제목이 같으면 우리 판이 «영영» 안 들어오던 것 |
| v10.89 | 「탕수육」에 수육 그림이 뜨던 것 |
| v10.93 | 백업이 «실패를 성공이라고» 말하던 것 (다운로드 팝업·복사 실패) |
| v10.97 | 백업 폴백을 복사 → 파일로 |
| v11.01 | 「이번 주 픽」이 어긋나던 것 |
| v11.03 | 자랑카드를 표지로 하면 **동그랗게 잘리던 것** ＋ 위 잘림·「한끼」 글자·아래 휑함 |
| v11.05 | 「누룽지삼계탕」에 **오징어누룽지 그림**이 붙던 것 |

### ✨ 새로 생긴 것
| 판 | 무엇 |
|---|---|
| v10.87·88 | **일기 잠금** — 자물쇠 단추 · 비번 네 자리 · 선 굵게 |
| v10.90 | **꼬르곰·펭펭 감정 36컷** — 일기 꾸미기에 |
| v10.91 | 레시피 **수제 떡갈비** ＋ 전용 그림 |
| v10.92 | **백업 강조 ＋ 폰↔패드 기기 이전 안내** (온보딩 마지막 장에 「이미 다른 기기에서 쓰고 있었어요」) |
| v10.94 | 장보기에 담을 때 **«사는 양»으로** — 분량 떼고 이름만, 유저가 고칠 수 있게 |
| v11.02 | 🔖 **책갈피**(옛 즐겨찾기) — 목록에서 바로 꽂는다 · 이름 일곱 곳 통일 |
| v11.04 | 책갈피 크기 — 작은 격자 30px · 큰 격자 36px |
| v11.06 | 안 꽂은 책갈피를 흐리게 — 꽂음/안 꽂음이 갈린다 |
| v11.08 | **한끼 일기·레꾸자랑 도움말** — 일기에서 「?」를 누르면 «내 레시피» 사용법이 뜨던 것 ＋ 레꾸자랑엔 도움말 자체가 없던 것 |

### 🍳 레시피·장보기
| 판 | 무엇 |
|---|---|
| v10.99 | 올리브유 브랜드 빼기 ＋ 8/24 다섯 편 검수 완료 |
| v11.00 | **한살림 = 사러가기를 아예 안 단다** (조합원 전용이라 막다른 길로 안 보낸다) |
| v11.01 | 주부의 장바구니 1판 — 올리고당·하바티치즈 |
| — | **9월 검수 12편 반영**(BASICS_VERSION 67→68) ＋ **24편에 검수 표시** — 창업자 판정 그대로 |
| — | 확인 대기 셋 닫힘 — 달래장·참기름 「큰술」 확정 · 갈치조림 **35분 유지**(창업자 판정) |
| v11.07 | 🦀 **꽃게찜 → 「꽃게간장조림」 통째 교체**(어머니 레시피 · 2인분 · BASICS_VERSION 68→69) ＋ 창업자 새 컷 둘(`fe_306` 꽃게간장조림 · `fe_307` 나물비빔밥) |

⭐ **`BASICS_VERSION` 을 67 → 69 로 올렸다** — 안 올리면 **이미 깔린 폰엔 옛 레시피가 그대로 남는다**
   (v10.75 에서 실제로 그랬다 · 지금은 `check-basics-version` 게이트가 막는다).

### 📄 법·문서 (스토어 신고와 맞물리는 것)
| 판 | 무엇 |
|---|---|
| v10.95 | 개인정보처리방침 **연락처 두 칸** |

---

## 3️⃣ 📝 출시 노트 초안 — ⚠️ **언어당 500자**(공백·줄바꿈 포함)

⭐ **테스터 의견을 맨 앞에** (창업자 2026-08-05 *"테스터 의견을 넣고 수정한걸 반영해야 구글에서 알지"*)
⛔ `<ko-KR>` 태그는 지우지 말 것. ⛔「완벽합니다」 금지.

```
받은 의견으로 고쳤어요.
· 자랑카드를 표지로 하면 동그랗게 잘리던 것 → 카드 전체가 표지로
· 백업이 실패해도 성공한 것처럼 보이던 것 → 이제 파일로 확실히 저장
· 한끼 일기에서 도움말을 열면 다른 화면 설명이 뜨던 것
· 레꾸자랑에 도움말이 없던 것

새로 생긴 것
· 책갈피 — 목록에서 바로 꽂고 모아 봐요
· 일기 잠금 — 자물쇠 단추와 네 자리 비번
· 폰을 바꿔도 살리는 법을 앱 안에서 안내해요
· 장보기에 담을 때 사는 양으로 바뀌어요
· 꼬르곰·펭펭 감정 스티커 36컷

레시피는 직접 만들어 보고 다듬었어요.
· 꽃게간장조림을 새로 넣고 9월 레시피를 손봤어요
```
🔢 위 초안 = **327자** (세어 봤다 · 500 안이라 여유 있음 · 붙여넣기 전에 콘솔 글자수로 한 번 더 볼 것)

---

## 3️⃣-② ✅ **언제 구웠나 — 지켰다**

📮 창업자 = *"**검수 하고 한번에 굽자.**"* → *"**여기까지하고 aab 한판 굽자**"*

✅ **9월 검수 12편을 반영하고 «그 뒤에» 구웠다.** 레시피 고침이 한 판에 같이 담겼다.
   📌 굽고 나서 고쳤으면 **또 한 판**을 구워야 했다.
⏳ **11월 13편 검수는 아직** — 그건 `hold/주간레시피-11월-0813` 에 있고 **앱에 없어서** 이 판과 무관하다.
   그 검수가 끝나면 앱에 넣고, 그때 **17판**을 굽는다.

---

## 4️⃣ 굽는 법 (다음 판 = **17**)

1. GitHub → **Actions → 「AAB 빌드」 → Run workflow**
2. `versionCode` = **17** · `versionName` = **비워둔다**(`version.js` 에서 자동으로 읽는다) · `targetSdk` = `36`
   ⭐ 비우는 쪽을 권한다 — **사람이 옮겨적는 자리는 반드시 언젠가 낡는다.**
3. ⭐ 성공을 확인한 «뒤» **받는 링크를 그 자리에 올린다**(절대원칙 · 창업자 2026-08-15)
   `https://github.com/peachfam0307-glitch/hankki/actions/runs/<run id>`
   ＋ 아티팩트 이름 `hankki-aab-<버전>-17` ＋ **zip 이라 풀어야 한다**
   ⛔ run id 는 `actions_list` 의 **`id`** 다 — `run_number`(13 같은 작은 수)가 아니다
4. Play Console → **비공개 테스트-베타테스트** 트랙 → 「전체 출시 시작」

## 4️⃣-② ⏭ **16판을 올린 뒤 할 것**

⛔ **굽는 건 끝났지만 «올리는 건» 창업자 몫이다** — 콘솔은 클로드가 못 연다.
· 올리고 나면 이 문서 1️⃣ 을 **「콘솔에 올라간 최신 = 16」** 으로 고친다
· 출시 노트는 아래 3️⃣ 초안을 쓴다(⚠️ **언어당 500자**)

## 5️⃣ ⛔ 이번 판에 «넣지 않는» 것

**BILLING 권한** — `android/twa-manifest.json` 29줄을 건드리지 않는다.
붙이면 스토어에 **「인앱 구매 제공」 라벨**이 붙고, 그러면 넷을 같이 맞춰야 한다
(콘텐츠 등급 재작성 · 데이터 보안 재신고 · `privacy.html` · Play 인앱구매 표시).
✅ **재심사 승인 뒤 다음 AAB 에.** (창업자 확정 2026-08-08 *"a가자"*)
