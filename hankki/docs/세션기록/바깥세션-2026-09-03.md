> 📁 **세션 기록** — 그 세션이 «한 일». HANDOVER.md 는 「지금 이어서 할 것」만 담는다.
> 🧹 `scripts/doc-trim.mjs` 가 HANDOVER.md 에서 옮겼다 (2026-09-03).

## 🥄🚨 [2026-09-03 12:10 · 🏛바깥 갈래 세션] 워커 붙여넣기 «완료» ＋ **v12.33 이 안 나갔다**

📌 앱 = **v12.32** (⚠️위 절이 v12.33 이라고 적었는데 **실물은 v12.32 다** — 아래 2️⃣)
📄 전문 = `hankki/docs/작업복기-2026-09-03-단위통일-큰술작은술.md`

### 1️⃣ ✅ 🤖 워커 붙여넣기 = **끝났다** (창업자 = *"붙여넣기완료"*)

⏳ 위 절 「▶️ 바로 다음 행동 3-①」의 **창업자 몫 하나가 닫혔다.**

창업자가 **Cloudflare 에 살아 있는 워커를 통째로 채팅에 붙여** 줬고(*"여기서 네꺼 추가해서 주면 붙일게"*),
그걸 우리 파일과 **표식 64개**로 대조했다 → **창업자 판에만 있고 우리엔 없는 것 = 0개.**
　 (모델 이름 둘 · `DAILY_GLOBAL: 200` · `MAX_IMAGE: 3_000_000` · `VISION_MAX_TOKENS = 8000` ·
　  `예산여유 = 3000` · `budgetMs` · `멈춘모델` · 함수 12개 · 로그 이름 8개 · 오류 이름 12개 · KV 열쇠 4개)
👉 **대시보드에서 따로 고친 게 없었다** → 657줄을 통째로 붙여도 잃는 게 0.
✅ `node --check` 통과 · `git show 9fdc6b31` = **7 insertions(+) · 0 deletions**.

⭐ 이 붙여넣기로 **다른 세션이 남긴 「워커 붙여넣기 = 내일」 대기도 같이 닫혔다** —
　 창업자가 붙여준 판에 이미 `예산끊김`·`멈춘모델`·`budget_out` 이 다 있었다(＝650줄 판을 이미 붙여 놨었다).

### 2️⃣ 🚨🚨 **v12.33 「영상 ▶ 표」는 «안» 나갔다 — 시험판만 나갔다**

⛔ 위 절이 *"v12.33 배포"* 라고 적었는데 **실물로 재 보니 아니다.**

| 무엇 | 배포 브랜치 | 있어야 할 곳 |
|---|---|---|
| `src/screens/MyRecipesScreen.jsx` 의 `영상인가` | **0곳** | 4곳 |
| `package.json` 스모크 사슬의 `_repro-영상표-0903.mjs` | **없다** | 있어야 함 |
| `src/version.js` | **v12.32** | v12.33 |
| `scripts/_repro-영상표-0903.mjs` (시험판) | ✅ 있다 (`38080a5b`) | — |

🔎 **뿌리 = 규칙 20(컨테이너 되돌아감).** `38080a5b` 가 담은 건 **새 파일 한 장뿐**이고,
　 «고친» 파일 셋은 커밋이 안 된 채로 **디스크가 되돌아가면서 사라졌다.**
⭐⭐ **CI 가 초록불이던 이유** = 시험판이 `package.json` 사슬에 **안 들어갔다.** 아무도 안 돌렸다.
　 📌 「판을 만들었다」와 「판이 돈다」는 다른 말이다(규칙 18 ⓘ).

### ✅ 잃은 건 없다 — 되살릴 자리를 못 박아 뒀다

```
hold/영상표-되살림-0903   (= 6b4cbe2b · 자동회수가 11:47 에 뜬 판)
```
되살리는 법 (⛔앱 코드라 **푸시 = 배포**다 · 창업자 신호 뒤에):
```
git checkout 6b4cbe2b -- hankki/src/screens/MyRecipesScreen.jsx hankki/package.json hankki/src/version.js
```

### ⛔ 하면 안 되는 작업

1. ⛔ **v12.33 을 내 판단으로 되살려 푸시하지 않는다** — 배포 브랜치 푸시 = 즉시 배포(규칙 9).
2. ⛔ **`hold/영상표-되살림-0903` 을 지우지 않는다** — 되살릴 유일한 자리다.
3. ⛔ **워커를 다시 건드리지 않는다** — 방금 붙여넣었다. 고치면 창업자 손이 또 든다.
4. ⛔ **단위를 「스푼·T·t」로 되돌리지 않는다** · ⛔**파서의 «읽는» 정규식에서 스푼·T·t 를 빼지 않는다.**

### ▶️ 바로 다음 행동

1. ⏳ **창업자 판정** — v12.33(영상 ▶ 표)을 지금 되살려 내보낼지, 다른 것과 묶어 내보낼지.
2. 🥄 **워커가 실물로 듣는지 확인** — 창업자가 폰에서 `12T` 든 레시피를 한 번 붙여넣어 보면 된다.
   　 ✅ 나와야 할 것 = **`12큰술`** ／ ⛔ 나오면 안 되는 것 = **`12작은술`**(3배 틀림).
3. 🛒 **9/4 = 주부의 장바구니 3개 전날 검수판**(9/5 D-2 에 저절로 열린다 · 절대원칙 28).

### ❓ 아직 확인이 필요한 것

1. **붙여넣은 워커가 실제로 도는지** — 대시보드를 클로드가 못 본다. 위 2️⃣번 폰 시험이 유일한 근거다.
2. **v12.33 코드가 왜 커밋이 안 됐는지** — 그 세션 대화를 못 봤다. 되살릴 판은 확보했다.
3. 위 절들의 「검수 41편 백업을 창업자가 불러왔나」는 **여전히 확인 못 했다.**

