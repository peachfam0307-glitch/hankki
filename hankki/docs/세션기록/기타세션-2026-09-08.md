> 📁 **세션 기록** — 그 세션이 «한 일». HANDOVER.md 는 「지금 이어서 할 것」만 담는다.
> 🧹 `scripts/doc-trim.mjs` 가 HANDOVER.md 에서 옮겼다 (2026-09-08).

## 📊 [2026-09-08 밤 · 이용 통계 갈래] 왜 하는 작업인지

**우리가 유저를 못 보고 있었다.** 창업자 = *"콘솔만 업뎃기다리기는 좀.. 막막해 우리도 알수있는 방법이 필요한 듯"*

🔢 뿌리 실측 = `grep "gtag|analytics|logEvent" src/` → **0건.** 분석 도구가 하나도 없었다.
그래서 **비로그인 사용자가 통째로 안 보였다.** 로그인 30명은 「계정을 만든 사람」이지 「쓰는 사람」이 아니다.

## 전에 무슨 작업을 했는지 (이번 세션 · v12.85~v12.88)

- 📈 **이용 통계를 켰다** — `src/stats.js` · measurementId 는 창업자가 Firebase 콘솔에서 켜고 줬다.
  지킨 것 넷 = ①죽어도 앱은 돈다 ②첫 화면 안 늦춘다 ③유저가 끌 수 있고 끄면 그 자리에서 멎는다 ④레시피·일기 내용 안 보낸다
- 🐛 **첫 배포가 죽었다(run 2285) — 원인을 두 번 잘못 말했다.** 로컬 스모크가 통과하자
  창업자에게 *"통계는 범인이 아니야"* 라고 했는데 **틀렸다.** CI 로그 실물을 읽고 정정했다.
  뿌리 = `firebase/analytics` 가 gtag 를 자기가 내려받다 실패하면 오류를 **밖으로 던져** try/catch 가 못 잡는다.
  ✅ gtag 직접 주입 ＋ `s.onerror = () => {}` — 실패의 «모양»을 바꿨다(원칙 34). 오류 2개 → 0개.
- 🖼 **화면 기록** — `App.jsx` 의 `useEffect [tab, stack]` 한 곳이면 push·replace·pop·탭이동 네 갈래가 다 잡힌다.
  🔒 `보내도되는화면` 자물쇠 = 적힌 15개 이름만 통과.
- ⛔ **그게 안 잡힐 뻔했다** — `screen_view` 는 GA4 예약어이자 **앱(네이티브) 스트림의 것.**
  웹(TWA)은 `page_view` 라야 한다. ⭐앱은 멀쩡하고 오류도 없어 **콘솔 숫자가 0 일 때까지 아무도 모를 버그**였다.
  ✅ `page_view` ＋ 화면 이름을 `page_title` 에 · `send_page_view: false` · 첫 화면은 gtag 붙은 뒤 보낸다.
- 🔀 **끄는 스위치를 방침 «페이지 안»으로 묻었다**(v12.87) — 창업자 *"설정에서 딱 안보이게"*
  ⛔없앨 수는 없다 = GA 약관 7조 ＋ Play 「사용자 데이터」 정책. 카카오톡도 4단계 깊이에 있다.
- 🔒 **`check-통계고지` 검사 6 → 10개** — 방침에 진짜 단추가 있나 · 앱과 같은 열쇠인가 ·
  다른 탭에서 꺼도 멎나 · 설정에 되살아나지 않았나.
- ✅ **Play 데이터 보안 — 창업자가 「애널리틱스」를 추가해 개시 개요를 다시 냈다.** 방침·앱·신고 셋이 맞는다.
- ✅ **정정** — 「Play 콘솔이 죽었다」는 틀렸다. **정상 3~7일 지연이고 소급 채워진다**(지원팀 답변).
- 🧹 옛 복기 6장을 보관소로 옮기고 가리키던 경로 8줄을 고쳤다.
- 📄 복기 = `hankki/docs/_archive/작업복기/작업복기-2026-09-08-3-이용통계.md`

## 앞으로 뭘 해야 하는지

- ⏳ **내일 아침** — `analytics.google.com` → 보고서 → 참여도 → 개요 = **「어제 앱 연 사람 몇 명」**
  (⛔Firebase 콘솔이 아니다 — 우리는 웹 스트림이라 반쪽만 보인다)
- ⏳ **모레 아침** — 보고서 → 참여도 → **페이지 및 화면** = 「뭘 많이 썼나」. 하루가 쌓여야 모양이 보인다.
  화면 이름 = `home`홈 `myrecipes`레시피 `log`일기 `shop`장보기 `brag`레꾸자랑 `profile`프로필
  `import`**가져오기** `detail`상세 `cook`요리모드 `editor`레시피쓰기 `diary`일기쓰기 `favorites`최애 `cooked`요리달력 `inbox`받은것
- ⏳ **9/9 콘솔** — 9/2 이후가 채워지는지. 안 채워지면 그때는 진짜 문제다.
- ⏳ **답 안 난 질문** — 창업자 = *"유저들이 가져오기보다 앱 자체 레시피를 보는거네"*
  ⛔ **아직 확인 못 했다.** 「가져오기 10회」가 적은 이유가 ⓐ앱 레시피로 충분 ⓑ어렵거나 모름 ⓒ앱을 안 엶 —
  셋 중 뭔지 모른다. ⭐`import` 화면에 들어왔는데 안 끝낸 사람이 보이면 ⓑ고,
  그러면 **레시피 늘리기보다 가져오기 고치는 게 먼저**다.
- ⏳ **레시피는 「더 만들기」보다 「더 빨리 열기」** — 재고 96편이 잠겨 있고 마지막이 2027-02-22 다.
  ⛔단, 그중 **17편은 검수를 안 받았다** — 빨리 열려면 그것부터.

## 하면 안 되는 작업 (이 세션에서 더해진 것)

- ⛔⛔ **「이용 통계 보내기」 스위치를 없애지 말 것** — GA 약관 7조(거부 수단 우회 금지) ＋
  Play 「사용자 데이터」 정책에 걸린다. 없애려면 «첫 실행 동의 팝업»을 대신 넣어야 하고 그건 훨씬 나쁘다.
- ⛔⛔ **끄는 자리를 설정 목록으로 되돌리지 말 것** — 창업자가 *"설정에서 딱 안보이게"* 라고 정했다.
  자리가 둘이 되면 한쪽이 반드시 낡는다(게이트가 막는다).
- ⛔ **`screen_view` 로 되돌리지 말 것** — 웹 스트림에서는 안 잡힌다. `page_view` 다.
- ⛔ **`s.onerror = () => {}` 를 지우지 말 것** — 그 한 줄이 배포를 살린다.
- ⛔ **통계에 레시피 제목·일기 글·검색어를 싣지 말 것** — `보내도되는화면` 자물쇠를 넓히지 않는다.
- ⛔ **방침 페이지의 열쇠 이름(`hankki:stats:off`)을 한쪽만 바꾸지 말 것** — 앱과 방침이 갈린다.

## 바로 다음 행동

👉 **창업자 폰에서 둘 확인** — ①설정 목록에 「이용 통계 보내기」가 **없어야** 함
   ②프로필 → 개인정보처리방침 → 「가. 앱 이용 통계」 아래 **[끄기] 단추**가 눌리는지
👉 **내일 아침 첫 것 = `analytics.google.com` 에서 첫 숫자 보기**

## 아직 확인이 필요한 것

· ⏳ **창업자 폰의 실물** — v12.88 이 폰에 닿았는지, 방침 스위치가 눌리는지 (브라우저에선 정상 확인)
· ⏳ **첫 숫자가 실제로 들어오는지** — 콘솔은 내가 못 연다. 창업자 캡처라야 안다.
· ⚠️ **`support.google.com` 은 이 환경에서 못 연다** — GA 예약어·약관 7조는 **공식 원문을 직접 못 봤고**
  여러 경로로 같은 말을 확인했다. 어긋나면 이 두 판단부터 다시 본다.
· ⏳ **Play 데이터 보안 심사 결과** — 창업자가 「애널리틱스」를 넣어 다시 냈다(결과 대기)

