# 🗓 2026-08-24 작업복기 — 「레시피열쇠」 이름·그림 · 음식 96컷 자르기

> 앱 **v11.29 → v11.31** (배포 둘) · 커밋 14 · 배포 브랜치 tip `4ac896d0`
> ⛔ 기억으로 적지 않았다 — 커밋 로그·파일·게이트로 확인했다(규칙 29).

---

## 1️⃣ v11.30 — 「AI 스캔 N회」 → **「레시피열쇠 N개」** (세 화면 18곳)

📮 창업자 = 후보 10개를 실물 화면에 얹어 본 뒤 *"열쇠나 국자..가 젤 나은것같아"* → 근거를 보고 **"가"** → *"바포해"*

⭐⭐ **이름을 «한 곳»에 뒀다** = `src/ocr.js` 의 `KEY_NAME`·`KEY_SHORT`·`KEY_UNIT`·`keyCount()`
- v11.02 「책갈피」 때 같은 기능 이름이 **일곱 곳**이었고 한 곳만 바꾸자 말이 갈라졌다.
- 다음에 또 바뀌면 **한 줄만** 고친다.

🔢 **국자를 접은 근거 둘(실측)**
1. 이름이 뜨는 곳이 **세 화면**(가져오기·레시피 편집·**냉장고 영수증**) — 「국자 = 퍼온다」가 영수증 읽기에서 뜻이 안 맞는다
2. 창업자가 콕 집은 **「횟수제가 자연스럽다」**가 국자엔 안 걸린다 — 열쇠는 쓰면 없어지는 게 당연한데 국자는 백 번 퍼도 그대로

⛔⛔ **첫 판이 영수증 화면에 «못 갔는데» 「옛 이름이 없다」가 초록불이었다** — 안 간 화면엔 당연히 옛 이름도 없다.
   **통과했는데 아무것도 안 쟀다**(규칙 18 ⓘ). ✅이제 «도착했나»를 먼저 재고, 못 갔으면 뒤 칸을 초록불로 만들지 않는다.

⛔ 길을 두 번 틀렸다(둘 다 화면을 찍어 보고서야 알았다 · 절대원칙 21) —
   ⑴냉장고는 하단 탭이 아니라 **「장보기」 안**에 있다 ⑵「가져오기」는 전체화면이라 하단바가 없다

---

## 2️⃣ v11.31 — 🔑**진짜 열쇠 그림** ＋ 📏**가져오기 상자 줄간을 하나로**

📮 *"열쇠컷도 적용해"* → *"열쇠 크기 키우고 … **페이지가 정신이 없게 느껴져.** 줄간도 균일하게"*
   → *"네모박스안에 있는 글자 줄간 말한거야."* → *"열쇠는 한개짜리가 좀 별론데.. **리본달린거로 교체하자**"*
   → *"**제일많이 써요가 칸을 벗어났어.**"* → *"좋아."* → *"ㄱㄱ"*

🔑 **v11.30 은 이름만 갈았고 그림은 여전히 ✨반짝이였다** — v11.02 「책갈피」와 같은 자리(*"모양이 같아야 누르고 싶다"*).
   게다가 그 반짝이는 **「AI 자동정리」 표식**이라 **재화와 기능이 같은 그림**을 쓰고 있었다.
- 남았으면 🎀`key_one`(리본＋이름표 열쇠) · 다 썼으면 🔒`key_hole`(열쇠구멍) — **글자 없이도 「끝났다」가 읽힌다**
- 28px · 220px 로 줄여 넣었다(ui 자산 긴변 중앙값 215px)

⭐⭐ **「정신없다」의 정체 = 색도 크기도 아니고 «줄간이 상자마다 달랐던 것»**
🔢 실측(`_probe-가져오기줄간-0824` · 상자 5개 · 글줄 28개) = 줄간이 **네 가지**였다
   `normal` · **1.4**(잔량 알약) · **1.45**(AI 카드) · **1.5**(히어로 설명) ＋ 바닥 `.t-sub` **1.85**
✅ 제일 넉넉한 값(**1.5**)으로 맞췄다 — 좁은 쪽에 맞추면 두 줄짜리가 답답해진다
⛔ **범위를 넓히지 않았다** — `.t-sub` 의 1.85 는 창업자가 정한 값이라 **그 값 자체는 안 건드리고** `.imp` 로 «가져오기 상자 안»에서만 눌렀다

📐 **＋ 「제일 많이 써요」가 칸을 벗어나 있었다** (창업자가 눈으로 잡았다 · 내가 만든 게 아니라 원래 있던 것)
🔢 카드 오른끝 대비 = 412px −20 · **390px ＋2** · **360px ＋32** · **320px ＋72** → 고친 뒤 네 폭 전부 −98 이하
⭐ 뿌리 = 제목이 `nowrap` 이고 알약이 `flexShrink: 0` 이라 **둘 다 줄어들 수가 없다** → `flexWrap: wrap`

⛔⛔ **내가 두 번 「스모크 통과」라고 잘못 말했다**
   ⑴ `EADDRINUSE 4413` — 배경으로 돌리다 끊긴 판이 띄운 **임시 서버 다섯 개**가 포트를 물고 있었다
   ⑵ `dist 가 src 보다 8분 낡았다` — **스모크가 도는 «중»에 내가 CSS 를 또 고쳤다**
   📌 둘 다 게이트가 «맞게» 잡은 것이고 **내 절차가 틀렸다.** 끝난 뒤 exit code 를 보고 말한다.

---

## 3️⃣ 어제 할 일 목록 대조 — **2순위가 통째로 비었다**

📮 창업자 = *"이중에 완료된건 완료처리하자. **문서 확인하고 정확히 파악해줘**"*

**닫힌 것 7개**(전부 코드·파일로 확인) — v11.27 검색 재료 아이콘 · v11.28 요리모드 문구·말풍선 ·
v11.29 표지→일기·달력 · `ig_add01` 김치 컷 · v11.30/31 레시피열쇠 · 토마토 5편 자동 공개
＋ 창업자 정정 = *"레꾸사진은 내가 착각한거야. 그건 완료처리해도 돼."*

⛔ **내가 창업자 목록의 순서를 「앞뒤가 안 맞는다」고 지적했는데 근거가 없었다** —
   grep 0건이라 틀렸다고 말할 수 없었다. 정직하게 보고했고 창업자 = *"아니 그냥 **네가 준대로 할게.**"*

📄 결과 = `docs/로드맵-우선순위.md` **1099줄 「2026-08-24 «저녁» 갱신 ⭐현행」**

---

## 4️⃣ 🍱 음식 96컷 자르기 — 하루의 절반

📮 창업자 시트 **16장**(6컷씩) ＋ *"이거 먼저 뽑아서 순차적으로 줄거야. **지금 앱에 반영된거 갈아끼울거거든**"*

### 파이프라인 (확정)
```
tools/시트-라벨지우기.py <시트> <낼파일> 2 3
tools/cut.py <지운시트> <낼폴더> <접두어> --diecut 3 --drop 0.001
tools/컷-구멍뚫기.py <컷> <원본칸>
```

### ⛔ 사고 ① — **`--drop` 기본값이 접시 테를 뜯었다**
📮 창업자가 확대해서 잡았다 = *"확대해서 보면 파먹인 그림들이 있어"* · *"특히 **1번이 제일 아랫줄에 많고**"*
   → **내 측정과 정확히 일치했다**(s04).
- 뿌리 = 접시 테는 **넓은 흰 띠**라 본체와 가늘게 이어져 도구가 「떨어진 조각」으로 오해한다
- ✅ **`--drop 0.001`** — `0` 은 먼지 때문에 「떨어진 조각 0개」 절대원칙에 걸린다
- ⛔⛔ **이 규칙은 `docs/그릇프레임-만드는법-2026-08-23.md` 에 «어제» 적어뒀는데 옮겨오지 못했다** — **규칙 12 위반**

### ⛔ 사고 ② — **`--punch` 로는 손잡이 구멍을 못 뚫는다**
- 손잡이의 흰색은 «알파의 구멍»이 아니라 **불투명한 몸통**이다
- `0.004` → 접시 테 하이라이트를 먹어 **남색·빨강 초승달**이 생겼다(진한 판에서 보였다)
- `0.012`·`0.015` → 아무것도 안 뚫린다
- ✅ 별도 후처리 도구 `tools/컷-구멍뚫기.py` — **채움률**(넓이÷외접상자)로 가른다
  손잡이 구멍 **0.62~0.77** ↔ 접시 테 하이라이트 **0.05~0.30** · 종지 가운데는 두께 49px 로 잡는다
- ⭐ **`cut.py` 는 한 줄도 안 고쳤다**(창업자 절대원칙 *"자르기도구는 건드리지마"*)

### ⛔ 사고 ③ — **라벨 지우개가 다섯 시트에서 죽었다**
- `MAXINK 0.30` 이 낮았다 — 이름이 길면 글자 띠가 진해진다(측정 최대 **0.464**) → **0.50**
- 여러 줄 라벨을 지우면 **자기가 지운 자리를 「그림 손실」로 세던** 자기검사도 고쳤다(`지운자리` 추적)
- 📮 원인을 창업자가 알려줬다 = *"그건 내가 정확하게 뽑으려고 **설명을 추가한건데 글자에 같이 붙여졌더라**"*
- 결과 **16/16 · 그림 손실 0px**

### ✅ 끝 — **손잡이 냄비는 «그리는 쪽»을 바꾼다**
📮 창업자 = *"어떻하지 ㅠㅠ 이거 안되는거면 빨리 말해 손잡이 없는거로 바꿀게"* →
   *"토큰이 얼마 없어. 더이상 끌지말자. **손잡이 있는 냄비에 들어간 음식 다시뽑을거야 리스트줘.**"*
⭐ **2026-08-23 「그릇 프레임」과 같은 결** — 코드로 푸는 것보다 **그리는 쪽을 바꾸는 게 싸다.**

### 🏷 저장 — 「유지 70」 ／ 「다시뽑을것 26」
📮 창업자 = *"나머지는 저장해둬(**이름표새로 붙여서 따로**) 구별되어야 하는거니까."*
- 96컷 **이름표를 원본 시트 라벨에서 «읽어»** 붙였다(짐작 아님) — **파일 이름이 곧 이름표**
- `docs/stickers/창업자-2026-08-24/음식-유지70/` ／ `음식-다시뽑을것26/`
- ⛔ **다시뽑을 26컷도 지우지 않는다** — 새 컷과 대조할 판이다

⛔⛔ **목록을 「도구가 만진 것」으로 만들었더니 샜다**
   처음엔 「구멍이 뚫린 컷 ＋ s07」로 **25개**를 뽑았는데 **스키야키(`s13_02`)가 빠졌다** —
   손잡이가 «귀 모양»이라 구멍이 없어 뚫기 로그에 안 남았다.
   ✅ **진한 판을 눈으로 훑어서 잡았다**(절대원칙 21). **26이 맞는 수다.**
   📌 **「도구가 만진 것」과 「눈에 보이는 것」은 다른 목록이다.**

### 🔴 ＋ 창업자가 검수판에서 잡은 것
📮 *"저게 뭐야 확대해서 보는데 **빨간 줄이 그어져있잖아**"* → 재보니 **빨간 검수 배경**이었고
   그 색과 우연히 맞은 김치 픽셀은 **14px** 뿐이었다.
📮 그래서 체커보드로 바꿨더니 = *"**체커보드에서는 잘 안보여. 진한판으로 확대해서 보여줘야지**"* — **맞는 말**이고
   우리 절대원칙에도 *"흰 잔재는 까만 판을 키워야 보인다"* 로 이미 있다.

---

## 5️⃣ 오늘 만든 도구

| 도구 | 무엇 |
|---|---|
| `tools/컷-구멍뚫기.py` | 손잡이·종지 구멍만 뚫는다(채움률) ＋ 안쪽만 · 모양 다듬기 · 흰 다이컷 링 |
| `tools/칸-먼지지우기.py` | 안 보이는 먼지를 지워 「떨어진 조각 0개」 게이트를 통과시킨다 |
| `tools/시트-라벨지우기.py` | `MAXINK` 0.50 ＋ 여러 줄 라벨 자기검사 고침 |
| `scripts/_검사-파먹힘-0824.py` | 시트 원본 ↔ 컷 알파를 견줘 파먹힌 비율을 잰다 |
| `scripts/_판-남은음식아이콘-0824.mjs` | 앱 픽커 − 창업자가 준 것 = **남은 276컷** |
| `scripts/_probe-가져오기줄간-0824.mjs` | 상자 안 글줄 줄간 전수 |
| `scripts/_probe-알약넘침-0824.mjs` | 네 폭에서 알약이 칸을 넘나 |

---

## 6️⃣ 오늘 배운 것 (한 줄씩)

1. ⭐ **「도구가 만진 것」과 「눈에 보이는 것」은 다른 목록이다** — 스키야키가 그렇게 샜다
2. ⭐ **어제 적어둔 규칙을 오늘 옮겨오지 못하면 규칙이 없는 것과 같다** — `--drop` 이 그랬다(규칙 12)
3. ⭐ **「정신없다」·「칙칙하다」 같은 말의 정체는 재보면 나온다** — 이번엔 **줄간이 네 가지**였다
4. ⭐ **코드로 푸는 것보다 그리는 쪽을 바꾸는 게 싸다** — 손잡이 구멍을 하루 팠는데 그릇을 바꾸면 0이다
5. ⛔ **스모크는 끝난 뒤 exit code 로 판정한다** — 도는 «중»에 소스를 고치면 게이트가 «맞게» 죽는다

---

## 7️⃣ 안 한 것 (다음)

- ⏳ **96컷을 앱에 안 넣었다** — 창업자 = *"음식은 잘라만 두고 **한번에 적용한다**"*
- ⏳ **손잡이 냄비 26컷** — 창업자가 다시 뽑는다(조건은 「손잡이 없는 그릇」 하나만 바꾸면 된다)
- ⏳ **남은 276컷** — `docs/남은-음식아이콘-2026-08-24.md` 갈래 순서대로
- 📅 **8/28 명의 이전**(창업자) · **8/31 9/1 자동공개 전날 전수 검수**
