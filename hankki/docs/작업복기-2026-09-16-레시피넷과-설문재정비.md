# 작업복기 2026-09-16 — 레시피 넷 · 펭펭 전수검사 · 설문 재정비

**세션** = `session_016KvtRY6zJFv8cZcsmX6fJr` · 앱 **v13.58 → v13.63** · `BASICS_VERSION` **156 → 160**

---

## 1️⃣ 한 일 — 실물로 확인한 것만

### 🍲 레시피 넷 (전부 창업자가 «직접 만들면서» 준 값 · 내가 보탠 것 0)

| 편 | 무엇 | 버전 |
|---|---|---|
| **두부참치찌개** | 재료·걸음 통째로 갈아엎고 검수 통과 → 2026-10-12(월) 로 되살림 | 157 |
| **뚝딱 버섯 볶음밥** | 비빔장 간장 4 → **2큰술** (창업자 *"4큰술은 너무짜;;"*) | 156 |
| **꼬치 없는 꼬치전** | 계란물을 **두 번** 나눠 (바닥에 먼저 · 재료 위에 다시) · 걸음 9→10 | 159 |
| **잡채** | 소고기 → **돼지고기** · 당면 **뜨거운 물** · 검수 붙임 | 160 |

⭐ **넷 다 「이미 나간 편」이라 `BASICS_VERSION` 을 올렸다** — 안 올리면 이미 깔린 폰이 옛 값을 그대로 본다(절대원칙 18ⓙ).

### 🐧 레꾸자랑 옛 펭펭 전수검사 (창업자가 시켰던 것 · 드디어 함)
43장을 **그림을 열어** 다 봤다. 42장 정본 · **`scene_n_09`(캠핑) 하나만 옛 펭펭.**
창업자 판정 = *"둘다 그냥 둬도돼 ㅋㅋ"* → 안 뺐고, **판정을 코드 주석에 박았다**(다음 세션이 또 빼지 않게).

### 🗣 연구소 설문 재정비
「다음엔 뭘 먼저 만들까요 + 보기 둘」 → **「한끼에 뭐가 있으면 좋겠어요?」 열린 물음**으로.
창업자가 구글 폼도 장문형으로 고쳤다.

### 📅 홍보물 올리는 때를 한 곳에
`docs/홍보-언제올리나-2026-09-16.md` 신설 — 매번 그때그때 정해지던 것을 모았다.

### 🛡 게이트 둘 신설
- `check-두스토어.mjs` — 홍보물에 애플·구글 둘 다 (만들자마자 셋 잡음)
- `check-줄설명.mjs` — 주간 줄 설명과 실제 편이 어긋나면 배포 차단

---

## 2️⃣ ⛔ 내가 틀린 것 — 넷

### ⓐ **배포가 실패했는데 내 커밋이 원인이었다**
`_판-식비시안-0916.mjs` 를 만들어 푸시했더니 **검수판 게이트가 「고르는 판인데 저장을 안 한다」고 잡아 배포를 막았다**(run 2569 `failure`).
실제로는 **그림 3장 찍는 판**이라 오검출이었고, **다른 세션이 게이트를 고쳐줬다**.
📌 **뿌리 = 내가 푸시 전에 게이트를 안 돌려봤다.** 빌드·스모크만 보고 올렸다.

### ⓑ **hold 브랜치를 「20여 개」라고 말했다 → 실제 297개**
기억으로 말했다. `git branch -r | grep -c hold/` 한 줄이면 3초였다.

### ⓒ **개발자 인증을 「할 일」로 남겨둬 창업자를 헤매게 했다**
창업자 = *"개발자인증은 못찾아서 못했어"* → **못 찾은 게 아니라 2026-09-09 에 이미 닫힌 건**이었다.
🌲 뿌리 = **고정메모 핀엔 「닫혔다」인데 `CLAUDE.md` 와 로드맵엔 「마감 9/30 · 안 하면 앱이 삭제된다」가 남아 있었다.**
📌 **끝난 걸 대기로 두면 다음 사람이 없는 일을 찾아 헤맨다**(규칙 12ⓑ 그대로). 고쳤다.

### ⓓ **UI에 유니코드 이모지를 넣었다**
식비 시안에 🛒💰🍜 를 박았다. **절대원칙 위반**이다(커스텀 스티커만).
⭐ **내가 눈으로 열어보고 잡았다**(규칙 21) — 안 열어봤으면 그대로 나갔다.

### ＋ 청소 도구가 «지금 세션» 스크래치패드를 지웠다
`sweep.mjs --치움` 이 이 세션(`2414fcda…`)을 **「옛 세션」이라며 지웠다**. 식비 시안 PNG 가 날아갔다.
🟢 **잃은 것은 없다** — 시안은 이미 창업자에게 보냈고 생성기가 저장소에 있어 한 줄이면 다시 찍힌다.
⚠️ 다만 `CLAUDE_SCRATCHPAD_DIR` 로 지금 세션을 빼기로 돼 있는데 **안 먹은 것**이라 여기 남긴다.

---

## 3️⃣ ⭐ 오늘 배운 것

### 「이름으로 거르면 이름만 바꿔 들어온 건 못 막는다」
옛 펭펭 씬을 `scene_camp|cook_kitchen|…` 이름으로 걸렀는데, **`scene_n_09` 는 「새 씬」 이름표를 달고 그림만 옛 펭펭**이었다.
📌 그래서 창업자가 **「눈으로 직접 봐」**라고 한 것이다. 이름이 아니라 실물이 답한다.

### 「검사가 무엇을 보는지 봐야 한다」 — 두 번 나왔다
- `r.chips &&` 는 **빈 배열도 참**이라 보기를 비우니 화면에 빈 줄이 남았다 → 「있나」가 아니라 **「비었나」**
- 내 코트색 잣대(R−B)가 **콤비 컷에선 안 맞았다** — 곰이 화면을 많이 차지해 왜곡. **숫자가 애매하면 눈으로 본다**

### 「어느 쪽이 이겨도 행동이 안 바뀌는 물음은 묻지 않는다」
설문이 죽어 있던 까닭이다. 창업자가 먼저 느꼈다 — *"이게 너무 의미가 없게 느껴져서.."*
- 주간 식단표 = 설계 0 → 1등해도 못 만듦
- 새 꾸미기팩 = 유료로 갈 것 → 1등하면 배신
📌 규칙 31의 세 번째 잣대 그대로다.

### 「재료만 바꾸면 만드는 법이 거짓말이 된다」
잡채에서 소고기를 돼지고기로 바꿀 때 **1·4걸음도 같이** 바꿨다.
9/12 가지 사고(`ids` 만 바꾸고 `why` 를 안 고쳐 나흘간 거짓말)와 **같은 자리**다.

---

## 4️⃣ 💰 식비 기록 — 설계까지 하고 «미뤘다»

리서치·설계 관문 통과·시안 셋까지 끝내고 **창업자 판정 대기**로 뒀다.
📄 전문 = `docs/식비기록-설계-2026-09-16.md`

⭐ 오늘 나온 숫자 몇 개:
- 국내 스마트폰 이용자의 **43%** 가 가계부 앱 사용 · 뱅크샐러드 **148만** · 편한가계부 **70만**
- 가구당 월 식품비 **90만 6천원** · 그중 **외식 48.4%**
- ⛔ 카드 연동은 **마이데이터 허가(자본금 5억)** 가 필요해 못 한다 · SMS 읽기도 Play 정책이 막는다

📮 창업자 = *"식비는 판단이 어려워.."* → **내가 「지금은 미룬다」로 답했다.** 설계는 그대로 살아 있다.

---

## 5️⃣ 🚀 배포 — 오늘 여섯 판

| 버전 | 무엇 | run |
|---|---|---|
| v13.58 | 여는 요일 월요일로 · 브리또 홈에 · 가지 설명 고침 | 2564 ✅ |
| v13.59~60 | 두부참치찌개 · 볶음밥 · 묶음 69편 · 기름 옮김 | 2571 ✅ |
| v13.61 | 꼬치전 | 2572 ✅ |
| v13.62 | 잡채 · 펭 기록 | 2573 ✅ |
| v13.63 | 설문 열린 물음 ＋ 셋 | 2576 ⏳ |

⚠️ **run 2565~2569 가 줄줄이 취소·실패했다** — 여러 세션이 연달아 푸시하면 앞엣것이 취소된다.
📌 **커밋은 안 사라진다**(뒤엣 것 밑에 깔린다) — 마지막 run 이 초록불이면 다 나간 것이다.

---

## 6️⃣ 🛒 쿠팡
**오늘 쿠팡 링크를 누른 적 없다.** 장보기 마음껏 해도 된다.
⚠️ 다른 세션이 눌렀는지는 **모른다**.

---

## 7️⃣ ⏳ 남은 것

| | 무엇 | 누가 |
|---|---|---|
| 💰 | 식비 기록 — 지금은 미룸 (설계는 살아 있다) | 창업자 판정 |
| 📱 | 인스타 스토리 투표(식비 어떻게) — 만드는 법 알려줬다 · **월·금·일 저녁에** | 창업자 |
| ✍️ | 패드 펜 글쓰기 — **어디에 쓰는 건지 안 정해졌다**(원문에 없다) | 창업자 |
| 🍱 | 주간 식단표 — **설계 0**. 물어보기 전에 「무엇을 짜주나」부터 | 같이 |
| 🗂 | hold 브랜치 **297개** 정리 | 클로드 |
| ⏳ | 검수 안 받은 레시피 **17편** (30일 안엔 0번) | 창업자 |
| 🥬 | 한살림만 있는 재료 8개 | |
