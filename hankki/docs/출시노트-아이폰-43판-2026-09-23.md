# 🍎 출시노트 — 아이폰 **43판** · 2026-09-23

> ⏳ **창업자 확인 전에는 콘솔에 안 넣는다.**
> ✅ **버전 = 1.0.7 그대로 · 빌드만 43.** 📮 창업자 2026-09-23 14:28 = *"이거랑 같이 구우려고 취소했어"* (1.0.7 심사 제출을 취소했다).

## ✅ 「이 버전의 새로운 기능」 (2줄 · 103자)

```
내가 담은 레시피만 모아서 볼 수 있어요.

· 목록에서 꾹 눌러 여러 편을 한 번에 폴더로 옮겨요
```

📮 **이 문장은 창업자가 골랐다** (2026-09-23 08:36 = *"1.3"* · 후보 셋 중 1·3).
   ⭐ `changelog.js` 의 `who: '창업자' · asc: true` 줄에서 그대로 뽑았다
   (`node scripts/changelog.mjs --asc --from v14.04`).

## 🔢 줄마다 근거

| 줄 | 판 | 근거 |
|---|---|---|
| 내가 담은 레시피만 모아서 | v14.05 | 「✏️ 내 것」 칩 신설. 잣대 = `data/seed.js` 의 `내것인가`(번호가 `basic-` 이 아닌 편). 전수 재현 17/17(`_repro-내것판정-0923.mjs`) |
| 꾹 눌러 한 번에 폴더로 | v14.05 | 고르기 모드 바에 ［📁 폴더］ 신설. 상세→편집→폴더→저장 **넉 단계 → 두 단계** · 여러 편 한꺼번에 |

## ⛔ 일부러 뺀 것
- **할로윈 접시 · 할로윈 배경 · 기본 그릇 서랍** = 열쇠 뒤라 유저가 못 본다. 쓰면 거짓말이 된다.
- **번호 판정 구멍 막기 · 재현판 17칸** = 유저가 못 느낀다.

## ✅ 버전 번호 — **1.0.7 그대로**
📮 창업자 = *"이거랑 같이 구우려고 취소했어"* — 1.0.7 심사 제출을 «취소»했다.
→ 새 버전(1.0.8)을 만들 필요가 없다. **1.0.7 안에서 빌드만 43 으로 바꾸고** 위 글로 교체한 뒤 다시 제출한다.

## ✅ 진행 기록
| 때(KST) | 무슨 일 |
|---|---|
| 14:27 | 43판 TestFlight 업로드 성공 (로그 실물 = *"Successfully uploaded the new binary to App Store Connect"*) |
| 14:46 | 창업자 = *"아직 41인데?"* → 애플 «처리 중»이라 목록에 늦게 뜬 것(우리 빌드는  이라 올리고 안 기다린다) |
| 14:47 | 창업자 = *"추가했어"* → **1.0.7 에 빌드 43 추가 완료** |
| 14:48 | 창업자 = *"제출했어"* → ✅ **App Store 1.0.7(빌드 43) 심사 제출 완료** |

⚠️ TestFlight 목록엔 **「14.05 (43)」** 로 뜬다 — 버전 숫자는 앱 안 버전을 쓴다(release-ios.yml:95). 1.0.7 과 달라도 붙는다(41판도 그랬다).

## 📌 43판
| 칸 | 값 |
|---|---|
| 빌드 번호 | **43** |
| 앱 버전 | v14.05 |
| 웹 배포 | run 2756 `success` · head_sha `05c3c6f3` · 2026-09-23 09:07 KST |
| 관문 | 빌드 exit 0 · 스모크 **229/229** (아이폰 갈래에서 다시 돌림) |

## 🗂 42판은 무엇이었나
41판과 **내용이 같다** — 1.0.7 을 다시 올리려면 새 빌드 번호가 필요해서 구운 것.
👉 **43판이 나오면 42판은 쓸 일이 없다.**
