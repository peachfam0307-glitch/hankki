# 📊 계측 전수검사 — 2026-09-12 (⭐현행)

> 📮 창업자 = *"이상하게 심어놓고 침소봉대하고;; 당황스럽네"* → *"다 확인해 하나하나. 짐작하지말라했자나"*
> → *"제대로해. 다 고쳐"* → *"다 재고 고쳐"*

## 1️⃣ ⛔⛔ 무슨 일이 있었나

2026-09-10 에 심은 계측이 **반쪽**이었다. 그런데 클로드가 그 숫자로
**「38명 중 1명만 저장했다」**며 사업 판단을 말했다. **근거 없는 말이었다.**

📌 **잘못 잰 숫자는 «안 잰 것»보다 나쁘다** — 그 위에서 판단하기 때문이다.

## 2️⃣ 🔢 전수검사에서 나온 것 여덟 (⭐전부 코드를 «직접 열어» 확인했다)

| | 무엇 | 방향 | 근거 |
|---|---|---|---|
| ⓐ | `recipe_saved` 가 **세 길 중 하나만** | 빠짐 | `addRecipe` = App.jsx:692 · EditorScreen:787 · ImportScreen:239 |
| ⓑ | `shop_added` 가 **네 길 중 하나만** | 빠짐 | `addShopItem(s)` 네 자리 |
| ⓒ | `cook_done` 이 `if (!오늘것)` **안** | 빠짐 | CookScreen — 「만들었어요」 먼저 누르면 0건 |
| ⓓ | `갈래고름` — 흐름 «안»의 **여덟 길**이 안 세짐 | 빠짐 | `setFlow()` 직접 호출 |
| ⓔ | `갈래고름` — 되돌아와 또 누르면 또 셈 | 부풂 | 둘러본 한 사람이 4건 |
| ⓕ | `decor_saved` — 열었다 **뒤로만** 눌러도 셈 | 부풂 | `items` 초기값 = `recipe.decor` · 뒤로가기 = 저장 |
| ⓖ | 꾸미기 둘만 `보내기()` 사용 | 화면 1건 손실 | 「못보낸화면」 칸을 덮는다 |
| ⓗ | **「📷 사진으로 시작하기」가 빈 종이를 염** | ⭐동작이 틀림 | `choose('write')` |

⭐⭐ **ⓗ 는 계측 문제가 아니었다** — 시트가 내내 「사진 찍으면 레시피가 돼요」라 해놓고
　 빈 종이를 줬다. **유저가 속는 자리**였다. 동작을 `photo` 로 고치니 계측도 저절로 맞아졌다.

## 3️⃣ ✅ 고침 — 전부 재현판으로 쟀다 (`_repro-계측전수-0912` · 10칸)

```
① 흐름 «안»에서 갈래를 바꿔도 세진다          (전엔 0건)
② 같은 갈래를 세 번 눌러도 한 번만            (전엔 3건)
③ 상세의 「장보기 담기」가 한 번 세진다        (전엔 0건)
④ 꾸미기를 열었다 «그냥 나가면» 0건           (전엔 1건)
⑤ 「사진으로 시작하기」가 진짜 사진 갈래로     (전엔 빈 종이)
```

## 4️⃣ ⛔⛔ 재현판을 만들며 «내가» 틀린 것 둘 — 재현판이 잡았다

**⑴ 중복 방지 설계가 틀렸다**
　 「목록으로 나가면 셈을 비운다」로 짰는데, **목록↔갈래를 오가는 게 바로 그 둘러보기**라
　 매번 비워져서 **그대로 3건**이었다. → 화면이 떠 있는 동안은 안 비운다.

**⑵ ④칸이 «아무것도 안 보고 있었다»**
　 **안 꾸민** 레시피로 재서 **옛 코드로 되돌려도 통과**했다(`items` 가 비어 옛 잣대로도 false).
　 **꾸며진 편(꽃게탕)**으로 바꾸니 옛 코드에서 **진짜로 죽었다**(1건).
　 📌 규칙 12 — 「옛 값으로 진짜 걸리는지」 돌려봐서 잡았다.

## 5️⃣ 🔎 덤으로 찾은 것 — 캡처가 «안 찍힌 게» 아니었다

`new URL(...).pathname` 이 **한글을 %인코딩**해서 `_%EA%B3%84…jpg` 로 저장되고 있었다.
→ 「캡처가 안 찍혔다」는 내 말이 틀렸고, 그 찌꺼기가 저장소에 커밋까지 됐다.
✅ `join(ROOT, …)` 로 만들고 **`SHOT=1` 일 때만** 찍는다.

## 6️⃣ ⛔ 이 문서가 남기는 규칙

1. **계측을 심을 땐 「그 일이 일어나는 길이 몇 개인가」를 먼저 센다.** 한 곳만 보고 심으면 반쪽이 된다.
2. **「빠지나」와 「부푸나」를 둘 다 잰다.** 하나만 재면 반대쪽이 조용히 틀어진다.
3. **재현판을 만들면 «옛 코드로 되돌려» 진짜 죽는지 본다.** 안 죽으면 그 칸은 아무것도 안 보는 것이다.
4. ⛔ **고치기 전 숫자로 결론 내지 않는다.** 2026-09-12 에 그래서 하루를 틀렸다.

## 7️⃣ ⏳ 아직 «안» 심은 것 (자리는 찾아 뒀다)

| 잴 것 | 이름(안) | 자리 |
|---|---|---|
| 레꾸자랑을 **보냈다** | `brag_shared` | `BragScreen.jsx:186` (`res.shared === true`) |
| 냉장고에 재료를 **넣었다** | `pantry_added` | `PantryView.jsx:102·581` |

⛔ 지금 심어도 읽을 사람이 6명·3명뿐이라 못 읽는다 — 수가 늘면 그때.
