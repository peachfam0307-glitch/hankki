# 🔐 데이터 보안 재신고 — 클라우드 로그인이 나가기 «전»에 (2026-08-27)

> ⭐ **이 문서가 현행이다.** 클라우드 로그인을 내보내는 날 이것부터 본다.

## 📮 왜 지금 파나

창업자 = *"오늘 로그인되게 만들수있어?"* → *"조사 시작해"* → *"다각도로 리서치해줘. 안나오는게 있으면 넘겨짚지말고 지피티나 퍼플렉시티한테 검색해달라고 해줘."*

⛔ 우리 앱은 **TWA** 라 웹을 푸시하는 «그 순간» 유저 폰에 로그인이 뜬다.
   그런데 지금 Play 데이터 보안 신고는 **「사진 · 기기 ID」** 뿐이다 → **푸시하는 순간 신고가 틀려진다.**
   📌 8/05 · 8/15 에 «신고가 낡아서 다시 하는» 일을 이미 두 번 겪었다. 세 번째를 만들지 않는다.

---

## 1️⃣ 🔢 우리 앱이 기기 «밖»으로 보내는 길 — 전수 (코드 실측)

`grep -rn "fetch(|XMLHttpRequest|sendBeacon" src/` ＋ 파이어베이스 SDK 호출을 전부 셌다.

| | 어디로 | 무엇이 나가나 | 지금 신고 |
|---|---|---|---|
| ① **OCR 프록시** `src/ocr.js:75` | `hankki-ocr.annyeong-hankki.workers.dev` → Google Vision | **사진** ＋ 기기 난수 `uid` | ✅ **신고됨**(2026-08-05) |
| ② **Firestore** `src/cloud.js` | `hankki-6a768` (서울) | 레시피 · 일기 · 냉장고 · 장보기 · **프로필(닉네임·소개)** ⛔사진은 털어내고 보낸다 | ⛔ **안 됨 — 새것** |
| ③ **Firebase Auth** `src/cloud.js` | 같은 프로젝트 유저 DB | **이메일 · 이름 · 프로필사진 주소 · 고유 ID** | ⛔ **안 됨 — 새것** |
| ④ **링크 읽기** `src/linkReader.js` | **`r.jina.ai` · `api.allorigins.win` · `noembed.com`** | 유저가 공유한 **URL** | ⛔ **안 됨 — «이미 나가고 있다»** |

### ⛔⛔ ④가 오늘 새로 드러난 것이다 — 클라우드와 무관하게 이미 나간다

`App.jsx:425` 의 **공유 받기**가 `fetchLinkRecipe(link)` 를 부른다. 그 함수가 유저 URL 을 **제3자 셋**에 보낸다.
· ⛔ 셋 다 **우리 서비스 제공업체가 아니다** — 완전히 남의 회사다(Jina AI · AllOrigins · noembed).
· ⛔⛔ **2026-08-05 에 우리는 이걸 「선언 대상 아님」으로 넘겼는데 근거가 «WebView»였다** —
  *"사용자가 오픈 웹을 탐색하는 WebView 에서는 데이터 공유를 선언할 필요는 없습니다"*
  📌 **그런데 이건 WebView 가 아니다.** 우리 코드가 URL 을 서버로 «보낸다». 근거가 잘못 적용됐을 수 있다.
· ⚠️ **위반이라고 단정하지 않는다** — 아래 ❓⑤ 로 물어본다.

### ✅ ②에서 «안» 나가는 것 (실측)
`사진털기()` 가 `data:` 로 시작하는 값을 «전부» 턴다 → **직접 넣은 사진·일기 사진은 클라우드에 안 간다.**
＋ 잠긴 일기는 **비번으로 잠근 채로** 올라간다(`백업용잠그기`).

---

## 2️⃣ ✅ 공식으로 «닫힌» 것

### Firebase Auth 가 갖는 것
> *"Firebase users have a fixed set of basic properties—**a unique ID, a primary email address, a name and a photo URL**—stored in the project's user database."*
> 🔗 https://firebase.google.com/docs/auth/users

→ 구글 로그인을 붙이는 순간 **이메일 · 이름 · 프로필사진 주소**가 우리 프로젝트에 저장된다.
⛔ 화면에 「창업자」라고 뜨는 게 그거다. **«우리가 안 만들어도» SDK 가 갖는다.**

### Firestore ＋ Auth 를 같이 쓰면
> *"If you use Cloud Firestore with Firebase Authentication, and if an end-user is signed-in, then **every request from Cloud Firestore automatically includes the applicable User ID** from Firebase Authentication."*

### 로그인 필수는 정책 위반이 아니다 (2026-08-27 창업자가 공식 원문으로 닫았다)
> *"Google 팀에서 로그인하고 모든 앱 기능에 액세스하는 데 필요한 정보를 제공해야 합니다"*
> 🔗 https://support.google.com/googleplay/android-developer/answer/15748846
📄 전문 = `docs/클라우드저장-구현계획-2026-08-21.md` 맨 아래 「앱 액세스 권한」 절

### 계정 삭제 수단
> 앱 «안»에서 계정을 만들거나 로그인할 수 있으면 **앱 안 삭제 경로 ＋ 웹 링크**를 둘 다 줘야 한다(2024-04-15 전면 시행)
> 🔗 https://support.google.com/googleplay/android-developer/answer/13327111
✅ **둘 다 이미 있다** — 웹 `public/delete-account.html` · 앱 안 `ProfileScreen.jsx:344` 「계정 · 데이터 삭제」

---

## 3️⃣ ⛔ 내가 «못 여는» 것 — 짐작으로 채우지 않는다

🔢 실측(2026-08-27) — 프록시가 막는다:
· `firebase.google.com` = **EGRESS_BLOCKED** (공식 안내 페이지 `docs/android/play-data-disclosure` 가 거기 있다)
· `support.google.com` = 막힘 · `play.google.com` = 막힘
✅ 되는 것 = `WebSearch` 하나뿐이고 **요약만** 온다 → **표가 안 실린다.** 위 ①~③은 요약에 실린 문장으로 건진 것이다.

---

## 4️⃣ ❓ 그래서 «남에게 물어볼 것» 일곱 — 그대로 복사해서 쓴다

> ⭐ 창업자 지시 = *"안나오는게 있으면 넘겨짚지말고 지피티나 퍼플렉시티한테 검색해달라고 해줘"*
> 📌 **답이 오면 이 절에 그대로 붙여넣는다.** 그때 신고 답이 확정된다.

```
Google Play 데이터 보안(Data safety) 신고에 대해 «공식 문서 원문과 링크»를 붙여서 답해줘.
추측이면 추측이라고 반드시 표시해줘. 우리 앱 상황은 아래와 같아.

[우리 앱]
- 안드로이드 TWA (웹앱을 감싼 것). 파이어베이스는 «웹(JS) SDK» 를 쓴다. 안드로이드 SDK 가 아니다.
- 구글 로그인(Firebase Authentication, signInWithPopup)만 쓴다. 이메일/비번 로그인 없음.
- 로그인은 «선택»이다 — 로그인 안 해도 앱 전부를 쓸 수 있다. 클라우드 저장만 로그인이 필요하다.
- 로그인하면 Cloud Firestore(서울 리전)에 유저가 쓴 레시피·일기·냉장고·장보기 목록과
  닉네임·소개글을 올린다. 사진은 «올리지 않는다»(코드에서 전부 걸러낸다).
- 그와 별개로, 유저가 링크를 앱에 공유하면 그 URL 을 제3자 서비스 세 곳
  (r.jina.ai, api.allorigins.win, noembed.com)에 보내서 본문 텍스트를 받아온다.
- 사진에서 글자를 읽을 때는 우리 Cloudflare Worker 를 거쳐 Google Cloud Vision 으로 보낸다(이미 신고함).

[물어볼 것]
1. https://firebase.google.com/docs/android/play-data-disclosure 페이지에서
   «Firebase Authentication» 과 «Cloud Firestore» 줄의 표 내용을 «원문 그대로» 옮겨줘.
   (수집하는 데이터 종류와 Play 데이터 보안의 어느 항목에 해당하는지)
2. 그 페이지는 «안드로이드 SDK» 기준인데, 우리처럼 «웹(JS) SDK» 를 쓰는 경우에도 같은 신고를 하나?
   웹 SDK 용 공식 안내가 따로 있나?
3. 유저가 앱에서 쓴 «레시피·일기 같은 글»을 우리 서버(Firestore)에 저장하면
   Play 데이터 보안의 어느 항목으로 신고하나? (「파일 및 문서」? 「기타 사용자 생성 콘텐츠」? 「앱 활동」?)
   한국어 콘솔의 «정확한 항목 이름»으로 알려줘.
4. Firebase Auth 가 «프로필 사진 URL» 만 갖고 사진 파일 자체는 안 갖는 경우,
   Play 데이터 보안의 「사진 및 동영상 → 사진」을 체크해야 하나?
5. 유저가 공유한 URL 을 «제3자» 서비스(우리 서비스 제공업체가 아닌 회사)에 보내서
   본문을 받아오는 경우, Play 데이터 보안에서 이걸 「공유됨(shared)」으로 신고해야 하나?
   어느 데이터 항목인가? (「웹 검색 및 방문 기록」?)
   ※ 구글 문서에 "사용자가 오픈 웹을 탐색하는 WebView 에서는 데이터 공유를 선언할 필요 없다"는
     문장이 있는데, 우리는 WebView 가 아니라 «코드가 직접 서버로 보내는» 경우다. 그래도 같나?
6. 데이터 보안 신고를 고쳐서 «게시»하면 언제 반영되나? 구글 검토를 기다려야 하나,
   아니면 바로 반영되나? 앱 업데이트와 별개로 처리되나?
7. 앱에 «구글 로그인»이 새로 생기면 «콘텐츠 등급(IARC) 설문»을 다시 해야 하나?
   설문 문항 중 계정 생성·로그인과 관련된 문항이 있나?
```

---

## 5️⃣ ⏰ 그래서 오늘 로그인을 내보낼 수 있나

| | 상태 |
|---|---|
| 코드 | ✅ **끝났다** — v11.57 위에 세웠고 build 0 · smoke 89/89 |
| 앱 안 계정 삭제 경로 | ✅ **있다** (`ProfileScreen.jsx:344`) |
| 웹 삭제 페이지 | ✅ **있다** (`public/delete-account.html`) |
| `@@시행일@@` | ⏳ 배포하는 날 채운다(1분 · 내 일) |
| **Play 데이터 보안 재신고** | ⛔ **막혔다** — 위 ❓ 일곱이 닫혀야 «정확히» 찍는다 |
| 콘텐츠 등급 | ❓ 문서 둘이 갈린다(❓7) |

👉 **판정 = ❓이 닫히면 그날 바로 나간다.** 코드는 기다리지 않는다.
⛔ 신고를 짐작으로 찍고 내보내지 않는다 — 그게 8/05·8/15 에 두 번 겪은 그 자리다.

---

# 🔀 AI 둘 대조 — 퍼플렉시티(무료) ↔ 지피티 (2026-08-27 밤)

> 📮 창업자 = *"퍼플렉시티는 무료모델이라 감안하고 봐야해"* · *"지피티는 돌리는중"*
> ⭐ **창업자가 둘 다 돌린 게 값을 했다** — 하나만 봤으면 ⑤에서 반대로 갔다.

## ✅ 둘이 «같은» 답 — 이건 쓴다

| | 답 |
|---|---|
| **Auth 자동 수집** | Firebase user agent · **IP 주소** · User agent strings · Firebase App ID |
| **Auth 사용에 따라** | 표시 이름 · 이메일 · (전화번호) · **Firebase Authentication User ID** |
| **Firestore** | 자동은 user agent뿐 · **우리가 넣는 데이터는 «우리가» 분류한다** · 로그인 상태면 요청마다 User ID 자동 포함 |
| **레시피·일기·냉장고·장보기·소개글** | **「앱 활동 → 기타 사용자 제작 콘텐츠」** (공식 예시에 *"사용자 약력·메모·서술형 답변"*) |
| **닉네임** | **「개인 정보 → 이름」** (공식 정의에 닉네임 포함) |
| **photoURL 만 있을 때** | 「사진」 체크 **불필요** — 단 둘 다 **「해석」이라고 표시**했다(공식 일대일 매핑 없음) |
| **웹(JS) SDK 전용 Play 문서** | **둘 다 «못 찾았다»** → 안드로이드 표를 그대로 베끼지 않고 **실제 전송**으로 판단 |
| **IARC 재설문** | 로그인이 생겼다는 이유«만»으론 불필요 — **설문 답이 바뀌면** 다시 한다 |

⭐ **로그인이 «선택»이라 이 데이터는 전부 「선택사항」으로 신고한다** — 공식 문서가 *"로그인할 때만 수집되고 로그인 없이도 앱을 쓸 수 있는 경우"* 를 선택적 수집 사례로 든다(지피티 인용).

## ⛔⛔ 갈린 곳 «하나» — ⑤ 링크 읽기. **지피티가 맞다**

| | 답 |
|---|---|
| 퍼플렉시티 | *"서비스 제공업체로 간주 → 「공유됨」 아님"* |
| **지피티** | ***"원칙적으로 「공유됨」에 해당"*** ＋ 공식 인용 |

📌 **공식 정의(지피티 인용)** =
> *"'공유'란 앱에서 수집한 사용자 데이터를 **서드 파티에 전송**하는 것을 의미합니다."*
> ＋ **퍼스트 파티 서버 → 서드 파티 서버 «서버 간» 전송도 공유에 포함**

🔎 **내가 퍼플렉시티를 의심한 근거도 그대로 살아 있다** —
   우리는 `r.jina.ai`·`api.allorigins.win`·`noembed.com` 과 **계약이 0**이다.
   「서비스 제공업체」는 *"개발자 «대신», 개발자의 «안내에 따라»"* 처리하는 주체인데 그런 약속이 없다.
   ＋ 퍼플렉시티 답 안에서 **정의가 꼬였다**(*"공유는 서드파티가 제3자로 넘기는 것"* ← 틀린 문장) ·
     인용 링크가 **전부 같은 URL 하나**였다.

✅ **WebView 예외는 적용 안 된다** — 둘 다 같은 결론.
   *"사용자가 오픈 웹을 «탐색»하는 경우"* 가 예외인데, 우리는 **앱 코드가 URL 을 골라 외부 API 로 보낸다.**
   ⛔ **2026-08-05 에 우리가 이 예외를 잘못 적용했다.** 그 판단은 물린다.

⭐⭐ **지피티가 «새로» 알려준 것 = 「사용자가 시작한 작업」 예외**
> 사용자가 직접 시작한 작업이고, **데이터가 제3자와 공유될 것을 사용자가 합리적으로 예상**할 수 있거나
> **명시적 공개·동의**를 거친 경우엔 「공유됨」으로 공개하지 않아도 된다.

👉 그래서 갈래가 셋이 된다 (⏳창업자 판정) —
   **㉮ 호출을 뺀다** = `App.jsx:425` 의 `fetchLinkRecipe` 한 줄 → **제3자로 나가는 URL 이 0** → 질문 자체가 사라진다
        ⛔ 잃는 것 = 공유받은 링크에서 제목·재료 자동 채우기(주소만 저장)
        📌 v11.19 에서 **「가져오기」 화면은 이미 그렇게 했다** — 공유 받기만 남은 것이다
   **㉯ 두고 「공유됨」으로 신고** = 항목은 **「웹 탐색 → 웹 방문 기록」**이 가장 가깝다(지피티도 **[해석]** 표시)
   **㉰ 두고 «안내를 붙여» 예외를 노린다** = 「링크 내용을 불러오려고 외부 서비스에 주소를 보내요」
        ⚠️ 예외 충족 여부는 **우리가 판정할 수 없다** — 위험을 안는 쪽

## 🚨🚨 ⑥ 이것이 「오늘 배포」를 가른다 — **신고는 «즉시 반영»이 아니다**

**둘 다 같은 답** = 데이터 보안 수정은 **구글 검토를 거친다.**
· 검토 **최대 7일**(예외적으로 더) · 승인 뒤 모든 기기에 반영까지 **며칠~최대 7일**
· ✅ **AAB 없이 데이터 보안만 고쳐 낼 수 있다**(정책 → 앱 콘텐츠 · 「검토를 위해 전송」)

⛔⛔ **그런데 우리는 TWA 라 «웹 푸시 = 즉시»다.** 순서가 뒤집히면 —
   **앱은 이미 이메일·이름을 받는데 스토어 신고는 「수집 안 함」**인 창이 최대 7일 생긴다.
   📌 2026-08-02 반려가 «신고와 실물이 어긋난» 자리였다. 같은 모양을 또 만들지 않는다.

✅ **그래서 안전한 순서 = 신고 먼저 → 승인 → 그 다음 웹 푸시.**
   ⚠️ *"승인 전에 내보내면 반드시 위반"* 이라고 «단정»하지는 않는다 — 그 문장을 못 봤다.
   ⭐ 다만 **위험이 한쪽으로만 크다** — 늦게 내보내서 잃는 건 며칠, 어긋나서 잃는 건 앱이다.

## 🗓 그래서 판정

⛔ **오늘(8/27) 로그인 배포는 «안 한다».**
✅ **오늘 할 것 = 창업자가 데이터 보안 신고를 고쳐 «제출»한다.** 그 순간부터 시계가 돈다.
⏭ 승인이 떨어지면 그날 배포한다(코드는 이미 끝났다 · build 0 · smoke 89/89).

## 📋 콘솔에 넣을 값 — 이대로 찍는다

| 데이터 | 항목(한국어) | 수집 | 공유 | 필수/선택 | 목적 |
|---|---|---|---|---|---|
| 구글 계정 이름·닉네임 | **개인 정보 → 이름** | 예 | 아니오 | **선택사항** | 앱 기능 · 계정 관리 |
| 구글 계정 이메일 | **개인 정보 → 이메일 주소** | 예 | 아니오 | **선택사항** | 앱 기능 · 계정 관리 |
| 구글 고유번호 · Firebase UID | **개인 정보 → 사용자 ID** | 예 | 아니오 | **선택사항** | 앱 기능 · 계정 관리 |
| 레시피 · 일기 · 냉장고 · 장보기 · 소개글 | **앱 활동 → 기타 사용자 제작 콘텐츠** | 예 | 아니오 | **선택사항** | 앱 기능 |
| 사진(직접 넣은 것) | 「사진」 | **아니오**(클라우드엔 안 올린다) | — | — | — |
| 공유받은 URL | **웹 탐색 → 웹 방문 기록** | ⏳㉮㉯㉰ 판정에 달렸다 | ⏳ | 선택 | 앱 기능 |
| OCR 사진 · 기기 난수 | 기존 그대로 | 예 | 아니오 | 선택 | 앱 기능 |

⚠️ **항목 «이름»은 콘솔 화면으로 마지막에 맞춘다** — 지피티는 「기타 사용자 «제작» 콘텐츠」,
   퍼플렉시티는 「기타 사용자 «생성» 콘텐츠」로 썼다. **한 글자 차이라도 콘솔 글자가 맞다.**
⚠️ **IP 주소는 별도 체크칸이 없다**(지피티) — 위치를 추론하지 않으면 체크할 곳이 없다. 콘솔에서 확인.

## ❓ 아직 열려 있는 것

· ⑤ 갈래 ㉮㉯㉰ — **창업자 판정**
· 항목 이름 표기 · IP 처리 — **콘솔 캡처로 닫는다**
· *"승인 전 배포가 위반인가"* — 그 문장은 못 봤다. **그래서 안전한 순서로 간다.**

---

## 📱 창업자용 판 (폰에서 콘솔 보며 쓴다)

**https://claude.ai/code/artifact/a60e9a36-d192-4716-afbe-e0d64c51c421**

· 항목 **11개**를 0~5단계로 — 들어가는 길 → 새로 켤 넷 → 그대로 둘 둘 → ⛔안 켤 둘 → 다시 볼 문항 → 검토 제출
· ☑️ **체크가 저장된다**(localStorage) · 📋 **항목 이름 복사** 단추 · 맨 아래 **결과 만들어 복사**
· 절대원칙(2026-08-19 창업자 *"무조건 검수판은 체크+복사되게"*) 지킴 — 복사가 막히면 **글자를 골라 주는** 폴백까지
⛔ 값이 바뀌면 **같은 URL 로 갱신**한다(새 주소 만들지 않는다).
