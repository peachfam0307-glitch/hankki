# 📊🔔 2026-09-19 (3) — 식비 계측 「읽는 판」 · 장부 12일째 · 인스타 꼬리표 · 알림 설계

> 앞 세션 두 개 = `작업복기-2026-09-19-식비공개와-릴스확정.md` 계열. 이 판은 **밤(20:00~22:10)** 몫이다.

## 1. 🍚 식비 계측 — 「재는 눈」은 갤럭시, 「읽는 판」은 내가

📮 창업자 = *"안녕, 우리 식비탭도 재는거 넣어야하지않아?"*

🔢 실측으로 확인한 구멍 = `FoodCostView.jsx` 에 계측 부르는 줄 **0개**.
　 식비는 «화면»이 아니라 장보기 탭 «안»의 칸(`ShopScreen.jsx:164` `view === 'cost'`)이라 `화면봄()` 에 안 잡힌다.

⭐ **같은 파일을 둘이 만지지 않게 갈랐다** — 눈 심기는 갤럭시 세션에 넘기고(표 그대로), 나는 읽는 판만.
⛔ 그때 내가 제안한 표에서 **한 칸을 고쳐 보냈다** = 「가게 이름」을 보내면 안 된다.
　 🔢 근거 = `store.jsx:1270` `addCostShop` 이 `name.slice(0, 20)` 으로 **유저가 친 글자**를 저장한다
　 → `stats.js` 규칙 ④(「내용은 절대 안 보낸다」)와 `privacy.html` 이 깨진다. 기본 목록이면 `cs_*` 아이디, 나머지는 전부 `custom`.

✅ 갤럭시가 20:36 에 넣었다(v13.78 · run 2674). 다만 내 제안과 «모양»이 달랐다 —
　 갈래를 매개변수가 아니라 **이벤트 이름 «뒤»**에 붙였다(`foodcost_added_shop` · `foodcost_shop_open_cs_coupang`).
⭐ **그래서 장부에 칸을 «안» 만드는 게 맞았다** — 갈래가 행동 칸에 그대로 들어간다. 따로 가르면 검산에서 **두 번 센다.**
　 대신 장부 규칙에 「만들지 말 것」을 박았다.

### 만든 것 = `계측-보고.mjs` §4.5 식비 깔때기
- 열었나(`foodcost_open`) → 적었나(`_shop`＋`_direct`) → 가게로 나갔나(`foodcost_shop_open_*`)
- **적은 비율**을 «≤»로 적는다 — 두 갈래를 다 한 사람이 두 번 세지니 더한 값은 «최대»다
- 연 사람 **20명 아래면 ⚠️** (창업자 *"20~30명 표본이면 볼 수 있어"*)
- 값이 없으면 절을 **안** 만든다 — 빈 표를 「0」으로 읽는 사고 막기

## 2. ⛔⛔ 그 김에 잡은 버그 둘 — 둘 다 「조용히 죽는」 꼴이었다

### ① 보고서가 «통째로» 멎었다
🔢 실측 = 식비 눈이 20:36 에 심기자 `계측-잣대.mjs` 가 「공정한 공통 구간이 없다」며 **exit 2**,
　 그 순간 `계측-보고.mjs` 가 **한 줄도 안 나왔다**(장부는 멀쩡했다).
⛔ 뿌리 = **나가는 값(exit code)을 믿었다.** 잣대의 exit 2 는 「비율을 견줄 수 없다」지 「심은 날을 모른다」가 아니다 — 목록은 다 찍혀 있었다.
✅ 고침 = 실패해도 stdout 을 받아 읽고, **한 줄도 못 읽었을 때만** 죽는다.
📌 **새 눈을 심는 날마다 날 일이었다.** 오늘 아니었으면 다음 달에 났다.

### ② `foodcost_added_` 가 「심은 날 모름」
🔢 실측 = 손으로 `git log -S"foodcost_added_"` 를 돌리면 멀쩡히 나온다.
⛔ 뿌리 = 잣대가 **코드 줄을 «통째로»** `-S` 에 넘기는데, `sh()` 가 셸로 돌아서 그 줄의 **백틱을 «명령 치환»으로 먹었다.**
✅ 고침 = 찾을 조각에 백틱·`$` 가 없으면 «그 조각»을 쓴다(`git log -S` 는 조각이면 충분하다).
🧪 전후 대조 = **늘어난 줄 셋뿐, 나머지 48가지 값 그대로.**

## 3. 📒 장부 12일째 (2026-09-19) — 검산 91 = 91

| 칸 | 값 |
|---|---|
| 개요 | 활성 8 · 새 4 · 재방문 4 · 조회 91 · 이벤트 106 · **참여 3분05초(12일 중 1등)** |
| 활성창 | **30일 242(↑1.68%)** · 7일 197(↓3.9%) · 1일 8 |
| Play 실적(28일) | 노출 2,120 → 설치 159 → **처음 실행 71** → 7일유지 2 · **전환율 56.36%** |
| 열쇠통(달) | 76/900 · 유저 28 · 오늘 AI 0건 · 기본인식 갈래 **없음 0** |
| 쿠팡 | 클릭 96 · 구매 3 · **유저 수익 0원**(전부 창업자 본인 구매) |
| 도시 | 부산·춘천·대구·거제·광주·평택·서울·영주 **한 명씩** |

### ⛔ 내가 틀렸다가 물린 것
「오늘이 광고 없는 «바닥값» 첫날」이라고 적었는데 창업자가 잡았다 — *"광고 오늘부터 딱 하나 붙였어. 꾸미기 릴스 새로 만든거"*.
9/18 = 광고 2개(묵은 소재) · 9/19 = 1개(새 소재). **갯수도 소재도 다르다** → 절대원칙(광고 걸린 값끼리 안 견준다)에 걸린다.
✅ 장부 `고침` 칸에 남겼다. **바닥값 구간은 아직 «없다».**

### ⛔ 식비는 「0」이 아니라 «눈이 없었다»
foodcost 넷이 21:06(run 2674)에야 나갔다 → 오늘 표에 없는 건 당연하다. **깔때기 첫 하루치는 9/20.**

## 4. 🏷 인스타 꼬리표 — 「광고가 일하는지」를 처음 보이게

📮 창업자 = *"갑자기 인스타로 유입되는 사람들이 너무 없어지네..."* → *"꼬리표 붙여줘"*

🔢 장부 12일치로 확인 = Organic Social 새 사용자 **36 → 16 → 8 → 6 → 4 → 0**
　 ⛔ 그런데 Direct 는 14·20·8·6·10·4 로 **안 끊겼다** — 그 안에 광고분·스토어검색분·재방문이 섞여 있다.

⛔ **스토어 꼬리표는 «이미» 붙어 있었다**(Play 09-08 · 애플 09-16). 2026-09-16 에 내가 「없다」고 틀리게 말한 자리다 — 그래서 **이번엔 파일을 먼저 열고** 말했다.
✅ 새로 만든 것 = 「인스타의 «어느 자리»에서 왔나」 · `docs/꼬리표-인스타-링크-2026-09-19.md`
　 프로필=`utm_medium=profile` · 광고=`paid`＋`utm_content=<소재>` · 릴스=`reel`
✅ **앱 코드는 안 고쳤다** — `get.html` 이 이미 주소의 꼬리표를 달고 `page_view` 를 보낸다(gtag 가 `page_location` 을 싣는다).
⏳ 실물 확인 = 내일 GA4 「트래픽 획득」에 `instagram / paid` 가 뜨는지 눈으로.

## 5. 🔔 알림(웹 푸시) 설계 — 「부를 길이 없다」를 가르는 실험

📮 창업자 = *"활성사용자가 이렇게 떨어진다는 거는 우리앱이 막상들어와도 레시피가 별로거나 볼게 없다는건가.."*

### 숫자는 반대쪽을 가리켰다
- 떨어진 건 **새 사용자**(50→4)지 재방문(10→4)이 아니다
- 오늘 참여 시간이 **12일 중 1등**(3분05초) · 레시피 탭 44초 · 상세 43초
- 즉 **들어온 사람은 오래 본다.** 「별로다」면 이 값이 거꾸로 나와야 한다

### 대신 코드에서 확인된 사실
🔢 `grep` = 푸시 코드 **0줄** · `android/twa-manifest.json` 의 `enableNotifications = false`
👉 **유저가 «스스로 기억해서» 아이콘을 눌러야만 돌아온다.** 우리가 한 번도 부른 적이 없다.
⭐ 그래서 「별로여서 안 돌아온다」는 **아직 관측된 적 없고**, 「부를 길이 없다」만 확인된 사실이다.

✅ 설계 = `docs/알림-설계-2026-09-19.md` · 창업자가 확정한 것 넷
| | |
|---|---|
| 권한 묻는 자리 | **장바구니 담을 때 · 냉장고 재료 넣을 때** (⛔앱 켜자마자 안 묻는다 — 거절당하면 영영 못 부른다) |
| 월·수 레시피 | **오후 3~4시** (15:30 제안 · *"장보러가기전이어야 하는데"*) |
| 토 장바구니 | **토요일 아침 9시** |
| 새 꾸미기 | **열리는 날 저녁 8시** → 첫 알림 = **2026-10-01(목) 20:00 핼러윈** |
| 횟수 | ⛔ **하루 한 번만** · 겹치면 하나로 합친다 |

🔢 꾸미기 「열리는 날」은 실물이 있다 = `src/data/seasonDecor.js:14` 명절창(추석 09-09~09-30 · 핼러윈 10-01~11-02)
　 ⛔ 음력이라 해마다 밀린다 → 알림은 **그 파일을 읽어서** 보낸다(두 곳에 적으면 어긋난다)

💰 **돈은 거의 0원** — 서비스워커·매니페스트는 우리 코드, 보내는 쪽은 **이미 쓰는 Cloudflare 워커·KV**
　 ⚠️ 유일한 좁은 문 = **KV 쓰기 1,000/일** → 알림 보낼 때 «읽기만» 하게 짠다
⛔ **앱을 다시 내야 한다** — `enableNotifications` 는 «껍데기를 굽는» 설정이라 웹 배포로는 안 켜진다

## 6. 📚 바깥 숫자로 확인한 것 (창업자가 불안해한 자리)

| 물음 | 찾은 답 |
|---|---|
| 초반엔 다 이런가 | Play 앱의 **68%는 다운로드 1,000을 영영 못 넘는다**(AppBrain) · 대부분 100~500에서 멈춘다 |
| 얼마나 지켜보나 | PMF 는 **30일 잔존률**로 본다 · ASO 효과는 **2~4주** · ⛔「이 숫자면 PMF」라는 보편 기준은 **없다** |
| 광고 안 돌리면 망하나 | **반대다** — 잔존률이 건강해지기 «전»에 광고를 부으면 밑 빠진 독. 🔢 우리 9/14 = 50명 와서 **28초** |
| 30일 잔존률 중앙값 | **약 4%** — 우리 DAU/MAU 3.3%는 그 언저리 |
| 깔고 안 여는 55% | ⛔ **기준값을 못 찾았다.** 찾은 건 「설치 중 약 25%는 딱 한 번 열고 끝」뿐 |

## 7. 🚨 오늘 밤 사고 하나 — 자동 동기화가 작업 트리를 쓸어갔다

🔢 실측 = §4.5 를 다 만들고 커밋하려는데 `nothing to commit, working tree clean`. 원격이 HANDOVER 커밋으로 앞서가서 훅이 `reset --hard` 했다.
✅ 살아남은 까닭 = **패치 조각을 전부 스크래치패드에 파일로 남겨 뒀다.** 그대로 다시 얹고 잣대 출력이 **글자 하나까지 같은 것**을 확인했다.
📌 교훈 = `tools/subst.py` 의 `--old-file`/`--new-file` 을 쓰면 **작업이 날아가도 조각이 남는다.** 앞으로도 그렇게.

## ⛔ 안 한 것 / 남은 것
- 알림 **문장** — 창업자 말투가 들어가야 하는 자리라 내일 맑은 머리로
- **15:30 이 맞는지** — GA4 시간대 그래프(참여도)로 굳힌다 · ⛔창업자 캡처 필요
- **7일 기기 유지율 「2」** 가 %인지 대수인지 — 그 숫자에 마우스 올린 설명 한 줄 캡처 필요
- 유통기한 푸시(2판) — 명의 이전은 끝났지만 방침·Play 데이터보안 신고가 딸려온다
