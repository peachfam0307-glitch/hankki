> 🗄 **보관소** — 끝난 세션 기록이다. ⛔창업자가 지시할 때만 연다(절대원칙 24).
> 🧹 `scripts/doc-trim.mjs` 가 HANDOVER.md 에서 옮겼다 (2026-09-04).

<!-- 원래 자리: ## 바로 다음 행동 -->
### ✅ **[2026-09-04 · 끝났다] 패드 릴스 시안 12장 ＋ 창업자 지적 넷 — 앱 코드는 v12.47 로 나갔다**

📮 창업자 = *"이따 와서 시안볼게. **날아가지 않게 잘 가지고 있아**"* (＝쉬러 갔다)

📦 **다 담겨 있다 = `hold/패드릴스-0904` 브랜치** (푸시 완료 · ⛔배포 브랜치엔 안 올렸다)
   다시 뽑는 법 = `BACKUP=<백업.json> node hankki/scripts/_shot-패드릴스-0904.mjs`
                 → `node hankki/scripts/_판-패드두장-0904.mjs`
   ⚠️ 백업은 **창업자 개인 데이터** — `/tmp/claude-0/창업자백업.json` 에만 있고 저장소엔 «없다».
      컨테이너가 되감기면 **창업자에게 다시 받아야 한다**(9/4 아침에 준 그 파일).

✅ **이번에 고친 것 넷** (창업자 지적 넷에 하나씩)
   ⑴ 요리모드 재료 = 패드에서 **두 열 ＋ 19px → 30px** (앱 코드 · `CookScreen.jsx`＋`styles.css`)
   ⑵ 한끼일기 = **내가 가을로 꾸민 일기**를 넣었다(「오늘의 한끼」 틀 ＋ 크라프트)
   ⑶ 릴스② = **짙은 숲 살림노트**로 옷을 통째로 갈았다(바탕·글씨·장식·테두리·번호 다섯 다)
   ⑷ 릴스② 2칸 → **6칸** (큐레이션 셋 ＋ 영수증 잡음)

▶️ **창업자가 돌아오면**
   ① 시안 12장 판정을 받는다(바탕·글씨·카피·짜임)
   ② 통과하면 **영상으로 뽑는다** = `bash hankki/design/promo/인스타-2509/조립-패드두장.sh`
      ⚠️ 그 판은 아직 **한 릴스만** 만든다 — 릴스①/② 를 갈라 두 번 돌리게 `CARD_DIR`·`OUT` 을 준다
      ⚠️ 반짝임 먼저 = `SET=스샷 node hankki/scripts/_판-반짝임-0903.mjs`
   ③ ⑴번(앱 코드)은 **창업자가 「배포해」 할 때만** 배포 브랜치로 옮긴다(규칙 9·13)

❓ **아직 못 한 것**
   · 「사진 넣기」 안내가 캐릭터 뒤로 **아주 살짝** 비친다(거의 안 보인다 — 창업자가 보면 더 덮는다)
   · 일기는 패드 «가로»에서 세로 종이 하나라 좌우가 빈다(앱 화면 그대로라 판으로는 못 고친다)

