# ✂️ 표준 도구가 «못 자르는» 시트 — 원인까지 좁혔다 (2026-08-18)

> 📮 창업자 = *"저게 왜 생긴거야?? **원본있을텐데 다시잘라 땜빵하지말고**"* → *"네 판단대로해"*
> 🎯 이 문서 = **다음에 같은 벽을 만나면 «여기서» 시작한다.** 처음부터 다시 파지 않게.

---

## 1️⃣ 무슨 일이었나

창업자 제보 = *"누룽지 삼계탕에 **이모지같은게** 들어있어"*
→ 그 「이모지 같은 것」 = **김 구름**(`fe_114` 오징어누룽지 컷의 오른쪽 위).

⭐⭐ **구름은 창업자가 준 원본 그림에 «원래» 있다.** 우리가 만든 게 아니다.
⛔ 그런데 **원본 시트에선 그릇과 «떨어져»** 있고, **우리 낱개에선 가는 꼬리로 «이어져»** 있다.
   → **자르기가 둘을 붙였고**, 그래서 절대원칙 ②(떨어진 조각 0개)를 그대로 빠져나갔다.

📌 창업자 말이 정확했다 — **원본에서 다시 자르면 구름은 저절로 떨어져 나간다.**

---

## 2️⃣ ⛔ 내가 먼저 한 «땜빵» — 창업자가 잡았다

앱 컷(`fe_114.png`)의 **알파를 지워서** 구름을 없애려 했다. 세 번 손댔고 그때마다 자국이 남았다.
⭐ **왜 나쁜가** = 구름이 그릇 테두리 «위»에 겹쳐 그려진 자리는 **지워도 그 아래가 없다.** 구멍이 된다.
✅ 창업자 지적 즉시 `git checkout` 으로 되돌렸다.

---

## 3️⃣ 🚫 그런데 «다시 자르기»가 안 된다 — 표준 도구가 자기 검사에 죽는다

| 시도 | 결과 |
|---|---|
| 시트 전체 (`cut.py <시트>`) | 🚫 **외곽선 파먹음 96.1%** (기준 99.5%) |
| 그 칸만 크롭 | 🚫 **떨어진 조각 7개** (옆 그림이 딸려옴) |
| 더 좁게 크롭 | 🚫 **외곽선 파먹음 94.7%** |
| `--drop 0` (조각 안 떼기) | 🚫 **95.3%** — 조각 떼기가 원인이 «아니다» |
| `--smooth 0.5` | 🚫 94.7% — 가장자리 흐림이 원인이 «아니다» |
| **`--diecut keep`** | ✅ **외곽선 검사 통과** · 🚫 흰 테 두께 0.27%(규칙 0.35~1.30%) |

---

## 4️⃣ ⭐ 원인 — **원본에 흰 다이컷이 «이미» 그려져 있다**

`keep`(＝원본 흰 테 두께를 실측해 그대로) 만 외곽선 검사를 통과했다는 것이 근거다.
→ 이 시트의 그림들은 **흰 테 ＋ 칼선을 이미 달고** 있다.
   우리 도구가 그 위에 **또** 흰 테를 두르면 **원본 칼선이 흰 띠 «사이»에 갇히고**,
   검사는 그걸 「진갈색 외곽선을 파먹었다」로 읽는다.

📌 **`CLAUDE.md` 에 이미 적혀 있는 사고와 같은 뿌리다** —
   *"원본에 흰 다이컷이 이미 그려져 있으면 그걸 «살리지» 말고 버리고 우리 두께로 다시 두른다.
     살리면 흰 테 ／ 원본 회색선 ／ 또 흰 테 로 겹쳐 회색선이 흰 띠 «안»에 갇힌다(2026-08-13)."*

⛔ 그래서 `keep` 은 쓰면 안 되고(두 겹이 된다), 버리면 검사에 걸린다. **양쪽이 다 막혀 있다.**

---

## 5️⃣ ✅ 그래서 어디까지 했나 · 무엇이 남았나

**✅ 창업자가 본 문제는 고쳤다 (v11.05 배포)**
「누룽지삼계탕」에 붙던 그림을 **구름 없는 삼계탕(`fh_k06`)** 으로 바꿨다.
→ 구름 컷(`fe_114`)은 이제 **「누룽지탕·오징어누룽지」에만** 붙는다.

**🚫🚫 [창업자 확정 2026-08-18] 자르기 도구는 «손대지 않는다».**
> 📮 창업자 원문 = *"**자르기도구는 건드리지마.**"*
⛔ `tools/cut.py` 를 이 건으로 고치지 않는다. 아래 「손댈 방향」은 **창업자가 다시 열라고 할 때만** 쓴다.
⭐ 왜 = 창업자가 *"폭발직전"* 이라며 만든 절대원칙이고, 느슨하게 하면 **1,731컷 전부의 안전장치가 약해진다.**

**⏳ (닫아둔 것) 도구의 검사가 「원본 칼선」과 「그림 외곽선」을 구분 못 한다**
고치려면 `tools/cut.py` **442~456줄 절대원칙 관문**을 손대야 한다 — ⛔지금은 안 한다.

⭐ **손댈 때의 방향(다음 사람에게)**
`_원본선` 을 셀 때 **「원본 흰 테 «바깥»의 칼선」은 빼고** 센다.
= 그림 본체(흰 테 안쪽)의 외곽선만 보호 대상으로 삼는다.
🧪 고치면 **규칙 12** 로 확인할 것 — 일부러 외곽선을 깎은 판이 여전히 exit 1 로 죽어야 한다.

---

## 6️⃣ 🔎 전수 조사 — 「자르기가 붙인 혹」이 얼마나 있나

침식(5×5)으로 «가는 목»을 끊어 세어 보니 **1,731컷 중 22컷**(1.3%).
⛔ **그런데 정작 `fe_114` 가 그 22개에 «없다»** — 목이 두꺼워 안 끊긴다.
   그리고 22개 대부분은 캐릭터 팔·리본처럼 **원래 가는 부분**이다.
📌 **이 잣대는 시끄럽고 대상은 놓친다 → 게이트로 만들지 않았다**(시끄러운 게이트는 죽은 게이트).

---

## 7️⃣ 📌 배운 것

⭐ **「떨어진 조각 0개」 검사는 «붙여버린 것»을 못 잡는다.** 붙으면 조각이 아니게 되니까.
⭐ **원본과 결과를 견주지 않으면 「자르기가 만든 것」과 「원본에 있던 것」을 못 가른다.**
   이번에도 **원본 시트를 열어보고서야** 구름이 원래 떨어져 있었다는 걸 알았다.
⭐ 가설을 둘 세웠고 **둘 다 틀렸다**(조각 떼기 탓 / 흐림 탓). 옵션을 하나씩 돌려서야 답이 나왔다.
📌 **짐작으로 도구를 고치지 않고, 돌려서 갈랐다.**
