# 🔗 링크 본문 읽기 — 「서버 되면 꼭」 (2026-08-21)

> ⭐ 이 문서가 사는 이유 하나 = **창업자가 「꼭 하자」고 한 것을 잊지 않기 위해서.**
> 창업자 원문 = *"**C는 되면 좋으니까 서버되면 꼭 하자**"*

---

## 무슨 일이 있었나 (한 문단)

창업자 = *"우리 링크는 아예 안돼 원래"* → *"AI가져오기처럼 링크 넣으면 자동으로 레시피가 작성되는게
아니라 그냥 보관함에 담기고 직접입력해야하잖아. **그건 내가 말한 저장이 아니야 그걸 누가써**"*

갈래 셋을 냈고 창업자가 **ⓑ**(정직하게 이름 바꾸기)를 위임했다(*"네가 알아서해."*).
**ⓒ(서버로 제대로 읽기)는 「서버 되면 꼭」으로 살아 있다.**

---

## ⛔ 왜 지금 안 되나

`src/linkReader.js` 가 **남의 서버 두 곳**에 얹혀 있다.

| 단계 | 부르는 곳 | 문제 |
|---|---|---|
| ① 본문 읽기 | `r.jina.ai` | 남의 무료 서비스 — 되고 안 되고가 **우리 손 밖** |
| ② HTML 읽기 | `api.allorigins.win` | 같은 문제 ＋ 느리다 |
| ③ 제목만 | `noembed.com` | 제목 하나 건지는 정도 |
| 유튜브 | — | **아예 시도 안 함**(`linkReader.js:120`) |

⭐ 그래서 창업자 폰에서 **거의 언제나 실패**했다.
⛔ 그런데 앱은 「블로그 글 읽어오기」라고 **약속하고 있었다.** 그게 「고장」으로 읽힌 진짜 이유다.

---

## ✅ 지금(ⓑ) 해둔 것 — 되살리기 쉽게

**코드를 지운 게 아니라 「자리를 표시해 두고」 뺐다.**

| 표시 | 파일 | 무엇 |
|---|---|---|
| `⏳ 서버 되면 되살릴 것 ①` | `src/screens/ImportScreen.jsx` | `readLink()` 함수 자리 |
| `⏳ 서버 되면 되살릴 것 ②` | 〃 | 「본문 자동 읽기 시도 (베타)」 버튼 자리 |
| `⏳ 서버 되면 되살릴 것 ③` | 〃 | 「읽는 중…」 기다림 화면 자리 |

⭐ **`src/linkReader.js` 는 «안 지웠다»** — 공유받기(`App.jsx:374`)가 아직 쓴다.
   그쪽은 **백그라운드**라 실패해도 유저를 붙잡지 않고, 문구도 이미 정직하다
   (「링크는 담았어요 · 내용은 자동으로 읽지 못했어요」).

⭐ 검사도 있다 = `scripts/_repro-링크정직-0821.mjs` (18칸).
   심장 = **「안 되는 걸 약속하는 글자가 남아 있나」**를 화면 innerText 로 잰다.

---

## ⏳ 서버 되면 할 일 (순서)

1. **Worker 에 본문 읽기 길을 낸다** — `ocr-proxy/worker.js` 에 `/read?url=` 하나.
   ⭐ 이미 있는 것을 쓴다 = **CORS·IP 제한·Origin 검사**가 그 파일에 이미 있다.
   ⛔ 🚨 **그 파일을 «그대로» 대시보드에 붙여넣지 말 것** — 파일 맨 위 경고 참조
      (batch 코드는 «만들어만 두고 안 올린» 것이다).
2. `fetchLinkRecipe` 의 남의 서버 세 곳을 그 길로 바꾼다.
3. 위 표의 **①②③ 을 되돌린다**(git 히스토리 = 이 문서와 같은 날 커밋의 부모).
4. **문구를 되돌린다** — 「링크 주소만 담아두기」 → 「링크 붙여넣기」 등.
5. `_repro-링크정직-0821.mjs` 의 **금지 목록에서 「본문 자동 읽기」를 뺀다.**

### ⚠️ 그때 생각할 것 둘

- 💰 **돈이 드나** — 본문 읽기는 Vision 과 달리 **글자만** 오간다. Worker 무료 한도 안이면 0원.
  ⛔ 다만 **남의 사이트를 대신 긁는 일**이라 IP 제한을 우리가 관리해야 한다.
- ⚖️ **저작권** — 「조리법은 아이디어라 저작권이 없다」는 이미 확인했다
  (`docs/레시피-저작권-확인-2026-08-01.md`). ⛔남의 **글·사진을 그대로 옮기는 것**만 안 된다.
  → 읽어온 글을 **파서로 재료·순서만 뽑는** 지금 방식이 그 선 안이다.

---

## ⛔ 안 할 것

- ⛔ 이 일을 **9월 결제 켜기보다 먼저** 하지 않는다(창업자 = *"되면 좋으니까"* — 급한 게 아니다).
- ⛔ 「남의 서버를 하나 더 찾아 붙이기」로 때우지 않는다. 같은 문제가 반복된다.
