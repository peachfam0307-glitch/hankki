# 📦 AAB versionCode 12 — 지금 할 것 (2026-08-05)

> 창업자 *"준비해줘 이거 확인하고 바로 하게"*
> 앞 계획은 8/07~08 이었지만(창업자 확정), **오늘 v9.78 이 배포돼서 지금 구워도 된다.**
> ⚠️ 8/13~14 의 versionCode **13** 은 그대로 남는다 — 재심사(8/16) 직전 한 판.

---

## 0️⃣ 한 줄 요약

**GitHub Actions 에서 「AAB 빌드」를 `versionCode 12` 로 돌리고, 나온 파일을 Play Console 비공개 테스트에 올린다.**
클로드가 값은 다 맞춰뒀다. **창업자가 할 건 버튼 두 번 ＋ 업로드.**

---

## 1️⃣ 먼저 확인 — 서명키 Secret 4개가 아직 있나

⚠️ **이것만 확인하면 나머지는 자동이다.** 없으면 빌드가 「서명키 Secret 이 비어 있다」로 멈춘다.

`github.com/peachfam0307-glitch/hankki` → **Settings** → **Secrets and variables** → **Actions**

여기에 이름 네 개가 보이면 된다(값은 안 보이는 게 정상):

```
ANDROID_KEYSTORE_BASE64
ANDROID_KEYSTORE_PASSWORD
ANDROID_KEY_PASSWORD
ANDROID_KEY_ALIAS
```

- ✅ **있으면** → 2️⃣로.
- ⛔ **없으면**(8/1 계획에 「다 쓰면 지운다」가 있었다) → 다시 넣어야 한다.
  ⚠️⚠️ **값은 채팅에 붙여넣지 말 것.** 창업자가 그 화면에서 직접 입력한다.
  만드는 법은 `docs/AAB-재빌드때-한번에-2026-07-31.md` §7 에 그대로 있다.

📌 **클로드는 Secret 이 있는지 «볼 수 없다».** 마지막으로 확인된 건 **8/3 02:57(UTC) 빌드가 성공했다**는 사실뿐이다.

---

## 2️⃣ 빌드 돌리기

`github.com/peachfam0307-glitch/hankki` → **Actions** 탭 → 왼쪽 목록에서 **「AAB 빌드 (targetSdk 지정)」**
→ 오른쪽 **`Run workflow`** 버튼

칸이 넷 뜬다. **전부 기본값 그대로 두면 된다** (클로드가 맞춰뒀다):

| 칸 | 값 | 왜 |
|---|---|---|
| Branch | `claude/chatgpt-conversation-link-kvn5ph` | 배포 브랜치 |
| **versionCode** | **`12`** | 콘솔 최신이 **11**(9.49)이라 그보다 커야 한다 |
| versionName | **비워둠** | 비우면 `src/version.js` 에서 **자동으로 읽는다**(지금 **9.78**) |
| targetSdk | `36` | Google Play 요구치(기한 8/31) |
| dryRun | **끔** | 켜면 서명 없이 연습만 하고 파일이 안 나온다 |

**`Run workflow`** 누르고 기다린다(보통 몇 분).

---

## 3️⃣ 결과 내려받기

실행이 **초록불**로 끝나면, 그 실행 화면 **맨 아래 `Artifacts`** 에 zip 이 생긴다. 내려받아 압축을 푼다.
→ 안에 **`.aab`** 파일이 있다.

---

## 4️⃣ Play Console 에 올리기

**비공개 테스트** 트랙 → **새 버전 만들기** → `.aab` 업로드 → 출시 노트 → 검토 → 게시

### ⚠️ 지난번에 걸린 함정 셋 (또 밟지 말 것)

1. **`이미 사용된 버전 코드입니다`** — 같은 파일을 **다시 업로드**해서 난다.
   다른 트랙에 넣을 땐 **`⬆업로드` 말고 `📁 라이브러리에서 추가`**.
2. **이메일 목록엔 「추가 버튼」이 없다** — 칸에 치고 **`Enter`**. 안 누르면 `변경사항 저장`이 회색이다.
3. **막히면 번호를 올려 새로 굽는 게 빠르다** — 콘솔과 씨름하는 것보다.
   (8/1 에 예비로 8 을 구웠는데 결국 안 썼다. 번호는 버려도 값이 안 든다)

### ⛔ 트랙은 「명수」로 고른다

**비공개 테스트** 트랙엔 **`한끼테스터` 목록이 붙어 있어 바로 전원에게 나간다.**
지금은 재심사(8/16) 전이라 **테스터에게 나가는 게 맞다** — 반려 사유 ②(「업데이트로 의견을 반영했다」)에 그대로 쓰인다.
⛔ **14일 동안 테스터 목록 체크 두 개(한끼2·한끼테스터)는 절대 풀지 말 것.**

---

## 5️⃣ 이 판에 실려 나가는 것 (출시 노트 재료)

**v9.78** — 오늘(8/5) 배포된 웹과 같은 내용이다.

- 📦 **처음 켤 때 받는 양 215MB → 4.1MB**
- 🖼 어두운 배경에서 **음식 아이콘이 하얗게 보이던 것** 수정
- 🖼 **아이콘을 바꿔도 한 번에 안 바뀌던 것** 수정
- 🍱 **음식 아이콘 16종 추가** ＋ 「계란찜」에 계란말이가 붙던 것 수정
- 🌧 「비 오는 창」 배경 ＋ 비 내리는 효과
- 🔒 유료팩 자산 준비(⛔**결제는 안 켰다** — `sellable` 전부 `false`)

📌 **셋은 창업자 폰 제보로 그날 고친 것**이다 — 재신청 답변의 「의견 → 고침」 기록에 그대로 들어간다.
목록 = `docs/의견-로그.md`

---

## 6️⃣ 클로드가 미리 맞춰둔 것

| 파일 | 무엇 |
|---|---|
| `.github/workflows/build-aab.yml` | `versionCode` 기본값 **11 → 12** · 설명을 「Play 최신 = 11(9.49)」로 · **낡은 주석 정정**(「한 번도 실행해 본 적이 없다」 → 8판 중 최근 4판 성공) |
| `android/twa-manifest.json` | `appVersionCode` **7 → 12** · `appVersionName` **9.35 → 9.78** (워크플로가 덮어쓰지만 **낡은 값을 안 남긴다**) |

✅ **런처명 게이트 통과** — `ㅤ한끼ㅤ` 4글자·채움 문자 앞뒤 1개씩(대칭).
⚠️ 이 값은 **새 AAB 를 뽑아야** 폰에 반영된다. 지금 폰 이름이 왼쪽으로 쏠려 보이는 건 옛 AAB 라서다.

---

## 7️⃣ 끝나고

- ⏭ **8/13~14 = versionCode 13** (재심사 직전 한 판)
- ⏭ **8/16 프로덕션 재신청**
- 👀 며칠에 한 번 **일일 활성 기기** 그래프 — 2~3 → **목표 6~7**
  ⛔ **DAU 그래프는 보지 말 것**(2026-08-05 콘솔에 구글 장애 배너)
