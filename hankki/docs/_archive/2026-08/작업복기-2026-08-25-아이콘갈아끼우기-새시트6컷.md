# 🍽 2026-08-25 — 「106컷 갈아끼우기」의 반쪽을 채웠다 ＋ 새 시트 6컷이 깨끗하게 잘렸다

> ⭐ 이 문서 = 그날 확정된 것만. 초안·중간본은 안 적는다(절대원칙 20).

---

## 1️⃣ v11.33 — 저장된 레시피의 «박힌 icon» 을 새 컷으로 (배포 완료 · run #1705)

📮 창업자 = *"배포됐다는데 내 폰에서는 전혀안보여."* → 확인 뒤 *"ㄱㄱ"*

### ⛔⛔ 뿌리 — v11.32 는 «규칙»만 갈았다

아이콘은 레시피를 만들 때 `recipe.icon` 으로 **박혀서 굳는다**
(`Thumb.jsx` = `recipe.icon || guessFoodIcon(제목)` — **박힌 값이 규칙을 덮는다**).

v11.32 는 `ICON_RULES` 의 **키만** 갈았으므로 →
**앞으로 «새로» 만드는 레시피만** 새 그림을 받고, 이미 저장된 건 영영 옛 그림이었다.

🔢 **실측** — 창업자가 본 것이 정확했다

| 보는 곳 | 새 컷으로 바뀐 편 |
|---|---:|
| 창업자가 직접 저장한 레시피 | **0편** |
| 열린 기본 레시피 57편 | **9편** (전부 파스타류) |
| 아이콘 고르기 목록 | 106개 다 있음 |

📌 **규칙 18 ⓙ 를 v10.76 에 이어 또 밟았다** — 「새로 까는 사람」만 보고 «이미 깔린 폰»을 안 봤다.

### ✅ 고침

- `store.jsx` `migrateBasics` 끝에 **`ICON_SWAP_V88`(옛키→새키 90쌍)** ＋ **`ICON_FORCE_V88`(제목 5요리)**
  · 90쌍은 **v11.32 커밋 diff 에서 기계로 뽑았다**(손으로 안 적었다)
- `BASICS_VERSION` **87 → 88** — 안 올리면 이미 깔린 폰에서 그 패스가 안 돈다
- `version.js` **v11.33**

### ⛔ 일부러 «뺀» 다섯 — 옛 키를 다른 요리가 아직 쓴다

| 옛 키 | 아직 쓰는 요리 | 그래서 |
|---|---|---|
| `fe_86` | 브런치 | 제목으로만 → `fe_455` |
| `fe_87` | 피자 | 제목으로만 → `fe_456` |
| `fh_k25` | 냉면·막국수 | 제목으로만 → `fe_394`(물냉면) |
| `fj_c03` | 필라프 | 제목으로만 → `fe_462`(볶음밥) |
| `fy_y05` | 샐러드(범용) | 제목으로만 → `fe_444`(토마토샐러드) |

⭐ **키로 통째 갈면 그 요리가 엉뚱해진다.** 그래서 «제목이 정확히 같을 때»만 잡는다.

### 🔒 안전장치

- `thumb` 이 `icon` 이 아니면(직접 넣은 사진·카드·글자 표지) **건드리지 않는다**
- **옛 그림 파일은 안 지웠다** — 되돌릴 수 있다

### 🧪 게이트 = `scripts/_repro-아이콘갈아끼우기-0825.mjs` (smoke · 10칸)

⭐⭐ **심장 = 「저장된 값이 진짜로 바뀌었나」다. ⛔「규칙이 새 키를 내놓나」가 아니다.**
   규칙은 v11.32 에서도 멀쩡했는데 창업자 폰은 안 바뀌었다. 그래서 소스를 안 보고
   **`localStorage` 에 실제로 뭐가 들어 있나**를 읽는다.

**규칙 12 검증** — `BASICS_VERSION` 을 87 로 되돌리니 **갈아끼우기 다섯 칸이 죽고**(5/10)
「안 건드려야 하는 것」 셋은 초록불로 남았다. 되돌리면 10/10.

---

## 2️⃣ 새 시트 6컷 — 「자르기 편차」가 실물로 풀렸다

📮 창업자 = *"이게 네가 말한대로 추가해서 뽑은거 잘되나 봐봐(컷)"*

**어묵꼬치 · 쫄면 · 핫도그 · 토스트 · 만두튀김 · 오이샌드위치** — **6/6 깨끗.**
4배로 확대한 접시 테두리가 **끊긴 데 없이 매끈**하다.

### ⭐⭐ 결정적인 반례 = 시트 17

| | 흰 여백 | 테두리 선 | 결과 |
|---|---:|---|---|
| 시트 02·03·12 | 43~52% | 있음 | ⛔ 전멸 |
| **시트 17** | **25.9~31.9%** | **있음** | ⛔ **파먹혔다** |
| **새 시트** | **8.7~12.1%** | 있음 | ✅ 6/6 |

⭐ **테두리가 «있는데도» 파먹힌 시트가 있다**
→ **자르기 성패를 가르는 건 «테두리»가 아니라 «흰 여백»이다**(상관계수 −0.796 이 실물로 확인됐다).

### ⚠️ 남은 것 = 납작하다

| | 가로/세로 |
|---|---|
| 창업자가 통과시킨 106컷 | 1.01 ~ 1.40 (중앙 **1.18**) |
| **새 시트** | **1.41 ~ 1.55** — 106컷 최대치보다 전부 위 |

원인 둘 — ①시트가 `1774×887`(2:1)이라 칸이 가로로 납작 (원본 32장 중 **31장이 `1254×1254` 정사각**)
②보는 각도가 낮다(45도쯤)

⛔⛔ **「동그랗게」와 「안 파먹히게」가 서로 당긴다** — 위에서 볼수록 빈 접시 바닥이 넓게 보인다.
   **각도만 올리면 자르기가 다시 깨진다**(시트 17이 그 증거).

### ✅ 다음 시트 프롬프트 (창업자에게 드림)

```
1254 × 1254

접시를 살짝 위에서 내려다본 각도로.
완전히 수직으로 내려다보지 말고, 옆에서 본 것도 아닌 그 중간으로.

음식이 그릇을 꽉 채우게. 넓은 빈 접시 여백이 안 보이게.
접시 테두리에는 진갈색 얇은 선.
```

**목표 = 가로/세로 1.1~1.3 · 흰 여백 25% 아래**

📁 낱개·원본·측정값 = `docs/stickers/음식-창업자-2026-08-25/README.md`
⏳ **앱엔 아직 안 넣었다** — 창업자 판정 대기(절대원칙 13)

---

## 3️⃣ 앱에 반영 안 된 목록을 닫았다

- `?` 셋을 **원본 시트 17번을 열어 라벨을 직접 읽어** 채웠다 —
  **매운해물볶음 · 야채볶음 · 브로콜리볶음** (＋ 두부조림 → **두부조림(간장)**)
- **합계 = 86종 · 92컷** (다시 뽑을 86 ＋ 준비 끝난 6)

📄 목록 = `docs/음식컷-다시자를86컷-2026-08-24.md`

---

## 4️⃣ 질문 도장 기록에 가짜가 111건 쌓여 있었다

`_test-지원팀문의-0818` 의 **PASS 칸은 「일부러 통과」하는 검사**인데,
통과하는 순간 `ask-log.json` 에 도장이 하나 찍힌다.
스모크가 돌 때마다 하나씩 쌓여 **115건 중 112건이 같은 가짜 도장**이었다.

⭐ **그냥 지저분한 게 아니다** — `submission-gate` 는 이 기록을 보고 「이미 물어본 것」을 막는다.
   가짜가 쌓이면 **진짜 질문이 「중복」으로 잘못 막힌다.**

✅ 검사가 PASS 칸 앞뒤로 기록을 **떠놨다 되돌린다**(`finally` 라 죽어도 되돌린다) ＋ 가짜 111건 정리(진짜 4건 보존)
🧪 두 번 연달아 돌려도 4건 그대로 · 스모크 전체 뒤에도 4건 그대로

📌 **파일 맨 위 주석이 *"통과 쪽은 일부러 안 잰다 — 기록이 쌓여서"* 라고 «이유까지 적어놨는데»
   나중에 PASS 칸을 붙이면서 그 이유를 안 봤다. 주석은 못 막는다 — 되돌리는 코드가 막는다.**

---

## 5️⃣ 오늘 배운 것

⭐ **「규칙을 고쳤다」와 「이미 저장된 값이 바뀌었다」는 다른 말이다.**
   v11.32 는 전자만 했고 게이트 50여 개가 전부 초록불이었다. **창업자 폰을 열어야만 드러났다.**

⭐ **반례 하나가 상관관계를 확정한다.** 시트 17(테두리 있음 · 흰 여백 26~32% · 파먹힘)이
   「테두리가 원인」 갈래를 죽이고 「흰 여백이 원인」을 남겼다.

⭐ **검사가 자기 기록을 더럽히면 그 기록을 쓰는 다음 검사가 조용히 망가진다.**
