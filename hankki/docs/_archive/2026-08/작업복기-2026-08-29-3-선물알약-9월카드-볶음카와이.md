# 작업복기 2026-08-29 (3) — 선물 알약 · 9/1 카드 7장 · 「볶음」 카와이

> 🗂 이 세션이 한 것만 적는다. 같은 날 다른 세션의 복기 둘(`-2 가져오기-열쇠안쓰는길` ·
> `한장씩-AAB18판`)은 **안 건드렸다**(세션 분담 — 남의 절을 고쳐 쓰지 않는다).

## 1️⃣ 선물 택 알약 = 오렌지 (`--gift`)

📮 창업자 = *"알약색은 안바꿩?"* → 실물 셋을 보고 **"좋아"**

- `styles.css` `:root` 에 **`--gift: #a85f2e`** 신설 — ⛔테마마다 갈라 두면 한 곳만 고쳐 조용히 어긋난다(v11.17 사고)
- `DecorEditor.jsx` 의 `GiftTag` 배경을 `var(--brown)` → **`var(--gift)`**
- ⭐⭐ **왜 오렌지가 튀나** = `--brown` 이 사실 **더스티 블루(`#5878a0`)** 라 앱 포인트가 전부 파랗다.
  그래서 **오렌지 알약 하나만 유일하게 튄다** = 「선물이 눈에 띈다」의 뿌리.
- 🔢 대비 실측 = 흰 글자 **4.84**(기준 4.5) · 다크 카드 3.39(이론 상한 3.56이라 거의 최적)
  ⛔ 더 밝게(#c9743a)는 흰 글자가 3.5로 죽고, 더 어둡게(#9e5729)는 다크 카드와 3.00까지 붙어 묻힌다
- 🛠 판 = `scripts/_shot-선물알약-0829.mjs` — 세 테마에서 **화면에 칠해진 색을 읽는다**(흉내가 아니다 · 절대원칙 30)

⛔ **＋ 그 자리에서 잡은 것** — `Stickers.jsx:1729` 의 `hint` 주석엔 「창업자가 직접 썼다」인데
   **값은 내 초안**이었다(커밋 히스토리에도 없다 = 그 편집이 유실됐다).
   창업자 문구 **「직접 찍은 음식 사진을 접시에 담아보세요」** 로 갈아끼웠다.

## 2️⃣ 테마 스와치 2×2 (v11.93)

📮 창업자 ＋ 폰 캡처 = *"이렇게말고 2×2로 올리자 빼빼로인줄..ㅋㅋ"*

- 넷이 한 줄에 들어가 **글자가 세로로 쪼개졌다**(빼빼로)
- `ProfileScreen.jsx` → `gridTemplateColumns: 'repeat(2, minmax(0, 1fr))'` ＋ 버튼의 `flex: 1` 제거
- ⛔⛔ **`1fr` 은 `minmax(auto, 1fr)` 이다** — `word-break: keep-all` 과 만나면 안 끊기는 낱말이 칸을 벌린다(v11.24 교훈).
  진짜 1/n 은 **`minmax(0, 1fr)`**
- 🔢 칸 폭 82 → **153px**(390px 기준). 360px 이하에선 「뮤트로 그레이지」가 낱말 단위 2줄(빼빼로 아님)
- ⛔ **줄 수 잣대가 거짓말을 했다** — `getClientRects().length` 로 재서 320px 2줄을 **1줄로 보고**했다.
  **인라인 요소만 줄마다 rect 를 준다**(block 이면 두 줄이어도 1개) → **Range 로 글자 상자**를 재야 진짜 줄 수가 나온다.
  캡처를 `Read` 로 열어보고 잡았다(절대원칙 21)

## 3️⃣ 하트컷 `au_b13` 제거 (v11.94)

📮 창업자 = *"하트컷은 빼자."*

- 가을 꼬르곰 16컷 중 **이 하나만 앞치마가 하트**고 나머지는 계란후라이(시그니처)
- ⛔ **서랍엔 이미 빠져 있었고 카드 풀에만 남아 있었다** → 「서랍엔 없는데 카드엔 나오는」 어긋남이 될 뻔했다
- ⛔ PNG 와 `PHOTO_RATIO` 는 그대로 — 그 키로 저장한 카드가 깨진다

## 4️⃣ 여름 카드 스킨을 9/1 에 뺀다 — `isPeakSeason` 신설

📮 창업자 = *"레꾸자랑은 9월에는 여름꺼는 빠지는거 아냐?"* → **"9월1일에 빼야지 가을시작이니까."**

- `season.js` 에 **`isPeakSeason = (s, now) => seasonsNow(now)[0] === s`** — 전환기 겹침을 **안** 센다
- `ShareDrawCard.jsx` 가 여름 판정에 이걸 쓴다 → **9/1 에 바다·물결 카드가 바로 빠진다**
- ⛔ **스티커·서랍 정렬엔 쓰지 말 것** — 거기선 겹침(`OVERLAP_DAYS = 14`)이 «일부러» 넣은 배려다
  (창업자 2026-07-30 *"여름에 준비한 아이템들은 며칠 못하고…"*)

⛔⛔ **여기서 내가 두 번 틀렸다 — 제일 크게 배운 것**
  1. `SEASON_AT`(월 기준 `m>=9 → autumn`)**만 읽고** *"9/1부터 가을이니 여름 빠진다"* 라고 창업자에게 답했다
  2. `OVERLAP_DAYS = 14` 를 발견하고 `isSeason` 을 **실제로 돌려보니** 9/1~9/14 는 **둘 다 true** 였다
  📌 **뿌리 = 상수 하나만 보고 함수의 답을 짐작했다**(규칙 18). 겹침 로직이 그 위에 한 겹 더 있었다.

## 5️⃣ 9/1 레꾸자랑 카드 스킨 7장 — 창업자에게 실물로

📮 창업자 = *"레꾸자랑 뭐뭐 올라가는지만 확인하자"* → *"어떻게 올라가는지 샘플만들어놨었나>?"* → **"7장을 보여달라고"**

- 🛠 `scripts/_shot-자랑카드-0901.mjs` — **FakeDate 로 시계를 9/1 로 속이고** `?card=<키>` 로 스킨을 지정해 찍는다
  (`ShareDrawCard.jsx:161` · 뽑기 운에 안 맡긴다)
- 7장 = **warm · panel · pola · mag · arch · night ＋ chuseok** (여름 빠짐 · 핼러윈은 10/1부터)
- ⛔ **첫 판이 「카드 못 찾음」 7장이었다** — 레꾸자랑 탭은 *"자랑할 레시피를 눌러주세요"* 라
  **레시피를 먼저 골라야** 카드가 뜬다. 화면 전체 캡처를 `Read` 로 열어보고 알았다(절대원칙 21)
- ✅ 7장을 타일로 묶어 창업자에게 보냈다(전부 `Read` 로 눈으로 확인한 뒤)

## 6️⃣ v11.95 — 「볶음」 대표 컷이 아직 카와이였다 ⭐제일 중요

📮 창업자 제보(남편 폰 캡처) = *"레시피 기본인식했는데 남편폰. 또 카와이 아이콘이 자동인식됐어.
   **카와이 다 내린거 맞아?**"*

- 🔢 실물 확인 = 「차돌박이 볶음」 → **`fe_64`**(접시에 눈·볼터치·입) · `ICON_RULES:1457` 의 **[볶음·볶이]** 가 부른다
- ⭐⭐ **뿌리 = 8/27 에 새 사진 119컷으로 갈아끼울 때 «이름이 1:1로 짝지어진 컷»만 바꿨다.**
  「볶음」처럼 **넓은 낱말이 부르는 «일반 컷»**은 짝이 없어서 **옛 카와이가 그대로 남았다.**
  📌 이 자리는 **가장 많은 요리가 지나가는 문**이다 — 한 줄이 수십 편의 표지를 정한다.
- ✅ 고침 = `[['볶음','볶이'], 'fe_64']` → **`'gr_387'`**(제육볶음 실사 · 얼굴 없음 · 흰 접시)
- ✅ 안 깨졌다(실측) — 떡볶이 `gr_003` · 버섯볶음 `gr_457` · 낙지볶음 `gr_267` · 김치볶음밥 `gr_337`
  (구체 규칙이 위에 있어 먼저 이긴다 — 「구체어 먼저」 v10.89)
- ⛔ 파일은 안 지웠다 — 그 키로 저장한 레시피가 깨진다
- ⚠️ **이미 저장된 레시피는 안 바뀐다**(`recipe.icon` 이 박혀 규칙을 덮는다 · 규칙 18 ⓙ) → 창업자에게 알렸다

### ⏳ 남은 것 = 옛 컷 173개 전수 검사 (창업자 판정 대기)

🔢 실측 = `ICON_RULES` 가 부르는 컷 = **옛(`fe_`·`fh_`…) 173개 · 새(`gr_`·`n`…) 365개**
⛔ **그중 몇 개가 카와이인지는 «안 셌다»** — 173장을 눈으로 봐야 안다(얼굴 감지를 자동으로 못 한다)
📌 **「짧은 낱말」로 걸리는 옛 컷 34줄**이 특히 위험하다 — 그 한 줄이 여러 요리를 덮는다:
   `fe_503`(튀김) · `fh_k18`(생선) · `fe_395`(면·라면·우동…) · `fe_65`(치킨) · `fe_494`(초밥) ·
   `fe_87`·`fe_456`(피자) · `fh_k22`(김밥) · `fh_k29`(파전) · `fe_67`(솥밥) · `fe_76`(게장) …
⭐ **다음 세션이 할 일** = 173컷(또는 위 34줄 대표컷)을 판으로 뽑아 **창업자가 「카와이/괜찮음」을 고른다**(규칙 11)

## 7️⃣ CI 에서만 죽는 검사 — 원인은 못 밝히고 «다음에 보이게» 만들었다

- 첫 배포(run #1938)가 `_repro-원문보기-0828.mjs` 하나로 죽었다(「공심채볶음」 30초 타임아웃)
- ⛔ **원인 규명 실패** — 로컬 3번 ＋ CPU 부하 1번 = **4번 다 통과.** 재현 안 됨
- ⛔ 포트 충돌 가설을 세웠다가 **틀렸다**(그 판은 스모크 목록에 없었다)
- ✅ 그래서 고침 = 「고쳤다」가 아니라 **「다음에 또 죽으면 원인이 보이게」** —
  시트 치우기 ＋ **「도착했나」 확인** ＋ 증거 남기기(화면 글자·저장된 레시피 목록·캡처)
- ⭐ 못 가면 **뒤 칸을 초록불로 만들지 않는다**(규칙 18 ⓘ — 통과했는데 아무것도 안 잰 것)
- 🧪 규칙 12 = 일부러 못 찾을 제목으로 바꾸면 ①-b 가 뜨고 4칸이 죽는다

## 8️⃣ 창업자 판정 — 인스타 홍보는 접는다

📮 창업자 = *"인스타 홍보는 크게 효과가 없는 것 같아. 거의 건너뛰기하고..
   내가 제일 자신없는게 홍보라서.. **유저 별로 없어도 욕심안부리려구**"*

⭐ **우리 구조와 맞는 판단이다** — 한끼는 레시피가 쌓일수록 값이 커져 **「온 사람이 남기」가 「많이 데려오기」보다 세다.**
   ＋ 매달 컷이 저절로 열려 앱이 혼자서도 굴러간다.
⛔ **클로드가 먼저 인스타·홍보를 다시 꺼내지 않는다.** 창업자가 꺼낼 때 답한다.

## 📦 오늘 나간 판

| 판 | 무엇 |
|---|---|
| (선물 알약) | `--gift` 오렌지 ＋ 창업자 문구 |
| **v11.93** | 테마 스와치 2×2 |
| **v11.94** | 하트컷 `au_b13` 카드 풀에서 제거 |
| (여름 스킨) | `isPeakSeason` — 9/1 에 여름 카드 빠짐 |
| **v11.95** | 「볶음」 카와이 → 실사 `gr_387` (run #1944 success) |

🔒 전부 build 0 · smoke 108/108
