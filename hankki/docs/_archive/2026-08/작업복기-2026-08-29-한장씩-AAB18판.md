# 🌙 작업복기 — 2026-08-29 새벽 (🎨앱 갈래)

> 세션 = 2026-08-28 밤 ~ 2026-08-29 01:20 KST
> ⛓ **같은 시각 다른 세션이 🤖AI 다듬기(열쇠) 갈래를 팠다** — ⛔그쪽 절은 안 건드렸다(세션 영역 분담).
> 이 복기는 **🎨앱 갈래**만 적는다.

---

## 1️⃣ 한 것 (시간 순)

### ⑴ 📱 v11.80 배포 — 「한 장씩 따로따로」
- 앞 세션에서 만든 ⓑ를 창업자 **"배포해"** 로 배포 브랜치에 얹었다(cherry-pick → `fc867061`).
- ⛔ 커밋 메시지가 *"아직 안 나갔다 · 배포 브랜치엔 안 올린다"* 로 **낡아 있었다** → 고쳐서 올렸다.
- ✅ **run #1882 · `completed` ＋ `success` ＋ `head_sha fc867061`** 로 확인.
  ⛔「푸시했으니 됐겠지」로 말하지 않았다.
- ⭐ **`cancel-in-progress: true` 를 발견했다** — 배포가 도는 중에 푸시하면 **그 배포가 취소된다.**
  (앞서 run #1878~1880 이 「cancelled」였던 이유) → 초록불 뜰 때까지 아무것도 안 올렸다.

### ⑵ 🤖 「지금까지 쓰던 게 AI 였나」 — 창업자 물음에 코드로 답했다
📮 창업자 = *"근데 지금까지 우리가 쓴 규칙으로 한거 ai들어간거였잖아."* ·
　 *"열쇠가 없는 유저도 ai를 무료로 사용… **그럼 열쇠를 누가사..**"*

| | 읽기 | 정리 |
|---|---|---|
| 파일 | `src/ocr.js` | `src/parseRecipe.js` (1,022줄 · **`fetch` 0건**) |
| AI 인가 | 구글 Vision = **AI 맞다** | **아니다** — 밖으로 안 나가면 AI 를 부를 방법이 없다 |

⛔⛔ **「열쇠 없어도 AI 로 읽는다」는 틀렸다 — 코드가 막는다**
　 `ocr-proxy/worker.js:157` = 웰컴 소진 ＋ 월 5회 초과면 `user_quota` 로 **거절**(구글을 안 부른다)
　 → `ocr.js:410` 에서 tesseract 로 떨어진다 → 화면 = 「무료 레시피열쇠를 다 써서 **기본 인식**이에요」
　 ⭐ **열쇠가 원래부터 팔던 것 = 「구글로 읽기」.** 창업자가 알던 게 맞았고 그 AI 가 「읽기」였을 뿐이다.

⭐⭐ **그런데 창업자가 「AI 정리」로 기억한 것도 «우리 탓»이다** —
　 `ImportScreen.jsx` 카드 제목이 **「AI 자동 정리」** ＋ 「이미 돼요」 뱃지고,
　 그 안에 **「글 붙여넣기 … 재료·순서로 정리해요」**·**「AI가 정리한 결과는…」**이 적혀 있다.
　 📌 **틀린 건 창업자 기억이 아니라 우리 화면 글자다.**

⭐ 이 대조가 **다른 세션의 창업자 확정**(*"「구글이 읽고 규칙이 정리」라는 중간 조합은 없앤다"*)에 근거로 들어갔다.

### ⑶ 📦 AAB 18판 — 준비 → 굽기 → 비공개 업로드
📮 창업자 = *"3."* → 갈래 둘에 **"A하자 네말이 맞아"** (＝R8·인셋 먼저 · BILLING 은 이전 뒤)

**ⓐ 연습 실행(dryRun)으로 실물부터 봤다** — ⛔짐작으로 sed 를 짜지 않았다(문서가 그렇게 시켰다)
```
156:    buildTypes { release { minifyEnabled true } }   ← ⭐⭐ 이미 켜져 있었다
 26:    com.android.tools.build:gradle:8.9.1
214:    com.google.androidbrowserhelper:androidbrowserhelper:2.6.2
```
⭐ grep 이 여섯 낱말을 훑었는데 **3줄만 걸렸다** → `shrinkResources`·`proguardFiles` **0건** 확정.

**ⓑ 콘솔이 말한 셋 중 «하나가 예상과 달랐다»**

| 콘솔 | 실물 | |
|---|---|---|
| 리소스 축소 안 됨 | `shrinkResources` 0건 | ✅맞다 |
| AGP 9.0 이상으로 | 8.9.1 | ✅맞다 |
| **최적화 안 됨** | **`minifyEnabled true` 가 이미 있다** | ⚠️**안 맞는다** |

📌 **17판도 `minifyEnabled true` 로 구워졌는데 콘솔이 그 판을 보고 「최적화 안 됨」이라 했다.**
⏳ 갈래 = ⓐ`proguardFiles` 가 없어서(**유력**) ⓑAGP 가 낮아서 ⓒ콘솔이 재는 게 딴 것 — ⛔짐작으로 안 굳혔다.

**ⓒ 넣은 셋 ＋ 안 넣은 둘**
- ✅ `shrinkResources true` · `proguardFiles getDefaultProguardFile("proguard-android-optimize.txt")` · `androidbrowserhelper 2.6.2 → 2.7.1`
- ⛔ **AGP 9.0** — bubblewrap 이 낸 파일이 옛 DSL 을 쓰고 **최신 bubblewrap 이 아직 8.9.1 을 쓴다** = 그쪽이 준비 안 됐다
- ⛔ **BILLING** — 창업자 확정 = 명의 이전 뒤
- 🔒 넷을 `grep -q … || exit 1` 로 확인 — **못 찾으면 굽기 «전»에 죽는다**
  🧪 규칙 12 = 진짜 모양이면 4/4 통과 · 모양을 바꾸면 **넷 다 잡고 실패** (여기서 돌려봤다)

**ⓓ 굽기 결과** — 전 단계 success · 81초
🔗 run `33186581541` · 아티팩트 **`hankki-aab-11.80-18`** · 3.82MB(zip · `.aab` ＋ `.apk`)

**ⓔ 콘솔이 「크기」 질문을 대신 재줬다** (창업자 캡처)

| | 17판 | **18판** |
|---|---|---:|
| 신규 설치 | 1.11MB | **1.09MB (−18.8KB)** |
| 업데이트 | 365KB | 403KB |
| **지원 안 되는 기기** | — | **0** (폼팩터 7개 전부) |

⭐⭐ **R8 ＋ `shrinkResources` 가 기기를 하나도 안 깎았다** — 제일 걱정하던 자리가 초록불.
⭐ **−18.8KB 는 R8 덕이 맞다** — AAB 는 껍데기라 웹 내용이 안 섞인다.

**ⓕ 비공개 테스트에 올렸다** ＋ 출시 노트 297자 (창업자)

### ⑷ 🏪 스토어 스샷 — 창업자 확정 「브랜드 관계없어」
📮 창업자 = **"브랜드 관계없어"** · *"다들 인스타에서 브랜드 광고해주지않아?"*
⭐⭐ **8/28 지시(*"우리 이거 내일 아예 다시만들어야해"*)가 뒤집혔다.**
　 그날 뺀 이유는 *"위반 근거는 없다… 그래도 이득 0 · 손해 크다"* 였는데 **창업자가 「이득이 있다」고 판정했다.**
✅ **01 정리 장 = 「공심채볶음」 판으로 확정** (창업자가 만들어 왔다)
　 왼쪽 = 안녕한끼 인스타 게시물 → 오른쪽 = 앱 결과 · 하단 띠 = 「공유 › 더보기 › 한끼 = 누르면 끝」
⚠️ **저장소 파일은 아직 옛 판이다** — 8장 전부 마지막 커밋 2026-08-27 · 01 은 콩국수(풀무원·오월햇살).
　 `hold/*`·`wip/*` 전수로 훑어도 새 판이 없다 → **창업자 기기에만 있다.**
⚠️ **함정 하나 짚었다** — 스샷은 **폰·7인치·10인치 «세 자리»**에 각각 들어간다. 한 자리만 바꾸면 태블릿에 옛 판이 남는다.

---

## 2️⃣ ⛔ 내가 오늘 틀린 것 (숨기지 않는다)

**⑴ 엉뚱한 폴더를 보고 「스토어 스샷 완성본이 없다」고 말했다**
　 내가 본 곳 = `design/promo/스토어스샷-2507/`(저장소 뿌리) · 진짜 = **`hankki/design/promo/스토어스샷-2508/`** 이고 **8장이 다 있었다.**
　 📌 **규칙 18 그대로 — 「없다」가 아니라 «내 확인 방식이 틀린 것»이었다.** 창업자가 *"아까 만들었자나 ㅠㅠ"* 로 잡았다.
　 ⭐ 이 저장소는 **뿌리가 둘**(`/home/user/hankki` ↔ `/home/user/hankki/hankki`)이라 늘 이 함정이 있다.

**⑵ 출시 노트 초안에 과장 «둘»** — 실물로 재서 고쳤다
　 ⓐ「음식 그림을 **전부**」 → 픽커에 실린 게 **430컷**인데 일부만 갈렸다. **「전부」는 거짓이 된다.**
　 ⓑ「한 장씩 보내 **크게 보여요**」 → **크게 보이는지는 «받는 앱»이 정한다.** 확정 못 한 걸 약속하지 않는다.
　 📌 AAB17 노트에서 **같은 함정을 두 번 밟았다**(「사진을 찍으면」·「링크를 붙이면」).

**⑶ 「APK 를 폰에 깔아봐」라고 권했다가 뒤집었다**
　 우리 `.apk` 는 **업로드 키**로 서명돼 있고 폰에 깔린 건 **Play 앱 서명 키**다 → 서명이 달라
　 **기존 앱을 지워야 깔린다** → **레시피·일기가 날아간다**(클라우드 저장이 아직 없다).
　 ⭐ 그래서 **비공개 테스트 트랙이 «더 안전한» 길**이었다 — 폰이 테스터라 자동으로 받고 데이터가 그대로다.

**⑷ 창업자가 *"갑자기 어려워지네..ㅠ"* 라고 했다** — 내가 기술 얘기를 너무 길게 쏟았다.
　 📌 **비개발자 눈높이 · 짧게**(규칙 3). 그 뒤로 줄였다.

---

## 3️⃣ ⭐ 오늘 배운 것

**⑴ 「연습 실행」이 값을 했다** — 문서에 *"굽는 날 로그로 확인하고 정한다. ⛔짐작으로 미리 적어두지 않는다"*
　 라고 적혀 있었고 그대로 했더니 **`minifyEnabled true` 가 이미 켜져 있는 걸 알았다.**
　 짐작으로 sed 를 짰으면 「R8 을 켰다」고 잘못 말했을 것이다.

**⑵ 「빌드가 됐다」 ≠ 「앱이 멀쩡하다」** — R8 은 **런타임에 터진다.** 그래서 며칠 써보는 게 유일한 검사다.

**⑶ 창업자 한 마디가 하루치 일을 지웠다** — *"브랜드 관계없어"* 로 「스샷 8장 다시 만들기」가 통째로 사라졌다.
　 📌 **내가 판정하지 않고 물어본 게 맞았다**(규칙 11).

**⑷ 「말이 옮겨질 때 «무엇에 대한 말인지»도 옮긴다」** — 구글 답 ④가 **「앱 이전」**에 대한 것인데
　 우리 길은 **「계정 소유자 변경」**이다. 그대로 걸리는지 **확정 못 한다**고 밝히고 썼다.

---

## 4️⃣ 📌 남은 것 (내일 이어받을 자리)

1. 📱 **AAB 18 — 폰에서 넷 확인** ①켜지나 ②레시피 되나 ③꾸미기·공유 되나 ④런처 이름 `한끼` 가운데
   　 이상하면 `shrinkResources` 부터 되돌려 다시 굽는다(공짜 · **versionCode 18 그대로**)
2. 🏪 **스토어 스샷 01(공심채) 을 세 자리에 올린다** ＋ 새 판을 **저장소에 넣는다**(지금 창업자 기기에만 있다)
   　 ⏳ 나머지 7장도 같은 결로 갈지는 창업자 판정
3. 📱 **v11.80 패드 실물 판정** — 카톡이 한 장을 크게 그리나(⛔폰이 아니라 **패드**)
4. 🔑 **명의 이전** = 구글 검사 대기 · **마감 2026-09-25** · ⛔기다리는 동안 안 건드린다
5. 💳 **BILLING** = 이전 뒤 · ⛓넷을 같이 ＋ `hold/결제서버-0819` 를 먼저 배포
6. ⭐ **프로덕션에 18판을 올린 뒤 「권장 조치 3개」가 줄었나** — 그게 「최적화」 수수께끼의 답이다

---

## 5️⃣ 🔒 상태 (실측)

- 앱 버전 **v11.80** · 안 커밋된 변경 **0** · 로컬 = 원격 (`529905a6`)
- 마지막 배포 뒤 **앱 코드 변경 0** → 빌드 다시 안 돌렸다(run #1882 가 build 0 · smoke 101/101)
- 내일(2026-08-30) **저절로 열리는 것 없음**
