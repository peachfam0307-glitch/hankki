> 🗄 **보관소** — 끝난 세션 기록이다. ⛔창업자가 지시할 때만 연다(절대원칙 24).
> 🧹 `scripts/doc-trim.mjs` 가 HANDOVER.md 에서 옮겼다 (2026-09-05).

<!-- 원래 자리: ## ▶️▶️ 2026-09-05 아침 — **바로 다음 행동** (뿌리까지 찾아뒀다 · 코드는 «아직 안 건드렸다») -->
### ✅ [2026-09-05 08:5x] 보관함 버그 — «고쳤다 · hold 에 담았다 · ⛔배포 전»
📮 창업자 확정 = *"보관함은 **ai가 읽어서 성공했을때만 옮기고**, ai가 실패해서 기본규칙이 적용되면 **그대로 남게** 하자."*
📍 **`hold/보관함AI대기-0905`** (배포 브랜치 `6ddda124` 위) — 창업자 「배포해」 전까지 여기 있다.
| 자리 | 무엇 |
|---|---|
| `App.jsx` `채우기(r, { AI끝남 })` | 졸업(status)은 **AI끝남 && 다읽었나** 일 때만. 파서 판은 졸업 안 시킴 · AI 성공 판만 `{ AI끝남: true }` |
| `RecipeDetailScreen.jsx` 만회 | 성공하면 **그때 졸업**(올리기만) — 없으면 AI 실패한 편이 영영 갇힌다 |
| 화면 잣대 `다읽었나(parsed)` | ⛔**안 건드렸다** (2026-09-01 요구 그대로) |
🧪 `_repro-보관함AI대기-0905.mjs` 9칸 — **옛 코드에서 4칸 빨간불**(진짜 잡는 판) · 탈출구 셋(AI 성공·만회·「그대로 저장」) 살아 있음
🔧 같이 고친 검수판 셋 = 앱이 아니라 «판»이 틀렸던 것 — `보관함정리-0828`(옛 잣대) · `소식장바구니-0829`(9/05 처럼 7일 안 장바구니 두 묶음이면 거짓 빨간불) · `문서다이어트-0903`(HANDOVER 가 작아지자 +60KB 로는 막음 문턱을 못 넘김)
⚠️ 이 컨테이너 = Playwright 가 `chromium_headless_shell-1234` 를 찾는데 `-1194` 만 있다 → 스모크는 `SMOKE_CHROMIUM=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` 를 줘야 돈다(CI 는 무관)
⏳ **아직 안 한 것** = 창업자 같은 아침 요청 *"ai로 다시다듬기가 잘 보이는데 있으면 좋을 것 같아"* — 그 단추가 지금 어디 있는지 재는 것부터
👉 **다음 = 창업자 폰 판정**(규칙 13) → 「배포해」 → 배포 브랜치에 합쳐서 민다

<!-- 원래 자리: ## ▶️▶️ 2026-09-05 아침 — **바로 다음 행동** (뿌리까지 찾아뒀다 · 코드는 «아직 안 건드렸다») -->
### 🚀 [2026-09-05 11:0x] v12.55 · v12.57 — «실물로 나갔다 / 나가는 중»
| 판 | 무엇 | 실물 |
|---|---|---|
| **v12.55** | 🗃 보관함은 AI 성공 때만 졸업 ＋ 🍜 카와이 판독 기록 390개 정리 | ✅ run **#2141**(`38dabd58`) success · 10:37 KST — **v12.53 뒤 첫 초록불** |
| v12.56 | (다른 세션) 레꾸자랑 한복 3컷 ＋ 씬 9 내림 | ⛔ #2143 failure — `_repro-11월카드` 가 «옛 기대»(여름 씬 나온다)로 죽음 |
| **v12.57** | 🤖 **임시보관함 「AI로 다듬기」 단추**(창업자 *"오 아주 좋아"* → *"배포해"*) ＋ 11월카드 판 뒤집음 | ⏳ run **#2144**(`f424e6ff`) — 결론은 다음 절에 |
⭐ **얹는 규칙은 `src/retidy.js` 「한 곳」** — 상세 화면 자동 만회 ＋ 보관함 단추가 같이 쓴다(복사 금지 · `_repro-보관함AI대기-0905` ④-ⓐ/ⓑ/ⓒ 가 잰다).
⚠️ 오늘 배운 것 = **같은 브랜치에 두 세션이 2분 간격으로 밀면 배포가 서로를 취소한다**(#2126~#2139 전부 cancelled). 밀기 전에 «도는 게 없나» 본다.
⚠️ 스모크 도중 `git checkout`·훅의 자동 되돌림이 있으면 «dist 가 낡았다»로 수십 판이 헛죽는다 — 도는 동안 저장소를 안 건드린다.
　 ↳ **[정정 11:1x]** #2144 는 다른 세션의 푸시에 «취소»됐다. 내 v12.57 커밋은 그 세션의 **v12.58**(`dfd17a25`) 밑에 그대로 실려 **run #2145** 로 나간다 — 결론은 아래 절.

