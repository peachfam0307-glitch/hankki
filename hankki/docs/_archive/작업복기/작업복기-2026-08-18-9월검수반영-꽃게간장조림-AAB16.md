# 🗓 2026-08-18 복기 — 9월 검수 반영 · 꽃게간장조림 · AAB 16판

> 커밋 **43개** · 앱 판 **v11.02 → v11.08 (7판)** · AAB **16판 구움**
> ⭐ 이 문서 = 그날 «무엇을 했고 무엇을 틀렸나». 규칙·확정은 `CLAUDE.md` 로 갔다.

---

## 1️⃣ 🍳 9월 검수 — **끝났다**

📮 창업자가 검수판에서 판정한 것을 반영했다.

| | |
|---|---|
| 반영 | **12편** (＋24편에 검수 표시) |
| `BASICS_VERSION` | **67 → 68 → 69** |
| 9월 4주 | **9/07 · 9/14 · 9/21 · 9/28 전부 ✅검수 완료** |

⭐ **확인 대기 셋이 닫혔다** — 달래장 「큰술」 · 참기름 1큰술 · **갈치조림 35분 «그대로»**
   ⛔ 내 추천은 45분이었는데 **창업자가 안 채택했다.** 그대로 뒀다.

⛔ **`BASICS_VERSION` 을 안 올리면 이미 깔린 폰은 옛 레시피를 쓴다** — v10.75 에 실제로 그랬다.
   지금은 `check-basics-version` 게이트가 막는다.

## 2️⃣ 🦀 꽃게찜 → **꽃게간장조림** 통째 교체

창업자 어머니 레시피로 갈아끼웠다. **2인분** · 35분 · id 는 그대로(`basic-kkotge-jjim` — `weekly.js:108` 이 부른다).

· 간장 = *"1큰술씩 추가하며 간을 맞춰요"* ← **창업자가 직접 쓴 문장**
· 창업자 새 컷 둘 = **`fe_306` 꽃게간장조림** · **`fe_307` 나물비빔밥**

⛔⛔ **「구체어 먼저」 함정을 하루에 셋 밟았다** — `ICON_RULES` 는 **위에서 첫 매칭이 이긴다**:
   꽃게간장조림 → `fe_34`(장조림) · 꽃게조림 → `fe_66`(목살조림) · 나물비빔밥 → `fh_k01`(돌솥비빔밥)
   ✅ `check-foodtab` 에 **반대 방향까지** 못 박았다(「장조림」은 여전히 `fe_34` 여야 한다).

⛔ **`recipe.icon` 이 규칙을 덮는다** (`Thumb.jsx` = `recipe.icon || guessFoodIcon(제목)`) — 박힌 값부터 본다.

## 3️⃣ 📔 도움말 둘 (창업자 제보)

📮 *"레꾸자랑은 도움말이 없네?"* · *"한끼일기에 도움말에도 책갈피가 있어 잘못쓴것 같은데"*

⛔ **일기 탭은 `MyRecipesScreen` 을 `initView="log"` 로 여는데 `<TabTips tab="myrecipes">` 가 «고정»이었다.**
   → 일기에서 「?」를 누르면 **일기에 없는 기능**(책갈피·여러 개 삭제)이 안내됐다.
   ⭐ **제목은 이미 갈라 놨는데 도움말은 안 갈랐다** — 갈치조림 메모 사고와 같은 뿌리.
   📌 **기능을 나눴으면 그 기능을 «설명하는 글»도 같이 나눈다.**

⛔ **「가져오기 도움말이 없다」는 내가 틀렸다** — `TabTips` 만 보고 말했다. 화면 안 카드로 이미 있다.

## 4️⃣ 📦 AAB 16판 — 구웠다

🔗 https://github.com/peachfam0307-glitch/hankki/actions/runs/32117415820
· 로그 실측 = `versionCode 16` · `versionName "11.08"` · `targetSdk 36` · 바탕 `fc4e70cd`
· ⛔ BILLING 안 들어갔다(재심사 승인 뒤 다음 판)
· ⏭ **올리는 건 창업자 몫** — Play Console → 비공개 테스트-베타테스트

⛔ **아티팩트 이름이 `hankki-aab--16` 으로 나왔다** — 버전 칸을 비우고 구웠는데 **이름만 그 빈 칸을 다시 읽었다.**
   AAB 속 값은 멀쩡하다. 📌 우리 절대원칙이 「다음 판 번호는 아티팩트 «이름»이 답이다」라 그냥 두면 근거가 사라진다.
   ✅ 워크플로를 고쳤다 — 버전을 정하는 단계가 **이름까지 만들어 넘긴다**(17판부터 정상).

⚠️ **화면 방향 검사가 「(못 읽음)」을 찍고 지나갔다** — 「됐다」도 「안 됐다」도 아닌 **아무것도 안 재는 검사**였다.
   ✅ 이제 «넣은 값»(`twa-manifest.json` = `default`)부터 보고, 세로면 거기서 죽는다.
   ⛔ **정직하게 — 16판은 가로가 열렸는지 굽기 로그로 확인 못 했다.**

---

## 5️⃣ ⛔⛔ 오늘 제일 큰 실수 — **저장만 하고 안 읽었다**

📮 창업자 = *"사업자 주소가 개인사업자라 집주소라고 얘기하고 저장도 해놨는데 뭔 딴소리를해."*
📮 = *"어차피 너 저장해도 보지도 않고 니멋대로 판단해서 딴소리하잖아"*

구글에 보낼 메일 초안에 *"주거지 주소 **대신** 사업장 주소를 등록할 수 있나"* 를 넣었다.
⛔ **둘이 같은 주소다**(개인사업자·자택을 사업장으로 냈다 — 첨부서류 0장). **물어도 답이 같다.**
⛔ 게다가 **8/17 에 창업자가 이미 ⓐ로 확정**했다(*"1 집주소 가능"*).

📌 **뿌리 = `CLAUDE.md` 793줄이 「⏳창업자 판정 대기」로 하루 낡아 있었다.**
   그 줄을 읽은 **두 세션이 8/18 에 둘 다 같은 얘길 꺼냈다.**
✅ **메일은 안 나갔다**(창업자가 안 보냈다). 실제로 보낸 건 「계정 소유권 이전」 질문 셋(티켓 `5-3194000041974`).

### 🔒 그래서 만든 장치 — `doc-guard.mjs --decided`

「판정 대기」 줄의 주제가 **다른 데서 이미 확정**됐으면 잡는다.
· **smoke 체인** → 걸리면 **배포가 막힌다**  · **세션 시작 브리핑** → 낡은 줄을 읽기 «전»에 본다
⭐ 짝이 넷이 됐다: 파일이 있다 / 코드에 값이 있다 / **창업자가 이미 판정했다** ← 이게 제일 나쁘다.

🧪 규칙 12 = 오늘 아침 `CLAUDE.md` 로 되돌리면 **793줄을 잡고 exit 1** · 고친 지금 exit 0
⛔ 만들며 두 번 헛돌았다 —
   ⑴ 첫 판이 **21개**를 뱉었다(죽은 게이트). 한 줄의 「」를 전부 주제로 삼아서.
   ⑵ 좁히다가 `창업자\s*(확정|판정)` 이 **「창업자 판정 «대기»」에도 걸려** 정작 잡을 793줄을 스스로 걸러냈다.
      📌 **옛 값으로 안 돌려봤으면 「2개 잡았다」에 안심하고 넘어갔다.**

---

## 6️⃣ 🧹 청소

| 옮긴 것 | 어디로 |
|---|---|
| 작업컷 **27장**(5.6MB) — 뿌리 `_shots/`, 아무도 안 부름 | `_archive/작업컷-2026-08-18/` |
| 끝난 일회용 스크립트 **30개** (124 → 94) | `scripts/_archive/` |
| 반영 끝난 문서 **4개** | `docs/_archive/` |

⛔ **죽은 코드는 «없었다»** — 내 탐지기가 두 번 거짓 경보를 냈다:
   ⑴ `\b`(낱말 경계)가 **한글에 안 걸려** 「카드표지로」를 안 쓴다고 했다(둘 다 실제로 쓴다)
   ⑵ 확장자 붙은 import·재수출을 못 봐 `today.js`·`weeklypick.js` 를 죽었다고 했다(각각 4곳·3곳이 쓴다)
   📌 **그대로 「정리했다」고 말했으면 멀쩡한 코드를 지울 뻔했다.** 지우기 «전»에 실제 호출을 봤다.

⛔ **스크립트 나이를 git 으로 못 잰다** — 되감김 복구로 브랜치가 «뿌리부터» 다시 세워져
   (`75e30dd0` 이 부모 없는 루트) 124개가 전부 8/17~18 로 보인다 → **파일 이름의 날짜**로 갈랐다.

### 🚪 훅이 «자기 탈출구»를 막았다 — 같은 자리 세 번째

보관소로 옮기려면 허가 표식을 만들어야 하는데 **그 `printf` 가 막혔다**(표식 «글»에 보관소 낱말이 들어서).
⛔ `archive-guard.sh` 머리말엔 *"echo·printf 는 안 막힌다. 막다른 길이 아니다"* 라고 **적혀만 있었다.**
✅ `guard-targets.py` = heredoc·`cat >` 에 이어 **echo·printf 리다이렉트도 지운다.** 셋 다 같은 뿌리.
🧪 `_test-archive-guard.sh` 에 **그 명령 자체**를 박았다 → **19/19**.

---

## 7️⃣ ⏭ 다음

| | 무엇 | 언제 |
|---|---|---|
| 🔴 | **AAB 16판을 Play Console 에 올린다** (창업자) | 지금 가능 |
| 🔴 | **10월 검수 20편** — 4주치(10/05·12·19·26) | **D-48** · 한 달치 미리 |
| 🟡 | 11~12월 검수 13편 | D-76~111 |
| 🟡 | 일기 백업 잠금 구멍 갈래 셋 판정 | 창업자 *"고민해봐야겠네"* |
| 🟡 | 인덱스 남은 것 — 상세 화면 단추 · 연한 모자 진하기 · 여럿으로 | |
| ⏳ | **재심사 결과** (8/16 접수) | 8/23쯤 |
