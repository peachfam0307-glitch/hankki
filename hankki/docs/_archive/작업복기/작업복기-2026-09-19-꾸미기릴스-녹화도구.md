# 🎬 작업복기 2026-09-18 밤 ~ 09-19 새벽 — 식비 릴스 · 검사판 «진짜» 배포 · 꾸미기 릴스를 «녹화 도구»로

> 세션 = `hold/식비공개-0918` (앱 세션 하나 · 배포는 v13.76 한 번) · 창업자 취침 = 02:45 KST *"다하고 남겨줘. 나 자러간다아~"*
> 🛒 **오늘 쿠팡 링크 누른 적 없다** — 배포 브랜치 오늘 커밋에 쿠팡 0건 · 이 세션이 연 주소는 앱 자체(127.0.0.1)뿐. 장보기 마음껏.

---

## 1️⃣ 한 것 (시간순)

### ① 식비 소개 릴스 — 완성 (35.9초) · 9/18 21시~23시
- 장면 6 (체크 → 금액 → 배달·외식 → 예산 → 주별·달별 → 냉장고) · 남색 격자 배경 ＋ 장면별 자막 ＋ 훅(창업자가 뽑아 준 가계부 3인 컷) ＋ 끝장 「지금 쓸 수 있어요」
- 🔢 뿌리를 잡은 것 = **Playwright `recordVideo` 는 페이지를 «키워서» 담지 않는다** — 틀을 1080×1920 으로 줘도 페이지는 CSS px 그대로 왼쪽 위에 그려진다(창업자가 4번 「화면이 작다」). 틀 = 페이지 크기로 하고 편집에서 키운다.
- 📄 `scripts/_녹화-식비흐름-0919.mjs` · `_영상-흐름릴스-편집-0919.mjs` · `_판-흐름릴스자막-0919.mjs` · 결과 `design/promo/인스타-2509/릴스-식비소개-2026-09-19.mp4`

### ② 「앱으로 열리나」 검사판 — «진짜» 배포 (v13.76 · `8cf42123`)
- 9/18 에 소스 57줄을 지운 커밋(`1a16359e`)이 배포 브랜치에 올라갔는데 **창업자 폰엔 그대로 떴다**(*"아까 앱에 쿠팡 테스트 아직도 안빠졌던데"*).
- 🌲 **뿌리 = `dist/` 를 다시 안 구웠다.** 이 앱은 `dist/` 를 커밋해 Pages 로 배포한다 → **소스 커밋 ≠ 배포.** 내가 그때 「배포 완료」라고 말한 게 틀렸다.
- ⛔⛔ **[9/19 11:3x 정정] 이 배포는 «안 나갔었다».** `npm run build` → `dist/` 포함 커밋(`8cf42123`)까지는 맞는데 **Actions 가 failure**(run 2664·2665)였고 나는 안 보고 「배포 완료」라고 말했다 — **같은 밤에 두 번**(소스만 지우고 한 번 · dist 굽고 또 한 번). 규칙 「푸시했다 ≠ 배포됐다」를 그대로 어겼다.
  · 죽인 칸 = `_repro-식비구멍-0917.mjs` 「날짜를 거꾸로 골라도 죽지 않고 그 사이를 센다」 — 씨앗은 오늘−1·오늘−20 인데 범위가 `'2026-09-01 ~ 09-17'` 로 «박혀» 있어 9/19 00:11 KST 부터 합 0. 앱은 멀쩡(뒤 「100년」 칸 통과) · 로컬 재현 = 같은 칸 실패 → 오늘 기준으로 고쳐 33/33
  · 다시 민 것 = `31c46fd0`(검사 한 줄 · 앱 코드 아님) → **Actions `success` 확인 뒤에만 「나갔다」** · 다른 재현판에 박힌 날짜 = grep 0건
  · 남은 흔적 = `_repro-앱문-0918.mjs`(검사 스크립트 · 유저 화면 아님)

### ③ 꾸미기 릴스 — 접시 하나로 몇 시간 (⛔이게 오늘의 사고)
- 창업자 = *"밤새도 못하겠다 심각하네"* · *"너눈으로보고하는거야?"* · *"와.. 몇시간만에 이거 한거야"* · *"이런 단순한걸3시간넘게 하는거 실화야.."*
- 🌲 뿌리 다섯 (전부 실측)
  1. **크기를 감으로 찍고 자리만 만졌다** — `s 0.98` 이 정답의 1.5배. 자리를 아무리 옮겨도 크기가 틀리면 안 맞는다.
  2. **그릇 실루엣을 화면 색 차이로 잡았다** — 카드 배경이 크림 그라데이션이라 흰 그릇을 놓쳐 2~3px 모자랐고 그만큼이 흰 테로 삐져나왔다 → **원본 PNG 알파**로.
  3. **실측 파일에 값이 다 있었는데 안 썼다** — 내가 눈대중으로 키우고 모서리로 밀어 포토코너가 잘렸다.
  4. **「모서리 끝 ＋ 안 잘리게」를 눈대중으로** — 그건 부등식(중심 ≥ 폭/2)이다.
  5. **후보를 하나만 내밀었다** — 틀리면 왕복이 한 번 더 는다 → 3~4개 같이.
- ✅ 재서 푼 것 = `tools/꾸미기-자리풀기.py`(재기·한계·덮기) · `scripts/꾸미기-값검사.mjs`(색 키 `lilac`→`t_lilac` 처럼 «조용히 흰색으로 떨어지는» 값을 exit 1 로)
- 🔢 접시 = 조건 «둘»(ⓐ구멍에 테 없음 ⓑ그릇 전체가 프레임 안) → 통과 s 0.67~0.80 → 창업자 「2번」 = `0.78 / 0.5094 / 0.46`

### ④ 꾸미기 릴스 — 슬라이드쇼 → «녹화»로 갈아엎음
- 낱장 10장을 이어붙인 판을 보냈더니 창업자 = *"붙이는 느낌이 없잖아 / 그릇은 갑자기 툭 끊어졌다가 나타나고 / 끌어다 붙이고 애들도 움직이고 있고 해야하는데"* · *"내가 아까 가을꺼 일부러 녹화해서 올려줬잖아 똑같은 방식으로"*
- 가을 레꾸 릴스(창업자 인스타 녹화 17.3초)를 프레임으로 뜯어 **눈으로** 봤다 — ×·⟳ 손잡이가 뜨고, 서랍이 탭마다 바뀌고, 조각이 «끌려가서» 붙는다. **녹화라야 나온다.**
- ✅ `scripts/꾸미기릴스-녹화.mjs` — 앱을 Playwright 마우스로 실제 조작하며 recordVideo
  · 탭 → 서랍 칸(aria-label=key) → 기본 자리 붙음 → 끌기(ease-out 22걸음) → 손잡이 방사형 끌기로 크기(`s = s0×끈거리/처음거리` · `s0` 는 화면 폭÷판폭) → 각도(`data-decor-item` 의 rotate 읽어 «항상» 목표각 · 6° 문턱 넘겨 12° 갔다 돌아옴)
  · 글자 넣기 → 치기 → 빈 자리 눌러 끝 → 다시 눌러 색·글씨 · 뒤집기(순서 갈래) · 효과(하트) · 색(coral)
  · 편집바 갈래는 «이미 열려 있으면 안 누른다»(또 누르면 접힌다) · 끝에 빈 자리 눌러 손잡이 풀기 · 「판 밖으로 나간 그림」 DOM 검사
  · 고화질 = 뷰포트 1080×2340 ＋ CSS zoom 2.769(recordVideo 는 뷰포트 그대로) · 렌더 느려 84초 → 배속을 «재서» 17.5초
- 🔁 창업자 판정 왕복 = 그릇 위로(2번) → 포토코너 잘림·각도·카롱 잘림 → 작은 하트 뺌·하트 글자 옆·살구 테마 → 마테 위·글자 아래 → 코너 안쪽·카롱 안쪽·마테 냄비 쪽 → 코너 오른아래 위로 → **「그거빼고 퍼펙트」 → 「와.. 완벽해..」**
- 🎞 최종 = `design/promo/인스타-2509/릴스-꾸미기-새우관자전-2026-09-19.mp4` (1080×1920 · 17.6초 · 살구 · 자막 없음 = 창업자가 단다)

### ⑤ 도구를 «정식»으로 — 창업자 *"아까 그 도구 폐기하고 이걸 만들어서 올려. 담에 이 도구가 바로 쓰이도록"*
- ⛔ 폐기 = `_영상-꾸미기릴스-0919.mjs`(슬라이드쇼) → `scripts/_archive/…-슬라이드쇼-폐기.mjs`
- ✅ 정식 = `scripts/꾸미기릴스-녹화.mjs <시안.json>` — 값검사 → 녹화(HQ) → 잘림 검사 → 굽기 → `design/promo/인스타-*`(제일 최근 폴더)
- ✅ 시안 = `docs/꾸미기릴스-시안-새우관자전-2026-09-19.json` — **다음 릴스는 이걸 복사해 값만 바꾼다.** 도구 파일에 숫자 없음.
- 📄 `docs/꾸미기-시안-그대로-얹는법-2026-09-19.md`(순서·함정·확정값 표·도구 표) · CLAUDE.md 절대원칙 · 최신-지도 핀(「릴스·시안·레꾸」로도 잡힘 — 전엔 「꾸미기」로만)
- 🗺 덤 = `tools.mjs` 가 `tools/` 를 안 보고 있었다(파이썬 도구 41개가 목록에 없었다) → 둘 다 훑고 shebang 은 설명으로 안 쓴다
- 🧪 HQ=0 로 끝까지 돌려 확인 = 값검사 통과 · 8조각 · 판 밖 0개 · 굽기 · 저장소 복사

---

## 2️⃣ 틀린 것 (⛔같은 걸 또 밟지 않게)

| # | 무엇 | 뿌리 | 이제 막는 것 |
|---|---|---|---|
| 1 | 「검사판 배포 완료」라고 말했는데 폰엔 그대로 | 소스만 커밋하고 `dist/` 안 구움 | HANDOVER 「하면 안 되는 것」에 박음 |
| 2 | 접시 몇 시간 | 눈대중 · 화면 색으로 실루엣 | 자리풀기 도구 · 알파로 잼 |
| 3 | 글자 흰색 | `lilac`(없는 키) → 조용히 `TEXT_COLORS[0]` | 값검사 exit 1 |
| 4 | 하트 색 `#e8b9bd` | 앱에 없는 hex 를 지어냄 | colorKey 만 · Stickers.jsx 에서 읽어 대조 |
| 5 | 코너·카롱 잘림·삐딱 | 붙을 때 `((n%5)-2)×4°` 가 저절로 붙는 걸 몰랐다 · 회전을 손잡이 parentElement 에서 읽어 늘 0 | `data-decor-item` 의 rotate · 항상 목표각 · 한계 +0.01 |
| 6 | 판이 «안 보임» | 채팅에 `<img src="/tmp/…">` (내 컨테이너 경로) | 파일로 보낸다 |
| 7 | bash 죽음 ×2 | 환경변수 이름 한글(`전체=1` · `여백=0`) | 전부 ASCII (규칙 24 에 이미 있었는데 또 밟았다) |
| 8 | 폴더 `인스타-2609` | 날짜로 폴더 이름을 지어냄(실제 규칙 `인스타-2509`) | 있는 폴더 중 최근 것 |
| 9 | 「9/19 3종 검수해야 한다」 | 부팅 알림만 보고 말함 — 이미 검수 끝(9/17) | `--pending` 돌려 확인 뒤 취소 |
| 10 | 슬라이드쇼로 릴스 | 「한 겹씩 쌓인다」를 스샷으로 이해 | 창업자 원본을 프레임으로 뜯어 «눈으로» 본 뒤 녹화 |

📌 한 줄 = **재서 풀 수 있는 걸 눈으로 찍었고, 확인 못 한 걸 「됐다」고 말했다.** 둘 다 규칙 15·18·21 그대로다.

---

## 3️⃣ 남은 것
- 📱 창업자 몫 = 꾸미기 릴스에 자막 달아 인스타 게시 · Play 콘솔 두 화면 · GA4 09-18 다섯 장 · 「오늘 우리 기기로 앱 열었어?」
- ⏳ 식비 공개판(`hold/식비공개-0918` 의 앱 코드) = 아이폰 승인 날 창업자 신호 뒤 배포 브랜치로
- ⏳ `hold/식비공개-0918` 가 배포 브랜치보다 24커밋 뒤 — 언젠가 배포 브랜치를 merge 해 맞춘다(⛔충돌 뒤 `git add -A` 금지)
- 소개 캐러셀(릴스와 다른 결) · 9/17 원재료 캐러셀 인사이트 숫자 · 스모크 판 미리보기 서버 안 닫는 버릇
