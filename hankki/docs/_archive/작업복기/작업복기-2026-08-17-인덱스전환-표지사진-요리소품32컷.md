# 작업복기 2026-08-17 (일) — 인덱스로 방향 전환 · 표지 사진 손짓 · 요리소품 32컷

> KST 기준. 앱 버전 **v11.01** · 작업 브랜치 **`hold/일기-그달-0817`**(배포 안 됨)
> build exit 0 · smoke exit 0 · 안 커밋된 변경 0

---

## 1️⃣ 오늘 창업자가 말한 것 (시간 순)

| # | 원문 | 결과 |
|---|---|---|
| 1 | *"처음들어가면 그달에만든게 보이는데 **일기쓰고 나면 날짜꺼만 보여**"* | ✅ 고침 |
| 2 | *"한달치니까 **겹치지 않게** 하자. 3번 같은 걸 만들면 3번 보이게 되잖아"* | ✅ 고침 |
| 3 | *"음식 아이콘에 **별은 뭐야?**"* → *"바꿔."* | ✅ 우리 스티커로 |
| 4 | *"평점 매기는데가 없으니까 안뜨는거 아닌가"* | ⛔ **내가 잘못 읽어 되돌림** |
| 5 | *"좋았던 레시피는 **인덱스로 남겨요(별대신)**"* | ✅ 방향 확정 |
| 6 | *"근데 그 북마크는 **나도 한번도 안썼어 번거로워서. 레시피에 들어가서 눌러야 하니까**"* | ✅ 목록으로 |
| 7 | *"**아이콘 바꾸기에 바로 내가 사진 올릴 수 있는 버튼**도 있었으면"* | ✅ |
| 8 | *"컥. 저 사진을 **이모지랑 똑같이 동그랗게** ＋ **확대 축소도**"* | ✅ |
| 9 | *"**해먹을 것, 맛있었던 것** 이런식으로 유저가 정해서 쓰고 아이콘을 누르면 그 레시피만 쭉"* | ⏳ 내일 |
| 10 | 스티커 시트 **8장** 제공 ＋ *"일단 **몇개를 넣을지 정해야해**"* | ✅ 32컷 자름 |
| 11 | *"딱 레시피 안에 넣기보다 **바깥에 걸쳐서** 넣는게 더 예쁜거 같아 **레꾸도 안해치고**"* | ⏳ 판 넉 장 대기 |
| 12 | *"나는 그냥 딱 저 아이콘만 붙이고 싶은데 **테두리없이**"* → *"웅 **테두리는 없어야이뻐.**"* | ✅ **확정** |

---

## 2️⃣ ⭐⭐⭐ 오늘 제일 크게 배운 것 — 재기 전에 «전제»를 의심한다

**내가 한 것** — 32컷을 앱이 실제로 그리는 크기(19px·16px)로 줄여 재고 이렇게 결론냈다:
> *"크림색 요리사모자는 크림 카드 위에서 **하얀 얼룩**이 된다(진한 획 남음 **0%**).
>  창업자가 고른 셋 중 둘이 여기 해당한다."*

**창업자 한 마디가 그 전제를 없앴다** — *"테두리없이"*.

🔢 지금 인덱스 = **34px 흰 동그라미 «안»에 19px 그림.**
   동그라미를 빼면 **그 34px 을 그림이 다 쓴다 → 19px ⇒ 30px.**
   ⛔ 손가락 닿는 칸은 그대로다. 작아지는 게 아니라 **«그림만» 커진다.**
   ✅ 30px 로 놓으니 **죽는다던 컷이 다 살아났다.** 크림색 모자도 또렷하다.

📌 **나는 「지금 놓인 방식」을 전제로 재고 있었다.**
   숫자는 맞았고 **무엇을 전제로 잰 숫자인지**가 문제였다 — 규칙 18 ⓘ 의 사촌.
📌 **「어떤 그림이냐」보다 「어떻게 놓느냐」가 먼저였다.**

---

## 3️⃣ 오늘 잡은 사고

### ⛔⛔ 창업자 물음을 잘못 읽어 빈 별을 띄웠다 → 되돌림
*"평점 매기는데가 없으니까 안뜨는거 아닌가"* 를 **「보이게 하자」**로 읽었다.
창업자: *"**요리기록 남기기 안하기로 하지 않았어???????????????????**"* · *"누를 시간 없어서 안하기로 했잖아"*
✅ 확정은 **2026-08-06 「만들었어요 → 토스트만, 시트 안 뜬다」**. 전부 되돌리고 게이트로 뒤집었다.
📌 **규칙 25**(제보를 받으면 어디·무엇·어떻게를 먼저 물어본다)를 안 지킨 것이다.

### ⛔⛔ `npm run build` 가 undefined 변수를 통과시켰다
`shown` 을 지웠는데 편집 하단바에 남아 있었고 **build exit 0**. grep 으로 잡았다.
**「전체 선택」 누르는 순간 화면이 죽을 뻔했다.**
⚠️ eslint 가 `package.json`·`node_modules` 어디에도 없다(npx 가 그때그때 받아온다)
→ smoke 에 걸면 **CI(`npm ci`·Node 20)에서 배포가 통째로 죽는다.** ⏳해법 미정.

### ⛔ JSX 주석을 «표현식 자리»에 넣어 빌드를 죽였다
`{cond && (` 바로 뒤는 표현식 자리라 `{/* */}` 이 객체 리터럴로 파싱된다. 그 자리엔 JS 주석 `//`.

### ⛔ bash 큰따옴표 안의 백틱이 명령치환으로 실행됐다 — **하루에 세 번**
주석 안 `` `src/photoPan.js` `` 등이 실행돼 글자가 사라졌다. heredoc `<<'EOF'` 또는 Edit 툴.

### ⛔ 새 재현판 셋에 `/opt/pw-browsers/chromium` 을 박았다 (CI엔 없다)
`const CHROMIUM = process.env.SMOKE_CHROMIUM` 로 고침. **smoke 에 물리기 «전»에 잡았다.**

### ⛔ `status: 'sorted'` 를 시드에 안 넣어 레시피가 목록에 안 떴다 — 30분 헤맴
앱이 아니라 **내 시드 문제**였다.

### 🐺 「몸통 아래 돌기」 잣대가 늑대를 외치고 있었다 (제일 큰 발견)
7컷이 죽었는데 **일곱 다 «그림 자체»**였다.
⛔ **뿌리 = 꼬리에 «아래쪽 투명 여백»까지 넣고 셌다.** 여백은 폭이 0이라 전부 「가는 줄」로 잡힌다
   → **몸통이 아래까지 꽉 찬 컷은 여백만으로 비율이 1.0**이 된다(키가 클수록 잘 걸렸다).
🔢 실측 = **앱 스티커 1940장 중 옛 잣대가 811장(42%)** · 새 잣대 131장(전부 줄기·끈·리본·글자 = 진짜 돌기)
✅ 고침 = ⓐ여백을 뺀다 ⓑ**「가는 목 아래에 다시 넓어지는 것」을 «더» 본다** ← 붙여서 **강해졌다**
🧪 규칙 12 = 32컷 **거짓 경보 0** · 라벨 조각을 심으면 **글자 12px 까지 32/32**
📌 **시끄러운 게이트는 죽은 게이트다. 42% 를 거는 잣대는 아무도 안 본다.**

---

## 4️⃣ ⏳ 내일 이어갈 것 — 여기부터

📄 **`docs/인덱스-스티커-진행-2026-08-17.md`** 를 먼저 연다.

1. 🔴 **걸치는 양 판정** — 찍어 둔 판 넉 장(A 지금 / B 안쪽·테없이 / C 살짝 9px / D 반쯤 15px＋기울임)
   ✅ A 는 죽었다(창업자 *"테두리는 없어야이뻐"*). ⚠️ D 는 오른쪽 열이 화면 가장자리에 바짝 붙는다 — 폰 확인 필요.
2. 🔴 **어떤 그림 · 몇 개** — 32컷 전부 후보로 살아 있다(19px 탈락 판단은 무효)
3. 🔴 ⭐**「안 눌린 상태」를 어떻게 그리나** — 수채화 스티커는 «빈 상태»를 못 만든다
   ⛔ 「안 눌리면 아예 안 보이게」 금지 — 인덱스를 목록으로 뺀 «이유»와 정면으로 어긋난다
4. 🔴 **남는 컷 → 레꾸·일꾸 데코 등록** (창업자 *"이거 쓰고 남은거는 레꾸나 일꾸에 넣자"*)
5. 🟡 **「해먹을 것/맛있었던 것」 이름표 여럿** — 데이터 모양이 바뀐다(`folder` 하나 → 여럿)
6. 🟡 **`hold/일기-그달-0817` 배포** — 창업자 「ㄱㄱ」 대기 (오늘 것 전부 여기 있다)
7. 🟡 **9월 검수판 24편 판정** (8/31 D-13 · 5편) — 창업자 확정 리듬 = 「한 달치 미리 ＋ 전날 한 번 더」
8. ⏳ 재심사 결과 · 구글 이메일 4문항 답 대기

---

## 5️⃣ 오늘 만든 도구 (전부 저장소에 있다)

| | 하는 일 |
|---|---|
| `src/photoPan.js` | 사진 「끌어서 옮기고 두 손가락 확대」 — 일기·표지가 **같은 코드**를 부른다 |
| `scripts/_repro-돌기잣대-0817.py` | 자르기 게이트가 늑대를 안 외치나 ＋ 진짜 라벨 조각은 잡나 |
| `scripts/_판-인덱스19px-0817.py` | 32컷을 19px·16px 로 줄여 재는 판(HTML) |
| `scripts/_shot-인덱스스티커-0817.mjs` | **앱을 띄워** 진짜 카드에 얹어 찍는다(A~D 넉 장) |
| `scripts/_repro-일기그달-0817.mjs` 외 3 | 오늘 고친 것들의 안전망(smoke 등록) |
