# 작업복기 — 2026-08-09 (일) 새벽 · 온보딩 열 장 · 속지 사진 분리 · 서랍 실측

> 세션 = 2026-08-08(토) 밤 ~ 2026-08-09(일) 새벽 KST
> 배포 세 판 — **v10.05**(run 1171 · `b895a24`) · **v10.05 2차**(run 1173 · `79b6900`) · **v10.06**(run 1174 · `04e219a`)

---

## 0️⃣ 한 줄

**온보딩이 아홉 장에서 열 장이 됐고, 속지마다 사진을 따로 담게 됐고, 서랍은 재 보니 못 넓힌다는 걸 알았다.**

---

## 1️⃣ 온보딩 일기 장면을 다시 꾸몄다 (창업자 지시)

📮 창업자 — *"우리 일꾸랑 레꾸랑 큰 차이가 없어서.. 우리 새로운 스티커 많으니까(일꾸전용) 그거 적용해서 꾸며볼래?"*
📮 이어서 — *"무지속지나 반 나눠진 속지에 모서리꾸미기 붙이고 기분느티커랑 별,. 우리가 안쓰던거 이용해서 펭펭이나 하나넣고"*

### 한 것
- `SlideD`(한끼 일기) 를 **진짜 속지 `dp_snap`** 위에 다시 그렸다. 좌표는 `papers.js` 실측값을 그대로 쓴다.
- 스티커 = `dgc04`(왼쪽 위 코너) · `dgc05`(오른쪽 아래·180° 회전) · `dn_star`(사진 위) · `dgf07`(기분) · `wt_td02`(달력 마테) · **`gp_pengft`**(여태 안 쓰던 펭펭 컷).
- 달력 칸에 음식 아이콘을 흩뿌렸다(`CAL` 배열).

### ⛔ 창업자가 잡은 것 넷 — 전부 자동 검수는 «초록불»이었다
| # | 제보 | 실제 원인 |
|---|---|---|
| ① | *"오른쪽 스티커가 두개가 겹쳤어"* | 기분 스티커 `dgf01` 이 메모지 **뒤에** 숨었다 → 메모지 빼고 `dgf07`(주황 햇살)로 바꿔 글칸 안쪽에 |
| ② | *"위에있는 마테는 떼자 똑같은게 2개야"* | `wt_dg01` 과 달력의 `wt_td02` 가 **같은 그림** → 위쪽 제거 |
| ③ | *"음식 이모지도 크기 좀만 줄이자 잘렸어(아래위가)"* | 칸 453×248 인데 `width:62%` 만 줘서 **높이가 310px** → `overflow:hidden` 이 위아래 **62px** 을 잘라냈다 |
| ④ | *"우리 온보드에 연어포케볼 한번도 안나왔었어?"* → *"2번그림에 포케볼있을텐데.."* | Slide2 가 이미 `fe_06` ＋ 제목까지 「연어 포케볼」 → **비빔국수 `fe_81`** 로 |

⭐⭐ **③에서 배운 것 = 「한 축만 잡으면 다른 축은 비율이 정한다」**
`width` 만 주면 높이는 그림 비율이 정하고, 그 값이 칸을 넘으면 `overflow:hidden` 이 **말없이 잘라낸다**.
→ **두 축을 다 잡고 `objectFit: contain`.**

📌 **네 건 다 자동 검수는 통과했다.** 검수가 보는 건 「넘쳤나·깨졌나·크래시났나」이지 **「겹쳤나·같은 게 둘인가」가 아니다.**
같은 뿌리 = 펭펭이 달력 음식 아이콘을 가린 것도 **캡처를 눈으로 봐서** 잡았다.

---

## 2️⃣ 🐛 두 달 넘게 잠복하던 진짜 버그 — 「안내 다시 보기」

📌 `ProfileScreen` 이 코치 키를 **손으로 나열**하고 있었다.
```js
['detail','shop','home','profile','myrecipes']   // ⛔ 옛날
```
그런데 앱이 실제로 쓰는 키는 **`home3`** 이고 `brag` 는 목록에 **아예 없었다.**
→ **v8.60(두 달 전)부터 홈 안내가 「다시 보기」로 안 돌아왔다.**

✅ **고침 = 키를 한 곳에서만 만든다** (`src/coach.js` 신설)
```js
const NS = 'hankki:coach:'
export const COACH = { home: `${NS}home3`, myrecipes: `${NS}myrecipes`, … }
export const COACH_KEYS = Object.values(COACH)
export const SEED_COACH_SEEN = `…Storage.prototype.getItem 를 접두어로 통째로 패치…`
```
🔒 **`check-mistakes.mjs` ⑨** = `src/` 와 배포 게이트 셋에서 `hankki:coach:` 를 **파일 밖에서 만들면 배포를 막는다.**
⭐ 규칙 12대로 **옛 방식으로 되돌려 진짜 걸리는 것까지 확인**했다.

### 같이 풀린 것 = 배포가 막혀 있던 이유
검사 스크립트 ~100개가 코치 키를 **이름으로** 심고 있었다 → 앱이 `home3` 으로 올라가자 오버레이가 화면을 덮어 `test-exit` 이 죽었다.
⛔ **원인 찾다 두 번 헛짚었다** — ⓐ stale `vite preview` 를 의심(하나 남아 있긴 했으나 원인 아님) ⓑ `git stash` 로 옛 코드를 확인했는데 **`Onboarding.jsx` 한 파일만 stash** 해서 *"내가 낸 게 아니다"* 로 잘못 갈랐다.

---

## 3️⃣ 📅 고정 날짜는 «반드시» 낡는다

📮 창업자 — *"그리고 날짜도 맞춰야해(온보드에 날짜 써있는 이미지가 있을거야)"*

`Card` 안에 `date = '2026.07.25'` 가 **글자로 박혀** 있었다. 온보딩을 여는 날이 언제든 7월 25일이었다.
```js
const _now = new Date()
const CARD_DATE  = `${…}.${…}.${…}`   // 2026.08.09
const DIARY_DATE = `${월}.${일} ${요일}` // 8.9 일
const THIS_MONTH = `${월}월`            // 8월
```
→ **카드·일기·달력 셋이 저절로 같은 날이 된다.**

⚠️ **딸려온 함정 = 컨테이너는 UTC · 유저 폰은 KST.**
Playwright 에 `timezoneId: 'Asia/Seoul'` 을 안 주면 **캡처가 하루 밀린다.**

---

## 4️⃣ 📊 「218종」이 낡았다 — 개수를 문서에 적지 않기로

📮 창업자 — *"218종 자동 맞는지도 확인해줘 어차피 다시 올려야 하니까"* · *"우리 음식 이모지 218종 맞는지도"*

**세어 보니 218 이 아니었다.**

| | 값 |
|---|---:|
| 파일 | **277장** |
| 픽커에 실린 것 | **236컷** |
| 일부러 내려둔 것 | **41컷** |
| 제목 자동매칭이 가리키는 사진컷 | **236** (픽커와 정확히 일치) |
| 깨진 참조·중복·빈 이름표 | **0** |

✅ `CLAUDE.md` 를 고쳤다 — **「개수를 여기 적지 말 것 — 두 번 낡았다(88→218→또). `node scripts/check-foodtab.mjs` 를 돌린다.」**

📌 이걸로 오늘 **「손으로 나열한 목록은 반드시 낡는다」를 세 번** 봤다 — 코치 키 · 음식 아이콘 개수 · 일기 저장 필드.

---

## 5️⃣ 🎨 배경색이 겹치나 — 재서 답했다

📮 창업자 — *"우리 배경색은 안겹쳐? 8장 다 다른색이던데 파랑은 한번 나왔었거든"*

**실측 = 파랑(213°)은 일기 슬라이드 하나뿐이고 나머지 여덟은 전부 웜톤.**
📮 창업자 확정 — *"색 안겹쳐 그대로 가자"* · *"좋아 깔끔하다 색도 보라빛돌아서 예쁘다"*

---

## 6️⃣ 🔍 온보딩 열 장 전수 검수

`scripts/_검수-온보딩전수-0809.mjs` 신설.
- ⛔⛔ **캐러셀 DOM 함정** — 열 장이 **전부 DOM 에 살아 있다.** 트랙의 **k번째 자식만** 읽어야 한다.
- 장수를 **`WANT_N` 한 곳**에만 적었다(이름에서도 장수를 뺐다 — 파일 이름은 반드시 낡는다).
- `timezoneId:'Asia/Seoul'`.

**결과 = 날짜 셋(2026.08.09 / 8.9 일 / 8월) 일치 · 문구 이상 0 · 유니코드 이모지 0 · 판 밖 넘침 0 · 크래시 0.**

---

## 7️⃣ 🛒 「장보기」 장면 신설 — 아홉 장 → 열 장

📮 창업자 — *"여기는 장보기가 빠졌구나.."* ＋ 스토어 스샷 시안 첨부 → *"이거 스샷화면이야"* → *"온보드에 왜 빠져있었지? 스샷이랑 다 똑같이 했었는데"*

**맞다. 스토어 스샷 ⑤ 「재료, 한 번에 사러가기」가 온보딩엔 통째로 없었다.**

`SlideS` 를 스샷 그대로 라이브 React 로 옮겼다 — 살 것 세 줄 ＋ 쇼핑몰 다섯 ＋ 곰펭 `duoCart`.

### ⛔ 창업자가 «범위»를 갈랐다
📮 *"온보드에는 없어도 될 것 같아. 4음식아이콘은."* ＋ *"스샷에는 넣자."*
→ **④음식 아이콘은 온보딩엔 «안» 넣고 스토어 스샷엔 «유지».** 그 결정을 `Onboarding.jsx` 주석에 못 박았다.
📌 **「스샷 = 온보딩」이 아니다.** 스샷은 설치 «전»에 파는 글이고 온보딩은 설치 «후»에 안내하는 글이다.

```js
SLIDES = [Slide7B, Slide7C, Slide1, Slide2, SlideD, Slide3, SlideS, Slide6, Slide7, Slide8]  // 열 장
```

---

## 8️⃣ 📷 속지 사진을 «속지마다 따로» (커밋 `99853d3`)

📮 창업자 폰 제보(전날) — *"기록3칸에 올린 사진들이 스크랩사진첩에 똑같이 붙어."*

⛔ **뿌리 = 여섯 속지가 전부 `photo`/`photo2`/`photo3` 한 자리를 같이 썼다.**
속지를 갈아입어도 사진이 안 날아가게 한 것인데, 칸 «모양»이 달라 엉뚱하게 붙어 보였다.
→ 창업자가 **②속지마다 따로 담는다**를 골랐다.

| 속지 | 사진 키 |
|---|---|
| photo 사진일기 | `photo` (옛 키 유지 — 저장본이 제일 많다) |
| card 레시피 기록 | `ph_card` |
| today 오늘의 한끼 | `ph_today` |
| snap 사진 기록 | `ph_snap` |
| list3 기록 3칸 | `ph_l1`·`ph_l2`·`ph_l3` |
| scrap 스크랩 사진첩 | `ph_s1`·`ph_s2`·`ph_s3` |

### ⭐ 「손으로 나열한 목록」을 또 없앴다
`DiaryScreen` 이 사진 필드를 **네 곳에 따로** 적고 있었다 → `ALL_PHOTO_FIELDS` 한 곳으로.

### ⭐⭐ 제일 중요한 것 — **이관을 «저장»까지 한다**
```js
useEffect(() => {
  if (!entry) return
  const m = migratePhotoKeys(entry)
  if (m !== entry) updateDiary(entry.id, m)   // 🚚 화면만 고치면 앱 재시작 때 또 옮겨간다
}, [entry?.id])
```
⛔ 화면만 고치면 옛 자리가 남아서 **앱을 다시 켤 때 다른 속지로 또 딸려간다.** 재현판이 이걸 잡았다.
⭐ `migratePhotoKeys` 는 **안 옮겼으면 원본 그대로 돌려준다**(`if (!moved) return entry`) — 밖에서 「바뀌었나」를 가릴 수 있게.

### 🔒 장치 둘
- `check-paperphoto.mjs` (smoke) — 속지끼리 사진 자리를 **공유하면 배포를 막는다.** ⭐**노드만으로** 돈다(`papers.js` 는 webp import 라 Node 가 못 읽어서 «글자로» 센다 — 배포 게이트는 파이썬·이미지 도구에 기대면 안 된다).
- `_repro-속지사진분리-0809.mjs` — ①이관 ②분리 ③복귀. **화면에 실제로 그려진 사진**으로 판정.

### ⛔⛔ 재현판이 «거짓 실패»했다 — 규칙 18 그대로
처음엔 「이관 실패」로 나왔는데, 실제로는 **빈 종이**를 보고 있었다.
Node 는 UTC 라 `setHours(12)` 가 **KST 21시** → 시드가 «어제» 일기가 됐고 앱은 «오늘»을 열었다.
✅ 시드 날짜를 **브라우저 «안»에서** 정하도록 고쳤다.
📌 **「안 된다」가 나오면 앱이 아니라 «내 확인 방식»부터 의심한다.**

---

## 9️⃣ 📏 서랍 넓히기 — 재 보니 «못 넓힌다»

📮 창업자(6번) — *"서랍이 좀 더 넓었으면 좋겠어. 설명버튼 높이를 조금씩만 줄이는 것도.."*

`_measure-서랍-0809.mjs` 로 **360×780 폰에서 실측**했다.

| 손댄 것 | 스크롤 칸 |
|---|---:|
| 시작 | **182px** |
| 여백 정리(scroll 6/12→3/6 · tools 4/4→2/2 · 선물 줄 · 안내 줄 46→44 · 갈래 칩 46→44 · rowGap 2→0) | **188px** (＋6) |
| `.decor-drawer { max-height: 44vh → 52vh }` | **188px** (변화 0) |
| `.decor-stage` 좌우 여백 26 / 34 / 42px 주입 | **188px** · 종이 폭 **242px 고정** |

⭐⭐ **왜 안 커지나 = 서랍은 「남는 공간」을 쓰는데 남는 공간이 없다.**
종이(고정) ＋ 서랍 ＋ 도구 바가 화면을 이미 꽉 채운다. **상한은 원인이 아니었다.**

📌 **진짜로 넓히려면 판 크기 계산을 손대야 하고, 그건 «종이가 작아지는 것» = 미감 판정(규칙 11) = 창업자 몫.**
⛔ 새벽에 시행착오로 밀어붙일 일이 아니라고 판단하고 **실측 사실을 `styles.css` 주석에 그대로 박아 두고 멈췄다.**
⭐ 옛 주석은 *"상한을 올리면 넓어진다"* 는 «내 짐작»이었는데, **재 보니 틀렸으므로 주석을 사실로 고쳤다.**

---

## 🔟 오늘 세 번 본 병 · 두 번 밟은 함정

### 🩺 같은 병 셋 — **「손으로 나열한 목록은 반드시 낡는다」**
1. 코치 키 5개 나열 → `home3` 도 `brag` 도 못 지웠다 (두 달)
2. 음식 아이콘 「218종」 → 실제 236 (두 번째로 낡음)
3. 일기 사진 필드 네 곳 나열 → 속지 늘 때마다 어긋난다

✅ 셋 다 **한 곳에서 만들고 거기서 읽게** 고쳤다. ＋ 게이트로 막았다.

### 🪤 두 번 밟은 것
- ⛔⛔ **`subst.py --new` 안의 백틱(`` ` ``)이 bash 명령치환으로 새어 낱말이 사라졌다 — 오늘만 두 번**(`papers.js` · `flex: 0 0 auto`). Edit 툴로 복구.
- ⛔ **`pkill -f "vite preview"` 가 자기 자신을 죽였다**(exit 144) → `pkill -f "[v]ite preview"`.

---

## 1️⃣1️⃣ 배포 세 판

| 판 | run | sha | 내용 |
|---|---:|---|---|
| **v10.05** | 1171 ✅ | `b895a24` | 일기 슬라이드 재꾸밈 · 코치 키 한 곳으로 · 날짜 오늘로 · 비빔국수 · 아홉 장 전수 검수 |
| **v10.05 2차** | 1173 ✅ | `79b6900` | 장보기 슬라이드 신설 → **열 장** |
| **v10.06** | 1174 | `04e219a` | 속지 사진 분리 ＋ 서랍 여백 정리 |

＋ `0fbabf3` = CLAUDE.md 218종 정정(문서라 배포와 무관).

---

## 1️⃣2️⃣ 🟡 안 끝난 것 (내일)

| | 무엇 | 왜 남았나 |
|---|---|---|
| 🟡 | **서랍 진짜로 넓히기** | 종이를 줄여야 한다 = **창업자 미감 판정**. 시안을 만들어 보여줄 것 |
| 🟡 | **잘린 fx 3컷**(burger·cupcake·icecream) | 대체 원본이 없다 |
| 🟡 | **스토어 스샷 8장 재제작** | 콘솔이 **8/8 = 가득 참**. 일기를 넣으려면 한 장 빼야 한다 → **재심사(8/16) 뒤에** |
| 🟡 | **뾰미 12컷 앱 등록** (`xb_` 접두어) | 잘라만 뒀다 |
| ✅ | ~~CLAUDE.md 핀 갱신~~ | v10.04·v10.05·v10.06 넣었다 |
| 🟡 | **`doc-guard --stale` 거짓 경고 3건** | `stickers/README.md` 101·143·145 — **「조건부 미래 할 일」**(친구가 늘면 정본을 다시 뽑는다)인데 파일이 있다고 「끝난 걸 대기로 뒀다」로 잡힌다. **매 세션 뜬다 = 시끄러운 게이트는 죽은 게이트** → 조건부 문장을 걸러내게 고칠 것 |

### ⏰ 달력
- **8/13~14** — AAB versionCode **13** (트랙 = **비공개 테스트-베타테스트** · ⛔BILLING 안 넣음)
- **8/16** — 프로덕션 재신청
- **8/24** — 9월 레시피 재고
- **8/31** — 9/1 자동 공개 전날 검수(56컷) ＋ targetSdk 기한
- **9/30** — Android 개발자 인증

---

## 1️⃣3️⃣ 📌 한 줄로 남길 것

> **「없다」·「안 된다」가 나오면 앱이 아니라 «내 확인 방식»부터 의심한다.**
> 오늘 그걸로 셋을 갈랐다 — 재현판의 시간대 · 배포 막힘의 진짜 원인 · 서랍이 안 넓어지는 이유.
> 그리고 **재 보고 못 하는 일이면, 못 한다고 그대로 적어 둔다.** 짐작으로 적힌 주석이 다음 사람을 또 헤매게 한다.
