# 작업복기 2026-09-07 — 소소 기능 캐러셀 ②③ 전수 검수 ＋ 릴스 「쌓이기＋타자」

> 갈래 = 📣 인스타 홍보. 앱 소스(`hankki/src`)는 한 줄도 안 건드렸다 — 생산기·조립 스크립트만.
> 브랜치 = `claude/booting-1zxf9t` (전부 커밋·푸시됨 · 원격과 차이 0)

## 1. 무엇을 했나

### ③ 「기록은 내 것」 릴스에 효과를 넣었다
- 창업자 *"그리고 효과도 넣어줘"* → 어느 산출물인지 물어 ③ 릴스로 확정.
- **시안 세 개**를 6.6초 클립으로 뽑아 보여줬다(앞 3장만) — ①쌓이기(slideup) ②넘김판(wipeleft) ③또각강조(fade＋줌펀치).
  · 전환 순간 프레임을 열어 눈으로 판정: ③은 두 장의 글자가 이중으로 비쳐 지저분했다.
- 창업자 = *"쌓이기로 가고 타자 효과까지 얹어줘"*
- ②편 생산기에 있던 **TYPE=1 프레임 뽑기**를 ③ 생산기(`_판-소소기록캐러셀-0907.mjs`)에 옮겼다.
  · 장마다 ①빈 판 → ②조각·메모·띠·스티커가 하나씩 28px 위에서 내려앉음 → ③제목이 커서 달고 한 자씩.
- **조립 스크립트 신설** = `design/promo/인스타-2509/조립-소소3릴스-타자.sh` (뒤에 ②도 쓰게 `T`/`W`/`OUT` 환경변수로 일반화)
  · 장 사이 = `xfade=slideup` · 4:5 를 늘리지 않고 종이색(`0xe7ebe0`) 여백에 앉힌다.
  · 결과 = ③ **44.2초** · ② **36.6초** · 둘 다 1080×1920 · 60fps

### ② 「홈이 알아서」 8장 전수 검수 (창업자 *"카드 겹침 잘림 다 확인해줘"*)
설계 100% 배율로 한 장씩 열어 봤다. 고친 것 넷:
| 장 | 흠 | 고침 |
|---|---|---|
| 1 표지 | 곰펭 스티커가 「우리집레시피 2개 새로 열렸어요 **외 11건**」 줄을 덮음 | 스티커를 짙은 판 위로 (`top 470 → 706`) |
| 3 제철 | 조각끼리 겹치고 부제가 꽃게 조각에 가림 | 제목y 496 · 조각 위치 3차 조정 |
| 4 영상 | 「인분 − 2인분 +」 줄이 카드 윗변에서 반 잘림 | `잘라 1000 → 1090` |
| 5 자주 | 반쪽 「⊙」 칩이 걸치고 칩 밑변이 제목에 물림 | 조각 `y495 w1000` ＋ `펭(886, …)` |
| 6 검색 | 검색결과 띠가 오른쪽으로 삐져나오고 「한식 · 0분」 줄이 밑변에서 잘림 | 띠 `left 480 → 392` · 카드 조각 `h 690 → 775` |
2·7·8장은 이상 없었다.

### ③ 3장·5장 — 창업자 제보 둘
- *"메모스티커도 안잘리게 해줘 아래위가 잘렸어"* (③ 03-메모)
  → 원본 앱 화면에서 포스트잇 자리를 실측해 조각을 다시 잡았다(`y 1020→985 · h 440→585 · 배율 0.74→0.66`).
    포스트잇 위(8월 22일·별점)·아래(분홍 접힘)와 「다진 대파 흰 부분 1/2대」까지 온전.
- *"해먹은 날이 달력에 쌓여요에서 달력에 쌓인게 별로 없어"* (③ 05-일기) — **맞는 지적이었다.**
  🔢 실측 = 창업자 백업의 일기가 **9월 6일치 · 8월 17일치**. 9월은 오늘이 7일이라 당연히 비어 보인다.
  ✅ **데이터를 지어내지 않고** `_shot-소소기록-0907.mjs` 에서 달력을 «이전 달»로 넘겨 다시 찍었다(8월 · 15번 · 요리 12일치).
  ⛔ 그러면서 **「이번 달 요리 수」 조각은 뺐다** — 그 카드는 달력을 넘겨도 «9월 값(2번)»이라 8월 달력과 어긋난다.

## 2. 낸 것 (창업자에게 보낸 것)
- ② `소소2-홈이-알아서.zip`(8장 · 7.2MB) ＋ `소소2-릴스.mp4`(36.6초)
- ③ `소소3-기록은-내-것.zip`(8장 · 4.8MB) ＋ `소소3-릴스-타자.mp4`(44.2초)
- 시안 3종(6.6초씩) — 판정 끝(쌓이기 채택)

## 3. 걸린 것·배운 것
- **bigout-guard 를 두 번 넘겼다**(96,934B 등) — 원본 100% 로 보되 **한 턴에 두세 장씩**, 폭 700~1080·품질 40~45 로.
- **앱 화면 재료(`소소기능-앱화면-2509/홈`)가 작업 브랜치에 없었다** → 배포 브랜치에서 뽑아 scratchpad 에 두고 `APP=` 로 넘겼다.
- ffmpeg-static 에는 **ffprobe 가 없다** → 길이는 `ffmpeg -i` 출력을 파싱해서 잰다(＋`set -e` 때문에 `|| true` 필요).

## 4. 남은 것
- ②③ 인스타 캡션·해시태그 아직 안 썼다.
- ④편(있다면) 기획 미착수.
- 6장 짜임은 「일부러 흘린」 느낌이 남아 있다 — 더 반듯하게 갈지는 창업자 판정.
