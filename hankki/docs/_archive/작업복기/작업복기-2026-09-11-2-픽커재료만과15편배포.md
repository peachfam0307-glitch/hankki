# 📔 작업 복기 — 2026-09-11 (밤) · 픽커 «재료만» · A 15편 · 큐레이션 셋

> ⭐ 이 문서는 **그날 있었던 일의 기록**이다(안 낡는다). 「지금 상태」는 `HANDOVER.md` 를 본다.

## 🚀 오늘 나간 것 — v13.16 (run 2382)

| | 무엇 | 창업자 신호 |
|---|---|---|
| ① | **냉장고 재료 픽커에서 요리를 다 뺐다** — 재료 172컷만 | *"냉장고재료아이콘에 음식이 있을필요없어 다빼고 재료만남기자"* |
| ② | **창업자 저장 레시피 15편** ＋ 새 아이콘 8컷 | *"어제 안나간 15편 레시피 한번에 올리자. 아이콘 달고"* → 전수 검수 |
| ③ | **주부의 장바구니 셋** — 쇼핑몰 배지 27개 누락·줄바꿈·「공식몰」 | *"뱃지 다 붙여줘. 컬리랑 다.전수해서"* · *"공식몰좋아."* |
| ④ | **갈치국 통째로 지움 · 갈치구이 남김** | *"갈치구이는 기본레시피로 넣어도 될 것 같고 국은 빼자"* |

📌 레시피 **72 → 87편**(지금 열린 것) · 앞으로 열릴 것까지 **184편**

---

## ⛔ 오늘 «잡은» 것 — 전부 눈으로 열어봐서 잡았다

### ⑴ 15편 표지가 «빈 접시»였다 (제일 컸다)
앱을 띄워 「대파 소스 목살 덮밥」을 열었더니 **그림이 안 떴다.**
- 🔎 뿌리 = `src/assets` 에 파일을 넣고 `FoodIcon` 갈래에 키를 넣어도
  **`Stickers.jsx` 의 `PHOTO_RATIO` 에 없으면 「빈 접시」**가 그려진다. **등록이 세 곳**이다.
- ⚠️⚠️ **검수판은 멀쩡했다** — 판은 파일을 직접 읽어 그리니까. **판만 보고 넘겼으면 15편이 다 빈 접시로 나갔다**(규칙 30).
- 📌 2026-09-09 「애호박국밥 빈 접시」와 **똑같은 자리**다. 그때 주석까지 남겼는데 또 밟았다.
- ✅ 비율 8개를 파일에서 재서 넣고 **전수 검사**(`_검사-A15표지-0911.mjs`)를 만들었다 — 15편 전부 ✅

### ⑵ 15편을 «옛 판»으로 넣었다
같은 폴더에 `A-다듬음.json` 과 `A-5판.json` 이 있었는데 **앞엣것**을 썼다.
- 🔢 그 판엔 창업자 판정이 **하나도 안 밀려 있었다** — 참치액·알룰로스·식용유가 그대로
- ✅ `A-5판.json` 이 최신 = 「초피액젓 (없으면 참치액)」·「아우노슈가 3/4큰술 (일반설탕 1/2큰술)」·「올리브유」
- 📌 **「다듬음」이 「5판」보다 최신처럼 들렸다. 이름으로 짐작하지 말고 내용을 열어야 했다.**

### ⑶ 쇼핑몰 배지가 27개 빠져 있었다 (3개는 이미 열려 있었다)
- 🔎 뿌리 = 판정이 **반쪽**이었다. `icoop`·`oasis`·`한살림` 은 표식으로 보는데 `coupang`·`kurly` 는 **링크로만** 봤다.
- ⚠️ 8/23 「하바티치즈는 왜 딱지없어?」 때 **「표식이 빠져도 링크로」**만 넣고
  **거꾸로 「링크가 빠지면 표식으로」는 안 넣었다.** 같은 사고의 뒷면이 27개로 돌아왔다.
- ✅ 둘 중 하나만 있어도 붙게 ＋ 자사몰은 **「공식몰」** → **126장 전부 배지가 붙는다**

### ⑷ 「더오담만 줄바꿈이 튄다」 — 앱 탓이 아니었다
- 🔎 범인 = `textWrap: 'balance'`(줄 길이를 맞추려고 **일부러 짧게 끊는다**)
- ⚠️ 바로 위 주석이 *"긴 설명은 한 글자도 안 바뀐다 → 손해가 없다"* 라고 적어뒀는데 **짧은 설명에서는 바뀐다.**
  📌 **재본 범위를 결론의 범위로 삼으면 안 된다.**
- ✅ 창업자가 글을 새로 줘서 **줄이 저절로 꽉 찼다**(남은자리 105px → 10px)

### ⑸ 픽커 검색이 「영계」를 못 찾았다
- 🔎 색인이 `ICON_RULES`＋`FOOD_NAMES` 만 보고 **`ING_RULES`(재료 이름표)를 안 봤다.**
  `ingIconOf` 는 「영계」를 아는데 픽커는 못 찾는 — **아는데 못 찾는** 상태였다.
- ✅ 같은 표를 쓰게 했다 → 재료 **173개 전수 통과**(한 글자 함정도 그대로 산다)

### ⑹ weekly.js 주석이 «거짓»이었다
「갈치구이·갈치국은 from 을 떼서 안 열리게 했다」 → **from 을 떼면 오히려 항상 열린다.**
실제로 둘 다 `from: '2026-09-14'` 가 붙어 9/14에 열릴 참이었다. 📌 **주석을 믿고 짐작했으면 못 잡았다.**

### ⑺ 내가 스모크를 죽였다
도는 중에 `version.js` 를 고쳐 dist 가 낡았고 재현판이 줄줄이 멈췄다.
📌 **스모크가 도는 동안은 `src` 를 건드리지 않는다.** ＋ `pkill -f "npm run smoke"` 가 자기 명령까지 잡는다(exit 144).

---

## 🛠 오늘 만든 도구

| 도구 | 무엇 |
|---|---|
| `_검사-재료픽커구멍-0911.mjs` | 앱이 아는 재료 이름 **173개를 하나씩 쳐 본다** |
| `_검사-몰배지전수-0911.mjs` | 시계를 2028년으로 옮겨 **85개를 다 열고** 배지를 센다 |
| `_검사-A15표지-0911.mjs` | 15편 표지가 **빈 접시가 아닌지** 앱에서 잰다 |
| `_뽑기-큐레카드-0911.mjs` | 앱 큐레이션 카드를 **펼친 채로** 잘라낸다(릴스용) |
| `_뽑기-갈래캡처-0911.mjs` | 갈래별로 「무엇이 들었나」를 앱 화면 그대로 |
| `_probe-더오담줄-0911.mjs` | 줄마다 **실제 폭·남은 자리**를 잰다 |
| `_넣기-A15편-0911.mjs` | 창업자 저장 레시피를 `basics.js` 로 옮긴다 |
| `_판-큐레릴스-0911.mjs` | 큐레이션 릴스 시안(⛔1판 — 앱 UI가 안 나와서 다시 짠다) |
| `_판-검수.mjs` **넓힘** | 날짜 말고 **아이디 앞자리로도** 뽑는다(`--id basic-own-`) |

---

## 🛒 큐레이션 — 9/12(토) 드립을 맞바꿨다

📮 창업자 = *"김치콩비지 기버터 피자 넣자. 이번주"*
- **9/12** = 마야항아리 기버터 · 더오담 김치콩비지찌개 · 톰볼라 버섯피자
- 밀려난 둘 = 들기름막국수 → 2027-01-30 · 낫또 → 2027-02-06 (1:1 맞바꿈)
- 🔢 **29주 85개 그대로** — 한 주도 안 비었다

📌 창업자가 콕 집은 것 중 **고추장은 큐레이션에 한 개도 없다**(실측) → *"고추장은 우선 패스하자"*

---

## 💬 창업자 질문에 답한 것

**「자동 압축을 80%에서 하게 할 수 없나?」** → **된다.**
`/autocompact 850k` (창업자가 이 값으로 설정함 · 1M 창의 85%)
- 이번 세션에 바로 먹고 **설정에 저장돼 앞으로도 적용**된다 · 되돌리기 = `/autocompact auto`
- 📖 출처 = code.claude.com/docs/en/model-config · platform.claude.com/docs/en/build-with-claude/compaction
