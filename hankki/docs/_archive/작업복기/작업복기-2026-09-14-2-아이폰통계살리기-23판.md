# 작업복기 · 2026-09-14 (2) — 아이폰 이용통계 살리기 · 23판 · 재로그인 합치기 (아이폰 세션)

> 갈래 = `claude/annyeong-yoi12y` · 오후 13:46 ~ 17:10 KST · 22 심사는 「심사 대기 중」 그대로.

## 1. 왜 시작했나
📮 창업자 = *"출시되면 로그인부터 유저들 로그인 몇명했는지. 어떤기능을 이용했는지 볼수있는 판이있어야해. 지금은 갤럭시는 하고있거든."*
- 읽은 것 = `앱-모니터링-운영` · `로그인-필수로-2026-09-07` · 09-08 세션기록(GA4 도입) · `GA4실측-2026-09-13` · `기준선-2026-09-12` · `무엇을못재나` · `이용통계-보는법` · `stats.js` 전문.
- 판은 이미 있었다(GA4 「페이지 및 화면」 · 화면 15 ＋ 행동 이벤트). 없던 것 = ①로그인 이벤트 ②**아이폰 앱이 잡히는지**.

## 2. 실측 — 아이폰 앱은 GA4 에 0 이었다
- 창업자 GA4 실시간 캡처 3장(13:59~14:02) — 딸 폰 22판으로 2분 돌아다녔는데 앱 화면 0. 잡힌 1명 = `/hankki/get.html` 징검다리로 온 남.
- 코드 = `main.jsx` 가 조건 없이 `통계시작()` → 껍데기(capacitor://localhost) 안에서 막힌다. 후보 ⓐ googletagmanager 스크립트 ⓑ 주소·쿠키. ⛔ 샌드박스는 google-analytics.com 이 막혀 실험 불가 → 어느 쪽인지 못 갈랐다.

## 3. 판정 — B (22 심사 그대로 · 23은 승인 뒤 업데이트)
| | A 빼고 재제출 | **B 승인 뒤 업데이트** |
|---|---|---|
| 심사 | 2.1 회신 흐름 끊김 | 그대로 |
| 첫날 | 화면 통계 됨 · 출시 밀림 | 로그인 수는 Firebase 계정으로 첫날부터 · 화면은 24 승인까지 0 |
📮 창업자 *"B로 가자 ㄱㄱ"* → 23은 «통계 확인용»으로 먼저(창업자 "ㄱ ㄱ") · 최종판(탭·아이콘) = 24.

## 4. 만든 것
- **`stats.js` 🍎 직접 전송** — 원문 = github.com/idarek/minimal-analytics-4(g/collect v=2 · cid/_fv/sid/sct/_ss/_s/seg/_et). `수집주소만들기()` 순수 함수 · cid/sid/sct/last/hits 를 `hankki:stats:*` 에 · 세션 30분(GA4 기본값과 같은 자) · `dl = https://…/hankki/ios#화면` → **페이지 경로 `/hankki/ios`** 로 아이폰만 갈린다(맞춤 측정기준 없이) · sendBeacon → fetch(no-cors, keepalive) · 저장소 못 읽으면 안 보냄(매번 새 사람으로 세지 않는다) · 시계 거꾸로 = 새 세션.
- **로그인 이벤트** — `로그인됨(공급자, 새계정)` → `login_google/apple` · `signup_*`(creationTime≈lastSignInTime 60초). 부르는 곳 = `cloud.js 로그인()` 성공 직전 한 곳(지켜보기 콜백 ⛔).
- 방침 = 시행일 09-14 · 8-다 「로그인·가입 사실과 방식 포함」(check-통계고지 ✅).
- 재현판 `_repro-아이폰통계직접-0914`(17칸) smoke:seq 등록 · 유저눈 재현 ✅(웹 gtag 길 변화 0).

## 5. 게이트·빌드
- 1차 195/197 — 실패 = check-basics-version(갈래가 배포 갈래보다 뒤) · check-iconmatch(앱 세션 몫) → 배포 갈래 v13.41(eefe59e8) 합침(package.json 만 충돌 · smoke:seq 합집합 198) → 2차 **199/199**.
- `[testflight]` 첫 커밋(ad7f2632)은 **문서만 바꿔 워크플로가 안 돌았다**(paths = ios-app/** · 워크플로 파일) → Fastfile 한 줄 얹어 3dd4f06f → run 23 success 15:53. 📌 **[testflight] 커밋은 ios-app 파일을 반드시 포함.**
- 인증서 취소 메일(15:48) = 23 빌드가 22 인증서를 지운 정상 동작. 22(처리 끝난 판)에 영향 없음.

## 6. 실물 검증 (딸 폰 23 · 창업자 GA4 캡처 4장)
- 실시간 페이지 `/hankki/ios` 활성 1 · 조회 9 → 25 · 실시간 개요 `home 11 · myrecipes 4 · detail 3 · profile 2 · log · search · shop` · first_visit/session_start 붙음 · **`login_apple` 1** · 로그아웃→재로그인해도 사용자 1.
- ✅ 아이폰 통계 살아남 확정. 문서 = `이용통계-보는법` §5.

## 7. 덤으로 잡은 구멍 — 재로그인이 폰 기록을 덮는다
- 창업자 실측 = 로그아웃→재로그인→「저장해둔 게 있어요·가져오기」→ 97편이 87편으로. 앱 재시작하니 97 복귀(store.jsx 163줄 열린 편 재채움) — 하지만 마지막 올리기 뒤 바뀐 건 잃는 구조.
- 뿌리 = 다른 계정만 물었고 같은 계정은 importAll(통째). `로그인()` 이 반환 전에 주인을 정해 뒤에서 읽으면 새 폰도 「같다」.
- 고침(e58b99da) = 로그인 «전» `기기주인()` → `syncMerge.받을때합칠까({이전주인, 주인전, 번호})` → 합쳐받기(합치기 = 더하기만). 새 폰·다른 계정·웹 변화 0 · 재현판 6칸 · 아이폰로그인필수 재현 그대로 ✅. 잃는 것 = 다른 기기에서 지운 편이 되살아날 수 있음(창업자 *"어차피 지운게 있는건 나뿐"*).

## 8. 승인 전 미리 해둔 것
- 징검다리 `get.html`(4e7ac4e0) = 아이폰·아이패드 → App Store(id6811288851) · `bridge_ios`/`bridge_go_ios`(📮 *"인스타프로필링크타고 아이폰유저가 몇명왔는지도"*) · 재현판 3칸(UA 지정 · apps.apple.com 가로채기). ⛔ 22 승인 뒤 「배포해」 때.
- 공유 화면 캡처 = `design/promo/아이폰스샷-2509/공유부품-22판-실물-딸폰.png`(다른 앱 0). 시스템 공유 시트 캡처는 없음(녹화는 HEVC 라 여기서 못 연다).
- HANDOVER·아이폰 문서에 「승인 뒤 빠짐없이 넣을 것 다섯」(📮 *"다 저장해놓고 승인나면 빠짐없이 넣어야해"*).

## 9. 내가 틀렸거나 헛돈 것
- `[testflight]` 를 문서 커밋에 달아 12분 헛기다림(paths 필터를 안 봤다).
- 재현판 마지막 줄 오타(`nashi`) · 징검다리 재현 첫 판이 Play 주소만 가로채 아이폰 이동 칸이 헛실패.
- 「23 뜨면 딸 폰 시험」을 두 번 앞서 말했다(빌드 전·처리 전).

## 10. 남은 것
- 22 결과(오늘 밤~내일 아침) · 앱 세션 탭·아이콘 → 24 · 시스템 공유 시트 캡처(원하면) · 딸 폰을 GA4 「우리」로 빼기(앱에 주소창 없음 · 설계 과제) · 합치기에 무덤 붙이기(두 기기 유저 늘면).
