# 🎑 작업복기 2026-09-09(밤) — 명절 장식 자리 · 한복 컷 · 추석 릴스

> 세션 = `session_016KvtRY6zJFv8cZcsmX6fJr` · 브랜치 = `claude/chatgpt-conversation-link-kvn5ph`
> 이 판에서 나간 버전 = **v12.96 → v12.97 → v12.98 → v13.00**

---

## 1. 한 것

### 🌕 홈 명절 장식이 「놓아보기 판」과 어긋나 있었다 (v12.97)
📮 창업자 = *"홈에 꼬르곰펭펭 저자리 아니야"* ＋ 목표 그림 첨부

**까닭이 셋이었다 — 하나만 고쳤으면 계속 어긋났다.**

| | 무엇이 틀렸나 | 결과 |
|---|---|---|
| ① | 판은 `x·y` 가 **가운데**인데(`translate(-50%,-50%)`) 앱은 `y` 를 **위**로 썼다 | 다들 제 키의 «절반»만큼 아래로 |
| ② | 판 배경은 **창 전체 캡처(540)** 인데 앱은 `.screen`(440)에 대고 그렸다 | 조각이 **19% 작아지고** 자리도 밀림 |
| ③ | 모션(`hk-m-float` 등)이 `transform` 을 갈아끼워 가운데맞춤을 지웠다 | ①을 고쳐도 안 먹음 |

✅ 자리(껍데기 `<span>`)와 움직임(안쪽 `<img>`)을 **따로 뒀다.**
🔢 검증 = 네 조각 다 판 값과 **2px 안**.

### 🇰🇷 홈의 「우리 애」 둘을 명절엔 한복으로 (v12.98)
📮 창업자 = *"한끼소식이랑 아직 안해봤어요 자리에.. 쟤들만 한복아니니까 이상해서.."*

- 창업자가 준 원본 두 장을 **표준 도구 `tools/cut.py`** 로 잘랐다(⛔도구는 안 건드림).
- 원본과 **같은 높이 320px** webp → 자리·크기·움직임 그대로, **그림만** 바뀐다.
- `홈컷 = { cs: { 소식: '한복곰', 다음: '한복펭' } }` · **추석 철에만.** 핼러윈은 컷이 없어 원래 컷.
- ⛔ 새 자리를 만들지 않았다 — 홈의 꼬르곰 자리는 「한끼 소식」 하나(창업자 확정).

### 🧒 듀오를 「이번 주 제철 · 월」 줄에 «박았다» (v13.00)
📮 창업자 = *"월요일 옆에 있자나 애들이!!!"* → *"고정해줘 스크롤하면 내려가는거이상해"*

⭐⭐ **아침의 「화면 고정」과 어긋나 보이지만 아니다.**
- 아침 지시(*"홈을 내리면 안따라와"*)는 **보름달** 얘기였다 → 화면 고정이 맞다.
- 듀오는 **「월」 알약 옆이 제자리**다 → **글에 붙어야** 한다. 화면에 붙이면 굴릴 때마다 어긋난다.

✅ `duo301` 만 `홈장식`(화면 고정)에서 빼서 **`줄장식`** 으로 옮겼다. 나머지 셋은 화면 고정 그대로.
🧪 재현판 `_repro-줄컷굴리기-0909.mjs` = 굴리기 **전·후로 「월」 알약과 사이 10px 그대로.**

### 🎬 추석 릴스 (v8 까지)
- 재료를 **오늘 판으로 다시** 찍어 한복 컷·바뀐 자리가 들어갔다.
- 보름달 칸이 카드 글자 중간에서 끊기던 것 → **흰 카드 시작 줄(y 330) 위**에서 자른다.
- 꼬르곰 칸이 납작하던 것 → **2.84:1 → 1.76:1**.
- 📱 **인스타 가림** — 창업자 실물 캡처로 쟀다: 영상 칸 y 210~1790(배율 0.823) · 닉네임＋음원 줄 아래끝 화면 320
  → **영상 좌표 134px**. 그날 올린 릴스 제목이 121px 이라 그대로 가렸다.
  ✅ 고침 = 판을 **줄이지 않고 190px 내려앉힌다**(창업자 = *"안전띠가아니라 글자위치를 내리면돼"*).

---

## 2. 밟은 함정 (다음에 또 밟지 말 것)

| | 함정 | 어떻게 드러났나 |
|---|---|---|
| 🅐 | **릴스 대본이 장식 자리 숫자를 «베껴 적어» 뒀다**(`y: 0.4240`) | 앱을 고쳐도 릴스는 옛 자리로 그렸다 → 창업자가 세 번 다시 짚었다. ✅이제 `seasonDecor.js` 에서 **직접 읽는다**(규칙 30) |
| 🅑 | 전역 `img{max-width:100%}` 가 기준을 **폭 0 인 껍데기**로 잡아 그림이 **0×0** | `maxWidth:'none'` 이 반드시 있어야 한다 |
| 🅒 | `subst.py --new` 안의 **백틱이 bash 에서 실행**돼 주석 낱말이 사라졌다 | CLAUDE.md 에 박힌 함정인데 또 밟았다 — 낫표「」로 쓴다 |
| 🅓 | 훅이 브랜치를 원격으로 맞추며 **내 커밋이 HEAD 에서 빠졌다** | 커밋 객체는 살아 있어 `cherry-pick` 으로 되살렸다. 잃은 것 0 |
| 🅔 | **두 세션이 같은 v12.99 를 달았다** | 나중에 올라가는 쪽이 올린다 → v13.00 |
| 🅕 | **`bigout-guard` 가 그림을 «바이트»로 셌다** | 511×360 한 장 = 241,274 B 로 셌지만 실제는 **245 토큰**. 240배 부풀림 → 오늘 경보 5회가 전부 «가짜» |

---

## 3. 안 끝난 것

- ⏳ **`bigout-guard` 자 고치기** — `_bigout-size.py` 에 「그림빼기()」는 넣었는데 **부르는 한 줄에서 승인 분류기가 막혔다.**
  지금은 **동작 그대로**(안전). 문턱 60,000 B 는 **안 올린다** — 자만 고친다.
- ⏳ **run 2332 초록불** — 2328·2329·2331 은 다른 세션 푸시가 앞질러 «취소»됐다(실패 아님).
  ⭐ 내 v13.00 은 2332 바닥에 들어 있어 **2332 가 초록불이면 같이 나간다.**
- ⏳ **글자 가리는 조각 둘** — 작은 보름달↔「꽃게간장조림」 · duo401↔「우리집레시피」.
  창업자 = *"일단 폰에서 봐보고 수정하면 되니까"* → 폰 확인 대기.

---

## 4. 다음 세션이 알아야 할 것

- ⛔ **장식 갈래가 «둘»이다** — `홈장식`(화면 고정) / `줄장식`(글에 박음). 섞지 말 것.
- ⛔ 릴스 대본은 자리 숫자를 **베끼지 않는다** — `seasonDecor.js` 에서 읽는다.
- ⛔ 인스타 릴스는 **위 190px 를 비운다**(닉네임·음원 줄이 134px 까지 덮는다).
- 📌 명절 창 = 추석 9/09~9/30 · 핼러윈 10/01~11/02 (`seasonDecor.js`).
