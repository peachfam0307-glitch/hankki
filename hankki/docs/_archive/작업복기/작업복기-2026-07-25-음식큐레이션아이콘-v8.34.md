# 작업복기 · 2026-07-25 — 음식 예시 아이콘 대량 + 쇼핑 큐레이션 아이콘화 (v8.30→v8.34)

> 하루 종일 **아이콘 마라톤.** 창업자가 앱에 없던 요리·재료를 스타일 맞춰 대량 생성 → 클로드가 blob 컷·등록·배선·배포. 레시피 아이콘 `fe_16~51`(36종) + 쇼핑 큐레이션 `cu_*`(27종). 앱 전체가 이모지에서 우리 그림체로 통일됨.

## 오늘 배포 (5회)
| 버전 | 내용 |
|---|---|
| v8.30~31 | 예시 6종(`fe_16~21` 카레·크루키·불닭냉면·요거트아이스크림·김치볶음밥·참치김치감태주먹밥) + 대구뭉티기·육회(`fe_22~23`) + **불닭냉면 반영 버그 수정** |
| v8.32 | 예시 4종(`fe_24~27` 새우크림파스타·양배추돼지고기볶음·치즈샌드위치·로제파스타) + '파스타=스파게티' 묶음 |
| v8.33 | **빈칸 리스트 24종 완성**(`fe_28~51`) |
| v8.34 | **쇼핑 큐레이션 27종 아이콘화**(`cu_*`) |

## 1. 레시피 완성요리 아이콘 `fe_16~51` (36종 추가 → fe_ 세트 51종 완성)
- **컷 파이프라인 확립** — 배경 near-white flood-fill(경계 연결분만 제거, 안쪽 흰색 보존) → 포그라운드 블롭을 **아이콘 중심으로 클러스터링 + union**(각 아이콘의 모든 조각을 합침) → bbox 크롭. **잘림·이웃조각 0.** (크루키 접시 오른쪽 잘림도 이 방식으로 해결 — '최대 블롭만' 방식은 연한 접시 가장자리를 떨궈서 잘렸었음.)
- **자동매칭 규칙**(`FoodIcon.jsx` `ICON_RULES`) — 신규 24개 전부 fe_ 섹션(범용·한식보다 위)에 등록, 24/24 검증. 가드 확인: 순대(단독)→순대 유지·오이→오이재료 유지·떡볶이 안 뺏김·낙지/주꾸미볶음도 오징어볶음 아이콘 공유·로제떡볶이 안 뺏김.
- **픽커**(`FOOD_ICON_GROUPS`) — 한식·양식·중식·일식·분식 칩에 신규 노출(카레 등 레시피 없어도 고르기 가능).
- **기존 레시피 반영** — 김치볶음밥·요거트아이스크림·크루키·불닭냉면·로제파스타·치즈샌드위치·새우크림파스타를 전용 아이콘으로 교체(`basics.js`).
- **`BASICS_VERSION` 37→40** — 내가 만든 레시피도 제목이 같으면 store.jsx v34로 자동 승급.

## 2. 🐛 반영 버그 수정 (중요)
- **증상**: 불닭냉면은 basics.js 아이콘을 fe_18로 바꿔도 앱에 반영 안 됨(크루키·요거트 등은 됨).
- **원인**: 편집(`touched`)했거나 이미 다른 PNG 아이콘이던 시드 레시피는 `migrateBasics`의 v13(시드 재동기화·touched 제외)·v34(내 레시피·이미 PNG면 스킵)가 **둘 다 건너뜀.**
- **해결**: `store.jsx`에 **v38/v39 강제반영 패스** 추가 — 예시 레시피는 **제목 매칭으로 아이콘 강제 교체**(단, 표지가 '아이콘'일 때만·사진 표지는 보존). `BASICS_VERSION`↑로 재실행 유도.

## 3. 🛒 쇼핑 큐레이션 아이콘화 `cu_*` (27종, v8.34)
- **방향 결정**: 레시피=**얼굴 있는 접시**(감정·꾸미기) / 쇼핑=**얼굴 없는 제품 일러**(구매·신뢰). 역할 분리가 UX적으로 맞음(창업자가 스타일 구분해 생성).
- **컷 특이점**: 시트에 **캡션 글자**가 상품 아래 붙어 있어서 → "제일 큰 상품 블롭 아래에 있는 블롭은 캡션으로 보고 버림" 필터로 상품만 추출(상품 라벨 스티커는 유지). 중복(간장·된장 등)은 더 선명한 시트에서.
- **매핑**: `curation.js` 22개 카테고리 전부 `icon` 필드 + 스톡·간식·햄은 **상품별 개별 아이콘**(쯔유/치킨/해물, 피자/부리또, 비엔나/슬라이스햄). `curIcon()` 글로브(`src/assets/curation/*.png`).
- **렌더**: `ShopScreen`의 이모지 3곳(카테고리 칩·섹션 헤더·상품 카드 타일)에서 `curIcon` 렌더, 없으면 이모지 폴백. `flat`/`catList`에 icon 전달(상품 아이콘 우선→카테고리 폴백).
- **검증**: Playwright로 실제 쇼핑화면 렌더 스샷 확인(칩·헤더·카드·상품별 아이콘 다 정상). 카레 🍛도 자동 해결.

## 결정·메모
- **카레 레시피는 안 만듦** — 카레는 쇼핑 큐레이션 카테고리로 이미 존재(레시피 아님). 창업자 확인.
- **미사용 아이콘 2개**(버린 것 아님): `cu_gochujang`(쇼핑에 '고추장' 카테고리 없음 → 레시피 재료용 or 나중 카테고리용 보관) / 올리브오일 틴케이스(병을 대표로).
- 콘솔(창업자): 브랜드 문구 콘솔 반영·게시 완료, 테스터 12명 유지, **관리형 게시 → 자동**으로 전환(문제 없음 — 검토 통과하면 자동 게시, 롤아웃 설정은 그대로).

## ⚠️ 빠진 것 (발견 — 다음 반영 필요)
- **온보딩 인트로 문구가 아직 옛 것** — 쇼핑화면 검증 스샷에서 발견. `src/components/Onboarding.jsx`가 여전히 "흩어진 레시피를, 한곳에" 옛 톤. 브랜드 개편(v8.28~29)이 매니페스트·태그라인만 바꾸고 온보딩은 안 바꿈. → 새 레꾸·감정 톤으로 교체 필요.
- **픽커 칩 길이** — 한식 칩에 아이콘 24개+ 붙어 꽤 길어짐(스크롤 OK지만 확인 요망).

## 내일 이거부터
1. **(창업자 대기) 프로모 영상 업로드** — 유튜브 비공개 → Play Console '동영상' 칸 링크 / 인스타 릴스(트렌드 음원). `design/promo/프로모영상-2507/`.
2. **온보딩 문구 새 톤으로** — 위 '빠진 것'. 저비용·바이럴(온보딩=가치 전달, 로드맵 출시전 집중 ③).
3. (선택) 쇼핑 '고추장' 카테고리 추가 여부 — cu_gochujang 활용.
4. **본진**: 테스터 12명 유지(7/31), 상표 결합출원.

---

## 🔧 오후 추가 작업 (같은 날 · v8.35→v8.36)
- **🐛 v8.35 꾸민 표지 손실 버그 수정** — 창업자 제보(공심채볶음): 배경지우기+꾸민 표지가 업데이트마다 사라지고 시드 아이콘이 다시 뜸. 원인=`migrateBasics` v13이 untouched 시드에 `...s` spread하며 `thumb`을 'none'→'icon' 되돌리고 `decorBg` 안 챙김(꾸미기는 touched를 안 세움). 수정=v13에서 decorBg 보존 + 꾸민 표지(thumb:'none'·decor·decorBg 있음)는 thumb 시드로 안 되돌림. ⚠️이미 날아간 건 재꾸미기 1회 필요(이후 유지).
- **v8.36 제보 4건 처리**:
  - ✅ **대구뭉티기** 레시피가 사시미 아이콘(fi_j09)으로 떠 있던 것 → `fe_22`(fe_22 만들 때 이 기존 레시피 존재를 놓쳤음). **교훈: 새 fe_ 아이콘 만들 때 같은 이름의 기존 레시피가 있는지 basics.js 전수 대조**(이번에 스크립트로 37개 대조 → 대구뭉티기만 걸림).
  - ✅ **베이컨/묵은지 파스타**가 옛 faceless `fy_y03`로 떠서 "우리 거 아닌 듯" 제보 → 창업자가 얼굴 있는 새 버전 생성 → `fe_52` 묵은지파스타·`fe_53` 베이컨크림파스타 추가, 두 레시피 교체+강제반영.
  - 오이무침(fe_36)=코드 정상, 그림이 애호박처럼 나온 생성이슈(재생성 선택). 카레 기본레시피=코드에 없음(옛 시드 잔재·삭제로 해결).
- **fe_ 세트 = 53종**(fe_16~53). BASICS_VERSION 41.
- **/할일 스냅샷** `docs/할일-전체정리-2026-07-25.md` 저장. **오늘 딱 하나 = 온보딩 문구 새 톤.**

---
_커밋·푸시 완료. 배포 브랜치 `claude/chatgpt-conversation-link-kvn5ph` = **v8.36** 라이브._
