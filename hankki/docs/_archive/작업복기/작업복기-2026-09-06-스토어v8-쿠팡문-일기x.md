# 작업복기 2026-09-05 밤 ~ 09-06 새벽 — 스토어 스샷 v8 · 쿠팡 문 · 일기 사진 ✕ · 릴스 시안

> 갈래 = 🎨앱 ＋ 📣홍보. 배포 브랜치 `claude/chatgpt-conversation-link-kvn5ph`. 앱 판 **v12.63 → v12.64**.

## 한 것 (시간 순)

1. **릴스 시안 A 확정** (레꾸자랑 · 17.2초 · 카드 1.5s×5 · 끝 카드 주아 ＋ 알약 CTA) — 창업자 *"아쥬조아"*. `design/promo/인스타-2509/시안-레꾸자랑-A.mp4`.
2. **유입 실측** — 인스타 306 조회 · 설치 27~33(평평). `docs/유입-실측-2026-09-05.md`. (다른 세션이 01:14 에 릴스 500·팔로우 8 을 얹었다.)
3. **쿠팡 「사러가기」 막힘 → v12.62·63** — 뿌리 = TWA 커스텀 탭을 쿠팡이 검문. 답 = `intent://…scheme=coupang` (검사 3 ⑥). 검색 65개는 v12.62 에서 *"된당"*, 직접 상품 4개는 v12.63 에서 이름 검색으로. `docs/쿠팡-링크-전수-2026-09-05.md`. 게이트 `_repro-쿠팡문-0905`(9칸).
4. **쿠팡 파트너스 가입** — AF8358570. ⚠️ 회원 이름이 창업자(손*은)인데 사업자는 남편 명의 → 고객센터 문의 중. 링크는 크롬에서 열림(검사 4 ①②). 앱 안 문(고지판＋링크 교체)은 대기.
5. **스토어 스샷 v8** — 시안 A~F 6갈래 → **D 에디토리얼 확정**(모눈·올리브·왼쪽 정렬·폰 화면 밖·세로 단계). 앱 화면 11장 다시 찍음(`_shot-스토어용화면-0822.mjs` 고침: 「요리모드 시작」·타이머·일꾸·랜덤카드). 생산기 `scripts/_판-스토어v8-0906.mjs` (＋ `CAROUSEL=1` 1080×1350) · 릴스 `design/promo/인스타-2509/조립-스토어v8릴스.sh`(15.15s). 재료 = `design/promo/스토어v8-원본-2509/v8-*.png`.
   - 창업자 실물 캡처로 바꾼 장 = 02 레꾸 표지 목록 · 03 간장비빔국수 · 06 갈비탕 일꾸(✕ 남음 · 임시).
   - 창업자 지시 반영 = 주아 · 은은한 배경 · 인스타 색과 갈라놓기 · 옛 펭펭(`gp_peng*`·`gp_duo*`) 금지 → `pjs_`·`duos_` 정본 · 곰 360/펭 290 · 샤랄라 별 · 08 부제 간격.
6. **일기 사진 ✕ 고침 → v12.64** — 제보 *"나갔다 들어와도 x가 보여"* · *"스티커 옆에 붙어 사진이 움직이거나 스티커가 사라져"*. 뿌리 = `PaperSheet` 의 `canShot`(넣기)이 ✕(지우기)도 갈랐다. 고침 = `photoClear` 프롭(기본 false) ＋ `DecorEditor` 가 탭 보고 얹음(스티커 탭 제외). 재현판 `_repro-일기사진x-0906` 5칸 · smoke:seq 추가. 창업자 「고쳐」(00:30) → 「배포해」(01:00).

## 배운 것 · 사고

- ⛔ **창업자가 직접 꾸민 실물이 있으면 내가 심지 않는다** — 콩국수 가을 꾸밈을 심어 *"콩국수 무슨일이야 ㅠㅠ"*. 홍보 재료는 먼저 «있나» 묻는다.
- ⛔ **「같은 맥락에서 검사」** — 카톡 브라우저에서 한 검사(검사 1)로 v12.60·61 두 판이 헛으로 나갔다.
- ⛔ **「⛔ 못 찾았다」 줄은 읽는다** — 0822 찍는 판의 `/요리 시작/` 이 8/22 이후 한 번도 안 걸렸는데 v7 까지 옛 캡처로 넘어갔다.
- ⛔ **latest-map 「같은 이름」 게이트** — 새 원본 폴더 파일은 `v8-` 접두. 배포 직전에 걸렸다.
- ⛔ **「디스크가 낡았다」 훅이 브랜치를 강제 리셋** — 커밋 전 작업이 날아가 다시 했다(01:24). 다른 세션이 푸시하면 **커밋을 먼저** 한다.
- 📌 캡처의 UI 조각(✕)은 손으로 지우지 않는다 — 밑 픽셀을 모르면 지어내는 것이다. 앱을 고쳐 다시 찍는다.
- 📌 README 00절은 `gp_*` 를 정본이라 적었지만 창업자 잣대(트렌치 벨트 버클)로는 옛 펭펭이다 → README 손볼 것.

## 남긴 것 (내일)

- ▶️ 미역국 릴스 올리기(창업자) → 캐러셀 고쳐 다시 → 스토어 8장 교체(콘솔 실물 캡처로 확인).
- 스토어 v8 1~7 고침 = 폰을 왼쪽·위로 · 05 타이머 띠 크게 · 07 「다시 뽑기·공유」 단추 보이게 · 06 은 v12.64 뒤 갈비탕 일기 재캡처.
- 캡처하는 법 캐러셀(인스타＋갤러리) — 같은 D 뼈대로.
- 쿠팡 파트너스 = 고객센터 답(명의) → 고지판 복구 ＋ 69링크 교체 ＋ 앱 안 문 검사 ＋ 최종승인 스샷.
- 하바티(직접 상품) 앱에서 열리는지 창업자 확인.
