# 작업복기 — 2026-09-04 (밤) · 유튜브 추적 끄기 · 9/5 검수 · 문서 다이어트

> 세션 = `session_01ABvrGc5ktFZ3ZZS4x5iQGY` · 배포 갈래 = `claude/chatgpt-conversation-link-kvn5ph`
> ⚠️ 이 세션은 «컨테이너가 113커밋 낡은 채로» 열렸다. 맞추고 시작했다.

---

## 0️⃣ 이 세션이 시작한 자리 — 창업자의 물음이 방향을 바꿨다

📮 창업자 = *"잠깐만 내가 뭔가 오해한 것 같은데 지금까지 우리 유튜브 관련해서 뭐 한거야?
   우리 앱에 우리가 레시피 올릴수있는걸 확인한거야? 아님 유저가?? **유저는 원래 올릴수있었어.** … 이걸 왜 하고 있었지???"*

⭐⭐ **맞는 지적이었다.** 갈래를 갈라 답했다 —

| | 누가 올리나 | 유튜브 조항 대상 |
|---|---|---|
| 유저가 링크 붙여 저장 | 유저 | 저장 자체는 아님 |
| **SNS 레시피**(수요일마다 우리가 여는 것) | **우리** | 상세는 «그림＋링크»라 임베드가 아니다 |
| **편집 화면 「영상 보면서 쓰기」** | 유저가 붙인 링크 | ⚠️ **여기 하나만 진짜 임베드** |

📌 그래서 **급한 일이 아니었다** — 앞 세션(9/4 낮)이 조회 도구·게이트를 만들었는데
   컨테이너 되감김으로 «푸시 전에» 날아갔고, 다시 만들지 않기로 했다.

---

## 1️⃣ 🛒 9/5 주부의 장바구니 셋 — 전날 검수 (절대원칙 28)

`_shot-공개검수-0905.mjs` 로 **브라우저 날짜를 내일로 돌려** 앱이 스스로 열게 하고 찍었다.
⭐ 데이터를 파싱해 내가 다시 그리지 않았다(절대원칙 30). **셋 다 눈으로 봤다**(절대원칙 21).

| 제품 | 그림 | 사러가기 | 판정 |
|---|---|---|---|
| 양념낙지젓 | ✅ 제 그림 | 없음 — **한살림이라 맞다**(창업자 2026-08-17 확정) | ✅ |
| 설성목장 한우 사골 곰탕 스틱 | ✅ 제 그림 | 쿠팡 | ✅ |
| 국내산 베이비 브로콜리 | ✅ 제 그림 | 쿠팡 | ✅ |

⭐ **어제 승인한 그림 8장이 제대로 붙었다** — 그 전엔 셋 다 «갈래 대표»를 물려받아
   사골스틱 자리에 해물가루육수 봉지가 떴다.
✅ `check-curicon` 초록불 · `--tomorrow` exit 0

⚠️ 남긴 것 = 쿠팡 둘은 **직접 상품 링크가 아니라 검색**으로 간다(엉뚱한 데는 아니다).

---

## 2️⃣ 📺 유튜브 — 조사 → 판정 → 고침

### 무엇이 사실인가 (앞 세션이 닫아둔 것 ＋ 이 세션 확인)
- 조항 = Developer Policies **III.E.4.j** — *"each YouTube video that it **embeds** on its site or app"*
- ⭐ 잣대는 **「임베드했나」** — 근거 셋이 전부 *embedded player* 를 말한다
- 그래서 **썸네일 `<img>` ＋ 링크는 조항 밖**이다(레시피 상세는 9/3 부터 그 모양)

### 창업자 판정 = ③ 미룬다 (＋ 추적은 지금 끈다)
📮 *"3번으로 하고 **한달쯤 뒤에 다시생각하자.** 영상을 보면서 받아적는 사람이 많을까 싶어. 요즘은 다 영상ai가 읽어주는데."*
📮 *"아동용 영상이면 추적을 꺼야 하는데 안 끄고 있어 **이거는 문제되는거면 끄자**"* → *"배포해"*

### ✅ v12.53 — `youtube-nocookie.com` (커밋 `dd1af80`)
- 고친 곳 = **한 줄**. 화면도 재생도 그대로, 호스트만 바뀐다
- ⭐ 절대원칙 34 = 숫자를 넓힌 게 아니라 **실패의 «모양»을 바꿨다**
  「모르는 영상에 추적이 켜진 채 나간다」 → 「모르는 영상도 추적이 꺼진 채 나간다」
- 🔢 실측(공식 도움말 ＋ 검색 다섯 곳 일치) = 개인화에 안 쓰임 ✅ · 비개인화 광고 ✅
  ⚠️ **추적 0 은 아니다** — 브라우저 저장소에 기기 식별자, IP 는 간다
- ⛔⛔ **이것이 「조회」를 대신하지 않는다.** 의무는 여전히 조회다

### ✅ 10/5 약속을 «달력»에 박았다 (커밋 `1d942f9`)
- ⛔ 문서에 안 적었다 — *「대기」라 적어둔 줄이 넉 달 뒤에도 「대기」였다* 는 걸 이미 배웠다
- ✅ «결정이 사는 곳»(`src/embed.js` **주석**)에 적고 `release-calendar.mjs` 가 그날 꺼내 보여준다
  ⭐ 주석이라 앱 번들에 한 글자도 안 실린다(달력은 파일 «글자»를 읽는다)
- ⭐⭐ 그런데 달력의 「그날 다시 볼 것」이 `paidPacks.js` **한 곳만** 읽고 있었다.
  ⛔ 유튜브 약속을 유료팩 파일에 끼워 넣는 건 «내가 편한 쪽»이라 안 했다.
  ✅ `약속이사는곳` 목록으로 **읽는 자리를 넓혔다** — 새 파일·새 장치 0개(절대원칙 35)
- 🧪 규칙 12 = 약속 6개 전부 살아 있다 · **10/4 에 서면 「10/05 · 📺 유튜브 임베드」가 뜬다**

### ⏳ 그날(10/5) 정할 것
- MFK 조회를 붙일지. ⭐ 붙이면 **회색 둘이 같이 지워진다** —
  ①아동용 여부 ②**공식 썸네일 주소**(지금 `i.ytimg.com` 주소는 우리가 조립한 것이라 그 자리도 회색이다)
- 🔢 쿼터 = `videos.list` **1단위** · 하루 10,000 · 한 번에 **50편 묶어도 1단위**
- ⭐ 키를 앱에 안 넣어도 된다 — **워커가 대신 물으면 된다**(`ocr`·`tidy` 둘 다 이미 KV 를 쓴다)
  ⚠️ 못 물어봤으면 **「영상 열기」로 물러나야 한다** — 이 한 줄이 그 설계의 목숨이다
- ⏳ 창업자 몫 = API 키 발급 한 번(무료)

---

## 3️⃣ 🧹 HANDOVER 85KB → 60KB — CI 빨간불의 «뿌리»였다 (커밋 `3f17a87`)

⛔ run #2116 이 `_repro-문서다이어트-0903` ⑥ 에서 죽었다 — **내 변경 탓이 아니었다.**
   「세션 시작 훅은 문턱 아래면 «조용»한가」 = 나온 값 **689자** · 바라던 **0**
   ＝ HANDOVER 가 경고 문턱(80KB)을 넘어 훅이 «말하고» 있었다.

⭐⭐ **세션을 열 때 훅이 정확히 이걸 알려줬다**(*"앞 세션이 안 치우고 나갔다"*). 내가 미뤘다.
📌 **알려주는 줄을 읽고도 미루면 «알림이 없는 것»과 같다.**

✅ `doc-trim --옮김` — 305줄 → 세션기록 · 28줄 → 보관소. ⛔한 글자도 안 지웠다.

---

## 4️⃣ 🎲 메모지 핀치 판이 «없는 글자»를 기다렸다 (커밋 `dcbf384`)

⛔ run #2117 = `waiting for locator('text=닭곰탕')` 30초 타임아웃. **로컬은 초록불.**

🌲 뿌리 = 씨앗과 열기가 «다른 편»을 볼 수 있었다
- 심기 = `find(title==='닭곰탕') || s.recipes[0]` ← 없으면 **물러난다**
- 열기 = `click('text=닭곰탕')` ← 글자로 **박아뒀다**
⭐ 씨앗이 물러나는 순간 열기는 «영영 없는 것»을 기다린다. **시한폭탄이었다.**

✅ 고침 둘 (⛔숫자 안 넓힘)
1. 심기가 «고른 편의 이름»을 돌려주고 열기가 그걸 누른다
2. 기다리는 «대상»을 갈랐다 — ①앱이 떴나 ②그 편이 있나

🧪 규칙 12 = 「닭곰탕이 아닌 편」을 일부러 심어도 7칸 초록불 · 되돌림 확인

---

## 5️⃣ ⛔ 내가 오늘 틀린 것 (숨기지 않고 남긴다)

| | 무엇 | 어떻게 잡혔나 |
|---|---|---|
| ① | 「2번 = 앱에 키를 넣는다」로 설명 | **워커가 있는데** 그걸 빼먹었다. 다시 알아보고 정정 |
| ② | **「뾰미＝도마뱀」** | 창업자가 *"아니 여우"* — 이름 순서를 잘못 읽었다(🦫카롱·🦊뾰미·🦎꼬비) |
| ③ | HANDOVER 정리를 미룸 | 그 사이 내 푸시가 그 게이트에 걸렸다 |
| ④ | 창업자가 준 **꾸미기 화면녹화를 저장 안 함** | 되감김＋요약으로 잃었다 — 다시 달라고 부탁해야 했다 |

⭐⭐ **④ 가 제일 아프다.** 나노바나나 원본은 `design/promo/인스타-2508/` 에 넣어뒀고
   README 에 *"업로드 폴더는 세션이 끝나면 날아간다 → 그래서 원본째 넣어 뒀다"* 라고 «내가» 적어놨다.
   **같은 규칙을 이번엔 안 지켰다.**

---

## 6️⃣ ⚠️ 이 세션에서 겪은 «장치의 흠»

### ⓐ 자동 맞추기가 내 커밋을 «두 번» 지웠다
옆 세션이 밀 때마다 `repo-sync`/`base-guard` 가 「낡았다」로 보고 원격으로 되돌렸고,
그때 로컬 커밋이 같이 날아갔다.
⭐ **`hold/유튜브추적끄기-0904` 에 먼저 담아둔 덕에 두 번 다 그대로 되찾았다**(규칙 20 이 값을 했다).
✅ 그 뒤로는 **fetch→reset→merge→commit→push 를 «한 명령»으로** 몰아서 밀었다.

### ⓑ `archive-guard` 가 `git add` 까지 막았다
`doc-trim` 이 보관소에 «만든» 파일을 커밋하려는데 훅이 경로만 보고 막았다.
규칙엔 *"git·Edit·Write 는 안 막는다"* 라고 적혀 있는데 실제로는 막혔다 — **막다른 길**(규칙 19).
🚪 창업자 지시 원문으로 표식을 만들어 풀었다.
⏳ **훅을 고치진 않았다** — 다음 세션이 판단할 것.

### ⓒ 배포 판이 계속 «취소»된다
옆 세션이 몇 분 간격으로 밀어서 앞 판이 끝나기 전에 새 판이 자리를 차지한다.
run #2114 취소 · #2115 취소 · #2116 실패 · #2117 실패.
📌 **코드는 안전하다**(원격에 다 들어가 있다). 초록불 하나만 뜨면 전부 나간다.

---

## 7️⃣ 📊 창업자가 준 콘솔 캡처 (기록만 · 판정 안 함)

기기 노출수 — 8/22 **202** → 8/31 **52** (내림세)
- 출처 = **Google Play 검색이 98~100%** (유료·직접은 하루 0~3)
- 나라 = 한국 **12~20%** · 나머지는 러시아·브라질·인도

⚠️ **여기서 결론을 내지 않았다** — 표본이 열흘이고, 9월 숫자를 보고 창업자와 같이 정하기로 했다.
