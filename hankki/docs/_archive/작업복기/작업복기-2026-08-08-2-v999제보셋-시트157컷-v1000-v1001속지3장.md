# 작업복기 2026-08-08 (두 번째) — v9.99 제보 셋 · 시트 157컷 완결 · v10.00 스티커 99컷 · v10.01 속지 3장

> 아침 07:29 「안녕」부터 낮 12시대 청소까지. **하루에 배포 세 판**(v9.99 · v10.00 · v10.01)이 나갔고
> 어제 받은 시트 14장이 «자르기 → 검수 → 판정 → 서랍 → 배포»까지 **끝까지** 갔다.

## 📱 아침 — v9.99 (창업자 v9.98 첫 사용 제보 3건, 그 자리에서)

제보 07:30~07:35 → 재현(옛 코드로 5건 걸림 확인 · 규칙 12) → 수정 → 15항목 통과 → hold 검수판 3장 → 「ㄱㄱ」 10:09 → run 1153 success.

1. **메모지·라벨 = 어디서 붙여도 글 상자** — 같은 그림이 데코 탭에선 «안 써지는 그림», 글자 탭에선 «글 상자»로 붙는 두 갈래였다(v9.88 「사진 두 길」과 같은 뿌리). `BOX_PAD` 있는 컷은 `addSticker`가 `addBox`로 보낸다(⛔프레임 `pf_`는 밑판이라 제외). ＋그림 글 상자에 「색」 갈래(글자색 15색).
2. **넘치는 칩 줄 막대를 «우리가 그린다»**(`HStrip`) — ⛔CSS론 불가: 안드로이드 크롬은 `::-webkit-scrollbar` 꾸밈을 통째로 무시하고, `scrollbar-width`를 병기하면 웹킷 꾸밈마저 꺼진다(크롬 121+). ⛔`.hscroll` 이름이 기존 클래스(styles.css 카드 가로줄)와 충돌 — **클래스 새로 짓기 전에 grep.**
3. **탭을 옮기면 종이 커서를 내려놓는다**(`dropCaret`) — 폰 뒤로가기는 키보드만 닫고 커서를 남겨서, 유령 「글씨·크기」 줄이 일꾸 서랍을 먹었다.

## ✂️ 시트 14장 → 낱개 157컷 완결 (task #86)

접두어 = `rs_t`맛평가12 · `rs_r`반응12 · `rs_c`사실화12 · `rs_q`귀요미12 · `rs_m`노랑12 · `rs_n`초록12 · `rs_h`장면15 · `rs_i`아이콘15 · `rs_p`미리12 · `rs_s`보관12 · `rs_g`그린12 · `rs_b`블루12 · `rs_x`스크랩7.

### 오늘 마저 잡은 함정 둘 (어제 함정 셋에 이어)

- **⑷ `--min 150`도 가는 획은 버린다** — 「기」의 ㅣ획(폭 4px ≈ 120px)이 걸러져 **「볶ㄱ」·「미리 만들어두ㄱ」·「유통 ㅣ한 임박」**이 됐다. 원본은 멀쩡했다(원본 대조로 «AI 오타 vs 자르기 소실»을 갈랐다). 사실화·미리만들기·반응평가·보관 네 시트를 **`--min 60`**으로 재컷. 귀요미는 글씨가 굵어 살아남았다 — **같은 파이프라인이 시트마다 다르게 죽는다.**
- **⑸ 4행 건지개 손잡이가 3행 캡션과 «같은 y대역»** — 조리법-사실화의 튀김 건지개 손잡이가 위로 길게 뻗어, 어떤 가로 경계도 캡션이나 손잡이 하나를 관통했다. → `rs_c10`만 **캡션 지운 밴드**로 따로 컷(손잡이 복원) ＋ `rs_c07`에 붙었던 손잡이 끝조각 제거.

### 검수

- 캡션 150컷 **전수 3배 판독**(검수 원칙 ⑥) — 컷마다 아래 30%를 모은 「캡션 스트립」 2판으로 한 번에 읽었다. **AI 오타 0**(「추섴」류 없음).
- 3단계 자동 검수 의심 0 · 「알파 0/255 경고 3컷」의 정체 = `rs_x01·05·07`(스크랩 색면 종이 — 가장자리가 원래 딱 떨어짐 · 정상).
- 「8요소」인데 낱개 7컷인 이유 = **파랑·초록 폴라로이드가 원본부터 겹쳐 그려져** 한 컷(가르면 가려진 모서리에 빈 자국).

## 🎯 창업자 판정 (아티팩트 선별안 → 버튼 질문)

- 변형쌍 4개: **조리법 귀요미 · 식사 노랑 도시락 · 건강 그린 · 상황 아이콘.** 탈락 벌(사실화·초록·사람손·블루) 51컷은 낱개 폴더에만 보존(팩 재료로 아껴둠).
- 「글자가 같이 붙어있는게 낫지?」 → **붙는 게 맞다** — 뜻이 글자에 있고(「덜 짜게」에서 글자를 떼면 간장 종지 그림), 기존 문구 스티커(말맛)와 역할이 안 겹친다(그림+정보).
- 스크랩종이 = **통짜 속지 확정**(*"요리사진모음기록용"*) → rs_x 낱개는 서랍에 안 올림.

## 🚀 v10.00 — 스티커 99컷 ＋ 공심채 (run 1158 · 71a2c69)

- 글자 탭에 8그룹: 맛 평가·반응 평가·조리법·요리 상황·식사 상황·미리 준비·보관·건강 태그. 실물 검수 `_shot-rsstickers.mjs`(라벨 8·99컷 깨짐 0·pageerror 0).
- **공심채 볶음 설탕 3/4큰술 → 3/4작은술**(창업자 제보 — 묵채와 같은 「작은술」이 맞다) · BASICS_VERSION 51(이미 깔린 폰 재동기화 · 규칙 18-ⓙ).
- 규칙 13 흐름 그대로: hold/레꾸상황-0808 → 검수판 3장 → 「검수끝나면 ㄱㄱ」 → ff-merge → 배포.

## 📄 v10.01 — 속지 3장 ＋ 쉬운말 문구 (run 1159 · 6cdcedd)

셋째 속지 「사진기록」이 낮에 도착(*"속지하나더있어"*)해 「속지 3장」이 다 모였다.

1. **사진 기록**(`dp_snap`) — 인쇄 점선 간격 2.5% ≠ 글줄 4.34% → **점선을 지우고 CSS 선으로**(도트·파랑 전례). 날짜 칸은 첫 캡처에서 삐져나와 fit 0.42로 조정(캡처가 잡았다).
2. **기록 3칸**(`dp_list3`) — 인쇄 줄 간격 4.3% = 글줄과 일치 → 그대로 앉힘(사진일기 문법). **사진칸이 셋** → `PaperSheet`의 photo가 **배열**이 됐다(write와 같은 문법 · 저장 키 photo/photo2/photo3 · 첫째가 'photo'라 옛 틀 호환). DiaryScreen이 «어느 칸을 눌렀는지»를 `photoKeyRef`로 잡는다 — 파일 고르기가 비동기라 안 잡으면 셋이 같은 사진을 비춘다.
3. **스크랩 사진첩**(`dp_scrap`) — **폴라로이드 창 셋이 기울어져 있다**(-9.4°·-10.4°·+5.8° — 창 내부 flood → 상단 변 «가운데 60%» 선형회귀로 실측. ⛔PCA는 정사각 창에서 주축이 불안정하고, 첫 잉크 라인은 둥근 모서리에 왜곡된다). `box`에 **`rot` 필드 신설** — 사진·제목이 같은 각도로 앉는다.
   - ⭐⭐ **틀 그림은 `mix-blend-mode: multiply`라 «흰 몸통이 사진을 못 가린다»** — 흰색은 곱하기에서 투명과 같다. 겹친 폴라(파랑 위 초록)에서 파랑 사진이 초록 몸통을 침범해 보였다 → 파랑 상자를 몸통 앞(right 74)에서 끊었다. 반대로 **창 테두리 «선»은 사진 위에도 계속 그려진다**(선은 어두워서) — 그래서 상자를 흰 영역이 아니라 **선까지** 채우는 게 정답이었다(틈 제거).
   - 체크리스트·태그는 그림 그대로(인쇄 줄 간격 3.3% ≠ 글줄 — 얹으면 어긋나고 지우면 정체성이 사라진다. 스티커·글자 넣기 자리).
4. **쉬운말 문구**(창업자 *"붙인걸 꾸미려면 이거 쉬운말로바꿔줘"*) — 「탭하면」→**「누르면」** · 「드래그로 이동」→**「끌어서 옮기고」** · 「크기/회전」→**「크기/돌리기」**(뒤집기·되돌리기와 같은 결).

## 🧹 청소

- **늘 실패하는 검사 수정** — `_shot-newpapers.mjs`가 제목을 `innerText`로 찾았는데 **제목은 input이라 innerText에 안 잡힌다**(3판 모두 ⛔를 찍으면서도 캡처는 통과라 무시하고 지나갔다). input `value`로 고쳐 3판 전부 ✅. 📌「실패할 줄 모르는 검사」의 쌍둥이 = **「늘 실패해서 아무도 안 믿는 검사」** — 어느 쪽이든 신호가 죽는다.
- **hold 브랜치 34개는 «지울 수 없었다»** — `git push --delete`가 **HTTP 403**(이 환경 git 자격이 브랜치 삭제를 차단 · 푸시는 됨). 규칙 18: 「안 된다」의 이유가 브랜치가 아니라 «내 권한»이다. 워크플로가 `hold/*`를 걸러 배포엔 무해 — 그대로 둔다.
- 옛 작업복기 1건 아카이브 이동(최근 3개 유지).

## 📌 배운 것 (내일의 나에게)

1. **같은 자르기 파이프라인이 시트마다 다르게 죽는다** — 글씨 굵기 하나로 살고 죽는다. 문턱(`--min`)은 시트의 «가장 가는 획»에 맞춘다.
2. **원본 대조가 「AI 오타 vs 자르기 소실」을 가른다** — 컷만 보면 못 가른다. 처방이 정반대다(제외 vs 재컷).
3. **multiply 층에선 흰색으로 아무것도 못 가린다** — 겹친 그림의 «가림»은 상자 좌표로 풀어야 한다.
4. **% 좌표계에서 회전 상자의 크기는 가로·세로 기준이 다르다** — 계산 검증은 픽셀로.
5. **캡처의 «어긋남»을 눈으로 판정하지 말고 색판 bbox를 재라** — 파랑판 하나만 깨끗하게 잡혔고, 그 하나가 「상자는 계산대로 앉았다」를 증명해 헛수정을 막았다.
