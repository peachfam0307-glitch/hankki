# 한끼 · 작업 복기 (2026-07-17, 두 번째 세션) — 출시 마무리 + 지식 체계화

> 오전 세션(`작업복기-2026-07-17.md`, 뒤로가기·코치마크·아이콘 확정 등)에 이어짐. 이 문서는 그날 저녁~밤 세션.
> 최신 할 일: `남은-할일-정리-2.md` · 전체 지식: `한끼-지식체계.md` · 오늘 삽질: `삽질-리스트-2026-07-17.md`.

---

## 1) 출시 마무리 4종 세트 — 전부 처리
1. **assetlinks 주소창 버그 해결 ✅** — `.well-known/assetlinks.json`에 Google Play 앱서명 지문(28:DF:DB…) 빠져서 설치앱에 주소창 떴음. 지문 2개 다 넣어 수정 → 폰에서 주소창 사라진 것 확인. 파일 편집이 대삽질(오답끝 한글브랜치+웹편집기 뻗음) → **PC github.dev(`.`키)** 로 해결. (상세 `assetlinks-고치는법.md`)
2. **스토어 등록정보 곰곰이 교체 ✅** — 앱 아이콘(icon-512-v6) + 그래픽(스토어-그래픽-곰돌이 1024×500) 업로드 → **게시 완료**. "저장 버튼 없어서" 헤맴 → 게시 개요의 "검토를 위해 변경사항 제출"로 저장되는 구조였음. (상세 `play-console-스토어이미지-교체법.md`)
3. **옛 짙은 "한끼" 홈 바로가기 정리 ✅** — 창업자 직접 완료.
4. **AAB 버전3 — 런처명 "한끼" ✅ 제출 완료** — PWABuilder로 새 AAB 생성:
   - Signing key = **Use mine**(기존 signing.keystore + 비번 3개) → version code **3** 으로 정상 인식.
   - App name·Short name = **투명글자 "한끼"**(제로폭공백, 3글자 통과). Package ID 동일.
   - Play Console 비공개 테스트 새 버전 업로드 → "전체 출시 시작"으로 **검토 중**.
   - ⚠️ **관리형 게시 ON** → 검토 끝나면 "게시 준비됨"으로 뜸 → **최종 `게시` 클릭 필요**(안 누르면 대기). ← 남은 유일 액션.
   - 투명글자 파일: `런처이름-한끼-복사용.txt`.

## 2) 장바구니 수정
- **요리맛샘 굴소스** 사러가기 → 네이버 기본값이던 걸 **쿠팡으로** 전환(`mall:'coupang'`).
- **초피액젓** 문구 "감칠맛이 깔끔해서" → **"감칠맛이 좋아"**.
- ⬜ **[결정 대기]** 네이버 기본값 **8개**(죽장연 된장·모에솔트 대파소금·위드잇 쯔유/치킨스톡/슬라이스햄·자연누리 훈제오리·보보리쿡시 보리면·자연다음 현미누룽지) 쿠팡 전환 여부 — 창업자 결정 대기.

## 3) 작업 명령어 시스템화
- 기존 `docs/작업-명령어.md`(말로 하는 지시어 12개)를 **진짜 `/슬래시 명령어` 13개**로 구현(`.claude/commands/`): doc저장·점검·청소·배포확인·복기·아카이브·릴리즈·마무리·문구점검·스샷·확정·리스트업 + 명령어(목록).

## 4) 전수조사 → 지식 체계화 (창업자 요청)
- 문서 26개 + 명령어 + CLAUDE.md **전수 정독**(병렬 정독 4팀 + 직접) → **`한끼-지식체계.md`** 마스터 인덱스 생성(일하는 방식·결정 프레임·상수·문서지도·현재상태·법무·명령어·불일치).
- **`삽질-리스트-2026-07-17.md`** — 오늘 삽질 12건(코드6·콘솔5·내 작업방식3) 증상→원인→해결→교훈 카탈로그.
- **문서 간 불일치 6건 정리**: 앱이름(surface 3종 구분)·version code(2아이콘/3런처)·아이콘(v6 canonical)·버전표기(APP_VERSION v5.1 canonical) = 4건 코드팩트로 확정 / 콘텐츠등급·설명이모지 = 2건 콘솔 확인 메모.

## 5) 새로 확정된 작업 규칙 (CLAUDE.md 반영)
- **말투 = 반말·한국어** (존댓말·영어 금지).
- **뭐든 작업/안내 전 "선 서치 → 검토 → 확실할 때만 말하기"** (추측 금지, 최우선).
- **"컴퓨터" 언급 시 즉시 컴퓨터 방식 전환** (폰 삽질 금지).
- **콘솔 안내 전 최신 메뉴 위치 확인** (2026: 성장→사용자 늘리기 등).
- **리스크 판단 → 전제조건 먼저 확인 → 순차 처리** (게이트 먼저, 한 단계씩).

## 6) 14일 비공개 테스트 규칙 정리 (검색 확인)
- **12명이 "연속 14일" 옵트인 유지** 필요. 중간에 1명 빠져 12 미만 되면 **스트릭 리셋**(다시 14일).
- **12명은 최소, 상한 아님** → 13~14명 미리 깔아 돌리면 1명 빠져도 **자동 유지**(여분이 처음부터 카운트됨). 딱 12명은 위험 → **버퍼 권장**.
- 빠진 뒤 새로 넣은 사람은 그날부터 14일 새로 시작 → **구멍 소급 불가**.
- 실제 옵트인/설치 수: 콘솔 `테스트 및 출시 → 비공개 테스트 → 테스터 → 테스터 보고서` 또는 `통계`(며칠 지연). Play Console이 깔끔히 안 보여줘서 **테스터 직접 확인이 제일 정확**. (등록 21명 / 목표 상시 13~14명)

---

## 남은 것 (다음 세션)
- 🔴 **AAB 버전3 검토 통과 후 최종 "게시" 클릭** (관리형 게시 — 안 누르면 대기). 몇 시간~하루 뒤 확인.
- 🟡 테스터 상시 **13~14명 버퍼** 만들기(대기자 1~2명 더 설치).
- ⬜ 장바구니 네이버 8개 쿠팡 전환 여부(창업자 결정).
- ⬜ 콘텐츠 등급(전체 이용가 유력)·자세한 설명 이모지판 — 콘솔에서 5초 확인.
- 장바구니 문구 검토 계속 / 큐레이션 리스트 채우기.
