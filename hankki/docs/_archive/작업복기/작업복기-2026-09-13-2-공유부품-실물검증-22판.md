# 작업복기 2026-09-13(2) — 제출 취소 → iOS Share Extension 완성 → 실물 다섯 갈래 검증 → 22판 (아이폰 갈래)
> 세션 = claude/annyeong-yoi12y · 09-13 19:1x ~ 09-14 01:2x KST · 앞판 = `docs/_archive/작업복기/작업복기-2026-09-13-아이폰-배포와-심사제출.md`

## 왜
창업자 *"이걸 한번도 안해보고 출시심사를 받다니.."* — 15:50 제출한 빌드 13 은 아이폰에 «공유 시트 가져오기»가 없었다. 제출 취소(19:1x) 후 *"이거 부품까지 다 넣어서 승인받자"* · *"제대로 설계하고 직접 다각도로 검수하고 시뮬돌려본다. 땜빵금지."*

## 한 것 (실물 근거 있음)
1. **iOS Share Extension** (`ios-app/ios/App/ShareExtension/`) — 사진≤10·URL·글 받음 → App Group `group.kr.hankki.app` 컨테이너 `inbox/<ts>-<uuid>/` 에 사진 사본＋meta.json. 앱 쪽 `ShareIntakePlugin.swift`(consume · `hankki://share` 알림) · `HankkiViewController.capacitorDidLoad` 에서 수동 등록(Capacitor 6+ 자동 등록 없음). 앱은 시작·visibilitychange·URL 셋에서 받아 임시보관함에 담는다(`src/iosShare.js` · App.jsx `공유담기`).
   - xcodeproj gem 으로 타깃·Embed·리소스 추가(`ios-app/scripts/add-share-extension*.rb`) · 공유 스킴 커밋 · match 두 ID · 워크플로 서명 검사에 appex(팀·App Group 양쪽·버전 일치 ITMS-90473·활성 규칙) 추가.
   - 애플 규칙 = 공유 부품은 호스트 앱을 못 연다 → 17판 「앱 깨우기」 해킹이 죽음 → 제거(18판). 「한끼에 담았어요」 1.5초 후 시트 닫힘.
   - 같은 사진 여러 번 공유 = iOS 가 바이트를 다시 내보내 열쇠 4개 씀 → 16×16 회색 지문 중복 제거(19판).
   - 부품 화면 = 검정 배경 글씨만 → 크림 카드＋꼬르곰(엄지)＋큰 글씨(22판, 창업자 01:01).
2. **포털** — 창업자가 App Group 만들고 `kr.hankki.app`·`kr.hankki.app.share` 둘 다에 할당(16판 실패 원인 = share ID 에 Configure 안 함 → 17판 ok).
3. **워커 둘 반영**(창업자 손으로 붙여넣기) — `hankki-ocr`(ALLOWED_ORIGINS 에 `capacitor://localhost`) · `hankki-tidy`(글 모델 먼저 → 비전 예비 · 앱 세션이 고침 v13.33). 「굴소스 안 됐어 근데 열쇠가 안 줄어」= 아이폰 원점 거절이 뿌리였다.
4. **파서·다듬기** — 가지덮밥(*"너무심한데"*) · 차돌 짬뽕(유튜브 붙여넣기) 실물 재현으로 `parseRecipe.js` 번호재료·꽉찬끊김·UNIT_DEF_LINE·머리 꼬리괄호 · `polish.js` 한다체요떼기(썬다요→썰어요) · `EditorScreen` 다듬기 끝 카드. 재현 셋(`_repro-아이폰안내·가지파서·짬뽕붙여넣기-0913`) smoke:seq 에. ⛔ 배포 갈래 합침은 「배포해」 때만.
5. **아이폰 안내·스토어 스샷** — 가져오기 카드 2장(캡처 / 사진 앱) = 공유 흐름 하나로, 초록 줄 = 결과만 · 스토어 스샷 01(아이폰 6.9"·아이패드 13") 공유 흐름으로 다시 굽기(`design/promo/아이폰스샷-2509/`). 창업자 01:07 **「다 그대로 ㄱㄱ」** 확정.
6. **딸 폰 실물 다섯 갈래 ✅** — ①캡처→공유 시트→한끼(영상) ②사진 앱→공유 ③앱 안 OCR＋AI(Vision 정확) ④글 붙여넣기(유튜브) ⑤Apple 로그인. 버리기·다듬기 끝말·임시보관함 1건·열쇠 셈까지.
7. **판 이력** = 16(실패·프로필) 17(성공·앱 깨우기 죽음) 18 19 20 21 22 전부 success. **심사 빌드 = 22**(13.33(22) · 서명 실측 ✅).

## 배운 것 / 삽질
- 공유 부품은 호스트를 못 연다 — 「담아두고 앱이 가져간다」가 유일한 길. 17판 하루치 삽질.
- 인스타는 글 복사가 안 된다(창업자) → 붙여넣기 시험은 유튜브로.
- 창업자가 안내를 못 알아들으면 안내가 틀린 것(「또는 아래 가져오기가 뭐야?」·「3번도 왜」) → 흐름 하나로.
- 세션 둘이 같은 워커를 만지면 «저장소 = 서버» 를 지켜야 한다(worker.js·worker-tidy.js 커밋).

## 내일(9/14 아침) — 창업자 「낼 아침에 다시하자」
① run 22 success 확인(끝남 ✅) ② 딸 폰 TestFlight 22 → 사진 공유 한 번 → 크림 카드 「한끼에 담았어요」 눈 확인 ③ 콘솔: 6.9형·13형 스샷 1번 교체 → 빌드 22 선택 → 저장 → 심사에 추가 → 제출.
