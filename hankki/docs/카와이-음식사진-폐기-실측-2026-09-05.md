# 🍜 음식사진 카와이컷 «폐기» — 지우기 «전» 실측 (2026-09-05 08:25)

> ⭐ **현행이다.** 카와이 폐기는 이 문서부터 읽는다.
> 📮 창업자 = *"우리 레시피꾸미기에 들어가는 음식사진 카와이컷도 모두 폐기 삭제."*
>    → 범위 좁힘 = *"그냥 카와이컷이 아니라 **음식사진카와이컷만**이야. **레꾸에 들어가는**"*
> ⛔ **아직 «아무것도 안 지웠다».** 이 문서는 지우기 전에 잰 값이다.

## 1️⃣ 🔢 실측 (2026-09-05 · 전부 이 세션에서 직접 셌다)

| 무엇 | 값 |
|---|---|
| 레꾸 음식사진 폴더 `src/assets/stickers/photo/` | **2,392개 · 443 MB** |
| 카와이 목록 `store.jsx` **529~583줄** `카와이_V96` | **324개** (고유 324) |
| 그중 `photo/` 에 **실제로 있는 파일** | **323개** |
| ⭐ **git 이 추적하나** | ✅ **2,392개 전부 추적** → **지워도 되살릴 수 있다** |
| 접두어 분포(폴더 전체) | fe 528 · gr 348 · mn 92 · ps 43 · sd 37 · kt 33 · dp 31 · dl 25 · ni 24 · hp 20 |

## 2️⃣ ⚠️⚠️ 옛 문서와 «갈렸다» — 실측이 맞다

`docs/카와이-아직남은것-2026-09-03.md` 는 이렇게 적혀 있다:
> 「store.jsx 카와이_V96 목록 = **650개**」 · 「FoodIcon 이 제목으로 자동 배정 = **153개** ⛔ 살아 있다」

🔢 **2026-09-05 실측**
- `카와이_V96` = **324개** (650 아님)
- `FoodIcon.jsx` 가 쓰는 id **424개 중 카와이 = 0개** ⭐ **자동 배정에 카와이가 «없다»**

📌 ＝ **9/3 이후에 자동 배정 쪽은 이미 갈린 것으로 보인다.**
⚠️ ⛔ 다만 **내 판정 방식을 의심해야 한다**(절대원칙 18) — 정규식으로 세었다.
   👉 **지우기 전에 앱과 같은 방식(모듈을 실제로 import)으로 한 번 더 셀 것.**

## 3️⃣ ⛔ 지우는 «순서»가 중요하다 — 잘못하면 더 나빠진다

| 하는 것 | 안 하면 |
|---|---|
| ① `photo/` 에서 카와이 323장 삭제 | — |
| ② `store.jsx` 의 `카와이_V96` 목록 정리 | 없는 파일을 가리키는 목록이 남는다 |
| ③ 어디서든 그 id 를 쓰는 곳 정리 | **빈 칸이 뜬다** — 「안 뜨는 것」보다 나쁘다 |

🔎 그 id 를 쓰는 파일(실측) = `src/store.jsx` · `src/screens/ProfileScreen.jsx`
⭐⭐ **①만 하고 ②③을 안 하면 «파일 없는 그림 자리»가 생긴다.** 절대원칙 34 — 실패의 «모양»이 나빠진다.
⛔ **이미 유저 폰에 붙은 것**은 v12.10 에서 갈린 것으로 문서에 적혀 있다(전수 판정 431장) — ⚠️**이 세션에서 재확인 안 함.**

## 4️⃣ ▶️ 다음 (⛔창업자 판정 대기)

1. ⭐ **323장을 «눈으로» 보여준다** — 컨택트시트 한 장(규칙 21·13). *"이거 맞아?"* 를 받고 지운다
2. 그 다음 ①②③ 을 **한 번에** 한다 (되돌리기는 git 에 있다)
3. 🔢 곁다리 이득 = **443MB 중 카와이 몫만큼 저장소가 줄어든다**(지금 디스크가 99% 찼다)

## 5️⃣ 🎬 같은 시각 — 창업자 영상은 «저장했다»

📁 `design/promo/인스타-2509/창업자-원본-2026-09-05.mp4` (**25,389,387 B**)
⭐ 9/4 에 잃은 그 사고를 되풀이하지 않으려고 **받자마자 저장소에 넣었다.**
⛔⛔ **다만 이 환경에서는 mp4 를 «못 연다»** — 있는 ffmpeg 가 playwright 빌드라
   `--disable-everything` 로 코덱이 거의 다 꺼져 있다(`Invalid data found when processing input`).
   ＝ **「뾰미 컷만 떼기」를 여기서 할 수 없다.** ⏳다른 길을 찾아야 한다.
