# 📔 2026-08-19 복기 ② — 아이콘 겹침 정리 · 편집 칸 순서 · 배포 되살리기

> ⭐현행 · 이 날 «후반»의 기록은 여기부터 읽는다. (전반 = `작업복기-2026-08-19-알보조사-내레시피14편-일기백업.md`)
> 앱 **v11.11 → v11.15** · 배포 **run #1552 success** · 오늘 커밋 **41개**
> 픽커 음식 그림 **466 → 391컷** · `BASICS_VERSION` **83 → 84** · 레시피 145편

---

## 1️⃣ ⛔⛔ 오늘 제일 큰 것 — **내가 「이미 있는 것」을 또 넣었고, 창업자가 눈으로 잡았다**

📮 창업자 = *"음식아이콘 전수검사해서 **같은이름 2번올라간것**(소스빼고) 정리하자.
   **무스비, 유부초밥…2개씩 있는거봤어. 카레라이스는 3개야.** 겹치는게 꽤많은데."*

🔢 파보니 창업자 말보다 훨씬 컸다:

| | |
|---|---:|
| 아침에 내가 넣은 컷 | 60 (픽커엔 57) |
| 그중 **파일 해시까지 같은 판박이** | **57** |
| **진짜 새 것** | **0** |

⛔⛔ **8/12 시트를 이미 앱에 넣어놨는데 8/19 아침에 «또» 넣었다.**
   「두부조림」·「지코바치킨」·소스 모둠 6개까지 전부 `fe_204`~`fe_260` 에 있었다.

### 왜 그랬나 — 뿌리 둘

⑴ **로드맵의 「새 음식 아이콘 54컷 판정 대기」 줄만 보고 넣었다.**
   `decided.mjs`(이미 «한» 일 찾기)를 **안 돌렸다.** 규칙 17이 정확히 이걸 막으라고 만든 도구다.

⑵ **`check-foodtab` 이 못 잡았다** — ①「한 컷 = 한 집」은 «같은 키»를 본다.
   **키가 다르면 그림이 완전히 같아도 통과**한다(규칙 18 ⓘ).

📌 그날 아침 나는 **「그림 ↔ 이름표」 대조를 60컷 다 눈으로 했다.** 그건 맞았다.
   ⭐ 그런데 **「이미 있는지」는 아무도 안 봤다.** 검사가 서로 다른 것을 본다.

---

## 2️⃣ 🔒 그래서 장치를 만들었다 — `check-foodtab` ⑥ 「판박이 그림」

· **파일 내용(sha1)** 으로 본다 — 이름표도 키도 못 속인다
· 고를 게 없는 잘못이라 **예외 목록을 두지 않는다**
· 「같은 이름·다른 그림」은 **경고만** — 어느 쪽이 예쁜지는 창업자가 정한다(규칙 11)

🧪 규칙 12 = 중복을 도로 심으면 **rc=1** · 빼면 rc=0
⭐⭐ **만들자마자 내가 놓친 15쌍을 더 잡았다** — 3개짜리에서 «한 쌍만» 같은 경우를
   내 계산(`max(diff)`)이 「다르다」로 넘겼다. **게이트가 사람보다 정확했다.**

---

## 3️⃣ ✅ 창업자 판정 — 「같은 이름·다른 그림」 18개

📮 창업자가 넷을 콕 집었다 → 계란말이 `fe_259` · 계란찜 `fe_260` · 깍두기 `fe_239` · 나박물김치 `fe_113`
   **넷 다 「원본이 큰 쪽」 추천과 같았다** → 나머지 14개도 같은 기준으로 → *"ㄱㄱ"*

⭐ 기준 = **앱은 그림을 238px 로 줄여 그린다. 원본이 클수록 안 뭉개진다**(검수 절대원칙 ③).

⛔ **눈으로 보다 하나 잡았다** — `fh_k15` 는 이름표가 「소고기구이」인데 그림은 **생고기 슬라이스**였다.

⛓ **레시피 icon 도 같이 갈아끼웠다** — 떡볶이 `fh_k27`→`fe_282` · 전복죽 `fe_281`→`fe_132`
   ⛔ 박힌 icon 이 `ICON_RULES` 를 덮으므로 안 고치면 내린 컷을 계속 부른다(v10.76 사고).
   → `BASICS_VERSION` **83 → 84**

⛔ **파일은 하나도 안 지웠다** — 그 키로 저장한 레시피가 깨진다. 픽커·규칙에서만 내렸다.

🔢 **466 → 409(판박이 57) → 391(겹친 이름 18)**

---

## 4️⃣ 📋 편집 화면 칸 순서 — 「맨 아래라 안 보인다」

📮 창업자 8/16 = *"자동으로 한식 양식 종류 저장해주면 좋겠어.
   솔직히 **맨날 까먹어 맨 아래 있어서 거기까지 잘 안보게돼**.."*
📮 8/19 = *"유저가 직접입력하게끔 **칸만 잘보이게** 두면좋겠어(**2인분 시간도 위로 올리고**)"*
   → *"캡쳐사진이 제일 위에있고 그 아래부터 제목 인분 재료 만드는법이쭉"* → *"**썸네일 아래 캡쳐 제목순**"*

### ⭐⭐ 창업자가 «자동 분류»를 스스로 접었고 그게 옳았다

📮 *"이것도 **충돌안하게** 잘짜야해 지금 어느정도 분류가 되어있다면서"*

🔢 실측이 창업자 걱정을 뒷받침했다 —
| | 위험 |
|---|---|
| 「구체어 먼저」 위반 **65곳** | 자동으로 채우면 엉뚱한 값이 «유저 레시피»에 박힌다 |
| `category` 실측값 **열 가지** (한식99·양식17·일식6·국탕6·간식5·아시안3·밥3·반찬3·중식1·전골1) | 고를 수 있는 칩은 **다섯**뿐 → **「고를 수 없는 값」**이 들어갈 수 있었다 |

📌 손으로 고르게 두면 이 위험이 **통째로 사라진다.** ⛔재론 금지.

### 바뀐 순서 — 실측 (390×844 · 스크롤 0)

| 칸 | 자리 |
|---|---:|
| 썸네일 | 60px |
| **캡처 사진으로 채우기** | **225px** |
| 제목 | 457px |
| **카테고리** | **546px** |
| **조리시간·인분** | **623px** |
| 재료 | 720px |

**전부 첫 화면에 들어왔다.** 전엔 재료(7줄)·만드는 법(7줄) «아래»였다.

⚠️ 옛 주석은 *"캡처는 제목+재료+만드는 법이 붙어 있어 … 아래로 뺐다"* 였다 — **일부러였고 이유도 있었다.**
⭐ 그런데 **쓰는 사람이 「거기까지 안 가게 된다」고 했다. 의도가 좋아도 결과가 그러면 진 것이다.**

---

## 5️⃣ ⛔ 창업자가 잡아준 것 — 「읽는 중」 표시가 떨어져 나갔다

📮 *"로딩느려질때 기다리라는 안내넣기로 했잖아. **2장이면 한장씩 읽으니까. 아무안내없이 기다리다 끌수도 있어**"*

⛔ **캡처 버튼만 위로 올리고 진행 표시는 제자리(63줄 아래)** 라 **누르고도 화면 위쪽이 조용했다.**
   → 진행 표시도 캡처 버튼 바로 아래로 옮겼다.

⭐⭐ **안내는 세 겹이고 다 살아 있다** (실물로 눈 확인)
| 언제 | 무엇 |
|---|---|
| 고른 직후 | 「사진 2장이라 AI 스캔 2장을 써요」 |
| 자르는 중 | 「2장 중 1장째 읽는 중이에요 · **자르는 동안 같이 읽고 있어요**」 ＋ 막대 |
| 자르기 끝 | 「글자 읽는 중… · **이 화면 그대로 두면 돼요**」 ＋ 꼬르곰 통통 |

⭐ **첫 장을 읽는 «동안» 둘째 장을 자르게 하는 설계** — 그래서 기다림이 안 느껴진다.
   내가 만든 게 아니라 **이미 그렇게 되어 있었다.** 좋은 설계였다.

🔒 재현판 = 캡처 흐름 **14칸 · 4회 연속 14/14** (창업자 *"여러번 검증 재현해봐"*)
   ＋ 부가정보 자리 판(소스 순서로 잰다 · 규칙 12 = 되돌리면 exit 1)

---

## 6️⃣ 🚨 배포가 «두 번» 멎었다 — 우리 잘못이 아니었다

`run #1537` = **33분** 멎다가 취소 · `#1538` = 15분. 그 앞 `#1536` 은 같은 단계를 **19초**에 끝냈다.

📄 **로그가 답했다** — `Get:5 … noble-security InRelease` 뒤로 **33분간 백지.**
   `--with-deps` 가 부르는 `apt-get` 이 **우분투 패키지 서버에서 멎었다**
   (`azure.archive.ubuntu.com` 이 전부 `Ign:` 로 무시되고 폴백 서버에서 멈췄다).

⭐ **우리 코드와 무관하다는 증거** = `#1537` 은 «문서만» 고친 커밋인데도 같은 자리에서 멎었다.

✅ 창업자 지시 = *"시간제한이랑 폴백넣어"* →
| 고침 | 왜 |
|---|---|
| `timeout-minutes: 10` | 없으면 job 기본값 **6시간**까지 매달린다 |
| 셸 안 `timeout 300` ＋ 폴백 | apt 만 잘라내고 **브라우저는 따로 받는다** |

⭐ `--with-deps` 만 apt 를 쓴다 — 브라우저는 playwright CDN 이라 apt 와 무관하다.
⛔ **`timeout-minutes` 만으론 폴백이 안 돈다**(단계째로 죽는다) — 둘 다 필요하다.
🧪 규칙 12 = 앞이 멎으면 폴백이 돈다(rc=0) · 폴백까지 실패하면 죽는다(rc=124) = **안전한 쪽으로 넘어진다**
⭐⭐ **넣자마자 값을 했다** — 다음 배포에서 정확히 5분 5초에 잘리고 폴백으로 통과했다.

---

## 7️⃣ 🕳 되감김이 «두 번» — 두 번째는 조용히 지나갈 뻔했다

### ⑴ 첫 번째 (16:37) — 다른 세션 푸시에 워킹트리가 덮였다
`hold/자동회수` 에 *"v11.13 · 안 커밋된 변경 7개"* 로 담겨 있어 그대로 되살렸다.

### ⑵ ⛔⛔ 두 번째 — **커밋 메시지만 새것이고 내용은 옛것으로 나갔다**
커밋 `ed35960b` 은 「편집 화면 순서 (v11.15)」라고 적혀 있는데 **안에는 옛 순서**가 들어 있었다.
커밋 직전에 되감김이 나서 트리가 v11.14 로 돌아갔고 **재현판 파일 하나만** 들어갔다.

📌📌 **「고쳤다」고 기록됐는데 실제론 안 고쳐진 것 — 제일 나쁜 조합이다.**
⭐ `git show <sha>:<파일>` 로 확인해서 잡았고 `hold/자동회수` 에서 되살려 `555cefcb` 로 다시 올렸다.

📌 **한 줄 = 「커밋했다」와 「내용이 들어갔다」는 다른 말이다.**

---

## 8️⃣ ⛔ 오늘 내가 틀린 것

| | 무엇 | 어떻게 드러났나 |
|---|---|---|
| ⑴ | **이미 있는 컷 60개를 또 넣었다** | 창업자 *"무스비, 유부초밥…2개씩 있는거봤어"* — `decided.mjs` 를 안 돌렸다 |
| ⑵ | 같은 일에 **번호를 두 번 다르게** 붙였다(③ ↔ ⓑ) | 창업자 *"2번이야 네가 답변하면서 기호가 바뀌었어"* |
| ⑶ | 판박이 계산에서 **3개짜리 한 쌍**을 놓쳤다 | 새 게이트가 15쌍을 더 잡았다 |
| ⑷ | 캡처 버튼만 옮기고 **「읽는 중」을 안 옮겼다** | 창업자 *"아무안내없이 기다리다 끌수도 있어"* |
| ⑸ | 재현판이 `input[type=file]` **첫째**를 잡았다(썸네일용) | 「안 열린다」로 잘못 나왔다 — 파일 칸이 «둘»이다 |
| ⑹ | `/hankki/` 로 열어 「200 인데 화면이 비었다」로 헤맸다 | `vite preview` 는 **루트**로 서브한다 |
| ⑺ | 저장 검사 앞에서 **자르기 시트를 안 닫아** 타임아웃 | 앱이 아니라 «판»의 문제였다 |

⭐ ⑸⑹⑺ 은 전부 **규칙 18** — 「없다」 전에 «확인 방식»을 의심한다.
⭐ ⑴ 이 제일 크다 — **도구가 있는데 안 썼다.**

---

## 9️⃣ 📦 오늘 나간 것

| 버전 | 무엇 |
|---|---|
| v11.12 | 아이콘 60컷 (⛔전부 이미 있던 것) |
| v11.13 | 판박이 57컷 내림 (466 → 409) |
| v11.14 | 겹친 이름 18컷 판정 반영 (409 → **391**) ＋ `BASICS_VERSION` 84 |
| **v11.15** | **편집 화면 칸 순서** — 썸네일 → 캡처 → 제목 → 카테고리 → 시간·인분 → 재료 |

🔒 **새 게이트 셋** = 판박이 그림 · 부가정보 자리 · 캡처 흐름 14칸
🛠 새 도구 = `_넣기-아이콘60-0819`(한 번 쓰고 마는 판)
🔖 판 = 겹친 그림 고르기 — https://claude.ai/code/artifact/dfa9f6c9-f861-455a-b722-0b97a5563bf7

---

## 🔟 ⏳ 남은 것

### 🔴 기다리는 것 — 하나뿐
**프로덕션 재심사 결과** (8/16 저녁 제출 · **오늘 3일째**). 8/02 때는 이틀이었다. 우리가 할 게 없다.

### 🟢 날짜가 정한 일
· **8/23**(D-4) = 8/24 레시피 5편 전날 확인 — ✅이미 검수 끝나 훑기만
· **8/31**(D-12) ⭐ = **9/1 자동 공개 78컷 전날 검수** — ⛔아직 · 창업자 몫
· 11/02~12/07 레시피 **17편** 검수 안 받음 (제일 가까운 게 D-75)

### 🟠 내가 맡은 것
· ✍️ **3차 판(반찬·고기 11편)** — 아직 안 만들었다 (`_판-내레시피쓰기-0818.mjs 3`)
· 🅱 **37편(1~2걸음) 채워서 검수판** — 아직
· 🍳 `fe_317` 팽이버섯 베이컨전 레시피(11/16 자리 · 창업자가 쓴다)

### 🔵 승인 뒤 바로 필요한데 지금 만들 수 있는 것
Worker `acknowledge`(⚠️없으면 꾸미기 팩 산 사람이 3일 뒤 팩을 잃는다) · 잔량 D1 ·
계정 삭제 페이지 · `navigator.storage.persist()` — **바깥 갈래(다른 세션)가 파고 있다**

### 🌏 창업자 물음 — 영어 버전 (조사 끝 · 판단 대기)
🔢 앱 한글 **96,856자**(레시피 145편이 69%) · 장바구니 42개 제품은 쿠팡·한살림·오아시스 ·
제철 44주는 한국 기준 · 「제목 치면 그림 붙기」 460줄이 한국어 낱말
⭐ **글자 번역은 되는데 앱 값어치의 절반이 「한국에서 장 보는 사람」용**이다.
⛔ 추천 = **지금은 안 한다** — 출시가 먼저다. 급하지 않고, 나중에 못 하는 것도 아니다.
