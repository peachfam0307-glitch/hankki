# 🔗 징검다리 계측 — 2026-09-12 (⭐현행)

> 인스타 프로필 → 스토어 구간이 여태 «깜깜»했다. 오늘 열었다.

## 📮 창업자 말
- *"이게 되는지 알면 인스타로 계속 사람몇명왔다 셀때 나한테 말했어야지"* — **맞는 지적이다. 내가 늦었다.**
- *"나는 이런게 되는지도 모르잖아"* — 알려주는 게 내 일이었다.

## 1️⃣ 여태 깜깜하던 자리
```
광고 → 프로필(콘솔이 셈 83) → [깜깜] → 징검다리 → [깜깜] → 스토어 → 설치(콘솔이 셈 15)
```

## 2️⃣ ✅ 심은 것 둘 (v13.21)
| 이름 | 언제 |
|---|---|
| `bridge` | 징검다리 페이지에 **왔다** |
| `bridge_go` | 스토어로 **갔다** (단추를 눌렀든 자동으로 넘어갔든 · **중복 안 셈**) |

🏷️ ＋ Play 링크에 **referrer 꼬리표** = `utm_source=hankki_bridge&utm_medium=instagram&utm_campaign=profile_link`
   형식은 구글 공식 = `&referrer=` 안에 utm_* 를 URL 인코딩 (https://support.google.com/analytics/answer/10917952)
   → 설치까지는 **Play 콘솔 「획득 보고서」가 센다.** 우리가 못 잇는 구간을 구글이 잇는다.

## 3️⃣ ⛔⛔ 제일 위험했던 자리 — gtag 가 붙기 «전»에 튕기면 계측이 통째로 날아간다
dataLayer 에 쌓아둔 것은 gtag.js 가 내려와야 밖으로 나간다. 1.2초 타이머가 그 전에 터지면 **두 건 다 증발.**
✅ 이동 타이머를 **gtag onload/onerror 뒤에** 시작 (상한 3초 — 계측이 스토어 가는 길을 막지 않는다)
📌 재현판 첫 판이 실제로 **0건**을 냈다. 그래서 잡았다.

## 4️⃣ ✅✅ 실물 확인 (2026-09-12 19:49 KST · 창업자 폰 · 인스타 인앱 브라우저)
GA4 실시간 「페이지 제목 및 화면 이름」 —
```
bridge      1
bridge_go   1
```
⭐⭐ **인스타 인앱 브라우저가 gtag 를 «안 막는다».** 제일 컸던 걱정이 실물로 죽었다.
⭐ 덤으로 v13.20 계측도 같이 찍혔다 = `first_open` · `home` · `import_photo` · `profile`

## 5️⃣ ⚠️ 아직 모르는 것
- **꼬리표가 Play 콘솔에 어떤 이름으로 잡히나** — `hankki_bridge` 로 뜰지 「기타」로 뭉개질지 [모름].
  👉 며칠 뒤 「사용자 확보 → 스토어 등록정보 획득」 캡처를 받아 확인한다.
- **`bridge_go` 중 실제 설치는 몇인가** — 사람 단위로 못 잇는다. 꼬리표로 콘솔에서 따로 본다.

## 6️⃣ ⚠️ 숫자를 읽을 때
- **측정 ID 가 앱과 같다**(G-L20V2VE1CS) → GA4 「사용자 수」에 **안 깐 사람도 섞인다.** `page_title` 로 갈라 본다.
- **창업자가 눌러도 세진다.** 인스타 인앱 브라우저는 주소를 못 쳐서 `?me=1` 같은 스위치가 **쓸 수가 없다**
  (창업자가 짚었다). → **누른 시각을 받아 내가 빼고 읽는다.** ⛔못 쓰는 스위치를 만들지 않는다(절대원칙 34).
- 📌 **창업자 테스트 = 2026-09-12 19:49쯤 1건**

## 7️⃣ 🧪 재현판 = `scripts/_repro-징검다리-0912.mjs` (7칸 · 스모크에 들어갔다)
⛔ 만들며 밟은 함정 = **스토어로 넘어가면 window 가 새로 나서 dataLayer 가 통째로 사라진다.**
   이동을 막아도 오류 페이지로 갈아타며 똑같이 사라진다.
   ✅ → dataLayer 를 나중에 읽지 않고, **담길 때마다 console 로 흘려보내** 테스트 프로세스에 쌓는다.
