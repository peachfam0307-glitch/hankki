# 🍽 빈 접시 — 뿌리와 «구조» 고침 (2026-09-15)

> 📮 창업자 = *"자꾸 빈접시나가는거 **우리 어제 다 잡은거 아니었어? 뿌리부터 찾아내. 근본원인을.**"*
> ⭐ 맞는 지적이다. **어제 고쳤다고 한 그 자리에서 하루 만에 또 났다.**

## 1. 무슨 일이 났나 — 이틀에 «두 번»

| 언제 | 무엇 | 결과 |
|---|---|---|
| **2026-09-13 15:21 · v13.35** (`97d471c0`) | `n3009`~`n3022` **열넷**이 `PHOTO_RATIO` 에 없었다 | 창업자 실물 제보 = *"음식아이콘안올라갔어"* |
| **2026-09-14 · v13.42** (`cd12e5d4`) | `n3030`~`n3033` **넷**이 «또» 없었다 | 그중 둘이 **이미 열린 편**이라 유저 폰에 빈 접시 |
| **2026-09-15 08:1x** | 브리또 두 편을 넣다가 발견 | 창업자 = *"아이콘이 없지않나..."* |

🔢 **이미 열려 있던 둘** (`from` 이 「처음부터」)
- **간단 우삼겹 갈비탕** (`n3031`)
- **충무김밥 오징어무침** (`n3032`)

＋ 아직 안 열린 둘 = 삼겹살 꽈리고추(11/02) · 갈비탕(12/14)

## 2. 🌲 뿌리 — 「값을 채웠고, 구조는 안 고쳤다」

### ⛔ v13.35 가 한 일 = **빠진 값 채우기**

그 커밋 메시지에 이렇게 적혀 있다:

> *"확인 = 음식 이름표가 쓰는 그림 중 표에 없는 것 **0개**"*

**그때 한 번 손으로 센 것**이다. 🔢 `git show 97d471c0 --name-only | grep -c "check-"` → **0**
— **검사 파일을 하나도 안 만들었다.** 주석만 달았다(그 주석은 지금도 `Stickers.jsx` 에 있다).

📌 그래서 다음 날 `n3030`~`n3033` 을 넣을 때 **아무도 안 막았다.**

### ⛔ 더 깊은 뿌리 = 「손으로 세 곳에 적어야 한다」

컷 하나를 넣으려면:

| | 어디 | 빼먹으면 |
|---|---|---|
| ① | `src/assets/stickers/photo/<키>.png` | 그림이 없다(당연히 티가 난다) |
| ② | `FoodIcon.jsx` 의 `FOOD_NAMES` | 이름으로 못 찾는다 |
| ③ | **`Stickers.jsx` 의 `PHOTO_RATIO`** | ⛔ **그림 주소가 통째로 사라진다 → 빈 접시** |

⭐ **③만 성질이 다르다.** ①②는 빼먹으면 «안 붙는» 정도인데,
③은 **①②를 다 해놔도 화면이 빈 접시**가 된다. 화면이 이 표를 «돌면서» 그림 목록을 만들었기 때문이다.

```js
// 옛 구조 — 표가 «주인»이었다
for (const key of Object.keys(PHOTO_RATIO)) {
  PHOTO_FAMILY[key] = { src: PHOTO_URLS[`../assets/stickers/photo/${key}.png`], ratio: PHOTO_RATIO[key] }
}
```

📌 **비율(모양 정보)을 안 적었다는 이유로 그림(존재)까지 잃는 구조**였다. 두 가지가 한 값에 묶여 있었다.

## 3. ✅ 고침 — 두 겹

### 1차 · 구조 (⭐이게 본체다 · 절대원칙 34)

**목록의 주인을 «손으로 적는 표»에서 «폴더에 있는 파일»로 옮겼다.**

```js
const 기본비율 = 1.25
export const PHOTO_FAMILY = {}
for (const 경로 of Object.keys(PHOTO_URLS)) {          // ← 폴더가 주인
  const key = 경로.slice(경로.lastIndexOf('/') + 1, -4)
  PHOTO_FAMILY[key] = { src: PHOTO_URLS[경로], ratio: PHOTO_RATIO[key] || 기본비율 }
}
```

⭐⭐ **실패의 «모양»이 바뀌었다** — 절대원칙 34 가 말하는 바로 그것이다.

| | 옛 구조 | 새 구조 |
|---|---|---|
| 비율을 빼먹으면 | ⛔ **그림이 아예 안 뜬다**(빈 접시) | ⚠️ 그림은 뜬다 · **비율만 조금 어긋난다** |
| 유저가 보는 것 | 「앱이 고장났네」 | 거의 눈치 못 챈다 |

⛔ **「값을 더 채운다」로 끝내지 않았다** — 그게 v13.35 가 한 일이고, 하루 만에 다시 났다.

### 2차 · 게이트 (그물)

- **`scripts/check-photoratio.mjs`** (스모크 등록) — 레시피가 쓰는 사진 키가 표에 없으면 **배포가 죽는다**
  · ⛔ 「레시피가 쓰는 키」만 막는다 — 서랍 여분 컷까지 막으면 시끄러워서 게이트가 죽는다
  · ⚠️ 거꾸로(표엔 있는데 파일이 없는 것)는 **경고만** — 지금 `cs_b29` 하나가 그렇다(레시피가 안 써서 화면엔 안 뜬다)
- **`scripts/_repro-빈접시-0915.mjs`** (스모크 등록) — **구조가 고쳐졌나**를 잰다
  · ⭐ ③번 칸이 **옛 구조로 돌리면 실제로 빈 접시가 나는지**까지 확인한다(안 나면 이 재현판이 헛것이다)

## 4. 🧪 검증 (규칙 12 — 옛 값으로 진짜 걸리는지)

| 무엇 | 결과 |
|---|---|
| `check-photoratio` 에 오늘 아침 상태 재현(n3030~33 빼기) | ⛔ **네 편 다 이름까지 찍고 죽었다** |
| 그 게이트가 «가짜 빨간불»을 냈다 | 주석 속 시각 `00:10` 을 키로 주웠다 → 주석 줄을 버리게 고쳤다 |
| `_repro-빈접시` ③번 = 옛 구조로 돌리기 | ✅ **실제로 빈 접시가 난다**(재현판이 헛것이 아니다) |
| 빌드 | ✅ exit 0 · `dist/assets/` 에 `n3031`·`n3034`·`n3035` webp 실려 있고 번들이 부른다 |

## 5. 📌 배운 것 — 세 번째를 막으려면

- ⛔ **「값을 채웠다」는 고친 게 아니다.** 같은 구조가 남아 있으면 다음 사람이 또 빼먹는다.
  📌 v13.35 커밋이 *"표에 없는 것 0개"* 라고 «그날의 결과»를 적었는데, 그건 **내일을 안 지켜준다.**
- ⛔ **주석으로 경고를 달아두는 것도 안 통한다.** 내가 어제 단 주석을 오늘 내가 또 밟았다
  (2026-08-08 에도 같은 일이 있었다 — 「새 파일을 쓸 땐 옛 파일을 안 본다」).
- ⭐ **두 가지 정보가 한 값에 묶여 있으면 하나를 빼먹을 때 둘 다 잃는다.**
  여기선 「존재」와 「모양」이 묶여 있었다. 갈라놓으니 하나를 빼먹어도 나머지는 산다.
