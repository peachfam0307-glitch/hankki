# 🗓 수요일(2026-09-02)에 할 것 — 창업자 지시로 미뤄둔 둘

> 📮 창업자 2026-08-30 = *"카드는 저장해둬 수요일에 하자."*
>    ＋ *"그리고, 요리모드 글씨체를 좀 바꿔볼까해. 그것도 수요일날 하자. 저장해줘."*
>
> ⛔ **미룬 것이지 접은 게 아니다.** 둘 다 창업자가 «하자»고 정한 일이다.

---

## 1️⃣ 레꾸자랑 카드 뼈대 — ✅✅ **ⓐⓑ 둘 다 끝났다 (2026-09-02) · ⛔아직 배포 전**

📮 창업자 = *"뼈대를 추가할 필요가 있겠다. 기존에 심심한 걸 하나 바꾸거나... **a,b둘다 가자**"*

### ✅✅ 2026-09-02 결과 — `hold/갈라세기-0902` 에 있다 (⛔검수 대기 · 규칙 13)

📮 창업자 = *"ㅇㅇ 카드 뼈대 하자"* → *"뽑아줘. 뼈대 예쁜걸루"*
   → *"다 이뿐데 **1번은 색이 노랑계열로** 가면 좋겠어. 또 **아래가 살짝 휑하네**.."*
   → *"2번은 가을 느낌 딱 나고 3번은 예쁜데 겨울느낌나"* → *"넘 이뻐. 3번도 가을꺼야?"*
   → *"**11월은 둘가추가하자. 여름씬은 이제 빼도 될 것 같아.**"*

**ⓐ 11월 뼈대 = 「둘」 (창업자가 하나에서 둘로 늘렸다)**

| 열쇠 | 결 | 창업자 판정 |
|---|---|---|
| **`post`** 엽서 | 우표 톱니 ＋ 소인 ＋ 주소 줄 · **노랑·은행잎** | *"1번은 색이 노랑계열로"* → 그대로 |
| **`ticket`** 티켓 | 절취선 ＋ 좌우 펀치 홈 ＋ 스텁 · **김장 붉은** | *"2번은 가을 느낌 딱 나고"* |
| ~~`snow` 첫눈~~ | 눈 언덕 두 겹 ＋ 눈송이 | *"3번은 예쁜데 **겨울느낌나**"* → **12월로 미뤘다**(코드는 살아 있고 풀에만 안 넣었다) |

· 자리 = `ShareDrawCard.jsx` 의 `SKINS` ＋ 렌더 갈래 셋 · 풀 = `isLateAutumn()` 일 때만 `post`·`ticket` 추가
· ⛔ **「아래가 휑하다」도 같이 고쳤다** — 엽서는 주소 줄, 티켓은 스텁이 아래를 채운다

**ⓑ 가을 옷 두 벌 = `WARDROBE.autumnLate` (11월에만 덮어쓴다)**

⛔⛔ **첫 판이 «너무 미세해서 창업자가 구별을 못 했다»** — *"둘다 똑같은데??"* · *"뭐가 다르다는거지??"*
   · *"미세하게 색이 좀 진해진 정도? **이건 구별 잘 못해 ㅋ 달라진줄몰랐어 나는**"*
🔢 실측 = 내가 **배경 밝기를 0.02 밖에 안 낮췄다.** → **여섯 벌 전부 ≥0.10** 으로 밀었다
   (`night` 만은 밝기 대신 **색조 +47°** — 이미 어두워서 더 낮추면 까매진다)
⭐ **뼈대는 그대로라 새로 그린 컷 0장** — 색·질감만 갈린다(원래 계획 그대로)

**＋ 여름 씬을 가을에서 뺐다** (창업자 *"여름씬은 이제 빼도 될 것 같아"*)

🔢 실측 = 폴라로이드 씬 풀에 **계절 거름망이 아예 없었다** → 11월 카드의 **약 26%** 가 여름 씬
   (0.65 × 4/10) — 2026-08-04 *"자랑카드 여름가을같이돌아감"* 제보와 **같은 모양**
✅ `SUMMER_SCENE = /^scene_(pool|sandcastle|picnic|night_market)\./` · `isPeakSeason('summer')` 일 때만 통과
⛔ **`camp`·`walk` 는 남겼다** — camp 는 밤 모닥불·담요·따뜻한 음료라 가을로 읽히고, walk 는 어느 철인지 모호하다
   ⏳ **창업자 판정 하나 = `walk` 도 여름으로 볼지** (지금은 사철로 둔 상태)

**🔒 게이트 = `_repro-11월카드-0902.mjs`(smoke · 11칸)**
   post·ticket 렌더 · 11월 「늦가을 한정」 배지 · 9월 「가을 한정」 · 뽑기 풀에 둘 다 등장 · 9월엔 0 ·
   여름 씬이 여름엔 뜨고 가을엔 0 · 사철 씬 ≥4
   ⛔ 만들다 **거짓 빨강**을 냈다 — `SEED_COACH_SEEN` 을 빠뜨려 코치마크가 클릭을 가로챘고
      「다시 뽑기」가 한 번도 안 눌렸는데 *"0번 뽑혔다"* 로 읽혔다(규칙 18 ⓘ). **누른 횟수를 같이 센다.**

**⏳ 남은 것** = ⑴`walk` 판정 ⑵`snow` 를 12월로 정식 편입할지 ⑶**아직 배포 전**(창업자 검수 뒤)

### 🔢 왜 나왔나 — 실측이 창업자 걱정을 뒷받침했다

창업자 물음 = *"우리 레꾸자랑 가을 3개월 동안 7장으로 돌리나?"*

| 기간 | 장수 | 무엇 |
|---|---:|---|
| 9/1 ~ 9/30 | **7** | 기본 6 ＋ 추석 |
| 10/1 ~ 10/15 | **8** | 기본 6 ＋ 추석 ＋ 핼러윈 |
| 10/16 ~ 11/2 | **7** | 기본 6 ＋ 핼러윈 |
| **11/3 ~ 11/30** | **6** | 기본 6 (⛔덤 0) |

⭐⭐ **진짜 문제는 «장수»가 아니라 «옷이 안 바뀌는 것»이었다.**
   같은 뼈대(WARM·NIGHT)를 9/15·10/20·11/15 에 찍어 나란히 보니
   **배경·색·문구가 석 달 내내 똑같다.** 바뀌는 건 곰펭 컷 하나뿐.
   🔎 `ShareDrawCard.jsx:146` 주석은 *"옷만 «계절마다» 갈아입는다"* 라고 적혀 있는데,
      그 「계절」이 봄/여름/가을/겨울 단위라 **가을 안에서는 한 번도 안 갈아입는다.**

### 📋 원래 계획 (2026-08-30 에 적어둔 것 · 기록용)

- **ⓐ 11월 전용 뼈대 한 장** — 11/3부터 덤이 0이 되는 구멍을 메운다.
  후보 결 = 김장 · 첫눈 · 늦가을. ⛔날짜 창은 `cardSeasons.js` 에 한 곳만 적는다.
- **ⓑ 가을 옷을 두 벌로** — 초가을(9월 · 밝은 갈색·억새) / 늦가을(11월 · 짙은 붉은·낙엽).
  ⭐ **뼈대는 그대로라 새로 그릴 컷이 0이다** — 색·질감만 가른다.
- **＋ 창업자 말** = *"기존에 심심한 걸 하나 바꾸거나"* → 뼈대 6종 중 약한 것을 갈아엎는 것도 같이 본다.
  ⛔ 어느 것이 「심심한지」는 **창업자 판정**이다(규칙 11) — 6종을 실물로 나란히 뽑아 물어본다.

### 🛠 이미 있는 도구 — ⛔새로 만들지 말 것

- `scripts/_shot-자랑카드-0901.mjs` — **`ON`(날짜)·`KEYS`(스킨) 환경변수를 받는다.**
  예 = `ON=2026-11-15 KEYS=warm,night node scripts/_shot-자랑카드-0901.mjs`
  ⭐ FakeDate 로 시계를 속여 «진짜 앱»에서 찍는다(절대원칙 30).
- 옷을 정하는 곳 = `ShareDrawCard.jsx` 의 `wearOf(key)` · 창 = `src/data/cardSeasons.js`

### ⛔ 지킬 것

- **파는 컷은 공유 카드에 한 컷도 안 나간다**(창업자 2026-08-03 절대원칙 · `check-packmix`)
- 카드 히어로는 496~648px 로 크게 쓴다 → **긴변 400px 미만 컷은 카드에서 뺀다**
- 뽑기 확률이 묽어지지 않게 — 뼈대를 늘리면 각 컷이 뽑힐 확률이 준다(지금 6장 = 각 17%)

---

## 2️⃣ 요리모드 글씨체 — ✅✅ **이미 끝났다(2026-09-01 배포). ⛔다시 들고 오지 말 것**

📮 창업자 = *"요리모드 글씨체를 좀 바꿔볼까해."* (2026-08-31 밤)
📮 → **2026-09-02** = *"요리모드 글씨체 어제 기염체로 싹 바꿨잖아"* ← **창업자 말이 맞다.**

🔢 **실물로 확인했다**(규칙 29 · 기억으로 적지 않는다)
| 근거 | 값 |
|---|---|
| `styles.css:2277` `.cook-steptext` | `font-family: 'Gaegu', 'Pretendard', sans-serif` |
| `styles.css:2289` `.cook-ing`(재료) | 같은 귀염체 |
| 커밋 | `2005098c` **v12.14 「요리모드 귀염체 확정」** ＋ `efb4cd3f` 「요리모드 재료도 귀염체」 |
| 나갔나 | ✅ **둘 다 배포 브랜치에 있다**(`git branch -r --contains`) |
| 판 | `_shot-요리글씨확정-0901.mjs` · `_shot-재료귀염체-0901.mjs` (둘 다 `smoke:seq`) |

⛔⛔ **그런데 이 절이 「⏳아직 안 정했다」로 남아 있어서 2026-09-02 에 내가 «다시 물었다».**
   창업자가 **정하고 그날 배포까지 한 것**을 「수요일에 물을 것」으로 들고 온 것이다.
   📌 **규칙 12ⓑ 그대로** — *끝난 것을 「대기」로 두면 반드시 다시 꺼낸다.*
   📌 그리고 **`decided.mjs`·코드를 먼저 안 돌렸다**(규칙 17·29). 한 줄이면 답이 나왔다:
   `grep -n "cook-steptext" src/styles.css`

⏳ **남은 것 = 없음.** 더 손보고 싶으면 창업자가 «다시» 꺼낼 때 시작한다.

<details><summary>그때 재둔 값 (기록용)</summary>

### 🔢 지금 값 (실측 · `src/styles.css`)

| 줄 | 지금 |
|---|---|
| `.cook-stepno`  (STEP 1 / 5) | 16px · 800 · `--brown` |
| `.cook-steptext` (걸음 본문) | **24px** · 1.5 · 600 |
| ↳ `min-height: 750px` | **28px** (390×844 · 412×915) |
| ↳ `min-width:700 + min-height:700` | **38px** (패드) |

⛔⛔ **글자 «크기»는 함부로 건드리지 않는다** — 28/38 은 창업자가 **그날 콕 집어 정한 값**이고,
   2026-08-21 「글자2」 일괄 확대(470곳) 때도 **요리 글자는 일부러 건너뛰었다**(CLAUDE.md v11.21 절).
   ✅ 그러니 이번 건은 **「글씨체(font-family)」**로 읽는 게 맞다. 크기를 바꾸자는 말이면 위 이력을 먼저 알린다.

⚠️ 앱이 쓰는 글꼴 = 본문 시스템 고딕 · 메모지·**요리모드 Gaegu**(귀염체) · 카드 제목 **Jua**.

</details>

---

## 3️⃣ [2026-08-30] 9/1 카드 = 창업자 지시 셋이 «이미 다 되어 있다» — 실측

📮 창업자 = *"레꾸자랑카드 여름한정만 9월1일에빼고 나머지 뼈대는 넣자. 다양하게.
   캐릭터는 가을컷+기본만 넣고. 여름은 빼고. 이해했어?"* ＋ *"검수판주면 고를게 여름 뼈대랑 캐릭터"*

⭐⭐ **셋 다 새로 만들 게 없다 — 앱이 이미 그렇게 돈다.**
   ⛔짐작이 아니라 `season.js`·`cardSeasons.js` 를 «실제로 불러» 날짜별로 계산했다(절대원칙 30).
   🛠 판 = `scripts/_probe-자랑카드-0901풀-0830.mjs`

| 날짜 | 뼈대 | 캐릭터(곰·펭·콤비) | 기본 풀에 섞인 여름 |
|---|---|---|---:|
| 2026-08-31 | **7** warm panel pola mag arch night **summer** | 11 · 9 · 12 | **0** |
| 2026-09-01 | **7** warm panel pola mag arch night **chuseok** | 18 · 11 · 14 | **0** |
| 2026-10-05 | **8** (＋halloween) | 18 · 11 · 14 | **0** |
| 2026-11-15 | **6** (기본만) | 18 · 11 · 14 | **0** |

- ① **여름만 9/1에 빠진다** — 카드 스킨은 `isPeakSeason` 을 쓴다(스티커의 `isSeason` 과 달리
  `OVERLAP_DAYS = 14` 겹침을 «안» 준다). 그래서 9/1 0시에 정확히 내려간다.
- ② **나머지 뼈대는 다 돈다** — 여름이 빠진 자리를 **추석(9/1~10/15)**이 채워 9월도 7종이다.
- ③ **여름 캐릭터가 애초에 안 섞인다** — 기본 풀은 `COOK` 정규식(요리하는 컷 명단)만 통과시키고,
  `SUMMER = /^(sm_|duo_(bingsu|naengmyeon|watermelon))/` **13컷은 여름 스킨에서만** 쓰인다.
  → 뼈대가 빠지면 **같이 사라진다.** 따로 뺄 코드가 필요 없다.

☑️ **검수판(폰) = https://claude.ai/code/artifact/54f261e3-a60b-4677-a4c9-e68c23a41f1d**
   · 절 셋 «전부» 고르기 ＋ 복사(절대원칙 2026-08-19) — 창업자가 *"고를게 여름 뼈대랑 캐릭터"* 라 둘을 콕 집었다
   · 만드는 판 = `scripts/_판-자랑카드9월-0830.py` (여름 컷 명단을 `ShareDrawCard.jsx` 의 `SUMMER` 에서 **읽어서** 쓴다)
   · 카드 그림 = `_shot-자랑카드-0901.mjs` 로 **진짜 앱을 찍은 것**
     ⛔ 여름 뼈대는 **`ON=2026-08-31` 로 찍어야 나온다** — 9/1엔 이미 빠져 있어서 캡처가 안 된다

⛓ **위 1️⃣ 의 근거가 이 표다** — 11/15 에 **6종으로 줄어든다.** 그래서 수요일 ⓐ(11월 전용 뼈대)가 필요하다.

---

## 📌 이 문서 상태 — 2026-09-02 기준

| 절 | 상태 |
|---|---|
| **1️⃣ 카드 뼈대 ⓐⓑ** | ✅**만들었다** · ⛔`hold/갈라세기-0902` 에서 검수 대기 |
| **2️⃣ 요리모드 글씨체** | ✅**2026-09-01 에 이미 배포됐다**(v12.14) — ⛔다시 들고 오지 말 것 |
| **3️⃣ 9/1 카드 실측** | ✅기록용(그때 이미 다 돌고 있었다) |

⭐ **「기존에 심심한 걸 하나 바꾸거나」는 저절로 닫혔다** — 창업자가 *"11월은 **둘가추가**하자"* 라고
   **바꾸기가 아니라 «더하기»를 골랐다.** 6종은 그대로 두고 11월에만 둘이 얹힌다.

⏳ **아직 창업자 판정이 남은 것 둘**
  ⑴ 폴라로이드 씬 **`walk`**(산책) — 여름으로 볼지 사철로 둘지(지금은 사철)
  ⑵ **`snow`**(첫눈 뼈대) — 12월 창으로 정식 편입할지 (코드는 다 있고 풀에만 안 넣었다)

⛔ 이 파일을 **지우지 않는다** — 「끝났다」로 고쳐 적는다(규칙 12 ⓑ · 끝난 걸 「대기」로 두면 다음 세션이 또 들고 온다).
