# 🗂 작업복기 2026-09-18 — 9/18 계측 누적 · 「유저가 열쇠를 안 쓴다」를 찾아낸 날

> 📮 창업자 = *"안녕 분석해서 보고서 쓸 준비 됐어?"* 로 시작해 밤 11시 반까지.
> 🌿 배포 갈래 = `claude/chatgpt-conversation-link-kvn5ph` · 마지막 초록불 = **run 2660 · `cb4ce48c` · success**

## 1. 📒 9/18 하루치를 장부에 넣었다

`docs/계측-일별-2026-09.json` 11일째. **검산 205 = 205 통과.**

| | |
|---|---|
| 활성 26 · 새 16 · 재방문 6 · 참여 1분56초 · 이벤트 253 | 조회 **205** |
| 경로 | **`/hankki/ios` 112**(3명 · 1인당 37회 · 13분24초) · `app` 64 · `get.html` 17 · `/hankki/` 12 |
| 채널 | Direct 14 · **Unassigned 11**(새 0 · 이벤트 126) · Organic Social 6 · Cross-network 2 |
| 가져오기 | 갈래 share 10 · photo 10 · write 4 → 읽힘 5 → **저장 8** |
| 🔐 로그인 | 원값 98 − **빼는 7명** = **진짜 91명** |

## 2. ⭐⭐ 제일 큰 발견 — **유저가 열쇠를 «갖고도» 안 쓴다**

창업자 물음 = *"열쇠 0인데 저장 8? 가져오기를 8번 했다고?"* → 코드를 봤다.

- `레시피저장()`을 부르는 자리 **셋 다 열쇠가 필요 없다** — 공유로 담기(`App.jsx:739`) · 링크만 담기(`ImportScreen:276`) · 새로 쓰기(`EditorScreen:843`)
- 그런데 **`import_read_ok` 5** 인데 Vision 은 0 → 「그냥 읽기」로 읽은 것이다
- 🔢 월 누계 = `기본인식 42건 · 갈래 {고름 35 · 없음 0 · 실패 4 · 막힘 3}` · `무료이용률 AI 44 vs 기본 56`
- ⭐⭐ **「없음 0」 = 열쇠가 없어서 못 쓴 사람이 «한 명도 없다».** 35건은 **있는데 안 쓴 것**이다

### 왜 안 쓰나 — 화면이 그렇게 생겼다
```
[ AI로 정확하게 읽기 · 열쇠 1개 ]   ← 비용만 적혀 있다
[ 그냥 읽기 · 열쇠 안 써요      ]   ← 이득만 적혀 있다
```
📮 창업자 = *"열쇠를 받을구멍이 없으니까 아끼는게 아닌가 싶어서"* → **실물 캡처 둘로 확인했다** —
가져오기 «목록»엔 🔑 숫자가 있는데 **상세로 들어오면 그 자리가 딱지로 바뀌며 잔액이 사라진다.**
🔢 로그인 91명 × 웰컴 30개인데 이 달 유저가 쓴 건 **28건(한 명당 0.3건)**. **모자라서가 아니라 얼마 있는지 몰라서 아꼈다.**

## 3. 🛠 그래서 고친 것 (배포됨 · run 2660)

| 무엇 | 어떻게 |
|---|---|
| 단추 글자 | 「그냥 읽기 · **열쇠는 안 쓰지만 인식률이 떨어져요**」 (창업자 문구 · 맞춤법만 「률」) |
| 초록 박스 | 「덜 읽혀요」 → 「인식률이 떨어져요」 ＋ **사진 갈래만 단추 «아래»로** |
| 딱지 | **「열쇠 N개 있어요 · 쓸지 고를 수 있어요」** · 열쇠 0이면 **「열쇠가 없어도」** |
| ⛔ 안 쓴 것 | 「한 개 50원어치」 같은 **값 표기** — 창업자가 값을 안 정했다. 적으면 그 값에 묶인다 |

⭐ 딱지가 **단추를 가르는 조건과 «똑같은 조건»**을 쓴다 — 안 그러면 말과 화면이 또 갈린다(열쇠 0인데 「고를 수 있어요」라던 거짓말이 그거였다).

## 4. 🔑 워커에 「기본인식 오늘치」 칸 신설 (⛔아직 안 돌고 있다)

설계 관문 통과 → `ocr-proxy/worker.js` 에 **일별 키**(`t:YMD` · `tf:YMD` · `tr:YMD:갈래`) ＋ 오늘 블록에 `기본인식`(갈래 넷 · 갈래합 · 쌓기시작).
🔢 KV 쓰기가 읽기당 3→6회 = 한 달 252회 = 무료 한도(하루 1000)의 1% 미만. TTL 40일 그대로.
⛔ **워커는 CI 가 아니다 — 창업자가 Cloudflare 에 붙여넣어야 돈다.**

## 5. 💸 돈 — 처음으로 전수를 봤다

| 나감 | 한 달 | 들어옴 |
|---|---:|---|
| 클로드 $110 · 광고 6~8만 · 애플 $99/년 | **22~24만원** | **0원** |
| 서버·Vision·Firebase | **0원** (Vision 은 매달 1,000건 무료 · 우리는 76건) | |

📮 창업자 = *"저거 다 내가 24시간 도는거 모르고 산다했잖아"* → **쿠팡 유저 수익 = 0원**(구매 3건 전부 창업자 본인).
📮 *"본전을 벌겠다는게 아니라 조금씩 뭐라도 결제가 이루어지는 시스템을 만들어놔야겠다는거야"* → **내일 결제를 켠다.**
📄 `docs/결제-여는-순서-2026-09-19.md` 한 장에 다 모았다(행정 순서 · W-8BEN 값 · 이미 있는 것 넷).

## 6. 📣 광고 — 껐다. 그리고 내가 틀린 걸 두 번 정정했다

- 📮 창업자 22:3x = *"광고 2개는 다 중단했어"* · *"새로운 릴스 나오기전에는 광고 스탑할게"*
- ⭐ **9/19 부터가 「광고 없는 바닥값」 구간**이다 — 「광고 켜기 전 바닥값은 영영 못 잰다」던 그 값을 여기서 잰다
- ⛔ **정정 ①** = 「가을 캐러셀이 더 좋은 소재다」 → 못 세운다. 글마다 **광고비도 기간도 다르다**(창업자 지적)
- ⛔ **정정 ②** = 「소재가 낡아서 도달이 떨어졌다」 → **빈도를 재보니 1.20~1.47.** 메타 공식이 말하는 피로 구간(3 이상)이 아니다. **까닭은 아직 모른다**

## 7. 🚨 오늘 내가 낸 사고 넷 — 전부 기록했다

1. **`| tail` 뒤 exit code 를 믿어 «빌드 깨진 판»을 밀었다** — 3분 만에 고쳤지만 배포 전이라 다행이었다. 이제 빌드는 **파일로 받아 종료코드를 눈으로 본다**
2. **배포 도는 중에 계속 밀어 12판을 취소시켰다**(2647~2658) — 우리 규칙에 있는 걸 열두 번 어겼다
3. **옛 문서(아이폰-1.0.1-제출)로 판단** — 「딸 아이폰은 열쇠 칸이 없다」가 틀렸다. 아이폰 세션이 git 으로 잡아줬다
4. **조건이 다른 것끼리 견줬다 ×3**(쿠팡 · 캐러셀 · 아이폰 릴스) → **새 절대원칙**을 HANDOVER 에 세웠다:
   ⛔ **광고가 걸린 값끼리는 견주지 않는다 — 광고비·기간이 같다는 걸 확인한 뒤에만**

## 8. 📄 오늘 생긴 문서

- `docs/로그인-전수-2026-09-18.md` — 98명 전수 ＋ **날마다 새 로그인**(09-15 가 13명으로 최고 = App Store 승인일) ＋ 빼는 사람 일곱
- `docs/우리가-쓰는-돈-2026-09-18.md`
- `docs/인스타-인사이트-30일-2026-09-18.md` — ⛔글별 견주기는 «못 쓴다»고 박아뒀다. 살아남는 건 **누가 보나(여성 83% · 25~44세 74.5%) · 프로필 983→링크 356(36%) · 월요일 오후 3~11시**
- `docs/결제-여는-순서-2026-09-19.md`

## 9. ⏭ 내일

1. 🍎 **애플 「유료 앱 계약」 ＋ W-8BEN** ← 기다림이 있어서 제일 먼저 (창업자)
2. 🔑 **워커를 Cloudflare 에 붙여넣기** (창업자) — 그래야 기본인식 오늘치가 쌓인다
3. 📒 쿠팡·열쇠통·유지를 계측 장부 스키마에 (클로드 · 어제 「1번」)
4. 🎬 새 릴스 — 「나만의 요리책」류 (광고는 그때까지 스탑)
