# 🗂 작업복기 2026-09-17 밤 — 계측을 «3층»으로 다시 짓고, 판단 절을 되살렸다

> 📮 창업자 = *"매일 누적되어야해"* · *"이게 우리 앱 자산이야"* · *"진짜 땜빵할꺼야? 뿌리부터 제대로 재고 분석해"* · *"제대로 설계해서 반영해. 자꾸 질질새게 만들지마"*
> 🌿 갈래 = `claude/annyeong-a76eig`(지정) → 배포 갈래에도 실었다 · 앱 코드 **0줄**(문서·도구만)

## 1. 🔥 오늘 다섯 번 틀렸고, 뿌리는 «하나»였다

| 틀린 것 | 무엇이 틀렸나 |
|---|---|
| 아이폰 표본 시작일 | 「21:3x」 → 「모름」 → 실은 **우리 문서에 2026-09-17 00:29 이 이미 적혀 있었다.** 내 문서를 안 읽고 창업자에게 물었다 |
| 날마다 표 | 09-14 에서 **사흘 멈춰 있었다.** 그날 물어본 것만 답하고 표에 옮기지 않았다 |
| 09-15·16 값 | 반나절치였다(저녁 캡처). 온종일은 42/36 · 22/16 |
| 「자랑 나흘 내리 0」 | 안 재고 있었던 것이다. `brag_shared` 는 09-14 21:53 에 심겼다 |
| 「광고로 온 사람이 덜 깐다」 | 뒤집혔다 — 광고·추천 **67.65%** vs Play 탐색 33.33%. **28일 합계로 원인을 지어냈다** |

⭐⭐ **뿌리 = 값이 Markdown «글»로만 있었다.** 표는 사흘 멈춰도 조용하고, 갈라 보려면 손으로 다시 짜야 하고,
　 몇 시 기준인지·그때 그 눈이 있었나를 적을 칸이 없었다. 그래서 땜빵 대신 구조를 바꿨다.

## 2. 🏗 계측 3층

| 층 | 파일 | 규칙 |
|---|---|---|
| ① 값 | `docs/계측-일별-2026-09.json` (19KB · 열흘) | 하루 한 덩어리. 빈 값은 `null` — ⛔0 이 아니다 |
| ② 판 | `scripts/계측-보고.mjs` → `docs/유저보고서-2026-09.md` | **도구가 만든다. 손으로 고치지 않는다** |
| ③ 판단 | `docs/계측-판단-2026-09.md` | **사람이 쓰는 유일한 파일.** 보고서 맨 위에 실린다 |

- 🧮 **검산이 안 맞으면 죽는다** — 화면＋행동＋기타 합 ≠ 개요 조회수면 캡처가 잘린 것이다. 일부러 깨뜨려 확인했다. 열흘 전부 통과(47/214/189/79/185/204/203/306/215/100).
- 🌱 **⬜ 눈 없음 · 🌱 그날 심음 · ⚠️ 도구 의심** — 보고서가 「0」과 「안 쟀다」를 스스로 가른다. 심은 날은 `계측-잣대 --전부` 를 «불러서» 받는다(⛔로직 두 벌 금지).
- ⭐ **실물이 도구를 이긴다** — 값이 있는데 심은 날이 더 늦으면 값을 지우지 않고 ⚠️ 로 도구를 의심한다.
　 `buy_pick_detail` 은 실제 09-10 15:59 인데 `git log -S` 가 09-12 로 읽어 **09-11 값 2명을 지울 뻔했다.** 지금 8곳이 잡힌다.
- 🧭 **판단이 낡으면 보고서가 알린다** — 첫 줄이 `갱신일: YYYY-MM-DD` 꼴이 아니면 도구가 죽고, 갱신일이 마지막 날짜보다 낡으면 맨 위에 「🔴🔴 판단이 낡았다」.

## 3. 📈 값에서 나온 것

| 무엇 | 크기 | 뜻 |
|---|---|---|
| **인스타는 «사람»을 주지 흐름을 안 준다** | 다시 온 비율 Direct **25.5%** vs Social **1.9%** (17일 · n=213) | 광고 성과를 「활성 사용자」로 재면 최대 3배 부푼다 |
| **스토어 → 설치 39.8%** | 09-13 31명→11 · 09-14 33명→13 (계산과 맞음) | ⭐**병목은 앱이 아니라 스토어 페이지다** |
| 징검다리 → 스토어 | 87~92% | ✅ 안 샌다 |
| 광고를 줄인 뒤 | 새 사용자 50→36→16 인데 재방문 2→10→7 · 머문 시간 28초→1분53초 | 줄어든 건 **원래 안 머물던 사람**이다 |

⛔ GA4 는 사람을 못 잇는다 — 「맞아떨어졌다」는 «크기가 같다»지 «같은 사람»이 아니다.
⛔ 광고 켜기 «전» 바닥값은 영영 못 잰다(광고 09-05 · 계측 09-08 20:25). 알려면 **광고를 하루 꺼야** 한다.

## 4. 🧪 같이 고친 장치

- `계측-잣대.mjs` — 화면 목록을 `src/stats.js` 의 `보내도되는화면` 에서 **읽는다**(낡아 있었다) · import 묶음 4→8 · `--전부` 추가
- `docs/계측-심은날-전수표-2026-09-17.md` — `--전부` 출력 48줄 통째로
- `docs/예약-알림.md` ＋ `scripts/알림.mjs` — 날짜를 적어두면 **그날 세션 브리핑에 저절로 뜬다**(09-18·09-20·09-30·10-25)
- 날마다 상세 머리글 `###` → `####` — doc-guard 가 「세대 10개」로 오인해 **내가 내 보고서를 못 읽던 것**
- 옛 51KB 손으로 쓴 보고서는 🗄보관소로(`git mv`) · 가리키던 여섯 곳 전부 고침

## 5. ⛔ 안 한 것 (내일)

1. **쿠팡 · 열쇠통(AI 다듬기·사진 읽기) · 유지(코호트)가 아직 장부 스키마에 없다** — 창업자 「1번은 내일」
2. 작은 것 다섯 — 「모름」이 «눈 없음»과 «캡처 안 받음»을 섞음 · ⚠️ 의심이 날짜별로 되풀이(8줄→5줄) · §1 연표에 묶음 접두가 이름인 척 · 「모두 48가지」 부정확 · §2 에 유입 칸 없음

## 6. 🚀 배포

`run 2635 · 83735c092 · success` (01:05 KST 눈으로 확인). 내 판단 절 `f88a30e` 는 그 안에 들어 있다.
2633·2634 가 cancelled 인 건 푸시가 겹쳐 앞 run 이 밀린 것 — 사고가 아니다.
