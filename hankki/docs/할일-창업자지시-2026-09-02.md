# 🗒 창업자 지시 할 일 — 2026-09-02 «낮» (앱 갈래)

> 🌙 **지금 판은 이 문서 «맨 아래» 「밤 절」이다** — 이 절은 **v12.22** 시점 기록이다.

> 📮 창업자 = *"내일 다각도로 조사해서 정해보쟈"* → 아침에 조사 → 판정 → 만들기 → *"배포해"*
> ＋ *"그동안 남은 할일 정리하고 한 것 문서에박아두자"*
>
> 🧑‍🤝‍🧑 **이 문서는 «앱 갈래» 세션 것이다.** 바깥 갈래(구글·돈·조사) 현행은
> `docs/할일-창업자지시-2026-09-01.md` 맨 아래 절이다 — ⛔한 글자도 안 고쳤다.

---

## ✅ 오늘 닫은 것 — **v12.22** (run #2031)

한 뿌리였다: **인스타로 담은 레시피가 임시보관함에 갇히는데 «나가는 길이 셋 다 막혀» 있었다.**

| | 무엇 | 뿌리 (재서 확인 · 짐작 아님) |
|---|---|---|
| **1** | 🗃 **저절로 졸업한다** | `App.jsx` 의 `채우기()` 가 제목·재료·순서·아이콘은 다 갱신하면서 **`status` 만 안 건드렸다.** AI 가 다 채워도 `unsorted` 로 남았다 |
| **2** | 🔢 **이미 갇힌 것도 꺼낸다** | `INBOX_V` 1 → 2. 장치는 이미 있었고 **번호만** 올렸다 → 창업자 폰 **항정살조림**이 나온다 |
| **3** | 🔘 **줄마다 나가는 단추 둘** | 임시보관함엔 ［열기］＋［🗑］뿐이었다 → 「채우러 가기」·「그대로 저장」 |
| **4** | 🔎 **왜 남았는지 줄마다 말한다** | 「순서를 덜 읽었어요」·「재료·순서를 덜 읽었어요」 — **저장된 값만 세면 나온다(⛔열쇠 0개)** |
| **5** | 💾 **편집 「저장」 이름 하나로** | 큰 단추가 **「정리 완료」**였다 → 위아래 둘 다 **「저장」** |
| **6** | 📌 **상단바를 붙였다** | `.topbar-back` 에 `position: sticky` 가 **없어서** 굴리면 「저장」이 떠내려갔다. ⛔다섯 화면이 같이 쓰는 클래스라 **편집 전용 `topbar-stick`** 으로 |
| **7** | 📷 **덜 읽힌 것 열면 캡처가 이미 펼쳐진다** | 그 전엔 `pin` 이 `null` 이라 **「보면서 쓰기」를 눌러야** 사진이 떴다 |
| **8** | 🖼 **캡처는 «표지»로 안 쓴다** | `image` 만 있으면 무조건 표지가 사진이었다. 공유받기가 **캡처를 `image` 에 담아서**(`App.jsx:454`) 인스타 «글자 사진»이 카드 표지가 됐다 |
| **9** | 🔢 **이미 박힌 캡처 표지도 푼다** | `COVER_V 1` — **도장(`thumb`)을 지운다**(`'icon'` 을 새로 안 쓴다 · 잣대를 바꾸면 그 편들도 따라온다) |

### ✅✅ 실물로 닫혔다 — 창업자 폰 (2026-09-02 08:39 KST · 배포 직후)
> 📮 창업자 = *"테스트중. **안내창도 잘 나오네. 자동으로 이동했어**"*

⭐ **「자동으로 이동」이 이 판의 심장이다** — 위 표의 **1·2**(`채우기()` 가 status 를 안 건드리던 한 줄 ＋ `INBOX_V 2`)가
　 창업자 폰에서 «진짜로» 돌았다는 뜻이다. 재현판 37칸이 아니라 **실물**이라 값이 다르다.
⏳ 아직 실물로 «안» 본 것 = **캡처 표지가 음식 아이콘으로 바뀌었나**(8·9) · **굴려도 「저장」이 안 사라지나**(6)

### ⭐ 잣대를 «한 곳»으로 모은 것 둘
같은 식이 여러 곳에 복사돼 있으면 **갈리는 순간 「담을 땐 되는데 AI 가 채우면 안 되는」 상태**가 된다.

| 잣대 | 어디 한 곳 | 그전엔 |
|---|---|---|
| **「다 읽었나」** | `store.jsx` `다읽었나()` | **두 곳** (`migrateInboxSorted` ＋ `App.jsx` 공유받기) |
| **「표지에 뭘 그릴까」** | `store.jsx` `기본표지()` | **세 곳** (`Thumb` ＋ `DecorEditor` ＋ `Editor`) |

---

## 🔎 오늘 «재서» 알게 된 것 — 기억이 아니라 실측

| 무엇 | 값 |
|---|---|
| 홈 「최근 저장」이 임시보관함 것도 보여준다 | `HomeScreen.jsx:268` `recent` 에 **unsorted 필터가 아예 없다**. ⛔내 메모의 「:211 이 unsorted 만 뺀다」는 **틀린 줄**이었다(:211 은 추천 pool) |
| 「그대로 저장」이 여러 줄을 옮기나 | **아니다** — 딱 그 줄 하나 (`_probe-그대로저장-0902`) |
| 고정 캡처 창이 읽을 만한가 | 1080×2340 캡처 → 화면 글자 **12.3px**(앱 최소 14px) · 한 번에 보이는 원본 **34%** · 확대는 **「크게 보기」뿐**(앱이 두 손가락 확대를 막아뒀다 — `user-scalable=no`) |
| ✕ 닫기를 누르면 옆 작은 그림도 사라지나 | **아니다** — 큰 창만 접힌다. 사진·썸네일·저장값 **그대로**, 자리엔 「캡쳐 보면서 쓰기」가 생긴다 |
| 기본 레시피가 이사에 걸리나 | **안 걸린다** — `source: 'hankki'`(캡처 이사는 `source: 'photo'` 만 본다) |

---

## ⏳ 남은 것

### 🙋 창업자가 정해줘야 도는 것
| 순 | 할 일 | 지금 상태 |
|---|---|---|
| **1** | 📏 **패드 가로 글줄 폭 580px 이 적당한가** | 👉 판 https://claude.ai/code/artifact/2d89b9b9-80f8-4f61-87ed-9d8703ffeea7 ｜ 지금 값으로 나가 있다 · 다른 값이면 **한 줄**만 고친다 |
| **2** | ✍️ **STEP 줄도 손글씨로 갈까** | 같은 판 ② 칸 ｜ ⛔귀염체로 가면 **60.4% 얇아진다** |
| **3** | 🖼 **그릇 프레임 판정 둘** | ⑴높이 있는 것/접시/둘 다 ⑵당김 정도 |
| **4** | 🍜 **음식 컷 셋** (팟타이 · 고추장제육볶음 · 닭갈비) | **창업자가 다시 뽑아야** 움직인다 |
| **5** | ❓ **「꼬르곰 잘린 거」** | ⛔**어느 화면의 무엇인지 한 줄**만 주면 바로 잡는다(규칙 25) |

### 🤖 클로드 몫
| 순 | 할 일 | 지금 상태 |
|---|---|---|
| **6** | 🔁 **ⓑ 자동 받기** — 공유받기가 방금 담은 레시피를 덮어 없앤 건 | ⛔**재현이 먼저다.** `_repro-클라우드동기화-0821` 에 ㉙「방금 담은 것이 살아남나」부터. #1 의심 = `안올린변경있나` 의 **「너무 크면 셈에서 뺀다」**(폰에만 있는 큰 것이 오히려 검사에서 빠진다). ⛔`App.jsx:400` `if (false)` 를 **재현 전엔 되살리지 않는다** |
| **7** | 📜 **붙여넣기로 담은 편은 원문이 맨 아래** | `EditorScreen` 1072줄/1302줄 ＋ **기본이 접힘**. 사진 있는 편은 오늘 고쳤고 **글자만 있는 편이 남았다** |
| **8** | 🔍 **「크게 보기」 단추·흐림띠가 캡처 «글자»를 덮는다** | 오늘 눈으로 잡았다(절대원칙 21) · 급하지 않다 |
| **9** | 🗓 **오픈 요일 나누기** — 월 레시피 · 수 제철 · 금 장바구니 | `weekly.js` = 아직 **「월요일에 다 바뀐다」** |
| **10** | 🛒 **주부의 장바구니 업뎃 확인 ＋ 요일** | **9와 한 몸** |
| **11** | 🎨 **레꾸·일꾸 패널 시안** | `tools.mjs 패널` = 0개 → 새로 만든다 |
| **12** | 🧾 **기본 인식(무료 OCR) 사용량 세기** | 유료 켤 때 **가격·장수 판정**에 쓴다 |
| **13** | 📦 **AAB 19판 ＋ 스토어 스샷** | 마지막 = **18판(v11.80)** · 지금 앱 **v12.22** ｜ 굽기는 클로드 · **올리는 건 창업자** |

### 🍳 검수 (절대원칙 28 — 저절로 열리는 문)
· **내일(9/3) 저절로 열리는 것 = 없음**
· ⏳ 검수 안 받은 레시피가 **앞으로 7번에 걸쳐 17편** 열린다 — **제일 가까운 게 2026-11-02(D-61)**
· 👉 **몰아서 미리** 뽑는다(창업자 확정 = *"한달치씩 미리 검수해두자. 전날도 무조건 한번 확인하고"*)

---

## 🔒 오늘 새로 박은 장치

| 무엇 | 하는 일 |
|---|---|
| `_repro-보관함정리-0828` **17 → 37칸** | 나가는 길 셋 ＋ 캡처 펼침 ＋ 표지 셋. 🧪규칙 12 = **일곱 판 전부 죽는 것** 확인 |
| `_shot-보관함나가는길-0902` | 임시보관함·편집(위/아래)·레시피 탭을 찍는다. **진짜 캡처 크기(1080×2340)를 달고** 찍어 「사진은 있는데 표지는 아이콘」이 눈에 보인다 |
| `_probe-그대로저장-0902` | 단추가 «그 줄 하나만» 옮기나 |
| `_probe-캡처창크기-0902` | 고정 창 글자 크기·보이는 비율·✕ 닫기가 뭘 없애나 |

---

## ⛔ 오늘 내가 밟은 것 — 다음에 안 밟으려고 적는다

1. **치환이 «조용히» 새서** `COVER_V` 블록이 파일에 안 들어갔고 **앱이 통째로 죽었다**(`ReferenceError`).
   → 그 뒤로는 **Edit 툴**(못 찾으면 스스로 에러)로 넣고 `grep` 으로 **눈으로** 확인한다.
2. **규칙 12 하다 `git checkout` 이 안 커밋된 고침을 통째로 날렸다** — 어제와 «같은 자리»다.
   → ⭐**규칙 12 «전»에 먼저 커밋한다.** 이건 규칙이 아니라 순서다.
3. **게이트 잣대를 「data: 그림이 있나」로 잡았다** — 빌드가 작은 아이콘 PNG 를 `data:` 로 인라인해서 **음식 아이콘까지 걸렸다**.
   → 「그 레시피의 «사진 그 자체»가 쓰였나」로 바꿨다(규칙 18 ⓘ).
4. **「사진 띄워놓겠다」를 지키는 줄이 «0» 이었다** — 게이트가 값을 **계산만 하고 판정을 안 했다.**
   → 📌 **「검사가 있다」와 「검사가 판정한다」는 다른 말이다.**

---

## ⛔ 안 하는 것
· `App.jsx:400` 자동 받기 `if (false)` **되살리기** — 재현 전엔 금지
· 홈 「최근 저장」에서 임시보관함 것 **빼기** — 창업자 판정 = *"홈화면에 계속 표시되니까"*
· 캡처 표지 판정을 **비율(세로로 길면)로 가르기** — 창업자가 «공유받은 사진은 다 아이콘»으로 골랐다

---
---

# 🌙 같은 날 «밤» 절 — 2026-09-02 21:00 ~ 23:30 (앱 갈래 · ⭐여기가 현행이다)

> 📮 창업자 = *"요리모드에 타이머도 펼친거 넣어주면 좋겠다"* → *"스토어 스샷+출시노트줘."*
> → *"aab올렸어"* → *"스샷 aab다 끝!"* → *"화사해졌어."* → *"복기 할일 정리해줘"*
>
> ⛔ 위 절(낮)은 **v12.22** 시점이다. 그 뒤 배포는 «바깥 갈래»가 이어갔고 지금 앱은 **v12.29** 다.

## ✅ 밤에 닫은 것

### 📦 AAB 19판 — 구웠고 창업자가 콘솔에 올렸다
| | |
|---|---|
| 링크 | https://github.com/peachfam0307-glitch/hankki/actions/runs/33632286924 |
| 아티팩트 | `hankki-aab-12.28-19` · 3.69MB · targetSdk 36 · 바탕 `653927cf` |
| 네이티브에서 바꾼 것 | **딱 한 줄** — `androidbrowserhelper 2.7.1 → 2.7.3` |

⭐ **2.7.3 이 «있는 판»인 건 실물로 확인됐다** — 없으면 Maven 이 못 찾아 빌드가 죽는다.
⚠️ **그게 콘솔 권장 조치 ①②를 고치는지는 ⛔아직 모른다.** 다음 vitals 가 답한다.
⛔ BILLING 안 들어감(`features: {}`) — 결제 스위치는 9월 판정 그대로.

### 📝 출시노트 AAB19 — **246자**/500
📄 `docs/출시노트-AAB19-2026-09-02.md` ｜ 🔖 복사 단추 판 = https://claude.ai/code/artifact/b2393718-e6a8-4215-8b99-c1805d0fb750
⛔ 「자동으로 동기화돼요」는 **일부러 뺐다** — 받는 쪽엔 안전장치가 걸려 있어 늘 저절로 되진 않는다
　 (`cloud.js:645`). **못 하는 걸 약속하지 않는다**(AAB18 의 「크게 보여요」와 같은 자리).

### 🏪 스토어 스샷 v7 — 8장 (창업자 판정 = *"화사해졌어."*)
📁 `design/promo/스토어스샷-2508/v7-01 … v7-08`

| # | 무엇이 바뀌었나 | 창업자 원문 |
|---|---|---|
| **01** | 「레시피 상세 재료」 → **「캡처하는 법」**(공유로 담기) | *"우리공유하는걸로 새로 만든거있잖아"* → *"1번 좋아"* |
| **02** | 씨앗 레시피 → **창업자가 «가을 프레임»으로 꾸민 여섯 편** | *"가을 프레임으로 꾸민거 있잖아.."* |
| **05** | **한 장에 두 화면** — 걸음 ＋ 타이머 펼친 것(15분) | *"요리모드에 타이머도 «펼친거» 넣어주면 좋겠다"* |
| **06** | 거의 텅 빈 달력 → **요리 6번 쌓인 달** | (내가 눈으로 잡았다) |
| **07** | 8/22 «여름» 파일 → **가을 아치 카드** | *"레꾸자랑도 가을꺼로하자 예쁜걸로"* → *"1번 좋아"* |

### 🐻🐧 한끼 친구들 ＋ 카와이가 남아 있던 여섯 자리 (⛔`hold/한끼친구들-0902` · 미배포)
· 프로필 픽커 = **정본 5인만** · 제목 **「한끼 친구들」**(`BUDDY_TITLE` 한 곳)
· 🚨 **파일도 코드도 안 지웠다 — 목록에서만 내렸다**(`BUDDY_GROUPS_내림`) → 이미 쓰던 사람은 그대로 보인다
· 🍜 카와이 **여섯** → 실사 : 프로필 기본 `fe_04`→`gr_343` ＋ **온보딩 다섯**
　 (`fe_15`·`fh_k27`·`fe_06`·`fe_09`·`fe_81` → `gr_014`·`gr_003`·`gr_349`·`fe_446`·`gr_232`)
· ⛔ 짝은 내가 안 골랐다 — 앱의 `ICON_RULES` 가 준 컷이다(절대원칙 30)

## 🔒 밤에 새로 박은 장치

| 무엇 | 하는 일 |
|---|---|
| `check-foodtab` **⑧** | **화면에 박아 둔 기본 아이콘이 픽커에 있나.** 🧪만들자마자 **온보딩 다섯을 진짜로 잡았다** |
| `_shot-프로필아이콘-0902` | 「한끼 친구들」 다섯만인가 · 기본 썸네일이 카와이 아닌가 — 눈과 숫자를 둘 다 남긴다 |
| `_shot-스토어용화면-0822` ⑧⑨ | ⑧`BACKUP=`＋`PICK=` 로 **창업자 요리책**을 찍는다 · ⑨`CARDS=` 로 레꾸자랑 카드 |

⭐⭐ **⑧이 값을 한 이유** = 2026-08-29 카와이 폐기 잣대(`store.jsx`)는 **레시피에 박힌 값**만 훑는다.
　 «화면 소스에 손으로 박은 값»은 **아무도 안 보고 있었다**(규칙 18 ⓘ). 그래서 여섯이 살아남았다.
　 그중 **다섯이 온보딩** = 갓 깐 사람이 제일 먼저 보는 화면이었다.

## ⛔ 밤에 내가 밟은 것

1. ⭐⭐ **요리책에 «어느 편을 넣나»를 내가 «맞히려» 했다 — 네 번 헛돌았다.**
   ①씨앗이 다시 심겨 창업자 것이 안 보임 ②「표지 사진 있으면」으로 골라 **안 꾸민 편이 맨 윗줄** ③「제일 꾸민 편으로 굴린다」로 **상단바가 사라짐** ④스티커 «개수»로 점수 → **진짜 가을 프레임(`pf_au04`)이 11번째로 밀림**
   📌 **개수는 「얼마나 붙였나」를 재지 「무엇으로 꾸몄나」를 못 잰다.**
   ✅ 맞히기를 그만두고 `PICK="제목,…"` 으로 **창업자가 고른 것을 그대로 받는다**(규칙 11).
2. **같은 제목이 백업에 «둘»** — 「가지덮밥」(7/27 맨몸 · 8/30 꾸민 것) → `find` 가 안 꾸민 쪽을 집었다.
   📌 **같은 이름이 하나뿐이라고 가정하지 않는다** — 유저는 같은 요리를 두 번 담는다.
3. **`globSync` 를 쓸 뻔했다** — Node 22 전용이고 **CI 는 Node 20**(2026-08-03 에 배포를 세 번 죽인 그것). 쓰기 전에 잡았다.
4. **온보딩 캡처가 클라우드 첫 화면을 못 지나갔다** — 「없다」가 아니라 «내가 못 갔다»였다(규칙 18).
5. **기존 파일 하나를 지웠다가 되살렸다**(`v7-01-정리-공심채.png`) — ⛔**파일은 안 지운다.**

## ⏳ 남은 것 — 위 낮 절 목록에서 «바뀐 것만»

### ✅ 낮 목록에서 닫힌 것
· **13번 AAB 19판 ＋ 스토어 스샷** → **끝**(위 참조)

### 🙋 창업자가 정해줘야 도는 것 (낮 것 그대로 + 밤에 생긴 둘)
| 순 | 할 일 | 지금 상태 |
|---|---|---|
| 1 | 📏 패드 가로 글줄 폭 580px | https://claude.ai/code/artifact/2d89b9b9-80f8-4f61-87ed-9d8703ffeea7 |
| 2 | ✍️ STEP 줄도 손글씨로 갈까 | 같은 판 ② |
| 3 | 🖼 그릇 프레임 판정 둘 | ⑴높이/접시 ⑵당김 |
| 4 | 🍜 음식 컷 셋 (팟타이·고추장제육볶음·닭갈비) | **창업자가 다시 뽑아야** 움직인다 |
| 5 | ❓ 「꼬르곰 잘린 거」 | ⛔어느 화면의 무엇인지 **한 줄**만 (규칙 25) |
| **14** | 🐻🐧 **`hold/한끼친구들-0902` 를 내보낼까** | 코드·게이트·판 다 됐다. **「배포해」만 기다린다**(규칙 9·13) |
| **15** | 🥤 **온보딩 칩 줄 셰이크(`gr_014`)가 컵이라 세로로 길다** | 옆 칩보다 작아 보인다 · 바꿀지는 미감 판정(규칙 11) |

### 🤖 클로드 몫 (낮 것 그대로)
| 순 | 할 일 |
|---|---|
| 6 | 🔁 ⓑ 자동 받기 — ⛔**재현이 먼저** · `App.jsx:400` `if (false)` 를 재현 전엔 안 되살린다 |
| 7 | 📜 붙여넣기 편은 원문이 맨 아래 (글자만 있는 편이 남았다) |
| 8 | 🔍 「크게 보기」 단추가 캡처 «글자»를 덮는다 |
| 9·10 | 🗓 오픈 요일 나누기(월 레시피·수 제철·금 장바구니) ＋ 🛒 장바구니 요일 — **한 몸** |
| 11 | 🎨 레꾸·일꾸 패널 시안 |
| 12 | 🧾 기본 인식(무료 OCR) 사용량 세기 |
| **16** | 📱 **인스타 홍보 영상** — 창업자 = *"우리 오늘은 바빠서 인스타 홍보 영상 못했네..."* |

### 🍳 검수 (절대원칙 28)
· **내일(9/3) 저절로 열리는 것 = 없음** (`--tomorrow` 로 확인 · 기억 아님)
· 🚨 **9/5(D-3) 에 주부의 장바구니 3개**가 저절로 열린다 — 도구 준비 끝:
　 `ON=2026-09-05 node scripts/_shot-장바구니열림-0902.mjs` (9/5 판정은 이미 9칸 통과)
· ⏳ 검수 안 받은 레시피 **17편**이 7번에 걸쳐 · 제일 가까운 게 **2026-11-02(D-61)**

## 📊 인스타 실적 (창업자 캡처 2026-09-02 · 30일)
조회 **812** · 반응 33 · 릴스 조회 **777** · **소개 링크 누름 35** · 프로필 방문 17
⭐ **링크 누름(35)이 프로필 방문(17)보다 많다** = 릴스에서 프로필을 안 거치고 바로 링크로 간다.
📈 8/29 부터 확 뛴다(최고 276) — 그전 4주는 거의 0.
⚠️ Play 콘솔(8/2~8/29)은 방문자 52·클릭률 46% 인데 **v7 스샷은 아직 저 그래프에 없다.**
　 👉 다시 볼 때 움직이는 건 **클릭률**이다(방문자가 아니라). **46% 가 기준선.**

## 🌿 갈래 상태 (전부 원격에 있다 · 잃은 것 0)
| 갈래 | 무엇 | 배포 |
|---|---|---|
| `hold/한끼친구들-0902` | 픽커 5인 ＋ 카와이 여섯 ＋ 게이트 ⑧ | ⛔대기 |
| `hold/스토어스샷-0902` | v7 8장 ＋ 찍는 판 ＋ 출시노트 | ⛔대기(앱 화면 안 바뀜) |
| `hold/aab19-0902` | `build-aab.yml` 2.7.3 | ⛔대기 |
| `hold/장바구니검수-0902` | 주간 장바구니 검수판 도구 | ⛔대기 |
