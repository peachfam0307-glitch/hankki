# 💰 결제 — 지금 어디까지 돼 있나 (2026-08-07 확인)

> 창업자 2026-08-07 *"우리 통신판매업증 나와서 이제 결제시스템 만들어야해.
> 출시하면 ai20장 사는건 바로 결제가능하게 넣어둬야 하니까."*
>
> 📖 **켜는 날 순서는 `docs/결제-켤때-읽을것-2026-08-05.md`** — 이 문서는 그 «앞 단계»,
> 즉 **「지금 무엇이 있고 무엇이 없나」**를 코드로 확인한 기록이다.
> ⛔ 두 문서를 헷갈리지 말 것. 저건 «그날 할 일», 이건 «지금 상태».

---

## 0️⃣ 한 줄

**코드는 이미 다 있다. 남은 건 전부 「Play Console 에서 사람이 하는 일」이고, 그건 재신청(8/16) 뒤다.**

---

## 1️⃣ ✅ 있는 것 (2026-08-07 코드로 직접 확인)

| | 확인 방법 | 상태 |
|---|---|---|
| `src/billing.js` — Digital Goods API · 구매 · 복원 | `wc -l` = **214줄** | ✅ |
| 상품 ID 표 (`SKU`) — 꾸미기팩 5 ＋ **`ocr_pack_20`** | 36~59줄 | ✅ |
| **소모성 / 영구 구분** | 33~48줄 주석 | ✅ |
| `scripts/test-billing.mjs` — 가짜 Play 로 구매 흐름 재현 | `package.json` smoke 체인에 있음 | ✅ **배포 게이트** |
| 서랍 자물쇠 · 팩 창 · 계절 게이트 · 유료 자산 192컷 | `check-packmeta`·`check-packleak` 통과 | ✅ |
| 판매 스위치 `sellable` | `paidPacks.js` 5곳 **전부 `false`** | ⏸ 일부러 |

⭐ **켤 때 코드에서 할 일 = `sellable: true` 한 줄.**

### 🔑 소모성 / 영구를 어떻게 가르나 (`billing.js` 33~48줄)

Play 는 상품에 그 표시가 «없다». **`consume()` 을 부르냐로 갈린다.**

| | 부르나 | 뜻 |
|---|---|---|
| 🎨 꾸미기 팩 5개 | ⛔ 안 부름 | **영구** — 한 번 사면 계속 내 것 |
| 📷 **`ocr_pack_20`** (창업자가 말한 ai 20장) | ✅ 부름 | **소모성** — 다 쓰면 또 산다 |

---

## 2️⃣ ✅ 프로덕션 «전»에 인앱 상품 등록 — 된다

전에 확인한 공식 원문 =
> *"Before setting up your in-app products and subscriptions, you must upload your APK or
> Android App Bundle (AAB) to a **production or testing track** and add the BILLING permission to your app."*

**우리는 비공개 테스트 트랙(「비공개 테스트 - 베타테스트」)에 versionCode 12 (9.79) 가 이미 올라가 있다**
→ 트랙 조건은 **이미 충족**.

⚠️⚠️ **2026-08-07 에 이 페이지를 «다시 열지 못했다»** — `support.google.com` 이 이 환경에서 **403**이다
(CLAUDE.md 에 이미 적혀 있는 제약). **전에 읽은 원문 기록으로 쓰는 것**이고,
콘솔에서 「인앱 상품」 메뉴를 열어보면 그 자리에서 닫힌다. ⛔짐작으로 굳히지 말 것.

---

## 3️⃣ ⛔ 없는 것 = AAB 에 BILLING 권한

`android/twa-manifest.json` **29줄** 실물:
```
"features": {},
"alphaDependencies": { "enabled": false }
```

**둘 다 비어 있다.** 켜려면:
```
"features": { "playBilling": { "enabled": true } },
"alphaDependencies": { "enabled": true }
```
→ **AAB 를 다시 구워야 한다.**

---

## 4️⃣ ⚠️⚠️ 판단이 필요한 갈림길 — BILLING 을 «언제» 넣나

### 왜 갈림길인가

BILLING 권한이 들어가면 스토어에 **「인앱 구매 제공」 라벨**이 붙을 수 있다.
그런데 CLAUDE.md 규칙은 **「결제 켜는 날 넷을 같이」** 다 —
콘텐츠 등급 재작성 · 데이터 보안 재신고 · `privacy.html` · Play 인앱구매 표시.
**하나라도 빠지면 신고 위반.**

### 🔎 찾아본 것 (2026-08-07)

라벨은 **「BILLING 권한 ＋ 등록된 상품」이 둘 다 있어야** 뜬다는 설명이 여러 곳에 나온다
(Play 도움말 `answer/1061913` · mobiloud 등). 상품을 안 만들면 권한만으로는 **안 뜰 가능성이 크다.**

⛔⛔ **공식 원문으로 «확정»은 못 했다** — 403. **모르는 것을 모른다고 적는다.**

### 갈림길 둘

| | 뭘 하나 | 위험 | 얻는 것 |
|---|---|---|---|
| **ⓐ 안전** | 8/13~14 AAB(versionCode 13)는 **지금 그대로** · BILLING 은 **재심사 승인 뒤** 다음 AAB 에 | **0** | 결제는 9월에 켜기로 이미 정했으니 **늦는 게 없다** |
| ⓑ 빠름 | 8/13~14 AAB 에 BILLING 을 미리 넣는다 | 라벨이 뜨면 넷을 못 맞춰 **위반** · 재심사에 영향 | AAB 한 판 아낌 |

### ⭐ 추천 = **ⓐ**

2026-08-05 에 창업자가 이미 정했다 — *"8월에 유저 들어오는거 보면서 유료를 9월부터 할지 말지 정하자"*.
**급할 이유가 없다.** ⓑ로 아끼는 건 AAB 한 판인데, 잃을 수 있는 건 **2주**다(8/2 반려가 정확히 그랬다).

### ✅✅ **[2026-08-08 창업자 확정] = ⓐ** (창업자 *"a가자"*)

⛔ **그래서 8/13~14 AAB(versionCode 13)에는 BILLING 을 «안» 넣는다.**
`android/twa-manifest.json` 29줄은 **지금 그대로** 둔다 —
```
"features": {},
"alphaDependencies": { "enabled": false }
```
⛔ **재심사(8/16) 전에 이 두 줄을 건드리지 말 것.** 라벨이 뜨면 「넷을 같이」를 못 맞춰 위반이 된다.
✅ BILLING 은 **재심사 승인 뒤 다음 AAB** 에 넣는다 → 순서는 아래 5️⃣ 그대로.
📌 **코드는 이미 다 있다**(`billing.js` 214줄 · 검사 배포 게이트) — **기다리는 것뿐이지 늦는 게 아니다.**

---

## 5️⃣ 재심사(8/16) 뒤 콘솔 4단계

1. **결제 프로필** — 사업자정보 ＋ 입금계좌 ＋ **전자상거래 라이선스 번호**
   👉 `제 2026-경북포항-0665호` — ⚠️**한 글자만 달라도 막힌다. 복사해서 넣을 것.**
2. **인앱 상품 6개 등록** — 꾸미기팩 5 ＋ `ocr_pack_20` 1
   ⚠️ 상품 ID 를 **코드와 «똑같은 글자»**로. 다르면 앱이 상품을 못 찾는다.
3. **BILLING 넣은 AAB** 굽고 올리기 (위 3️⃣ 두 줄)
4. **라이선스 테스트**에 본인 계정 등록 → 폰에서 실제로 눌러본다
   ⭐ **신청·심사·비용 0** — 구글이 주는 샌드박스다(토스는 이걸 받으려고 3개월 심사를 통과해야 했다)

---

## 6️⃣ ⏳ 아직 못 정한 것 둘

### ① 990원에 뭘 담나
전문 = `docs/결제-켤때-읽을것-2026-08-05.md` **3️⃣**. **그날 고른다.**

### ② ⛓ 산 장수를 어디에 두나 — **#63 이메일과 한 몸**
지금은 **기기마다 다른 임의 번호**로 세고 있다(`src/ocr.js` `deviceId()`).
→ **폰을 바꾸거나 앱을 지웠다 깔면 산 게 그냥 사라진다.**

⛔ **CLAUDE.md 규칙**: *"결제 코드에 「구매 → 복원」이 한 흐름으로 들어가기 전엔 실결제 스위치를 켜지 않는다."*

⭐ **인앱결제로 정하면서 이게 절반은 풀렸다** — 구글 계정이 구매를 기억하므로
**꾸미기팩(영구)은 저절로 복원된다.** 남은 건 **소모성인 `ocr_pack_20` 의 「남은 장수」** 하나다.

---

## 7️⃣ 📌 이 문서를 다시 열 때

- **8/16 재신청 뒤** → 5️⃣ 순서대로
- **결제 스위치 켜는 날** → `docs/결제-켤때-읽을것-2026-08-05.md` 로 넘어간다
- ⛔ **그 전엔 결제 프로필을 안 만든다** — **지금 필요가 없어서**다(화면을 «보고 캡처»만 하는 건 무관)
  ⛔⛔ **[2026-08-09 정정] 여기 적혀 있던 *"재심사가 흔들린다"* 는 근거가 없었다.**
  창업자가 *"근데 왜 콘솔건드리면 안돼? 근거가 있어?"* 라고 물어 저장소를 뒤졌더니 **출처가 0** — 내가 쓴 짐작이었다.
  ✅ 근거 있는 금지는 **BILLING 권한 하나뿐**(「인앱 구매 제공」 라벨 → 「넷을 같이」를 못 맞춰 위반).
  📌 **결론(재심사 뒤에)은 그대로 두되 «이유»를 바꿔 적는다** — 위험해서가 아니라 할 이유가 없어서.
  ⚠️ 이 줄을 **다른 일을 막는 근거로 쓰지 말 것.** 2026-08-09 에 가로모드 결정에서 실제로 그렇게 잘못 썼다.
