# ✂️🗄 자르기 안내가 못 뜨던 구멍 · 아이콘 서랍에 숨어 있던 컷 27개 (2026-09-15)

> 배포 = **v13.51** · run **2515** `conclusion: success` (head_sha `0c045778`)
> ⛔ 이 문서는 「고쳤다」가 아니라 **「왜 어제 고쳤다고 말했는데 안 됐나」**를 남긴다.

---

## 1. 🚨 창업자 제보 — 어제 다 했다는 게 안 되고 있었다

📮 *"너 이거 너무 고질병인데 음식아이콘 계속 저렇게 뜨는거랑 ai 우리 어제 다 고치고
　 네가 반영다 했다 했는데 왜 자꾸 저러는거야? 배포한거 맞아?"*
📮 *"자르기안내도 없어 어제 우리 문구까지 다 정했잖아"* · *"위에 빨간 얄약도 안띄우기로 했었고"*

### 먼저 «배포가 됐나»부터 실물로 봤다 — 됐다

| run | 커밋 | 결과 |
|---|---|---|
| 2501 | v13.47 | ⚠️ cancelled |
| 2503 · 2506 | 그 뒤 커밋들 | ✅ success |

⭐ v13.47 자체 배포는 취소됐지만 **그 뒤 커밋이 v13.47 코드를 품고 성공**했다 → 사이트엔 들어가 있었다.
＝ **「배포가 안 된」 게 아니라 「고쳤다는 게 실제로 안 먹는」 것이었다.**

---

## 2. 🌲🌲 뿌리 — 두 결정이 부딪혔는데 «아무도 안 알려줬다»

```
2026-09-13  창업자 = "굳이 귀찮게 자를필요 없으니까"
            → 통째로 읽기는 «자르기 화면을 안 띄운다»   (EditorScreen.jsx)

2026-09-14  창업자 = "안내가 잘~~보이게"
            → 그 «자르기 화면 안»에 안내 두 갈래를 만들어 넣었다
```

⛔⛔ **글자는 들어갔는데 그 화면이 안 열려서 한 번도 못 떴다.**
　　 나는 「넣었다」로 보고했다. **«닿는지»를 안 봤다**(규칙 21).

⭐ 어제 게이트로도 못 잡았다 — **문구를 grep 하면 그때도 초록불**이었기 때문이다.
　 「있나」를 재는 검사는 이 병을 영영 못 잡는다.

### ✅ 고침 = 갈래를 «없앴다»

자르기 화면을 **언제나** 연다. 두 자리(고른 직후 · 다음 장)를 같은 잣대로 묶었다.
⛔ 다시 가르지 않는다 — 가르는 순간 또 한쪽이 못 뜬다.

| | 안내 (둘 다 이미 코드에 있었다) |
|---|---|
| 🔑 열쇠 | `CropSheet.jsx:54` 기본 hint 「글자는 다 남기고 사진 부분만 잘라내면 돼요」 |
| 🆓 무료 | `EditorScreen.jsx` 「제목 · 재료 · 만드는 법만 남겨주세요」 ＋ 살구색 ＋ 흐린 줄 |

---

## 3. 🔴 빨간 열쇠 알약 — 한 화면이 서로 «반대말»을 했다

```
위 띠     :  초안을 채웠어요 · 열쇠 안 쓰고 읽어서 덜 정확해요
아래 상자 :  사진 1장에 레시피열쇠 1개를 써요        ← ⛔
```

⛔ 뿌리 = 그 줄에 **조건이 한 개도 없었다.** 어느 길로 오든 무조건 떴다.
✅ 고침 = `ocrNoVision`(어느 단추로 왔나) ＋ `무료판`·`freeRead`(읽고 나서 사실로 안 것) 셋 다 본다.
　　 앞엣것은 읽기 «전»에도 알고, 뒤엣것은 열쇠가 0이라 무료가 된 경우를 잡는다.

---

## 4. 🗄🗄 [역순] 창업자가 준 컷 27개가 «숨어» 있었다

📮 창업자 = *"역순으로 음식 들어가는지도 확인해봐"*

🔢 실측 — 음식컷 **35개** 중 **27개가 아이콘 고르기 서랍에 없었다.**

| 길 | 됐나 |
|---|---|
| 검색창에 이름 치기 | ✅ 35개 다 나왔다 |
| 손으로 훑어 내리기 | ⛔ **27개가 목록에 아예 없었다** |

⭐ 빈 접시와 **다른 병**이다 — 빈 접시는 「안 뜨는 것」, 이건 **「숨어 있는 것」**.
　 유저는 그 그림이 있는 줄도 모른다. 그래서 «조용히» 손해다.

### ⚠️ 창업자가 먼저 막았다 — 카와이인지부터

📮 창업자 = *"정확히 봐 혹시 카와이컷이 아닌지도"*
🔢 확인 = `store.jsx` 카와이 목록 **432개에 `n3` 는 한 개도 없다.** ＋ 컨택트시트로 눈으로도 봤다(전부 실사 그릇컷).
⭐ **묻지 않고 넣었으면 폐기 대상을 목록에 올릴 뻔했다.**

### 넣은 자리 (창업자 판정 — *"가지오믈렛은 양식, 솥밥은 밥. 다른건 다 맞아"*)

밥 4 · 국·탕·찌개 3 · 구이·튀김·전 7 · 볶음·조림·찜 10 · 반찬·나물·김치 5 · 포케·샐러드 2 · 양식 2 · 일식 2
⛔ **서랍 수는 24개 그대로** — 서랍을 만들지도 없애지도 않았다. 그림만 넣었다.

---

## 5. 🌽 스모크 빨간불 — 검사가 «옳았고» 자리가 틀렸다

`check-weeklypick` 이 2027-01-19 주를 「갈래 2가지로 뭉쳤다」고 잡았다.
뽑힌 넷 = 김치·절임 2(단무지 · **스위트콘**) ＋ 간편식 2.

⭐ **옥수수는 절임이 아니라 채소다.** 갈래는 「무슨 물건인가」지 「담은 꼴」이 아니다.
　 옥수수 둘(한살림 병조림 · 발투센 스위트콘)을 「김치·절임」 → 「버섯·채소」로 옮겼다.
⛔ **잣대는 한 글자도 안 넓혔다** — 그러면 창업자가 말한 「섞어서」(2026-08-28)를 버리는 것이다.
🔢 제품 131개 그대로. 하나도 안 잃고 자리만 옮겼다.

---

## 6. 🔒 게이트 둘을 새로 만들었다

### `scripts/_repro-자르기안내-0915.mjs`
**「문구가 있나」가 아니라 「거기에 닿나」**를 잰다.
① 자르기 화면이 열리나 ② 안내 두 갈래가 살아 있나 ③ 빨간 알약이 무료면 숨나 ④ 조건이 닿는 자리인가

### `scripts/check-iconpicker.mjs`
음식컷이 서랍에 다 들어갔나. **만들자마자 3개를 더 잡았다**(n3023 매운소갈비찜 · n3034·n3035 브리또 둘).
＝ 내가 24개만 보고 있었는데 실제로는 27개였다. **게이트가 제 일을 했다.**

⭐ 컷 하나를 살리려면 «네 곳»에 적어야 한다 — ①파일 ②비율표 ③이름표 ④서랍.
　 ①②는 `check-photoratio` 가 있었고 **④만 맨몸이었다.**

---

## 7. 📋 어제(9/14) 것 중 «안 된 것»이 또 있나 — 전수로 봤다

📮 창업자 = *"배포하기 전에 다른거 어제한 것중에 반영안된거 있나 확인"*

| 어제 정한 것 | 코드 |
|---|---|
| 무료면 「AI로 다시 다듬기」 단추 없음 | ✅ `EditorScreen` 2곳 |
| 무료엔 「AI가 더 다듬는 중」 꼬리 안 붙음 | ✅ `:648` |
| 임시보관함도 무료면 AI 얘기 0줄 | ✅ `InboxScreen:41-42` |
| 단위 칩 파란 알약(시안 B) | ✅ `:875` |
| 메모 죽은칸 게이트 | ✅ `check-deadfield.mjs` |
| 나라 기본값 「기타」 | ✅ `종류.js:28` |
| 폴더 줄에서 나라 걸러냄 | ✅ `:1362` |
| 소스 폴더 ＋ 마이그레이션 | ✅ `store.jsx:891` |

⭐⭐ **여덟 가지는 다 살아 있었고 «자르기 안내» 하나만 안 됐다.**
　 왜 그것만 빠졌나 = 여덟은 전부 **「코드가 조건을 바꾸는 것」**이라 넣으면 바로 먹는데,
　 자르기 안내만 **「그 화면이 열려야」** 먹는 것이었다. **도달을 안 재서 못 잡았다.**

---

## 8. ⚠️ 배포가 여섯 판 연속 취소됐다 — 세션 셋이 한 갈래에 몰려서

run 2509~2514 **전부 `cancelled`**. `concurrency: cancel-in-progress` 라 새 푸시가 앞 배포를 죽인다.
⛔ **고장이 아니라 낡은 판이 나가는 걸 막는 규칙**이다.

📮 창업자 = *"다른 세션들 그만 푸시하라고 해"*
⛔ **내가 못 보냈다** — 그 세션들은 클라우드에 따로 떠 있어 `ListAgents` 에 안 잡히고,
　 이 환경엔 세션에 말 거는 도구가 없다. **창업자가 직접 멈춰 줬다.**
✅ 그 뒤 run **2515 가 success**.

📌 다음에 또 이러면 = 창업자에게 「그 세션 창에 «초록불 뜰 때까지 푸시하지 마, hold/ 에 담아둬» 한 줄」.

---

## 9. 🔬 검증

| | |
|---|---|
| 빌드 | exit 0 |
| 전체 스모크 | **206/206** ✅ |
| 설계 관문 | 통과 (3각도 다 «고칠 것» 나옴) |
| 빈 접시 | `check-photoratio` **0** |
| 배포 | run 2515 `success` · 배포된 sha 를 열어 고침 넷 다 확인 |

### ⛔ 거짓 초록불을 밟았다가 잡았다
첫 스모크가 `exit 0` 으로 보였는데 그건 **`| tail -8` 의 exit code** 였다.
실제로는 `check-weeklypick` 이 죽어 있었다. **파이프 뒤의 0 을 믿으면 안 된다.**

### ⛔ 재현판 둘이 빨간불 — «검사부터» 고치지 않았다
`_repro-캡처흐름-0819` · `_repro-무료읽기-0829` 가 9/13 기준이라 죽었다.
기대값만 무르지 않고 **자르기를 실제로 눌러서 읽기까지 가는지**를 재게 고쳤다 → 둘 다 초록불.
＝ 흐름이 진짜 산다는 증거다.
⭐ `사진넣고자르기` 는 **이름은 「넣고자르기」인데 속이 비어 있었다**(9/13 에 비웠다). 이름대로 되돌렸다.

---

## 10. 📌 배운 것 (다음 세션에게)

1. ⛔ **「넣었다」와 「닿는다」는 다른 말이다.** 문구를 넣었으면 **그 화면이 열리는지**까지 봐야 한다.
2. ⛔ **결정이 부딪히면 아무도 안 알려준다.** 어제 결정이 그제 결정에 막히는 일은 또 난다.
3. ⭐ **창업자의 「혹시 ~ 아닌지도」를 흘리지 마라.** 카와이 확인이 폐기 대상을 목록에 올리는 걸 막았다.
4. ⛔ **파이프 뒤 exit code 를 믿지 마라.**
5. ⭐ **검사가 빨간불이면 검사가 옳을 가능성을 먼저 본다.** 옥수수가 그랬다.
