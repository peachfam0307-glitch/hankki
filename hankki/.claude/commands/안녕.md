---
description: 새 세션 인사(＝「부팅해」) — HANDOVER.md·CLAUDE.md·docs 자동으로 읽고 실제 파일로 검증한 뒤 여섯 가지 브리핑
---
# /안녕   (창업자가 **「부팅해」** 라고 해도 이것이다)
새 대화/세션 시작 인사. **창업자가 시키기 전에 먼저 문서부터 자동으로 읽고** 현재 상황을 파악한 뒤 반갑게 브리핑한다. (`/시작`·`/docs읽고시작` 의 읽기 + 인사말. 목적 = 매번 상황을 다시 묻거나 헤매지 않게.)

## ⛔⛔⛔ [절대원칙] **최근 문서부터. 옛 문서와 «섞지» 않는다.** (창업자 2026-08-13)

> 📮 창업자 원문: *"내가 안녕돌리면 **전날 혹은 최근문서부터 읽고 브리핑**하기로 했는데,
> **예전문서 읽어와서 짬뽕만들고있어.** 이것도 규칙으로 박아둬."*

⛔ **뿌리 = 이 아래 「읽을 문서」가 «손으로 적은 이름 목록»이었다.** 그래서 —
  ⓐ **날짜도 순서도 없다** → 7/21 문서와 8/13 문서를 «같은 무게»로 읽고 섞는다(＝짬뽕)
  ⓑ **목록 자체가 낡는다** → 실제로 여기 *"음식88·도구94"* 라고 적혀 있었는데 진짜는 **236컷**이다.
     **읽으라고 준 목록이 이미 틀린 값을 담고 있었다.**
📌 **목록을 사람이 관리하면 반드시 낡는다.** 그래서 «날짜와 git 이 정하게» 바꿨다.

## 하는 일 (순서대로, 자동)

**0-a. 🤝 «제일 먼저» 인수인계 파일을 읽는다 — `HANDOVER.md`** (창업자 확정 2026-08-23)
```
cat HANDOVER.md          # 저장소 맨 바깥 — 「지금 이어서 할 것」만 담겨 있다
```
⭐ **[2026-09-03] 이 파일은 「기억」만 담는다 — 세션이 «한 일»은 여기 없다.**
   `## 🗂 지난 세션 기록` 에 링크가 있고, **필요할 때만** 연다(⛔무턱대고 다 열지 말 것).
   📮 왜 = 창업자 *"프롬프트창은 왜저렇게 빨리차는거야"* → 이 파일이 11일 만에 9KB→301KB 였다.
⭐ 여기에 **「왜 하는 작업인지 / 전에 무슨 작업을 했는지 / 앞으로 뭘 해야 하는지 /
   하면 안 되는 작업 / 바로 다음 행동 / 아직 확인이 필요한 것」** 여섯이 들어 있다.
⛔ **다만 이 파일도 낡을 수 있다 — 아래 0-b 로 «실제 파일»과 대조한다.**
   갈리면 **실제 파일이 맞다.** 고치고, **무엇이 달랐는지 창업자에게 알린다.**
⛔ **확인하지 않은 일을 「끝났다」로 말하지 않는다.**
📌 전문·규칙 = `hankki/CLAUDE.md` 맨 아래 「🤝🤝 새 세션 인수인계」 절

**0-b. 🔍 실제 파일로 짧게 검증한다** (오래 걸리지 않는다)
```
cat hankki/src/version.js | grep APP_VERSION            # 진짜 앱 버전
git status --short && git log --oneline -3              # 안 커밋된 것 · 최근 커밋
git branch -r | grep hold/ | tail -5                    # 아직 안 나간 갈래
node hankki/scripts/doc-guard.mjs --gen hankki/docs/로드맵-우선순위.md   # ⭐현행 세대 줄번호
node hankki/scripts/check-docsize.mjs                    # 📏 문서가 대화 창을 먹고 있나
```
⭐⭐ **`check-docsize` 가 ⚠️·⛔ 를 뱉으면 «그 일보다 이것부터»** (창업자 2026-09-03
   *"한 세션이 계속 정리안하면 다른세션이 정리한다거나 브레이크를 건다거나"*)
   → `node hankki/scripts/doc-trim.mjs` 로 보고, `--옮김` 으로 덜어낸 뒤 일을 시작한다.
   📌 **앞 세션이 안 치우고 나갔으면 이 세션이 치운다.** 그래야 다음 세션이 대화를 열 수 있다.

**0-c. ⭐⭐ 그다음 이 한 줄을 돌린다 — 읽을 것과 «순서»가 나온다**
```
node hankki/scripts/hello-read.mjs
```
그 출력이 **① 어제~오늘 손댄 문서 → ② 주제별 최신 «찾는 표» → ③ 날짜 없는 상시 문서 → ④ 숫자는 돌려서**
순으로 준다. **위가 아래를 이긴다** — 최근 것이 옛것을 덮는다.

1. **그 목록 «순서대로» 읽는다.** ⛔목록에 «없는» 날짜 문서는 브리핑 근거로 쓰지 않는다.
   - 세대가 쌓인 문서는 **⭐현행 줄번호부터** 읽는다(목록이 줄번호를 같이 준다).
   - 옛 판을 굳이 인용해야 하면 **「옛 판이다」라고 밝히고** 쓴다.
   - ⛔ **개수·버전·현황을 문서에서 «읽어» 말하지 말 것** — `npm run assets` 처럼 **돌려서** 얻는다(④).
   - ⚖️ **저작권/상표 얘기가 나오면** 그때 열기: `docs/저작권상표-상담기록-전체-2026-07-21.md`(창업자가 준 원본 43건)
     ＋ `docs/_archive/작업복기/작업복기-2026-07-21-상표-캐릭터확정.md`(확정 결론) ＋ `docs/상표-변리사상담-회의록-2026-07-21.md`
     ⭐ 이 셋은 **날짜가 옛것이어도 「그때 있었던 일의 기록」**이라 안 낡는다 — 「짬뽕」 금지와 무관하다.
   - 🎨 **스티커·이모지 자산은** `docs/stickers/README.md`(⭐마스터 인덱스). **창업자한테 다시 묻지 말 것.**
     ⛔ 단 **개수는 거기서 읽지 말고 `npm run assets`** 로 — 그 문서의 숫자가 낡아서 이 사고가 났다.
2. **전체 지형 훑기**: `docs/` 폴더 + `.claude/commands/` 파일 **목록 전부** 확인(새 기록·명령어 놓치지 않게). 관련되면 그때 열어봄.
3. **반갑게 브리핑** (짧게) — ⭐**여섯 가지를 «다» 말한다**(창업자 확정 2026-08-23)
   ① **왜 하는 작업인지** ② **전에 무슨 작업을 했는지** ③ **앞으로 뭘 해야 하는지**
   ④ **하면 안 되는 작업** ⑤ **바로 다음 행동** ⑥ **아직 확인이 필요한 것**
   ⛔ 0-b 에서 `HANDOVER.md` 와 실제 파일이 갈렸으면 **그 차이도 같이 말한다.**
4. "이거부터 할까?" 제안. **작업은 창업자 확인 후** (파악·보고 먼저).
   ⭐ **다른 지시가 없으면 `HANDOVER.md` 의 「바로 다음 행동」부터 이어서 한다.**

## ⛔⛔⛔ [절대원칙] 「새 대화 시작해」 소리 금지 (창업자 2026-08-16)

> 📮 창업자 원문: *"자꾸 새대화하라했다고 한다하면 괜찮다고 그냥하자하고 대화하다가 또 그러고
> **짜증나려고해 이유가 뭐야? 방법을 제시하라고**"*

⛔ **대화가 길어져 「곧 요약된다」는 시스템 알림이 와도 창업자에게 «옮기지 않는다».**
   대화는 **안 끊긴다** — 앞부분이 저절로 요약될 뿐이고 중요한 건 전부 저장소 문서에 있다.
   즉 **아무 행동도 필요 없는 알림**이다. 옮기는 순간 창업자 일이 된다.
✅ **대신 조용히** — ⑴확정된 것 커밋·푸시 ⑵이어받기 문서 갱신 ⑶**그냥 계속한다.**
⛔ 금지 = 「새 대화 시작해」·「새 채팅에서 하자」·「대화가 꽉 차서」·「여기서는 정확도가 떨어져」
✅ 창업자가 **먼저** 새 대화를 하겠다고 하면 그때만 **방법만** 알려준다(⛔말리지도, 권하지도 않는다).
📌 전문·이유 = `hankki/CLAUDE.md` **규칙 26** (규칙 14 「쉬어 금지」의 쌍둥이다)

## 원칙
- ⭐⭐ **[최우선] 선(先)리서치 → 후(後)제안. 창업자에게 소모적·반복적 일 안 시킴.** (창업자 명시 2026-07-22 — 어긴 사고 있었음)
  - **제안·작업 전에** 그 일을 **먼저 다 리서치**하고 → **일의 순서**를 정하고 → **꼭 필요한 최소 작업만** 요청한다. **"선(先)제안 후(後)리서치" 절대 금지.**
  - **소모적·반복적·시행착오 작업(같은 거 여러 번 뽑기·이거저거 골라보기)은 창업자에게 안 시킨다.** 반복·시행착오는 전부 클로드가(→ `CLAUDE.md` 규칙8). 창업자 = **완성본 판단만.**
  - 위반 사례(2026-07-22): 상표 리서치도 없이 곰부터 뽑게 하고, 상반신 대충 잘라 글씨 붙임 → 하루 낭비. (→ `docs/삽질-리스트-2026-07-17.md` C5)
  - **작업 전 이 항목 + `docs/창업자-니즈-일하는방식.md` + `docs/삽질-리스트-2026-07-17.md` 를 실제로 읽고 통과.** (문서에 규칙 있어도 안 읽으면 소용없음 = 이번 근본원인.)
- 🐻🐧 **캐릭터 이름 = 꼬르곰(곰)·펭펭(펭귄) — 꼭 풀네임으로.** (창업자 명시 2026-07-25, 프로모 영상 계기)
  - **결과물(영상·카드·공유 문구·스토어·문서 등)엔 반드시 "꼬르곰"·"펭펭" 풀네임.** "곰펭"은 **둘을 함께 부르는 애칭**일 뿐 개별 이름 아님 → 개별 지칭에 "곰펭" 쓰지 말 것. **"콩콩"은 이름이 아님**(펭펭의 콩콩 튀는 모션 표현) → 이름 자리에 절대 쓰지 말 것.
  - ⚠️ **앱 UI(라벨·버튼·설명·안내문)도 "결과물"** → 유저가 보는 글자엔 "곰펭" 금지, **꼬르곰·펭펭** 풀네임. (예: 프레임 그룹 라벨·랜덤카드 설명). 코드 짤 때 label/문구에 '곰펭' 쓰지 말 것.
  - ⚠️ 실수 이력: 프로모 영상 2번째 이미지(2026-07-25)·프레임 그룹 라벨 "곰펭 넣기"+랜덤카드 설명(2026-07-26) → 다 **꼬르곰·펭펭으로 수정 완료.** 반복 금지.
- 🎀 **[진행중] 2026-07-26 창업자 제공 신규 시트 12장 = 순차 반영.** `docs/stickers/신규-창업자-2508/원본시트/` + 인덱스 `docs/stickers/README.md` **A0-3**. 창업자 "선 반영, 꼭 저장, 적당히(업뎃할 거 남겨)". **v8.58 반영** = 컬러소품 4(무지개·꽃다발·풍선·케이크)+메모라벨 프레임+여름식재료. **미반영(다음 업뎃)** = 나머지 데코 6장·**태국베트남 음식 10종(item④)**·주방도구 3장·플랫도형(리컬러). 새 세션은 이 목록 이어서 반영.
- 반말·한국어, 따뜻하게. 비개발자 눈높이.
- 앱 버전은 `src/version.js`의 실제 값으로, D-day는 KST 기준으로.
- ⏰ **모든 날짜·시각 기준 = 한국시간(KST, UTC+9).** 대화·문서 작성일·D-day·"오늘/어제" 전부 KST로 판단. 컨테이너/git 시각이 UTC라 하루 어긋날 수 있으니 날짜 말할 땐 반드시 `TZ='Asia/Seoul'`로 확인하고 말할 것. (예: UTC가 07-21 밤이어도 KST가 07-22면 오늘은 22일.)
- 📄 **공문서 날짜 규칙**: 파생 문서(예: CROS 저작권 신청서)는 기준 문서(설정집·종합기록)보다 **날짜가 빠를 수 없음**. 캐릭터 문서 날짜와 공문서 날짜는 서로 맞추고, 공문서가 더 앞서지 않게 확인. 창작 타임라인(초안·정본·최종 날짜)은 수정내력이라 지우지 말고 보존.
- ⚖️ **저작권/상표 = 지어내기 절대 금지.** 위 상담 문서·확정본에 있는 사실만 인용. 문서에 없으면 "문서엔 없다"고 말하고 지어내지 말 것. **변리사 무료상담은 이미 완료** → "전화해보라"고 다시 미루지 말 것(창업자가 시킨 경우만). 확정 결론(고정): ①상표 = **한끼 + 복슬곰 결합 로고 1건** 먼저 출원(~4.6만) — "한끼"는 보통명사라 단독은 식별력 약하고, 곰이 붙어야 식별력 생겨 보호됨(펭펭·곰단독은 나중). ②지정상품 = `G390802` 소프트웨어 고시명칭, **"전자출판물/전자서적" 절대 금지**(G5206 함정). ③상표는 AI곰이어도 됨(식별력만). ④저작권은 "되면 보너스"(AI 판례 없음), 디자인권은 굿즈 때. 이 결론 다시 뒤집거나 새로 지어내지 말 것.
