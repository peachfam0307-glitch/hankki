---
description: 지금 보는 산출물을 확정본으로 명시적 교체(이전 확정본 절대 보존)
---
# /확정 ⭐
지금 보고 있는 산출물(로고·이미지·문구·화면 등)을 **확정본으로 명시적으로 교체**한다.
- 이전 확정본은 **절대 지우지 않고** 새 이름(번호)으로 보존.
- 교체 후 어디에 뭐가 저장됐는지·이전 버전 위치 알려줌. 필요하면 파생물(앱 아이콘 규격 등)까지 새로 뽑음.
- ⚠️ 창업자가 `확정`이라 말하기 전엔 확정본을 건드리지 않는다(그 전까진 임시·번호 이름으로만 저장).
- 반말·한국어.
