---
description: 버전 올림 → 빌드 → 커밋 → 푸시 (표준 출고 절차)
---
# /릴리즈 (배포)
버전 올림 → 빌드 → 커밋 → 푸시 (기능 완성 시 표준 출고 절차).
- `src/version.js` 의 APP_VERSION 숫자 올림 → `npm run build` → 커밋 → `claude/chatgpt-conversation-link-kvn5ph` 푸시.
- 커밋 규칙: user.email noreply@anthropic.com, user.name Claude, amend --reset-author. 모델ID 절대 넣지 않음.
- 반말·한국어.
