// 🍱 창업자 새 음식 아이콘 48컷을 앱에 넣는다 (2026-08-11)
//
// 📮 창업자 *"내가 한끼찾아보면서 새로 받았어. 이걸로 바꿔주면 되고"*
//
// ⭐⭐ 설계 = **겹치는 자리엔 규칙을 «안» 준다.**
//    이미 «전용 그림»이 있는 요리(갈비찜·양념게장·황태무침·고마다래)에 새 규칙을 얹으면
//    내가 창업자 판정 없이 그림을 갈아치우는 것이 된다(규칙 11 위반).
//    → 그런 컷은 **픽커에만** 올린다. 창업자가 검수판을 보고 「이걸로 바꿔」 하면 그때 규칙을 준다.
//
// ⭐ 반대로 **36편의 ⛔사진없음·🔁겹침을 푸는 컷은 규칙을 준다** — 그게 이 시트를 받은 이유다.
//    ICON_RULES 는 «순서대로 첫 매칭»이라 맨 위에 넣으면 옛 규칙보다 먼저 걸린다.
//
// ⛔ 못 찾으면 죽는다 — 조용히 넘어가면 「넣었다」고 착각한다.
import { readFileSync, writeFileSync, copyFileSync, existsSync } from 'node:fs'
import { execSync } from 'node:child_process'

const 뿌리 = new URL('../', import.meta.url).pathname
const 낱개 = `${뿌리}docs/stickers/음식아이콘-창업자-2026-08-11/낱개/`
const 사진 = `${뿌리}src/assets/stickers/photo/`

// [컷, 키, 이름, 그룹, 규칙낱말]  ⛔규칙낱말이 빈 배열 = 「픽커에만」(겹치는 자리)
const 표 = [
  ['n0101', 'fe_156', '버섯들깨무침', '반찬·나물·김치', ['버섯들깨무침', '버섯들깨']],
  ['n0102', 'fe_157', '감자전', '구이·튀김', ['감자전']],
  ['n0103', 'fe_158', '낙지볶음', '볶음·조림', ['낙지볶음']],
  ['n0104', 'fe_159', '낙지전골', '국·탕·찌개', ['낙지전골', '해물전골']],
  ['n0105', 'fe_160', '오징어조림', '볶음·조림', ['오징어조림']],
  ['n0106', 'fe_161', '깻잎찜', '반찬·나물·김치', ['깻잎찜']],
  ['n0201', 'fe_162', '브로콜리구이', '구이·튀김', ['브로콜리구이', '브로콜리볶음']],      // ⭐36편 ⛔사진없음
  ['n0202', 'fe_163', '오징어새우전', '구이·튀김', ['오징어새우전', '오징어새우', '해물전']], // ⭐36편 ⛔사진없음
  ['n0203', 'fe_164', '소세지볶음', '볶음·조림', ['소세지볶음', '소시지볶음']],
  ['n0204', 'fe_165', '피클', '반찬·나물·김치', ['피클']],
  ['n0205', 'fe_166', '콩나물국밥', '국·탕·찌개', ['콩나물국밥']],
  ['n0206', 'fe_167', '묵밥', '밥', ['묵밥']],
  ['n0301', 'fe_168', '뚝배기파스타', '양식', ['뚝배기파스타']],
  ['n0302', 'fe_169', '미나리볶음밥', '밥', ['미나리볶음밥']],
  ['n0303', 'fe_170', '참치야채덮밥', '밥', ['참치야채덮밥', '참치덮밥']],
  ['n0304', 'fe_171', '갈비찜(매운양념)', '볶음·조림', []],   // ⛔fh_k11 갈비찜과 겹침 → 창업자 판정
  ['n0305', 'fe_172', '삼겹살볶음', '볶음·조림', ['삼겹살볶음', '대패삼겹볶음']],
  ['n0306', 'fe_173', '소고기구이', '구이·튀김', ['소고기구이', '소고기스테이크']],
  ['n0401', 'fe_174', '간장양념장', '볶음·조림', ['간장양념장']],
  ['n0402', 'fe_175', '샐러드소스', '반찬·나물·김치', []],     // ⛔고마다래는 fe_128 이 이미 먹고 있다 → 판정
  ['n0403', 'fe_176', '찰밥', '밥', ['찰밥', '약밥']],
  ['n0404', 'fe_177', '양배추볶음', '볶음·조림', ['양배추볶음']],
  ['n0405', 'fe_178', '장어덮밥', '밥', ['데리야키장어덮밥', '장어덮밥']],
  ['n0406', 'fe_179', '냉채', '반찬·나물·김치', ['해파리냉채', '냉채']],
  ['n0501', 'fe_180', '목살스테이크', '구이·튀김', ['목살스테이크']],
  ['n0502', 'fe_181', '가지볶음', '볶음·조림', ['가지볶음']],
  ['n0503', 'fe_182', '가지덮밥', '밥', ['가지덮밥']],                                   // ⭐36편 🔁겹침(fe_63)
  ['n0504', 'fe_183', '굴매생이떡국', '국·탕·찌개', ['굴매생이떡국', '매생이떡국', '굴떡국']], // ⭐36편
  ['n0505', 'fe_184', '갈비찜(간장)', '볶음·조림', []],       // ⛔fh_k11 과 겹침 → 판정
  ['n0506', 'fe_185', '매운양념장', '볶음·조림', ['매운양념장', '고추장양념장', '초고추장']],
  ['n0601', 'fe_186', '딸기라떼', '빵·디저트·음료', ['딸기라떼', '딸기우유']],
  ['n0602', 'fe_187', '매운콩나물국밥', '국·탕·찌개', ['매운콩나물국밥']],
  ['n0603', 'fe_188', '봉골레파스타', '양식', ['봉골레']],
  ['n0604', 'fe_189', '매운콩나물덮밥', '밥', ['매콤콩나물덮밥', '매운콩나물덮밥', '콩나물덮밥']], // ⭐36편
  ['n0605', 'fe_190', '태국식 소고기 후추 야채볶음', '동남아', ['느어팟프릭타이담', '후추소고기볶음', '태국식소고기']],
  ['n0606', 'fe_191', '매운콩나물불고기', '볶음·조림', ['매운콩나물불고기', '콩나물불고기']],
  ['n0701', 'fe_192', '마늘쫑볶음', '볶음·조림', ['마늘쫑볶음', '마늘종볶음']],
  ['n0702', 'fe_193', '마늘쫑장아찌', '반찬·나물·김치', ['마늘쫑장아찌', '마늘종장아찌']],   // ⭐36편 🔁겹침(fe_95)
  ['n0703', 'fe_194', '깻잎장아찌', '반찬·나물·김치', ['깻잎장아찌']],
  ['n0704', 'fe_195', '계란장', '반찬·나물·김치', ['마약계란장', '계란장', '달걀장']],
  ['n0705', 'fe_196', '구움찰떡', '빵·디저트·음료', ['구움찰떡', '인절미구이']],
  // ⛔ '불고기' 는 안 준다 — 너무 넓어 「콩나물불고기」·「차돌불고기」까지 다 먹는다
  ['n0706', 'fe_197', '간장불고기', '볶음·조림', ['간장불고기']],
  ['n0801', 'fe_198', '삼겹살구이', '구이·튀김', ['삼겹살구이']],
  ['n0802', 'fe_199', '닭봉구이', '구이·튀김', ['닭봉구이', '닭봉', '윙구이']],
  ['n0803', 'fe_200', '양념게장(매운)', '반찬·나물·김치', []], // ⛔fe_76 양념게장이 이미 전용 → 판정
  ['n0804', 'fe_201', '황태무침', '반찬·나물·김치', []],       // ⛔fe_147 황태채무침과 겹침 → 판정
  ['n0805', 'fe_202', '충무김밥', '분식', ['충무김밥']],
  ['n0806', 'fe_203', '묵은지볶음', '볶음·조림', ['묵은지볶음', '묵은지들기름볶음']],
]

// ── ① PNG 옮기기 ＋ 비율 재기 ──
const 비율 = {}
for (const [컷, 키] of 표) {
  const 원 = `${낱개}${컷}.png`
  if (!existsSync(원)) throw new Error(`⛔ 낱개가 없다 — ${원}`)
  if (existsSync(`${사진}${키}.png`)) throw new Error(`⛔ 키가 이미 쓰이고 있다 — ${키}`)
  copyFileSync(원, `${사진}${키}.png`)
  const [w, h] = execSync(`python3 -c "from PIL import Image;im=Image.open('${사진}${키}.png');print(im.size[0],im.size[1])"`)
    .toString().trim().split(' ').map(Number)
  비율[키] = Math.round((w / h) * 10000) / 10000
}

// ── ② PHOTO_RATIO ──
const St = `${뿌리}src/components/Stickers.jsx`
let s = readFileSync(St, 'utf8')
const 닻 = '  fe_152: 1.087, fe_153: 1.0723, fe_154: 1.1308, fe_155: 1.182,'
if (!s.includes(닻)) throw new Error('⛔ PHOTO_RATIO 닻을 못 찾았다 — Stickers.jsx 모양이 바뀌었다')
const 줄 = []
for (let i = 0; i < 표.length; i += 4) {
  줄.push('  ' + 표.slice(i, i + 4).map(([, k]) => `${k}: ${비율[k]}`).join(', ') + ',')
}
s = s.replace(닻, `${닻}\n  // 🍱 2026-08-11 창업자 새 시트 8장 → 48컷\n${줄.join('\n')}`)
writeFileSync(St, s)

// ── ③ ICON_RULES (맨 위 — 순서대로 첫 매칭이라 옛 규칙보다 먼저 걸린다) ──
const Fi = `${뿌리}src/components/FoodIcon.jsx`
let f = readFileSync(Fi, 'utf8')
const 닻2 = 'const ICON_RULES = ['
if (!f.includes(닻2)) throw new Error('⛔ ICON_RULES 닻을 못 찾았다')
const 규칙줄 = 표.filter(([, , , , kw]) => kw.length)
  .map(([, k, , , kw]) => `  [[${kw.map((x) => `'${x}'`).join(', ')}], '${k}'],`)
const 머리 = [
  '  // 🍱🍱 2026-08-11 창업자 새 시트 8장 → 48컷. **구체 요리명이라 맨 위에 둔다.**',
  '  //   ⭐ 여기 있는 것은 «겹치지 않는» 자리다. 이미 전용 그림이 있던 넷',
  '  //      (갈비찜 `fe_171`·`fe_184` · 샐러드소스 `fe_175` · 양념게장 `fe_200` · 황태무침 `fe_201`) 은',
  '  //      **일부러 규칙을 안 줬다** — 내가 창업자 판정 없이 그림을 갈아치우면 안 된다(규칙 11).',
  '  //      픽커에는 올라가 있으니 창업자가 골라 쓸 수 있고, 「이걸로 바꿔」 하면 그때 규칙을 준다.',
]
f = f.replace(닻2, `${닻2}\n${머리.join('\n')}\n${규칙줄.join('\n')}`)

// ── ④ 픽커 그룹 ──
for (const 라벨 of [...new Set(표.map(([, , , g]) => g))]) {
  const 키들 = 표.filter(([, , , g]) => g === 라벨).map(([, k]) => k)
  const re = new RegExp(`(\\{ label: '${라벨.replace(/[.*+?^$()|[\]\\]/g, '\\$&')}', items: \\[)`)
  if (!re.test(f)) throw new Error(`⛔ 픽커 그룹을 못 찾았다 — ${라벨}`)
  f = f.replace(re, `$1${키들.map((k) => `'${k}', `).join('')}`)
}

// ── ⑤ 이름표 — 규칙 첫 낱말이 이름이 되지만, 그게 어색하거나 규칙이 없는 컷은 못 박는다 ──
const 닻3 = 'const EXTRA_NAMES = {'
if (!f.includes(닻3)) throw new Error('⛔ EXTRA_NAMES 닻을 못 찾았다')
const 이름줄 = []
for (let i = 0; i < 표.length; i += 3) {
  이름줄.push('  ' + 표.slice(i, i + 3).map(([, k, n]) => `${k}: '${n}'`).join(', ') + ',')
}
f = f.replace(닻3, `${닻3}\n  // 🍱 2026-08-11 새 시트 48컷 — 규칙 없는 컷은 이게 «유일한» 이름이다(없으면 픽커 라벨이 빈칸)\n${이름줄.join('\n')}`)
writeFileSync(Fi, f)

console.log(`✅ 48컷 반영 — 규칙 준 것 ${규칙줄.length} · 픽커만 ${48 - 규칙줄.length}`)
console.log(`   픽커만(창업자 판정 대기) = ${표.filter(([, , , , k]) => !k.length).map(([, k, n]) => `${k} ${n}`).join(' · ')}`)
