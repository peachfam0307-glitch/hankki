> 🗄 **보관소** — ⛔창업자가 지시할 때만 연다 (절대원칙 2026-08-13 · CLAUDE.md 규칙 24)

# 🧰 다 쓴 «그날치» 검사·재현·판만들기 스크립트

`scripts/` 가 임시 스크립트 **102개**로 불어나 진짜 게이트(`check-*.mjs`·`test-*.mjs`)가 묻혔다.
**그날 한 번 쓰고 끝난 것**을 여기로 내렸다 — `/청소` 2026-08-14.

## 무엇을 내렸나
- **아무도 안 부르는 것만.** `package.json`·`smoke.mjs`·다른 스크립트·`docs/`·`CLAUDE.md` 를 전수 검색해
  **참조가 0인 것**만 옮겼다. 한 군데라도 부르면 `scripts/` 에 남겼다.
- 대상 = `_*0811*` · `_*0812*` (이틀 넘게 지난 것) — **45개**

## ⛔ 지우지 않았다
재현판은 **「그때 어떻게 쟀나」의 증거**다. 같은 증상이 다시 오면 여기서 꺼내 쓴다.
⚠️ 단 **꺼내 쓰기 전에 오늘 코드에서 도는지 먼저 확인할 것** — 앱이 바뀌면 재현판도 낡는다.

## 남긴 것 (`scripts/` 에 그대로)
문서·게이트가 부르는 것 **15개** ＋ 8/13·8/14 것 — 아직 이번 주 일이라 손이 간다.
