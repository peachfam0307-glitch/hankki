// 🔖🔖 **핀은 두 종이다 — 여기 «한 곳»에서만 정한다** (창업자 확정 2026-09-08)
//
// 📮 창업자 = *"요리사모자는 해볼것, 하트는 최애 두 종으로 가자"*
//    ＋ *"종류서랍은(필터줄) 2줄로 가도 좋을 것 같아"*
//    ＋ *"윗줄은 해볼것, 최애, SNS 자주만든것/ 아래는 한식 양식 등등.."*
//
// ⛔⛔ **저장을 «넓히지» 않는다 — 이게 이 파일의 존재 이유다.**
//   `r.favorite` 는 **참/거짓 그대로** 둔다(「꽂혔나」). 종은 `r.favPin` 에 «곁에» 담는다.
//   🔢 실측 = `.favorite` 를 읽는 곳이 코드에 **12군데**(시드 병합 · 백업 · 설정 통계 · 상세 · 목록 · 해볼 것 화면).
//      참/거짓을 「어떤 핀인가」로 바꾸면 그 12군데가 «전부» 옛 판 `true` 를 못 읽는 순간이 생긴다.
//   ⭐⭐ 실패의 모양이 다르다(절대원칙 34) —
//      · 지금 실패 = 잘못 눌러 한 편이 빠진다 → 다시 누르면 복구
//      · 넓혔다 실패 = **몇 달 모은 핀이 통째로 사라진다** → 복구 불가
//      2026-08-04 「빈 표지」 사고와 같은 뿌리다 — **없는 값으로 덮으면 그건 지우는 것이다.**
//   ✅ 그래서 `favPin` 이 «없으면» 요리사모자다. 이미 깔린 폰은 **한 줄도 안 바뀌고 그대로 돈다.**
//
// ⭐ 이름을 여기 두는 이유는 `favName.js` 와 같다 — 이 자리는 벌써 두 번 이름이 바뀌었다
//   (즐겨찾기 → 책갈피 → 해볼 것). 한 곳만 고치면 말이 갈라진다.
import { FAV_NAME } from './favName.js'  // ⭐ 확장자를 붙인다 — 게이트(node)가 이 파일을 그대로 읽어 종 이름을 가져간다

/** 기본 종 — `favPin` 이 없는 옛 레시피는 «전부» 이것이다. ⛔ 값을 바꾸지 말 것(옛 폰이 갈린다). */
export const PIN_DEFAULT = 'chef'

// 🔖 종의 «뜻» — 색만 다른 핀은 유저가 뭘 꽂았는지 못 외운다(30편이면 30번 기억해야 하는 짐).
//   · 요리사모자 = **해볼 것**(아직 안 해봤지만 하고 싶은 것)
//   · 하트 = **최애**(이미 해봤고 좋았던 것)
//   ⏳ 하트 그림은 «아직 없다» — 창업자가 클립 컷을 새로 뽑는 중(2026-09-08).
//      ⛔ 그림이 오기 전엔 이 종이 화면에 «안 뜬다»(꽂힌 게 0개면 칩이 안 선다) — 반쪽짜리 UI 를 내보내지 않는다.
// 🚨🚨 **[2026-09-08 저녁 · 사고] 두 종을 «껐다».** 📮 창업자 = *"갑자기 요리사해놓은게 하트로 다바뀌고
//    하트누르니까 차례로 사라졌어"* ＋ *"내꺼도 다 날아갔어"* ＋ *"광고 올렸는데 큰일이네"*
//   🔎 재현으로 확인한 뿌리(`_repro-핀순환-옛핀-0908.mjs`) — **이미 꽂아둔 사람에게 탭의 뜻이 바뀌었다.**
//      · 옛날 = 한 번 누르면 «빠진다»  · 두 종 뒤 = 한 번 누르면 «하트로 바뀌고», 두 번째에 빠진다
//      → 몇 달 모은 모자 핀이 «한 번 눌러 하트», «두 번 눌러 실종»으로 보였다. 칩도 「해볼 것 0」이 됐다.
//   ⭐⭐ 실패의 모양이 잘못됐다(절대원칙 34) — 「잘못 눌러 한 편이 빠진다」로 봤는데,
//      실제로는 **손에 익은 동작이 통째로 다른 뜻이 되어 목록이 사라진 것처럼 보였다.**
//   ✅ 그래서 지금은 **한 종(요리사모자)으로 되돌린다** — 탭 = 꽂기/빼기(옛 습관 그대로).
//      ⛔ 데이터는 «안 지운다» — 하트로 바뀐 편은 `favPin:'heart'` 를 그대로 갖고 있고,
//         두 종을 다시 켜는 날 하트로 되살아난다. 빠진 편은 `store.jsx` 의 되살리기가 도로 꽂는다.
//   ⏳ 두 종은 «탭이 아닌 통로»(고르기 판 등)로 다시 설계해서 올린다 — 창업자와 정한 뒤.
// ✅✅ **[2026-09-08 밤 · 다시 켰다] 📮 창업자 = *"하트는 최애에 넣어야 하는데 왜 안눌리게 만들어놨어"*
//    ＋ *"순환으로 릴스까지 다 만들어서 지금 인스타 올렸는데 갑자기 니멋대로 끄고 안된다고 하면 어떻게해"*
//    ＋ *"니가 된다고 해서 한거잖아. 아니 고치고 해야지"* → **끄지 말고 고쳤다**(절대원칙 38 신설).
//   🔎 재현(`_repro-핀사라짐-0908.mjs`)으로 확정 — **순환은 멀쩡히 돌았다.**
//      진짜는 「해볼 것」을 걸고 누르면 그 편이 최애로 옮겨가 **보던 목록에서 즉시 빠지는 것**이었다.
//      → 유저 눈엔 「눌렀더니 없어졌다」. 하나씩 누르면 하나씩 없어진다.
//   ✅ 그래서 실패의 «모양»을 바꿨다(절대원칙 34) — 바꾼 편을 **그 자리에 남기고**, **무엇이 됐는지 말해준다**
//      (`MyRecipesScreen` 의 `방금바꾼` ＋ 토스트). ⛔둘은 한 쌍이다 — 하나만 하면 반쪽이다.
export const PIN_TWO_KINDS = true
export const FAV_PINS = PIN_TWO_KINDS
  ? [{ key: 'chef', name: FAV_NAME }, { key: 'heart', name: '최애' }]
  : [{ key: 'chef', name: FAV_NAME }]

const KEYS = new Set(FAV_PINS.map((p) => p.key))

/** 레시피 → 종 열쇠. 모르는 값·빈 값은 «전부» 기본 종이다(옛 폰 안전). */
export const pinOf = (r) => (KEYS.has(r?.favPin) ? r.favPin : PIN_DEFAULT)

/** 그 종으로 꽂혀 있나 — 「꽂혔나(favorite)」와 「어떤 종인가(favPin)」를 «둘 다» 본다. */
export const isPinned = (r, key) => !!r?.favorite && pinOf(r) === key

/** 유저에게 보이는 이름 */
export const pinName = (key) => FAV_PINS.find((p) => p.key === key)?.name || FAV_NAME

// 🔁🔁 **짧게 누르면 «돌아간다»** (창업자 확정 2026-09-08)
//   📮 창업자 = *"길게 눌러야 핀을 바꿀 수 있어?/"* → 내가 냈던 「길게 누르기」를 창업자가 짚었고 **그 지적이 맞다.**
//      ⛔ 길게 누르기는 **숨은 동작**이라 유저가 있는 줄 모른다. 2026-08-17 에 창업자가
//         *"번거로워서 한번도 안썼어"* 로 북마크를 상세 밖으로 뺀 그 정신과 어긋난다 — 안 보이는 건 안 쓰인다.
//   📮 ＋ *"지금처럼 그림자 있어서 누르면 딱인데..."* — **연한 모자(그림자)가 「여기 누르면 된다」를 말한다.**
//      그래서 «비어 있는 칸»은 그대로 둔다. 하트의 연한 판은 **안 만든다**(빈 상태는 하나면 된다).
//
//   ⭐ 도는 차례 = **비어 있음 → 모자(해볼 것) → 하트(최애) → 비어 있음**
//      · **한 번 누르기가 그대로다** — 지금 쓰던 사람은 첫 누름이 진한 모자라 «달라진 걸 못 느낀다»
//      · **누르면 바뀌는 게 눈에 보인다** — 배울 게 없다(길게 누르기는 알려줘야 안다)
//      · 종이 **둘뿐**이라 세 칸 순환이 감당된다. ⛔다섯 종이 되면 이 방식은 못 쓴다 — 그때 다시 설계한다.
//   ⚠️ 정직하게 = **모자에서 빼려면 두 번** 눌러야 한다(모자→하트→비어 있음). 하트에서 빼는 건 한 번.
//      ⭐ 그래도 이쪽이 낫다 — 「빼기」보다 「꽂기」가 훨씬 잦고, 꽂기는 여전히 한 번이다.
/** 지금 상태 → 다음 상태. `null` 이면 «빼기»(favorite = false). */
export const nextPin = (r) => {
  if (!r?.favorite) return PIN_DEFAULT
  const i = FAV_PINS.findIndex((p) => p.key === pinOf(r))
  return i < 0 || i === FAV_PINS.length - 1 ? null : FAV_PINS[i + 1].key
}
